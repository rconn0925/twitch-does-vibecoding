---
status: resolved
trigger: "Live end-to-end: a straight-to-build reaches stage 'done' in ~50s but writes NO files into the workspace generation dir; the gallery publisher then creates a real but EMPTY public repo"
created: 2026-07-11
updated: 2026-07-11
---

# Debug: build reports 'done' but produces no workspace files (empty repo)

## Symptoms
- **Expected:** a winning prompt ("build a simple digital clock web page") scaffolds files (index.html etc.) INTO the persistent workspace generation dir (/home/builder/projects/app-1), which the host publisher then commits + pushes → a public repo with real code.
- **Actual:** build goes building→done in ~50s; app-1 contains ONLY `.claude/` (the agent's session dir) — zero project files. Publisher creates a real PUBLIC repo `TwitchVibecodes/build-a-simple-digital-clock-web-page` (correctly named, public) but it is EMPTY (GitHub "This repository is empty", no commits).
- **Timeline:** first real end-to-end run after the 260711-hak per-project-publishing merge (2026-07-11 ~19:05Z, taskId 8eb70ba1). BL-01 workspace-dir creation now works (app-1 exists). Never worked end-to-end before (tests use injected fakes).
- **Reproduction:** deterministic so far (1/1). Any winning prompt.

## Evidence
- app-1 tree (excluding .claude): empty. `find /home/builder -maxdepth 4 -name '*.html' -o -name '*.js'` (minus node_modules/.claude) finds ONLY `/home/builder/demos/compliment-button/index.html` — an artifact of the EARLIER sandbox-spawn debug session (which ran with --cd ~, before workspaceDir was wired), NOT this build.
- The `.claude/` dir inside app-1 implies the agent's cwd WAS app-1 for this run (session dir is created in cwd).
- App log: stage building (719326) → done (769179), ~50s, no tool/write activity logged (only stage transitions surface in pino; builder-feed taps write to the feed, not this log).
- Scaffold system prompt (prompt-boundary.ts:50, BUILD_SYSTEM_PROMPT_SCAFFOLD): says "inside your sandboxed workspace", "Your workspace is empty — scaffold the project from scratch", "Work entirely within your workspace and build the described app." It NEVER concretely instructs "create the files in your CURRENT WORKING DIRECTORY" or names the path. "Your workspace" is abstract.
- Prior debug build wrote to `demos/compliment-button/` — a subdir of the agent's OWN choosing — evidence the agent picks its own layout absent a concrete cwd instruction.
- AUDIT DB (task 8eb70ba1): rows id 65 + 67 are `comp02_decision` "COMP-02 in-flight output re-screen (D3-07)" approvals at +25.4s and +40.6s into the turn → the agent EMITTED at least two Write/Edit tool_use blocks. The "agent never called Write" variant is ELIMINATED. Files still absent from app-1 → the writes were denied post-stream or landed/blocked elsewhere.
- AUDIT DB row id 69: `gallery_publish` decision "no-changes" ("mirror already matches the workspace snapshot") — the repo was created by scaffoldRepo BEFORE the content check; empty-workspace publish creates the repo then no-ops the commit. Confirms Fix B shape: probe for committable files BEFORE `gh repo create`.
- turn-options.ts assembleSandboxedBuildOptions sets NO permissionMode, NO allowedTools, NO canUseTool → non-interactive default permission mode; file-edit tools require permission and are auto-denied in -p/SDK mode UNLESS sandbox/settings auto-allow them. The earlier compliment-button run that DID write predates quick-22l (lockdown `settings:` object) and quick-0iu (cwd=workspaceDir) — options differed.
- SMOKING GUN (2026-07-11, session transcript /home/builder/.claude/projects/-home-builder-projects-app-1/850054a8-dcc6-4eb7-ad32-0a7d7d5a1b7e.jsonl, copied to scratchpad failed-run-transcript.jsonl): agent Bash `pwd` → /home/builder/projects/app-1 (cwd correct); Write ×2 to /home/builder/projects/app-1/index.html (path correct); BOTH tool_results is_error=true "Claude requested permissions to write … but you haven't granted it yet."; agent then Bash-verified the file absent and ended with "The file write needs your approval" text → result success → done. Root cause is PERMISSION DENIAL, not prompt wording, not path resolution.
- Bash tool DID execute (err=false) — only file-edit tools were denied. Prompt directive is insurance only.
- workspace_state after the phantom done: generation=1, scaffolded=1 (markBuilt fired for an empty build) — next build would wrongly run CONTINUE mode. project_repos maps generation 1 → build-a-simple-digital-clock-web-page (the empty repo).
- LIVE RETEST #1 (post-fix, 2026-07-11 13:31): clock suggestion won round 5, build building→done in ~25s, agent WROTE /home/builder/projects/app-1/index.html (3781 bytes — Fix A CONFIRMED: acceptEdits unblocked Write). EMPTY-01 done-guard + publisher preflight passed; snapshot copied to mirror. NEW downstream bug unmasked: `git commit` in the mirror died exit 128 "Author identity unknown … unable to auto-detect email address (got 'ross@Ross.(none)')" — fresh `git init` mirror + no global git identity on the host; the publisher relied on host git config. publishNow correctly resolved status:failed loudly (T-hak-03 held). Fix: explicit `-c user.name/-c user.email` on the commit invocation (fixed constants, never host config).
- LIVE RETEST #2a (theme-toggle suggestion): build refused — the in-flight COMP-02 classifier hit "Reached maximum number of turns (1)" 3/3 attempts on a large-but-benign CSS batch → classifier-unavailable → fail-closed rejection (correct safety behavior; NOT caused by this session's changes — pre-build screen succeeded seconds earlier). PRE-EXISTING gate-robustness flake, flagged as follow-up.
- LIVE RETEST #2b ("show the current date below the clock", task 0e3d664b, CONTINUE mode): building→done ~29s; app-1/index.html grew 3781→4759 bytes (date added — continue prompt + acceptEdits both work); gallery publish decision=published, commit 218287b5b3d6b7218d3bb8dbc0d13d43973b1002. GitHub API confirms TwitchVibecodes/build-a-simple-digital-clock-web-page now contains index.html (4759 bytes), commit authored "Twitch Vibecodes <twitchvibecodes@users.noreply.github.com>", message = sanitized suggestion title. END-TO-END VERIFIED.
- Also learned: auto-cycle needs ≥2 pooled candidates to open a round ("pool-too-small" → restart phase) — one dev submit alone never builds.

## Current Focus
ROOT CAUSE CONFIRMED via the failed run's own session transcript (no reproduction needed):
/home/builder/.claude/projects/-home-builder-projects-app-1/850054a8-….jsonl shows the agent
(1) ran `pwd && ls -la` → cwd WAS /home/builder/projects/app-1 (cwd/prompt targeting fine),
(2) called Write twice with file_path=/home/builder/projects/app-1/index.html (correct path!),
(3) BOTH Writes returned tool_result is_error=true: "Claude requested permissions to write to
    /home/builder/projects/app-1/index.html, but you haven't granted it yet.",
(4) verified via Bash the file did not exist, then ended its turn with a polite "needs your
    approval" text → SDK result subtype=success → translate() → stage done.

reasoning_checkpoint:
  hypothesis: "assembleSandboxedBuildOptions (turn-options.ts) sets NO permissionMode, NO allowedTools, and no canUseTool — so the non-interactive sandboxed build turn runs in 'default' permission mode where file-edit tools (Write/Edit) require a grant nobody can give; every Write is auto-denied. The agent finishes its turn 'successfully' with zero files; translate() maps result:success → done; finalize marks built + publishes; the publisher creates the repo BEFORE checking for content → empty public repo. Secondary poisoning: markBuilt() flipped scaffolded=1 for an empty generation, and workspaceHasFiles (`ls -A`) counts the agent's own .claude dir → future builds in the generation would wrongly run CONTINUE mode."
  confirming_evidence:
    - "Transcript tool_result err=true ×2: 'Claude requested permissions to write to /home/builder/projects/app-1/index.html, but you haven't granted it yet.' — direct observation of the denial"
    - "Transcript Bash ls check: file absent after both Writes; agent's closing text explicitly says the write needs approval"
    - "audit_log rows 65/67: two in-flight COMP-02 approvals — Write tool_use blocks streamed (they stream before permission evaluation), consistent with attempted-then-denied"
    - "turn-options.ts: Options carries systemPrompt/abortController/cwd/lockdown-triple/sandbox/spawn ONLY — no permission grant of any kind"
    - "audit_log row 69: gallery_publish 'no-changes' AFTER the repo was already created (scaffoldRepo runs gh repo create before any content check)"
  falsification_test: "If the transcript had shown Write succeeding (no is_error) or writing to a path outside app-1, the permission hypothesis would be disproven and this would be a path-resolution bug instead. Observed: denial, correct path."
  fix_rationale: "Fix A (root cause): permissionMode:'acceptEdits' in assembleSandboxedBuildOptions — auto-accepts file-edit tools for the sandboxed turn; the WSL distro + CLI bubblewrap sandbox + COMP-02 in-flight screen remain the safety boundaries (permission prompts were never a boundary here — there is nobody to answer them). Plus a concrete 'create files in your current working directory' prompt directive (cheap insurance; static string, SAND-04 intact). Fix B (guards): (1) build-session withholds 'done' when the workspace has no committable files after an ok turn (new optional SandboxAdapter.workspaceHasCommittableFiles; auto-retry-once honored, then narrated failed decision) — so markBuilt/onBuildDone never fire for an empty build; (2) gallery-publisher preflights the workspace snapshot BEFORE gh repo create and no-ops when nothing is committable; (3) workspaceHasFiles HI-01 probe ignores dot-entries so a .claude-only dir still scaffolds."
  blind_spots: "acceptEdits behavior for brand-new file Writes verified only by SDK docs wording ('auto-accept file edit operations') — the live re-run is the proof. Bash network installs (npm) untouched/untested. The stale workspace_state.scaffolded=1 and the existing empty repo are corrupted state from the bug — must be reset/handled before the verification run (reset scaffolded=0; empty repo fills or Ross deletes)."

next_action: "NONE — session resolved 2026-07-11 (human-verified: 'confirmed fixed'). Archived to resolved/, src fix committed, knowledge-base entry appended. NOTE: workspace_state.scaffolded reset 1→0 mid-session (state repair); app-1 now legitimately scaffolded=1 after the verified clock build. .env test values (intake cooldown/pool caps) intentionally left uncommitted for Ross to revert before broadcast."

## Constraints / notes
- src/compliance/**, halt.ts, kill-switch, CLASSIFIER_SYSTEM_PROMPT are READ-ONLY.
- Two fixes likely needed: (A primary) make the agent reliably build INTO the workspace cwd — probably a concrete prompt directive ("Create all files directly in your current working directory") + verify cwd/Write path resolution; (B secondary) finalize should not report a clean 'done' for an empty workspace, and the publisher should NO-OP (not create a repo) when there are no committable files — so failed/empty builds don't litter empty public repos.
- The app is running (bn4087vtd). Distro vibecoding-build up. The empty test repo exists on TwitchVibecodes (token has no delete_repo scope; Ross can delete it via UI, or it fills once a build actually writes).

## Eliminated
- hypothesis: "the agent never attempted a Write/Edit at all (pure prompt-wording failure)" — eliminated 2026-07-11: audit_log rows 65+67 are in-flight COMP-02 output re-screens for task 8eb70ba1, which only fire on Write/Edit/MultiEdit/NotebookEdit tool_use blocks (build-session.ts extractWriteEditText). The agent attempted at least two writes.
- hypothesis: "sandbox spawn / workspace dir missing" — eliminated: app-1 is created (BL-01 fix verified), the agent session ran in it (.claude present), spawn works (prior debug session fix).
- hypothesis: "publisher push mechanism broken" — partially eliminated: the publisher successfully created the repo and pushed (pushed_at set); it just had nothing to commit. Repo creation + auth + naming all work.

## Resolution
root_cause: "assembleSandboxedBuildOptions (src/orchestrator/turn-options.ts) passed NO permission grant (no permissionMode, no allowedTools, no canUseTool) to the non-interactive sandboxed build turn. In the SDK's 'default' permission mode there is nobody to answer a file-write permission prompt, so EVERY Write was auto-denied ('Claude requested permissions to write to /home/builder/projects/app-1/index.html, but you haven't granted it yet.' — session transcript 850054a8). The agent ended its turn with zero files; result:success → translate() → phantom 'done' → markBuilt() + gallery publish → gh repo create ran BEFORE any content check → real but EMPTY public repo. (cwd + Write paths were CORRECT — /home/builder/projects/app-1 — this was never a prompt-wording or path bug.)"
fix: "(A root cause) turn-options.ts: permissionMode:'acceptEdits' on the sandboxed build turn ONLY (never bypassPermissions; host branch untouched); prompt-boundary.ts: concrete 'create files in your current working directory' directives added to both fixed prompts (static text, SAND-04 zero-interpolation intact). (B guards, EMPTY-01) build-session.ts: after an ok turn, a new optional SandboxAdapter.workspaceHasCommittableFiles probe withholds 'done' for an empty workspace → same auto-retry-once-then-narrated-decision route as a failed turn; markBuilt/onBuildDone never fire. gallery-publisher.ts: preflight readdir + workspaceCopyFilter BEFORE gh repo create → 'no-changes' no-op (no repo created) when nothing is committable. sandbox-process.ts: workspaceHasCommittableFiles impl (ls minus node_modules, error→true fail-toward-done) + HI-01 workspaceHasFiles now ignores dot-entries (agent's .claude can no longer flip an unbuilt generation into continue mode). State repair: workspace_state.scaffolded reset 1→0 (was corrupted by the phantom done)."
verification: "Gate: vitest 857/857 (12 new tests), tsc --noEmit clean, biome clean. LIVE (2 full rounds through npm run dev + POST /api/dev/submit): (1) scaffold-mode clock build wrote /home/builder/projects/app-1/index.html (3781B) — original symptom gone; (2) continue-mode 'show the current date below the clock' build modified index.html (4759B) and PUBLISHED: commit 218287b5b3d6b7218d3bb8dbc0d13d43973b1002 live on github.com/TwitchVibecodes/build-a-simple-digital-clock-web-page with index.html present (GitHub API-confirmed), author 'Twitch Vibecodes <twitchvibecodes@users.noreply.github.com>'. The previously-empty repo is now filled. App stopped afterward (restored initial state); .env test values untouched/uncommitted."
files_changed: [src/orchestrator/turn-options.ts, src/orchestrator/turn-options.test.ts, src/orchestrator/prompt-boundary.ts, src/orchestrator/types.ts, src/orchestrator/sandbox-process.ts, src/orchestrator/sandbox-process.test.ts, src/orchestrator/build-session.ts, src/orchestrator/build-session.test.ts, src/orchestrator/gallery-publisher.ts, src/orchestrator/gallery-publisher.test.ts]
follow_ups:
  - "DEFERRED (per human gate, not fixed in this session): PRE-EXISTING gate flake — in-flight COMP-02 classifier intermittently dies 'Reached maximum number of turns (1)' ×3 on large output batches → fail-closed refusal of a good build (observed live on a benign CSS batch). Safe direction, but will refuse real builds on stream. Candidate fix: maxTurns:2 or batch-size truncation in src/orchestrator/classifier-runner.ts (compliance/** itself untouched)."
  - "DEFERRED (per human gate, not fixed in this session): gallery_publish audit detail has a doubled 'app-1: app-1:' prefix (cosmetic, pre-existing — main.ts prepends generation to a detail that already contains it)."
  - "Ross may want to delete no repos now — the formerly-empty clock repo is filled. Nothing to clean up on TwitchVibecodes."
