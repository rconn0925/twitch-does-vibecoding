---
phase: 01-compliance-gate-kill-switch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - biome.json
  - vitest.config.ts
  - .gitignore
  - .env.example
  - README.md
  - src/shared/types.ts
  - src/shared/events.ts
  - src/state-machine/stream-mode.ts
  - src/state-machine/halt.ts
  - src/audit/schema.sql
  - src/audit/db.ts
  - src/audit/record.ts
  - src/operator-console/server.ts
  - src/operator-console/public/index.html
  - src/operator-console/public/console.css
  - src/operator-console/public/console.js
  - src/main.ts
  - src/state-machine/stream-mode.test.ts
  - src/audit/record.test.ts
  - tests/e2e/halt.e2e.test.ts
# scope_exception: 22 files exceeds the usual 15-file guidance — accepted as a documented
# exception. MVP-mode Walking Skeleton (Wave 1) mandates ONE vertical slice covering
# scaffold + UI + API + DB in a single plan; ~9 of these files are trivial scaffold/config
# (package.json, tsconfig, biome, vitest config, .gitignore, .env.example, README, html, css).
autonomous: true
requirements: [COMP-04, COMP-05]
must_haves:
  truths:
    - "Operator opens http://127.0.0.1:4900 and sees the current stream-mode pill (IDLE on fresh start)"
    - "Clicking 'Halt Everything' transitions the state machine to HALTED from any state with a single click, no confirmation modal (D-02, D-04 friction rules)"
    - "Every halt writes an append-only audit_log row capturing source, prior mode, and timestamp (COMP-05, D-16)"
    - "The console server is reachable only on 127.0.0.1 — never 0.0.0.0"
    - "Full local stack runs with one documented command (npm run dev)"
  artifacts:
    - path: "package.json"
      provides: "Locked stack: express@^5.2, ws@^8.21, better-sqlite3@^12.11, zod@^4, pino@^10, @anthropic-ai/sdk@^0.110, uiohook-napi@^1.5, tree-kill@^1.2, typescript@~6.0, vitest, tsx, @biomejs/biome"
    - path: "src/shared/types.ts"
      provides: "SuggestionCandidate, branded QueuedTask, StreamMode (6 states), GateResult, CandidateKind incl. project-switch (D-15), ReasonTag (D-18)"
      contains: "project-switch"
    - path: "src/state-machine/stream-mode.ts"
      provides: "Hand-rolled 6-state machine; HALTED reachable synchronously from every state"
      min_lines: 40
    - path: "src/audit/schema.sql"
      provides: "audit_log (append-only) + review_queue (mutable, incl. candidate_id/source/kind/submitted_at_ms so plan 01-05's approve() can reconstruct the full candidate for D-06 pool re-entry) — two-table split per RESEARCH.md Pitfall 5"
      contains: "CREATE TABLE IF NOT EXISTS audit_log"
    - path: "src/operator-console/server.ts"
      provides: "Express 5 localhost-only console server + ws state push"
    - path: "src/main.ts"
      provides: "Process entrypoint wiring state machine + audit + console"
  key_links:
    - from: "src/operator-console/public/console.js"
      to: "/api/halt"
      via: "fetch POST on Halt Everything click"
      pattern: "fetch\\(.*/api/halt"
    - from: "src/operator-console/server.ts"
      to: "src/state-machine/halt.ts"
      via: "triggerHalt() call from the /api/halt route"
      pattern: "triggerHalt"
    - from: "src/state-machine/halt.ts"
      to: "src/audit/record.ts"
      via: "recordHalt() append before returning"
      pattern: "recordHalt"
    - from: "src/operator-console/server.ts"
      to: "127.0.0.1"
      via: "explicit listen host argument"
      pattern: "127\\.0\\.0\\.1"
---

<objective>
Walking Skeleton for Twitch Does Vibecoding: scaffold the locked Node 24 + TypeScript 6 stack and prove the thinnest end-to-end safety slice — operator console page → HTTP route → state machine HALTED transition → SQLite audit write → audit read back — all runnable with one command.

Purpose: This is Phase 1 of an MVP-mode greenfield project. Every later slice (gate, hotkey, review queue) plugs into the shared types, state machine, audit ledger, and console server established here. The first user-visible capability is the single most important one in the whole project: the streamer can halt everything.
Output: Running skeleton with a clickable Halt Everything button that provably freezes the state machine and leaves an audit trail.

Scope note: this plan touches 22 files — above the usual 15-file guidance — as a documented exception: the Walking Skeleton mandate requires one vertical slice (scaffold + UI + API + DB) in a single Wave-1 plan, and roughly 9 of the files are trivial scaffold/config with near-zero context cost.
</objective>

## Phase Goal

**As a** streamer running a chat-driven live build show, **I want to** force every candidate instruction through one adversarially-tested compliance gate and be able to halt anything from anywhere in seconds, **so that** nothing chat submits can ever put my channel at risk of a Twitch ToS violation.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md
@.planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md
@.planning/phases/01-compliance-gate-kill-switch/01-UI-SPEC.md
@.planning/phases/01-compliance-gate-kill-switch/01-SKELETON.md

<interfaces>
<!-- Contracts this plan CREATES. Later plans (01-02, 01-03, 01-04, 01-05) implement against
     these exact names — do not rename without updating all five plans. -->

From src/shared/types.ts (to be created in Task 1):

- `type StreamMode = "IDLE" | "VOTING_ROUND" | "BUILD_IN_PROGRESS" | "FREE_REIGN_WINDOW" | "CHAOS_MODE" | "HALTED"`
- `type CandidateSource = "chat" | "channel_points" | "donation" | "chaos" | "operator"`
- `type CandidateKind = "suggestion" | "project-switch"`  // D-15: project-switch is a first-class instruction type; mechanics land in Phases 2/4
- `interface SuggestionCandidate { id: string; source: CandidateSource; kind: CandidateKind; twitchUsername: string | null; text: string; submittedAtMs: number }`
- `type GateDecision = "approved" | "rejected" | "held-for-review"`  // D-08 honest three-state vocabulary
- `interface GateResult { decision: GateDecision; category: GateCategory | null; rationale: string }`  // GateCategory union finalized in plan 01-02's categories.ts; declare as string alias here with TODO pointer
- `type QueuedTask = SuggestionCandidate & { readonly [QueuedTaskBrand]: true }` via `declare const QueuedTaskBrand: unique symbol` — the brand assertion may only ever appear in src/compliance/gate.ts (plan 01-05)
- `type ReasonTag = "tos-risk" | "boring" | "too-big" | "gut-feeling" | "other"`  // D-18
- `type RecoveryAction = "resume" | "discard-and-resume" | "reset-to-idle"`  // D-04
- `interface HaltContext { source: "hotkey" | "console"; reasonTag: ReasonTag | null; frozen: StateSnapshot }`
- `interface StateSnapshot { mode: StreamMode; activeTaskId: string | null; activeTaskPid: number | null; queuedTaskIds: string[]; haltContext: HaltContext | null }`

From src/shared/events.ts:
- Event name constants: `"state:changed"`, `"halt:triggered"`, `"gate:decision"`, `"review:resolved"`, `"project-switch:requested"` (D-15 vocabulary placeholder for Phases 2/4)

From src/state-machine/stream-mode.ts:
- `class StreamModeMachine { get mode(): StreamMode; snapshot(): StateSnapshot; transition(to: StreamMode): void; forceTransition(to: "HALTED", ctx: HaltContext): void; on(event, handler): void }`

From src/state-machine/halt.ts:
- `function triggerHalt(machine, deps, source: "hotkey" | "console", reasonTag?: ReasonTag | null): StateSnapshot` — synchronous; captures prior snapshot, forces HALTED, records audit, returns frozen snapshot
- `function recover(machine, deps, action: RecoveryAction, reasonTag?: ReasonTag | null): void` — D-04 triage-then-choose; full console wiring lands in plan 01-04

From src/audit/record.ts (insert-only; no update/delete helpers may be exported — plan 01-05 adds a fifth insert-only helper, recordSubmissionRefused, for D-02):
- `recordHalt(db, { source, priorMode, reasonTag })`
- `recordVeto(db, { taskId, suggestionText, twitchUsername, reasonTag, streamMode })`
- `recordGateDecision(db, { candidate, decision, category, rationale, streamMode })`
- `recordReviewResolution(db, { reviewId, resolution, suggestionText, twitchUsername, streamMode })`
- `listAuditRecords(db, { limit, eventType?, decision?, sinceMs? })`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the locked stack and write the failing halt e2e test</name>
  <files>package.json, tsconfig.json, biome.json, vitest.config.ts, .gitignore, .env.example, src/shared/types.ts, src/shared/events.ts, tests/e2e/halt.e2e.test.ts</files>
  <read_first>
    - .planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md (Standard Stack, Recommended Project Structure, Code Examples — audit schema)
    - .planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md (D-15, D-16, D-18 — types must encode these)
    - CLAUDE.md (Technology Stack section — locked versions)
    - .planning/phases/01-compliance-gate-kill-switch/01-01-PLAN.md `<interfaces>` block (the exact type contracts to create)
  </read_first>
  <behavior>
    - Test 1 (e2e, initially RED): starting the app on an ephemeral port with an in-memory DB, GET /api/state returns 200 with JSON body containing mode "IDLE"
    - Test 2 (e2e, initially RED): POST /api/halt with JSON body {} returns 200; subsequent GET /api/state returns mode "HALTED"
    - Test 3 (e2e, initially RED): after the halt, GET /api/audit?limit=10 returns at least one record with event_type "halt" and source "console"
    - Test 4 (e2e, initially RED): the server's bound address (from server.address()) is 127.0.0.1, never 0.0.0.0
  </behavior>
  <action>
    Initialize the greenfield repo per the locked stack (D references and versions from RESEARCH.md Standard Stack — all packages already passed the Package Legitimacy Audit; no [ASSUMED]/[SUS] entries exist).

    package.json: `"type": "module"`, `"engines": { "node": ">=24" }`. Dependencies: `express@^5.2.0`, `ws@^8.21.0`, `better-sqlite3@^12.11.0`, `zod@^4`, `pino@^10`, `@anthropic-ai/sdk@^0.110.0`, `uiohook-napi@^1.5.5`, `tree-kill@^1.2.2`. DevDependencies: `typescript@~6.0`, `tsx`, `vitest`, `@biomejs/biome`, `@types/node`, `@types/express`, `@types/ws`, `@types/better-sqlite3`. Scripts: `dev` = `tsx --env-file-if-exists=.env src/main.ts`, `test` = `vitest run`, `typecheck` = `tsc --noEmit`, `lint` = `biome check src tests scripts`. The `--env-file-if-exists=.env` flag (Node 22.9+, passed through by tsx) loads .env secrets per CLAUDE.md's stack guidance (native --env-file over the dotenv package) without failing when .env is absent. Run `npm view <pkg> version` for @anthropic-ai/sdk before install if more than two weeks have passed since 2026-07-08 (RESEARCH.md validity note).

    tsconfig.json: strict true, module NodeNext, moduleResolution NodeNext, target ES2023, noEmit workflow via tsx (no build output needed this phase). biome.json: recommended lint + formatter enabled. .gitignore: node_modules, data/, .env, *.db. .env.example documents ALL Phase 1 env vars up front so later plans never touch this file: CONSOLE_PORT=4900, AUDIT_DB_PATH=./data/audit.db, ANTHROPIC_API_KEY=, GATE_MODEL=claude-sonnet-5, GATE_MAX_RETRIES=2, PANIC_HOTKEY=F13, REVIEW_TTL_HOURS=4, AUDIT_RETENTION_DAYS=90.

    Create src/shared/types.ts and src/shared/events.ts exactly per this plan's `<interfaces>` block — including the branded QueuedTask with `unique symbol`, the D-15 `project-switch` CandidateKind, D-18 ReasonTag union, and D-04 RecoveryAction. Add a comment on QueuedTask: "The `as QueuedTask` brand assertion is only permitted inside src/compliance/gate.ts — enforced by tests/invariants/single-funnel.test.ts (plan 01-04)."

    Write tests/e2e/halt.e2e.test.ts per `<behavior>`. Build the app via an exported factory (e.g. `createApp({ dbPath: ":memory:", port: 0 })` from src/main.ts) so the test starts a real server on an ephemeral port and uses global fetch — no supertest (unaudited dependency; do not add it). The test MUST fail at this point because src/main.ts does not exist yet (RED).
  </action>
  <verify>
    <automated>npm install && npx tsc --noEmit --skipLibCheck src/shared/types.ts 2>&1 | head -5 && npx vitest run tests/e2e/halt.e2e.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm install` completes with better-sqlite3 and uiohook-napi resolving prebuilt binaries (no node-gyp compile failure) — RESEARCH.md flags this as the top Windows friction risk
    - src/shared/types.ts exports: StreamMode with exactly the 6 states IDLE, VOTING_ROUND, BUILD_IN_PROGRESS, FREE_REIGN_WINDOW, CHAOS_MODE, HALTED
    - `grep -c "project-switch" src/shared/types.ts` returns >= 1 (D-15)
    - `grep -c "unique symbol" src/shared/types.ts` returns 1 (QueuedTask brand)
    - ReasonTag union contains exactly: tos-risk, boring, too-big, gut-feeling, other (D-18)
    - `npx vitest run tests/e2e/halt.e2e.test.ts` FAILS (RED state — main.ts not yet created)
    - .env.example contains all 8 documented env vars
  </acceptance_criteria>
  <done>Repo scaffolded on the locked stack, shared type contracts exist, e2e halt test written and failing for the right reason (missing implementation, not syntax errors).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: State machine with HALT-priority transition + append-only audit ledger</name>
  <files>src/state-machine/stream-mode.ts, src/state-machine/halt.ts, src/audit/schema.sql, src/audit/db.ts, src/audit/record.ts, src/state-machine/stream-mode.test.ts, src/audit/record.test.ts</files>
  <read_first>
    - src/shared/types.ts (contracts from Task 1 — implement against these exactly)
    - .planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md (Pattern 2: HALT-Priority State Machine; Code Examples: audit_log + review_queue schema; Common Pitfalls #5: two-table split)
    - .planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md (D-02, D-04, D-16)
  </read_first>
  <behavior>
    - Test: fresh StreamModeMachine starts in IDLE
    - Test: forceTransition("HALTED", ctx) succeeds from EVERY one of the 6 states, synchronously (assert mode === "HALTED" on the very next line, no await)
    - Test: normal transition() rejects invalid moves (e.g. IDLE -> BUILD_IN_PROGRESS directly is invalid; IDLE -> VOTING_ROUND is valid) and throws a typed error naming both states (feeds UI-SPEC "Can't transition to {state} from {state}" error copy)
    - Test: triggerHalt() captures the pre-halt snapshot into HaltContext.frozen (D-04 triage data), emits "halt:triggered", and calls recordHalt exactly once
    - Test: recover(machine, deps, "resume") restores the frozen prior mode; "reset-to-idle" goes to IDLE; recover throws if mode is not HALTED; nothing auto-resumes (no timer/interval anywhere in halt.ts)
    - Test (audit): recordHalt/recordGateDecision/recordVeto each INSERT one row; listAuditRecords returns them newest-first; record.ts module source contains no UPDATE or DELETE SQL
    - Test (audit): review_queue table exists after migration with the full candidate-identity columns (schema only — the module logic lands in plan 01-05)
  </behavior>
  <action>
    Implement src/state-machine/stream-mode.ts as a hand-rolled explicit machine (RESEARCH.md rejects XState for ~6 states): a transition table `Record<StreamMode, StreamMode[]>` for legal moves, `forceTransition` accepting only "HALTED" as target (typed literal) which bypasses the table — HALTED must be reachable from every state with zero async work (D-02, Success Criterion 3). Emit "state:changed" via a Node EventEmitter using the names from src/shared/events.ts. Valid normal transitions for Phase 1: IDLE→VOTING_ROUND, VOTING_ROUND→IDLE, VOTING_ROUND→BUILD_IN_PROGRESS, BUILD_IN_PROGRESS→IDLE, IDLE→FREE_REIGN_WINDOW, FREE_REIGN_WINDOW→IDLE, FREE_REIGN_WINDOW→BUILD_IN_PROGRESS, IDLE→CHAOS_MODE, CHAOS_MODE→BUILD_IN_PROGRESS, CHAOS_MODE→IDLE, HALTED→(recovery only via recover()).

    Implement src/state-machine/halt.ts per the `<interfaces>` signatures: triggerHalt is SYNCHRONOUS — snapshot, forceTransition, recordHalt, return. It must NOT await anything (Pattern 2: instant transition, best-effort abort decoupled — the abort hook is added by plan 01-03 as a fire-and-forget call AFTER the transition; leave a named no-op extension point `deps.abortActiveWork?: (frozen: StateSnapshot) => void` invoked with void-and-catch semantics). recover() implements D-04's three actions; "discard-and-resume" marks the frozen activeTaskId as discarded (records a veto audit row) then restores prior mode or IDLE if the prior mode was BUILD_IN_PROGRESS.

    Implement src/audit/schema.sql based on the RESEARCH.md Code Examples, with one planned extension. audit_log exactly as researched: columns id, created_at_ms, event_type, source, twitch_username, suggestion_text, decision, category, rationale, stream_mode, task_id (append-only). review_queue EXTENDED beyond the research sketch to carry full candidate identity: id, created_at_ms, candidate_id (TEXT NOT NULL), source (TEXT NOT NULL), kind (TEXT NOT NULL DEFAULT 'suggestion'), submitted_at_ms (INTEGER NOT NULL), suggestion_text (TEXT NOT NULL), twitch_username, category (TEXT NOT NULL), rationale (TEXT NOT NULL), status (TEXT NOT NULL DEFAULT 'pending'), resolved_at_ms. Both indexes from the research sketch. The four extra columns (candidate_id, source, kind, submitted_at_ms) exist so plan 01-05's review-queue approve() can reconstruct the complete SuggestionCandidate — { id: candidate_id, source, kind, twitchUsername, text, submittedAtMs } — and re-enter it into the candidate pool per D-06 without joins or lossy defaults.

    src/audit/db.ts: better-sqlite3 connection factory `openDb(path)` that executes schema.sql idempotently (CREATE TABLE IF NOT EXISTS) and sets `journal_mode = WAL` for a file path, skipping WAL for ":memory:". src/audit/record.ts: the four insert helpers + listAuditRecords per `<interfaces>`. Append-only discipline (Repudiation mitigation T-01-02): this module exposes INSERT and SELECT only. Grep-gate hygiene: do NOT write the words UPDATE or DELETE anywhere in record.ts — including comments — so the append-only grep gate stays meaningful; put the "the sole DELETE in the codebase lives in src/audit/purge.ts (plan 01-04, 90-day retention D-17)" provenance note in db.ts or schema.sql instead.
  </action>
  <verify>
    <automated>npx vitest run src/state-machine/stream-mode.test.ts src/audit/record.test.ts && grep -vE "^\s*(//|\*|/\*)" src/audit/record.ts | grep -ciE "UPDATE|DELETE" | grep -qx 0 && echo APPEND_ONLY_OK</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` tests pass
    - forceTransition to HALTED works from all 6 states — test iterates every state explicitly
    - Comment-filtered grep for UPDATE/DELETE in src/audit/record.ts outputs 0 (append-only; comment lines excluded so prose cannot self-invalidate the gate)
    - schema.sql contains both `CREATE TABLE IF NOT EXISTS audit_log` and `CREATE TABLE IF NOT EXISTS review_queue` (two-table split, RESEARCH.md Pitfall 5)
    - `grep -c "candidate_id" src/audit/schema.sql` returns >= 1 (D-06 reconstruction columns present, incl. source, kind, submitted_at_ms)
    - halt.ts contains no `await` inside triggerHalt (source assertion: the function body is synchronous)
    - recover() with action "resume" restores the exact prior mode captured in HaltContext.frozen
  </acceptance_criteria>
  <done>State machine and audit ledger pass unit tests; HALTED is a synchronous, from-anywhere transition; audit writes are structurally append-only.</done>
</task>

<task type="auto">
  <name>Task 3: Operator console skeleton (status bar + Halt Everything) and main.ts wiring — e2e goes green</name>
  <files>src/operator-console/server.ts, src/operator-console/public/index.html, src/operator-console/public/console.css, src/operator-console/public/console.js, src/main.ts, README.md</files>
  <read_first>
    - .planning/phases/01-compliance-gate-kill-switch/01-UI-SPEC.md (ENTIRE file — colors, spacing, typography, copywriting contract are a binding design contract)
    - src/shared/types.ts, src/state-machine/halt.ts, src/audit/record.ts (implement routes against these)
    - tests/e2e/halt.e2e.test.ts (the contract this task must satisfy)
  </read_first>
  <action>
    Implement src/operator-console/server.ts: Express 5 app factory serving static files from operator-console/public plus JSON routes — GET /api/state (returns StateSnapshot), POST /api/halt (zod-validated body `{ reasonTag?: ReasonTag }`, calls triggerHalt(machine, deps, "console", reasonTag), returns the frozen snapshot), GET /api/audit (zod-validated query `{ limit?: number <= 200 }`, returns listAuditRecords — minimal JSON now; the filterable page ships in plan 01-04). Validate EVERY request body/query with zod v4 schemas per the project's untrusted-boundary rule (ASVS V5) — invalid input returns 400 with a terse error, never a stack trace. Attach a `ws` WebSocketServer to the same HTTP server: on connect push the full StateSnapshot as one JSON message, then push again on every "state:changed" event (full-state-on-connect-then-updates pattern from CLAUDE.md overlay guidance).

    The HTTP server MUST listen with an explicit host argument "127.0.0.1" (never 0.0.0.0, never default) — this is the console's only access control (RESEARCH.md Security Domain V2) and Test 4 asserts it.

    Console UI per UI-SPEC (hand-rolled semantic HTML/CSS/vanilla JS — no framework, no icon lib, system font stack): persistent status bar with the color-coded mode pill (one pill per StreamMode; HALTED uses the red-600 #DC2626 banner treatment, active pill border blue-500 #3B82F6) and the always-visible "Halt Everything" button (destructive red #DC2626, 44px min click-target, single click, NO modal — D-03 puts confirmation friction only on the hotkey). After a halt, show the optional one-tap reason-tag row (ToS risk / boring / too big / gut feeling / other / skip) which POSTs a follow-up but never blocks — the halt has already taken effect (D-18, UI-SPEC copywriting contract). Dark theme: page bg #0F172A, panels #1E293B, spacing tokens 4/8/16/24/32/48px, type scale 14/16/20/28 at weights 400/600 only. console.js: render ALL server-sourced text (mode, audit text) via `textContent`, never innerHTML — suggestion text is attacker-controlled in later plans and this file sets the pattern (stored-XSS mitigation T-01-04). Include the ~30-line ws reconnect-with-backoff wrapper and the UI-SPEC "Console disconnected from orchestrator" error state.

    src/main.ts: exported `createApp({ dbPath, port })` factory (used by the e2e test) that wires openDb → StreamModeMachine → console server, plus a run-as-entrypoint branch reading CONSOLE_PORT (default 4900) and AUDIT_DB_PATH (default ./data/audit.db, mkdir -p data). Structured pino logging for startup, halt, and errors — pino is operational logging, NOT the audit trail (RESEARCH.md anti-pattern). README.md: project one-liner, `npm install`, `npm run dev`, console URL http://127.0.0.1:4900, `npm test`, env var table matching .env.example.
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/halt.e2e.test.ts && npm run typecheck && npm run lint && grep -c "127.0.0.1" src/operator-console/server.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 e2e behaviors from Task 1 now PASS (GREEN): state starts IDLE, POST /api/halt flips to HALTED, audit row with event_type "halt" readable via GET /api/audit, server bound to 127.0.0.1
    - `npm run dev` starts; visiting http://127.0.0.1:4900 renders the status bar with mode pill "IDLE" and a "Halt Everything" button (exact copy per UI-SPEC)
    - POST /api/halt with body `{"reasonTag":"not-a-tag"}` returns 400 (zod rejects), with `{}` returns 200
    - console.js contains zero occurrences of `innerHTML` (grep -c "innerHTML" src/operator-console/public/console.js returns 0)
    - `npm run typecheck` and `npm run lint` pass clean
    - README documents the single full-stack local run command (Walking Skeleton deployment requirement)
  </acceptance_criteria>
  <done>The Walking Skeleton is alive: one command starts the stack, the streamer sees live mode state and can halt everything with one click, and the halt is audit-logged and readable back — routing, DB read+write, and UI interaction all proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → console HTTP/ws API | Operator's browser on localhost; bodies/queries still untrusted input |
| App → SQLite file | Audit ledger is the compliance record of truth |
| npm registry → node_modules | 8 production packages installed this task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | operator-console/server.ts listen() | mitigate | Explicit host "127.0.0.1"; e2e Test 4 asserts bound address; no auth exists on this surface so network exposure = full kill-switch compromise (RESEARCH.md V2) |
| T-01-02 | Repudiation/Tampering | audit/record.ts | mitigate | Module exports INSERT/SELECT only; comment-filtered grep gate asserts zero UPDATE/DELETE; the only DELETE ever allowed is the 90-day purge (plan 01-04) |
| T-01-03 | Tampering | /api/halt, /api/audit inputs | mitigate | zod v4 validation on every body/query at route entry; 400 on failure, no stack traces |
| T-01-04 | Elevation of Privilege (stored XSS) | console.js rendering | mitigate | textContent-only rendering rule established now, before attacker-controlled suggestion text renders in plan 01-04; grep gate on innerHTML |
| T-01-05 | Denial of Service | forceTransition HALTED (ungated by design) | accept | HALTED bypassing the gate is the deliberate design (CONTEXT.md domain: veto is highest-priority from any state); reachable only from localhost surfaces |
| T-01-SC | Tampering (supply chain) | npm installs (express, ws, better-sqlite3, zod, pino, @anthropic-ai/sdk, uiohook-napi, tree-kill) | mitigate | All 8 packages passed the RESEARCH.md Package Legitimacy Audit (slopcheck OK, official/long-standing repos, no postinstall scripts flagged); zero [ASSUMED]/[SUS] — no blocking checkpoint required |
</threat_model>

<verification>
- `npm run typecheck`, `npm run lint`, `npm test` all pass
- `npm run dev` then open http://127.0.0.1:4900 — mode pill visible, Halt Everything click flips pill to HALTED banner, GET /api/audit shows the halt row
- `netstat -ano | findstr :4900` shows the listener on 127.0.0.1 only
</verification>

<success_criteria>
- Walking Skeleton checklist complete: scaffold + routing + real DB write AND read + real UI interaction + documented local full-stack run command
- HALTED transition is synchronous from every state (Success Criterion 3 foundation)
- Every halt is audit-logged with source and prior state (COMP-05, D-16)
- Shared type vocabulary (incl. branded QueuedTask + D-15 project-switch) is the contract for plans 01-02/03/04/05
</success_criteria>

<output>
Create `.planning/phases/01-compliance-gate-kill-switch/01-01-SUMMARY.md` when done.
</output>
