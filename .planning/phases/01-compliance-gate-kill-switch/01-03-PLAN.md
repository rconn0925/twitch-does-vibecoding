---
phase: 01-compliance-gate-kill-switch
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/kill-switch/hotkey.ts
  - src/kill-switch/abort.ts
  - src/kill-switch/hotkey.test.ts
  - src/kill-switch/abort.test.ts
  - src/state-machine/halt.ts
  - src/main.ts
  - tests/fixtures/hung-process.cjs
  - tests/e2e/kill-switch.e2e.test.ts
  - docs/OPERATIONS.md
autonomous: false
requirements: [COMP-04]
must_haves:
  truths:
    - "Double-tapping the panic key within 2 seconds triggers HALTED from any state; a single tap does nothing; a third tap does not immediately re-trigger (D-03)"
    - "The HALTED transition completes synchronously and is never blocked by, or dependent on, the abort attempt succeeding (Pattern 2)"
    - "A synthetic hung child process that ignores SIGTERM is force-killed (process tree dead) within 5 seconds of the halt"
    - "A hotkey-triggered halt writes an audit_log row with source hotkey (COMP-05)"
    - "The Windows UIPI elevation gotcha is documented as a pre-stream operational rule"
  artifacts:
    - path: "src/kill-switch/hotkey.ts"
      provides: "uiohook-napi global listener with double-tap-within-2s debounce (D-01, D-03)"
      contains: "2000"
    - path: "src/kill-switch/abort.ts"
      provides: "tree-kill based best-effort process-tree termination + AbortController registry for in-process work"
      contains: "tree-kill"
    - path: "tests/fixtures/hung-process.cjs"
      provides: "Synthetic hung task: traps SIGTERM/SIGINT and loops forever (Success Criterion 3 fixture)"
    - path: "docs/OPERATIONS.md"
      provides: "Pre-stream checklist incl. never-run-OBS-elevated rule (UIPI, RESEARCH.md Pitfall 2)"
  key_links:
    - from: "src/kill-switch/hotkey.ts"
      to: "src/state-machine/halt.ts"
      via: "onPanic callback invoking triggerHalt(..., 'hotkey')"
      pattern: "triggerHalt"
    - from: "src/state-machine/halt.ts"
      to: "src/kill-switch/abort.ts"
      via: "fire-and-forget abortActiveWork AFTER the state has flipped"
      pattern: "abortActiveWork"
    - from: "src/kill-switch/abort.ts"
      to: "tree-kill"
      via: "treeKill(pid, 'SIGKILL', cb)"
      pattern: "SIGKILL"
    - from: "src/main.ts"
      to: "src/kill-switch/hotkey.ts"
      via: "startHotkeyListener wiring at process start"
      pattern: "startHotkeyListener"
---

<objective>
Make the halt reachable from anywhere and effective against anything: a global Windows panic hotkey (double-tap confirm) and a best-effort abort mechanism that kills even a hung, signal-ignoring process tree — with the state machine's HALTED transition fully decoupled from abort success.

Purpose: COMP-04's "from anywhere, in seconds, from any system state" half. Plan 01-01 gave the console button; this plan gives the focus-independent panic path and proves halt-vs-hung-task (Success Criterion 3). Phase 3's agent sessions will register their PIDs into this exact abort registry.
Output: Hotkey + abort modules, a synthetic hung-task e2e proving kill-within-5s, and the operational runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md
@.planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md
@src/shared/types.ts
@src/state-machine/halt.ts
@src/state-machine/stream-mode.ts
@src/main.ts

<interfaces>
<!-- Contracts this plan CREATES. Phase 3's orchestrator registers agent-session PIDs here. -->

From src/kill-switch/hotkey.ts:
- `function startHotkeyListener(opts: { key?: string; onPanic: () => void; logger }): { stop(): void }` — key from PANIC_HOTKEY env (default "F13"), mapped to UiohookKey; double-tap window constant DOUBLE_TAP_WINDOW_MS = 2000

From src/kill-switch/abort.ts:
- `class AbortRegistry { registerProcess(taskId: string, pid: number): void; registerController(taskId: string, ac: AbortController): void; unregister(taskId: string): void }`
- `function abortActiveWork(registry, frozen: StateSnapshot, logger): Promise<void>` — aborts every registered controller synchronously, then tree-kills every registered pid with SIGKILL; resolves when kill callbacks settle; NEVER awaited by triggerHalt
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Double-tap panic hotkey listener</name>
  <files>src/kill-switch/hotkey.ts, src/kill-switch/hotkey.test.ts, src/main.ts</files>
  <read_first>
    - .planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md (Pattern 4 double-tap debounce example; Standard Stack uiohook-napi row; Common Pitfalls #2 UIPI; Assumption A5)
    - .planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md (D-01, D-02, D-03)
    - src/main.ts (current wiring from plan 01-01 — extend, don't restructure)
    - src/state-machine/halt.ts (triggerHalt signature)
  </read_first>
  <behavior>
    - Test (debounce logic, uiohook mocked via injected event source): first keydown of the panic key arms; second keydown within 2000ms fires onPanic exactly once; second keydown at 2001ms does NOT fire and re-arms instead; after a successful fire the state resets so an immediate third tap does not re-trigger (needs two fresh taps); keydowns of other keys are ignored and do not reset the arm timer
    - Test: startHotkeyListener returns a handle whose stop() detaches the listener
    - Test: unknown PANIC_HOTKEY value falls back to F13 with a pino warning
  </behavior>
  <action>
    Implement src/kill-switch/hotkey.ts per `<interfaces>` and RESEARCH.md Pattern 4: separate the pure debounce state machine (exported as `createDoubleTapDetector(windowMs, onFire)` — unit-testable with fake timestamps, no native dep) from the thin uiohook-napi binding that feeds it real keydown events via `uIOhook.on("keydown")` + `uIOhook.start()`. Key selection: map PANIC_HOTKEY env (default "F13") to UiohookKey members; F13-F24 are ideal because nothing else on Windows binds them (RESEARCH.md note: avoid keys with OS/OBS-wide bindings). onPanic → callers pass `() => triggerHalt(machine, deps, "hotkey")` (D-02 freeze-everything semantics already live in triggerHalt).

    Wire into src/main.ts entrypoint branch (NOT the createApp test factory — native hooks must never load during vitest runs): start the listener at boot, log "panic hotkey armed: <key> (double-tap within 2s)" at info level, and guard the uiohook import behind a try/catch so a missing/broken prebuilt binary degrades to a loud pino error ("PANIC HOTKEY UNAVAILABLE — console Halt button is the only kill path") instead of crashing the process — live-reliability rule: the console halt path must survive hotkey-layer failure. Fallback if uiohook-napi's prebuilt doesn't load on this Windows build: node-global-key-listener is the documented alternative (RESEARCH.md Environment Availability) — do NOT install it preemptively; record the failure in the SUMMARY for a follow-up decision.
  </action>
  <verify>
    <automated>npx vitest run src/kill-switch/hotkey.test.ts && npm run typecheck && grep -c "DOUBLE_TAP_WINDOW_MS = 2000" src/kill-switch/hotkey.ts</automated>
  </verify>
  <acceptance_criteria>
    - Debounce tests pass: single tap never fires; double tap within 2000ms fires once; post-fire reset requires two fresh taps (D-03)
    - The uiohook import lives only in the entrypoint path — `npx vitest run` completes without loading the native module (no uiohook in any test's import graph)
    - main.ts logs the armed-hotkey message and survives (with error log) when the native hook import is forced to throw in a test
  </acceptance_criteria>
  <done>Global panic hotkey armed at boot with accidental-press protection, cleanly separated so tests never touch native code and hook failure never kills the process.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Abort mechanism + synthetic hung task — halt resolves in seconds against a process that refuses to die</name>
  <files>src/kill-switch/abort.ts, src/kill-switch/abort.test.ts, src/state-machine/halt.ts, tests/fixtures/hung-process.cjs, tests/e2e/kill-switch.e2e.test.ts</files>
  <read_first>
    - .planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md (Pattern 2 HALT-priority decoupling; tree-kill Standard Stack row — Windows taskkill /T /F semantics; Open Question 3 synthetic-hung-task recommendation)
    - src/state-machine/halt.ts (the abortActiveWork extension point left by plan 01-01 Task 2)
    - src/shared/types.ts (StateSnapshot.activeTaskPid)
  </read_first>
  <behavior>
    - Test (registry): registerProcess/registerController/unregister bookkeeping; abortActiveWork aborts all registered AbortControllers synchronously and calls tree-kill (mocked) once per registered pid with "SIGKILL"
    - Test (decoupling): with an abortActiveWork stub that never resolves (hung Promise fixture), triggerHalt still returns synchronously with mode already HALTED — assert mode === "HALTED" immediately after the call, and assert triggerHalt's own duration < 100ms
    - Test (e2e, real processes): spawn tests/fixtures/hung-process.cjs as a child; register its pid; triggerHalt; poll process liveness (process.kill(pid, 0) throwing ESRCH = dead); assert (a) mode flipped to HALTED in < 100ms, (b) the hung process is dead within 5000ms, (c) audit_log contains a halt row with source "hotkey" (trigger via the same triggerHalt call path the hotkey uses)
    - Test (abort failure visibility): when tree-kill errors, the error is pino-logged ("abort attempt failed after HALT — task may still be running") and the state remains HALTED — never re-thrown into the halt path
  </behavior>
  <action>
    Implement src/kill-switch/abort.ts per `<interfaces>`: AbortRegistry maps taskId → { pid?, controller? }. abortActiveWork(registry, frozen, logger): first `controller.abort()` every entry (cooperative, synchronous), then for each pid call tree-kill with "SIGKILL" wrapped in a promise (RESEARCH.md: plain child.kill() does not reach Windows process-tree descendants; tree-kill shells taskkill /pid X /T /F). Import tree-kill as the default export; it is CJS — use the ESM default-import interop.

    Update src/state-machine/halt.ts: replace the no-op extension point with the real call — AFTER forceTransition and recordHalt, invoke `void deps.abortActiveWork?.(frozen).catch(err => logger.error(...))`. The function signature stays synchronous; there is no await (Pattern 2: the operator sees HALTED instantly regardless of whether the target dies gracefully).

    Create tests/fixtures/hung-process.cjs: plain CommonJS Node script — `process.on("SIGTERM"/"SIGINT", () => {})` handlers that swallow signals, then `setInterval(() => {}, 1000)` to stay alive indefinitely. This models Phase 3's worst case: a wedged agent session that will not cooperate (PITFALLS.md Pitfall 6: "must not rely on the agent cooperating").

    Write tests/e2e/kill-switch.e2e.test.ts per `<behavior>` using an in-memory DB and a real spawned child. Generous CI-safety margins: liveness poll every 100ms, hard test timeout 10s.
  </action>
  <verify>
    <automated>npx vitest run src/kill-switch/abort.test.ts tests/e2e/kill-switch.e2e.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - e2e proves: HALTED in < 100ms AND signal-ignoring child process-tree dead in < 5s (Success Criterion 3: "halt resolves within seconds even against a synthetic hung task")
    - halt.ts still contains zero `await` in triggerHalt (source assertion — decoupling preserved)
    - Audit row with source "hotkey" present after the e2e halt (COMP-05)
    - tree-kill failures are logged, never thrown; state remains HALTED
    - No orphaned hung-process fixtures remain after the test run (afterEach kills by pid unconditionally)
  </acceptance_criteria>
  <done>The halt is provably effective against uncooperative work: instant state flip, forced process-tree death within seconds, everything audited.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operations runbook, then human-verify the global hotkey fires while other apps have focus</name>
  <files>docs/OPERATIONS.md</files>
  <read_first>
    - .planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md (Common Pitfalls #2 — Windows UIPI blocking global hooks under elevated foreground apps; Assumptions A4/A5)
    - src/kill-switch/hotkey.ts (the configured key + double-tap behavior the runbook documents)
    - .planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md (D-01, D-03, D-04 — recovery flow the runbook summarizes)
  </read_first>
  <action>
    BEFORE pausing for verification, write docs/OPERATIONS.md — the pre-stream operational runbook: (1) UIPI rule: never run OBS (or any app that will hold focus during a live show) elevated/"as Administrator" while the orchestrator runs non-elevated — Windows UIPI silently blocks the global keyboard hook while an elevated window has focus, making the panic hotkey appear intermittently dead; prefer keeping everything non-elevated over elevating the orchestrator (elevating the process that holds API credentials widens the blast radius). (2) Hotkey startup self-test: before every stream, focus a non-elevated window and double-tap the panic key; confirm the console flips to HALTED, then recover via Reset to Idle. (3) PANIC_HOTKEY configuration (default F13; how to change in .env). (4) Halt/recovery quick reference: double-tap = freeze everything (nothing deleted); triage from the console — Resume / Discard Task & Resume / Reset to Idle. (5) Known limitation log section (fill in anything discovered during this checkpoint).

    Then pause for the human verification below.
  </action>
  <what-built>Global panic hotkey (double-tap PANIC_HOTKEY, default F13, within 2s) wired to the HALTED transition — automated tests prove debounce logic and hung-process kill, but only a human can prove the OS-level hook fires while OTHER applications hold focus (the entire point of D-01), and whether UIPI interference exists on this specific machine.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://127.0.0.1:4900 — mode pill shows IDLE
    2. Focus a DIFFERENT application (e.g., Notepad — the console page must NOT have focus)
    3. Double-tap the panic key (default F13; if your keyboard lacks F13, set PANIC_HOTKEY in .env first) within 2 seconds
    4. Expected: the console page now shows the red HALTED banner; terminal logs show the halt with source "hotkey"
    5. Single-tap the key once, wait 3 seconds — expected: nothing happens (double-tap protection)
    6. Optional: focus an elevated app and repeat step 3 — if the hotkey does NOT fire there, that is the documented UIPI limitation; confirm docs/OPERATIONS.md describes it accurately
  </how-to-verify>
  <verify>
    <automated>test -f docs/OPERATIONS.md && grep -ci "elevated" docs/OPERATIONS.md && grep -ci "self-test" docs/OPERATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/OPERATIONS.md exists and covers: UIPI never-run-elevated rule, hotkey startup self-test, PANIC_HOTKEY configuration, halt/recovery quick reference
    - Human confirms: double-tap halts while another app has focus; single tap is inert
    - Any machine-specific hotkey anomaly is recorded in the runbook's limitation log and the SUMMARY
  </acceptance_criteria>
  <done>Runbook written; a human has confirmed the panic hotkey works focus-independently on the actual streaming machine (or the limitation is documented with the console button as the verified fallback).</done>
  <resume-signal>Type "approved" if the hotkey halts from another app's focus, or describe what happened (e.g., "nothing fired while Notepad focused") for diagnosis</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS keyboard input → uiohook native hook | Global input hook across all window states |
| Halt decision → OS process termination | Backend decision, host-level taskkill mechanism |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Denial of Service | Hotkey silently dead under elevated-foreground UIPI | mitigate | docs/OPERATIONS.md pre-stream rule (never run OBS elevated); startup self-test; console Halt button is an independent redundant path (D-01) |
| T-01-13 | Denial of Service | Hung task blocking the halt | mitigate | Pattern 2 decoupling: state flips before abort; tree-kill SIGKILL for signal-ignoring trees; e2e proof with hung-process fixture |
| T-01-14 | Spoofing | Anyone at the physical keyboard can trigger a halt | accept | Physical access to the streaming PC is the operator trust boundary by design; double-tap prevents accidents (D-03), not attackers |
| T-01-15 | Tampering | Hotkey layer crash taking down the orchestrator | mitigate | try/catch around native import; degradation logs loudly and leaves console halt path alive |
| T-01-SC | Tampering (supply chain) | No new package installs this plan | accept | uiohook-napi and tree-kill installed and audited in plan 01-01 (slopcheck OK, prebuilt binaries, no postinstall) |
</threat_model>

<verification>
- `npm test` green including the hung-process e2e
- Human checkpoint passed: hotkey halts while another app has focus; single tap inert
- docs/OPERATIONS.md exists with the UIPI rule and self-test procedure
</verification>

<success_criteria>
- Success Criterion 3 fully satisfied for both reach mechanisms: console button (01-01) + global hotkey (here), halt < 5s against a synthetic hung task, from any state
- D-01, D-02, D-03 implemented and human-verified; abort registry contract ready for Phase 3 agent sessions
</success_criteria>

<output>
Create `.planning/phases/01-compliance-gate-kill-switch/01-03-SUMMARY.md` when done.
</output>
