---
phase: 01-compliance-gate-kill-switch
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src/operator-console/server.ts
  - src/operator-console/public/index.html
  - src/operator-console/public/console.css
  - src/operator-console/public/console.js
  - src/audit/purge.ts
  - src/audit/purge.test.ts
  - src/main.ts
  - tests/invariants/single-funnel.test.ts
  - tests/e2e/console-flows.e2e.test.ts
  - README.md
autonomous: true
requirements: [COMP-01, COMP-04, COMP-05]
must_haves:
  truths:
    - "Escalated items appear in the console's Needs Review section with per-item Approve/Reject; no sound, no toast, no interruption (D-05)"
    - "Approving a held item re-enters it into the candidate pool; rejecting resolves it; both write review_resolved audit rows (D-06)"
    - "Pending reviews expire as expired-unreviewed at process startup and after the TTL, each with an audit row (D-07)"
    - "In HALTED, the console replaces the normal view with the triage panel showing the frozen in-flight task, queue, and prior mode, offering exactly Resume / Discard Task & Resume / Reset to Idle — nothing auto-resumes, no default selection (D-04)"
    - "Every queued task row has a Veto Task button; veto removes the task, records a veto audit row with the triggering input, and offers the optional one-tap reason tag row (D-18, COMP-04)"
    - "The audit page lists every filter decision and veto with the triggering input, filterable by event type and decision (COMP-05); records older than 90 days are purged automatically (D-17)"
    - "An automated invariant test proves no module outside compliance/gate.ts constructs a QueuedTask and no module calls TaskQueue.enqueue except through the pipeline (Success Criterion 1)"
    - "A dev-only submission form drives the full live slice: type text → gate classifies via Sonnet → lands in pool / review / rejected-with-audit — visible in the console"
  artifacts:
    - path: "src/audit/purge.ts"
      provides: "90-day rolling purge — the ONLY DELETE in the codebase (D-17)"
      contains: "DELETE FROM audit_log"
    - path: "tests/invariants/single-funnel.test.ts"
      provides: "Codebase-scan enforcement of the COMP-01 single-funnel invariant"
    - path: "src/operator-console/server.ts"
      provides: "Review/veto/recover/audit/dev-submit routes, all zod-validated"
    - path: "src/operator-console/public/console.js"
      provides: "Four console views per UI-SPEC: status bar, Needs Review, Active Queue, Audit Log + HALTED triage takeover"
  key_links:
    - from: "src/operator-console/public/console.js"
      to: "/api/review/:id/approve and /api/review/:id/reject"
      via: "fetch POST from Needs Review buttons"
      pattern: "api/review"
    - from: "src/operator-console/server.ts"
      to: "src/state-machine/review-queue.ts"
      via: "approve/reject/listPending calls"
      pattern: "reviewQueue|review-queue"
    - from: "src/operator-console/server.ts"
      to: "src/pipeline/submit.ts"
      via: "dev submission route calls submitCandidate — NEVER task-queue directly"
      pattern: "submitCandidate"
    - from: "src/main.ts"
      to: "src/audit/purge.ts"
      via: "purge at startup + unref'd 24h interval"
      pattern: "purgeOldAuditRecords"
    - from: "src/main.ts"
      to: "expireAllPending"
      via: "start-of-session review queue cleanup (D-07)"
      pattern: "expireAllPending"
---

<objective>
Complete the operator console into the full incident-response surface the UI-SPEC contracts: Needs Review queue, HALTED triage takeover, per-task veto with reason tags, filterable audit page, 90-day purge — and close the phase with the automated single-funnel invariant test plus a live end-to-end demo path (dev submission form → Sonnet gate → console).

Purpose: Wires plans 01/02/03 into one demoable vertical slice and delivers the remaining locked decisions (D-04, D-05, D-06, D-07, D-17, D-18). This plan finishes all three phase requirements.
Output: A console where the streamer can review escalations, triage a halt, veto anything, and audit everything — with the COMP-01 invariant machine-checked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md
@.planning/phases/01-compliance-gate-kill-switch/01-UI-SPEC.md
@.planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md
@src/shared/types.ts
@src/operator-console/server.ts
@src/operator-console/public/console.js
@src/state-machine/review-queue.ts
@src/state-machine/halt.ts
@src/queue/pool.ts
@src/queue/task-queue.ts
@src/pipeline/submit.ts
@src/audit/record.ts
@src/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Review queue + veto + triage + reason-tag API routes, and session-lifecycle wiring</name>
  <files>src/operator-console/server.ts, src/main.ts, src/audit/purge.ts, src/audit/purge.test.ts, tests/e2e/console-flows.e2e.test.ts</files>
  <read_first>
    - src/operator-console/server.ts and src/main.ts (current state after plans 01-01/01-03 — extend the existing route/wiring structure, do not rewrite)
    - src/state-machine/review-queue.ts, src/pipeline/submit.ts, src/queue/pool.ts, src/queue/task-queue.ts, src/state-machine/halt.ts (the module contracts to expose over HTTP)
    - .planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md (D-04, D-05, D-06, D-07, D-17, D-18)
    - .planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md (90-Day Rolling Purge code example)
  </read_first>
  <behavior>
    - e2e: POST /api/dev/submit {username:"tester", text:"add a dark-mode toggle"} with a stubbed classifier (deps-injected fake in the test app factory) → 202 {id}; poll GET /api/state until the pool contains the candidate; a held-for-review stub response instead lands the item in GET /api/review pending list
    - e2e: POST /api/review/:id/approve → item leaves pending, appears in pool (D-06); audit list gains a review_resolved row; the ORIGINAL gate_decision audit row is unchanged
    - e2e: POST /api/tasks/:id/veto {reasonTag:"too-big"} → task gone from queue, audit gains veto row containing the task's suggestion text and the reason tag (D-18, COMP-05)
    - e2e: POST /api/halt then GET /api/state shows haltContext.frozen (prior mode, queue ids); POST /api/recover {action:"reset-to-idle"} → mode IDLE; POST /api/recover with an invalid action string → 400
    - Unit (purge): rows older than 90 days deleted, newer rows retained, returns deleted count; purge is driven by AUDIT_RETENTION_DAYS env (default 90)
    - Unit (session start): createApp boots → expireAllPending ran (pre-existing pending review rows from a "previous session" are expired-unreviewed at startup, D-07) and purgeOldAuditRecords ran once
  </behavior>
  <action>
    Extend src/operator-console/server.ts with zod-validated routes (every body/query/param; 400 on failure): GET /api/review (pending items), POST /api/review/:id/approve and /api/review/:id/reject (optional {reasonTag}), POST /api/tasks/:id/veto (optional {reasonTag} — calls TaskQueue.remove then recordVeto with the removed task's text/username/streamMode), POST /api/recover ({action: RecoveryAction, reasonTag?} → halt.recover), POST /api/dev/submit ({username, text, kind?} → builds a SuggestionCandidate with source "operator" and calls submitCandidate — this route exists so the whole slice is demoable before Phase 2 ingestion; comment it as the pattern Phase 2 chat ingestion replaces). Extend GET /api/state to include pool contents, queued tasks, pending review count, and haltContext. Push a fresh ws snapshot on every mutation (reuse the existing "state:changed" push plus explicit pushes after review/veto/submit mutations).

    Create src/audit/purge.ts per the RESEARCH.md code example: `purgeOldAuditRecords(db, retentionDays)` — one DELETE FROM audit_log WHERE created_at_ms < cutoff. This is the codebase's only DELETE statement (append-only exception, D-17).

    Extend src/main.ts wiring: construct the real Anthropic client from ANTHROPIC_API_KEY (classifier deps), CandidatePool, TaskQueue, AbortRegistry, review-queue deps; on startup run expireAllPending(db) (D-07 clean-slate) then purgeOldAuditRecords, and schedule both on unref'd intervals (review expiry sweep every 15 min using REVIEW_TTL_HOURS; purge every 24h). createApp keeps accepting injected fakes (classifier stub for tests — no network in vitest).
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/console-flows.e2e.test.ts src/audit/purge.test.ts && npm run typecheck && grep -c "DELETE FROM audit_log" src/audit/purge.ts | grep -qx 1 && grep -rci "DELETE FROM" src --include=*.ts | grep -qx 1 && echo SINGLE_DELETE_OK</automated>
  </verify>
  <acceptance_criteria>
    - All e2e behaviors pass with the stubbed classifier (no network)
    - Exactly one "DELETE FROM" across src/**/*.ts, located in src/audit/purge.ts
    - Startup sequence proof: a pending review row inserted before createApp boots is expired-unreviewed after boot, with a review_expired audit row
    - Veto audit rows contain suggestion_text (the triggering input — COMP-05 wording) and the reason tag when provided
    - Invalid reasonTag/action values return 400 everywhere (zod)
  </acceptance_criteria>
  <done>Every operator action — review, veto, recover, dev-submit — works over the API with full audit trails and session-lifecycle hygiene (expiry + purge) wired at boot.</done>
</task>

<task type="auto">
  <name>Task 2: Console UI — Needs Review, Active Queue, HALTED triage takeover, Audit Log page</name>
  <files>src/operator-console/public/index.html, src/operator-console/public/console.css, src/operator-console/public/console.js, README.md</files>
  <read_first>
    - .planning/phases/01-compliance-gate-kill-switch/01-UI-SPEC.md (ENTIRE file — screens table, color roles incl. status-pill extension, spacing tokens, typography scale, and the verbatim Copywriting Contract; every string below comes from it)
    - src/operator-console/public/console.js and console.css (current status-bar implementation from plan 01-01 — extend its patterns: ws snapshot rendering, textContent-only rule)
    - src/operator-console/server.ts (the routes from Task 1 this UI consumes)
  </read_first>
  <action>
    Build the four views per UI-SPEC on the existing single page (vanilla JS view-switching off the ws StateSnapshot — no framework, no icons, text labels + status dots only):

    1. **Needs Review**: heading "Needs Review"; per-item card (suggestion text, username, category pill amber #F59E0B for held-for-review, classifier rationale) with "Approve" (accent blue #3B82F6, 44px min target) and "Reject" (destructive red #DC2626, single click, no modal — D-05 low-friction contract) buttons; empty state exactly: heading "Nothing waiting for review" / body "Escalated suggestions land here between rounds. Nothing needs your attention right now."
    2. **Active Queue**: queued tasks (from /api/state) each with a "Veto Task" button (destructive, no modal); pool items listed read-only with approved green pill #22C55E; empty state "Queue is empty" / "Waiting for the next round to start."; include the dev-submit form (labeled "Dev: simulate suggestion" — plain text input + username field) so the live slice is drivable from the page.
    3. **HALTED triage takeover** (D-04): when snapshot.mode === HALTED the normal content is REPLACED by the triage panel — Display-size (28/600) headline on the red banner stripe, the frozen state rendered as three groups (in-flight task, queued tasks, prior mode), and exactly three equally-weighted buttons with NO default/pre-selected option: "Resume" / "Discard Task & Resume" / "Reset to Idle" (accent color, 44px). Invalid-transition error state copy: "Can't transition to {state} from {state}" / "Choose one of the valid recovery actions below."
    4. **Audit Log**: table (Label 14/600 headers; Body 16/400 cells) over GET /api/audit with filter controls for event_type and decision plus a limit selector; decision pills per the status-only color extension (approved green / held amber / rejected red); empty-filtered state "No matching records" / "Try widening your date range or clearing a filter."

    After EVERY destructive action (Halt Everything, Veto Task, Reject, Discard Task & Resume) show the optional one-tap reason-tag chip row — ToS risk / boring / too big / gut feeling / other / skip — which fires the follow-up POST with the tag; skipping or ignoring it never blocks anything; the action has already taken effect (D-18, UI-SPEC destructive-confirmation contract: zero modals anywhere on this console).

    Rendering rules carried from plan 01-01: ALL dynamic strings (suggestion text, usernames, rationales) via textContent — suggestion text is attacker-authored (T-01-16); zero innerHTML with dynamic content. Keep the ws reconnect banner ("Console disconnected from orchestrator" per UI-SPEC). Spacing only from the 4/8/16/24/32/48 token set; fonts 14/16/20/28 at 400/600 only; dark palette #0F172A / #1E293B; accent #3B82F6 ONLY on the UI-SPEC's explicit interactive list; destructive #DC2626 ONLY on its explicit list. Update README with a console tour (four views + triage takeover + dev-submit usage).
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && grep -c "innerHTML" src/operator-console/public/console.js | grep -qx 0 && grep -c "Nothing waiting for review" src/operator-console/public/console.js && grep -c "Discard Task & Resume" src/operator-console/public/console.js</automated>
  </verify>
  <acceptance_criteria>
    - Zero innerHTML occurrences in console.js (stored-XSS gate)
    - Verbatim UI-SPEC copy present (grep-checkable): "Halt Everything", "Veto Task", "Nothing waiting for review", "Queue is empty", "No matching records", "Discard Task & Resume", "Reset to Idle"
    - Triage view renders exactly three recovery buttons and no default selection attribute/class
    - Reason-tag row appears after destructive actions and is skippable (manual spot-check documented in SUMMARY; automated e2e covers the API side)
    - No modal/confirm() anywhere: `grep -c "confirm(" src/operator-console/public/console.js` returns 0
    - Manual run-through: `npm run dev`, submit "make a slot machine simulator with play money" via the dev form (with ANTHROPIC_API_KEY set) → item appears in Needs Review; approve it → appears in pool; audit page shows gate_decision + review_resolved rows
  </acceptance_criteria>
  <done>The operator console is the complete Phase-1 incident surface: between-rounds review, one-click veto, halt triage, and after-the-fact audit review — matching the approved UI contract pixel-rules and copy.</done>
</task>

<task type="auto">
  <name>Task 3: Single-funnel invariant test + phase closeout verification</name>
  <files>tests/invariants/single-funnel.test.ts</files>
  <read_first>
    - .planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md (Pattern 1 — branded type honest caveat: the brand is greppable, not unbypassable; this test IS the automated half of Success Criterion 1's code review)
    - src/compliance/gate.ts, src/queue/task-queue.ts, src/pipeline/submit.ts (the funnel trio the invariant protects)
  </read_first>
  <behavior>
    - Test: recursively read every .ts file under src/ (fs walk, skip .test.ts), strip line and block comments, then assert: (a) the token `as QueuedTask` appears in exactly one file, src/compliance/gate.ts, exactly once; (b) `.enqueue(` is called only in files within src/pipeline/ or src/queue/ own tests — no ingestion/console/state-machine file calls it directly; (c) no file outside src/compliance/ imports from "@anthropic-ai/sdk" (the classifier boundary); (d) `toQueuedTask` is referenced outside gate.ts only from src/pipeline/submit.ts
    - Test failure messages name the offending file+line so future phases get actionable review feedback
  </behavior>
  <action>
    Implement tests/invariants/single-funnel.test.ts as a source-scanning vitest suite per `<behavior>` — comment-stripping before matching (grep-gate hygiene: prose in comments must not satisfy or violate the invariant). This is the machine-enforced half of Success Criterion 1 ("code review confirms no component can enqueue any other way"); it runs in every future `npm test`, so Phase 2's chat ingestion and Phase 4's paid paths inherit the check automatically. Document the invariant + its known limit (a malicious `as QueuedTask` is compile-time-only fiction; the test catches it, TypeScript alone would not) in a header comment.

    Then run the full phase closeout: `npm test` (all plans' suites including e2e), `npm run typecheck`, `npm run lint`, and — with ANTHROPIC_API_KEY available — `npm run gate:eval` one final time, capturing the eval report summary into the SUMMARY.md for the phase verifier.
  </action>
  <verify>
    <automated>npx vitest run tests/invariants/single-funnel.test.ts && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Invariant test passes against the real codebase AND fails when a sabotage fixture is introduced (prove once locally: add `as QueuedTask` to a scratch file under src/, watch the test fail, remove it — record the result in the SUMMARY)
    - Full `npm test` green; typecheck and lint clean
    - Final `npm run gate:eval` result (0 safety fails) or the missing-key follow-up recorded in SUMMARY
  </acceptance_criteria>
  <done>Success Criterion 1 is enforced by a permanent automated check; the whole phase's test surface is green in one command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Attacker-authored suggestion text → console DOM | Stored text rendered in the operator's browser |
| Operator browser → mutation routes (veto/approve/recover/submit) | Kill-switch and review authority over HTTP |
| src/** → build queue | The COMP-01 funnel, now machine-checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Elevation of Privilege (stored XSS) | console.js rendering suggestion text/rationales | mitigate | textContent-only rendering; grep gate innerHTML == 0; suggestion text is adversarial by design |
| T-01-17 | Elevation of Privilege | Review approve as a gate bypass | mitigate | Approval only resolves items the GATE already routed to held-for-review (one of 3 gray-zone categories); approved items re-enter the pool, not the queue directly; audit row records the operator resolution (D-06) |
| T-01-18 | Tampering | /api/dev/submit skipping the gate | mitigate | Route calls submitCandidate → classify(); invariant test forbids direct enqueue/`as QueuedTask` anywhere else; route is localhost-only like the rest of the console |
| T-01-19 | Repudiation | Vetoes without triggering input | mitigate | recordVeto requires suggestion_text of the removed task; e2e asserts it (COMP-05) |
| T-01-20 | Tampering | Purge as an audit-destruction vector | mitigate | Purge deletes strictly by created_at_ms < 90-day cutoff (D-17 retention design); only DELETE in codebase, count-asserted by grep gate |
| T-01-SC | Tampering (supply chain) | No new package installs this plan | accept | No installs; dependency set unchanged since plan 01-01's audited install |
</threat_model>

<verification>
- Full suite: `npm test && npm run typecheck && npm run lint` — green
- Live slice demo: dev-submit a gray-zone suggestion → Needs Review → approve → pool → veto from queue view → audit page shows the entire trail
- Invariant test present and proven sensitive (sabotage check documented)
</verification>

<success_criteria>
- Phase Success Criteria 1 (machine-checked funnel), 3 (console veto from any state), and 4 (every decision + veto logged and reviewable on the audit page) all demonstrably TRUE
- D-04, D-05, D-06, D-07, D-17, D-18 implemented per UI-SPEC contract copy
- COMP-01, COMP-04, COMP-05 complete for Phase 1 scope
</success_criteria>

<output>
Create `.planning/phases/01-compliance-gate-kill-switch/01-04-SUMMARY.md` when done.
</output>
