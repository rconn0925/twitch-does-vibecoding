---
phase: 01-compliance-gate-kill-switch
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/compliance/gate.ts
  - src/queue/pool.ts
  - src/queue/task-queue.ts
  - src/queue/task-queue.test.ts
  - src/state-machine/review-queue.ts
  - src/state-machine/review-queue.test.ts
  - src/pipeline/submit.ts
  - src/pipeline/submit.test.ts
  - src/audit/record.ts
  - scripts/gate-eval.ts
  - package.json
autonomous: true
requirements: [COMP-01]
user_setup:
  - service: anthropic
    why: "The gate-eval runner (Task 3) exercises the gate against live Sonnet (D-09). Unlike the Agent SDK's plan-credit billing, the raw Messages API always requires a metered API key — every fixture evaluated costs one Sonnet call (RESEARCH.md Environment Availability)."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console (console.anthropic.com) -> API Keys -> Create Key; put it in .env (never committed)"
must_haves:
  truths:
    - "classify() returns exactly one of approved / rejected(category) / held-for-review for any SuggestionCandidate (D-08)"
    - "The string 'as QueuedTask' appears exactly once in src/, inside src/compliance/gate.ts — no other module can construct a QueuedTask (COMP-01 single funnel)"
    - "Every gate decision is written to audit_log with the triggering suggestion text and username before classify() returns (COMP-05, D-16)"
    - "submitCandidate returns immediately; classification runs in the background and routes approved → pool, held → review_queue, rejected → audit only (D-10)"
    - "While HALTED, submitCandidate refuses new candidates with a distinct refusal result and an audit row — nothing is classified, pooled, or queued during a halt (D-02 'stop accepting input')"
    - "Approving a held item reconstructs the full SuggestionCandidate from the review_queue row and re-enters it into the candidate pool (D-06); unresolved items expire as expired-unreviewed (D-07)"
    - "gate-eval proves the live Sonnet gate against the full fixture suite: any wrongly-approved fixture is a SAFETY FAIL (exit 1)"
  artifacts:
    - path: "src/compliance/gate.ts"
      provides: "classify() chokepoint + toQueuedTask() — the ONLY sanctioned QueuedTask constructor"
      contains: "toQueuedTask"
    - path: "src/queue/task-queue.ts"
      provides: "Build queue whose enqueue() accepts only the branded QueuedTask type"
    - path: "src/queue/pool.ts"
      provides: "Pre-screened candidate pool rounds will draw from (D-10)"
    - path: "src/state-machine/review-queue.ts"
      provides: "held-for-review workflow: insert/approve/reject/expire against the review_queue table (D-05/06/07)"
    - path: "src/pipeline/submit.ts"
      provides: "submitCandidate — the ONLY intended ingestion entry point, gated on mode !== HALTED (D-02)"
      contains: "HALTED"
    - path: "scripts/gate-eval.ts"
      provides: "Live-Sonnet fixture evaluation runner for Success Criterion 2"
  key_links:
    - from: "src/compliance/gate.ts"
      to: "src/compliance/classifier.ts"
      via: "classifyWithSonnet call after prefilter pass"
      pattern: "classifyWithSonnet"
    - from: "src/compliance/gate.ts"
      to: "src/audit/record.ts"
      via: "recordGateDecision before every return path"
      pattern: "recordGateDecision"
    - from: "src/pipeline/submit.ts"
      to: "src/compliance/gate.ts"
      via: "classify() — the only route from candidate to pool/queue/review"
      pattern: "classify\\("
    - from: "src/pipeline/submit.ts"
      to: "stream-mode machine"
      via: "mode check refusing intake while HALTED (D-02)"
      pattern: "HALTED"
    - from: "src/state-machine/review-queue.ts"
      to: "src/queue/pool.ts"
      via: "approve() reconstructs the candidate from review_queue columns and re-adds to the pool (D-06)"
      pattern: "pool\\.add|CandidatePool"
---

<objective>
Assemble plan 01-02's classification engine into the single compliance chokepoint: classify() + toQueuedTask() (the codebase's only branded-QueuedTask constructor), the pre-screened candidate pool and brand-typed build queue, the held-for-review workflow, the async-on-submission pipeline with HALTED intake refusal, and the live Sonnet evaluation runner.

Purpose: COMP-01 is the project's hard requirement — no code path may enqueue a build task except through this gate. Everything Phase 2/3/4 ingests will call `submitCandidate()` and nothing else. This plan turns 01-02's RED gate.test.ts contract GREEN.
Output: A unit-tested, live-evaluated gate module; a build queue that is type-and-test-provably unreachable except via the gate; an intake pipeline that honors D-02's "stop accepting input" during a halt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md
@.planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md
@src/shared/types.ts
@src/compliance/categories.ts
@src/compliance/schema.ts
@src/compliance/prefilter.ts
@src/compliance/classifier.ts
@src/compliance/gate.test.ts
@src/audit/record.ts
@src/audit/db.ts
@src/audit/schema.sql
@src/state-machine/stream-mode.ts

<interfaces>
<!-- Contracts this plan CREATES; plan 01-04 wires the console against these. -->

From src/compliance/gate.ts:
- `async function classify(deps, candidate: SuggestionCandidate): Promise<GateResult>` — prefilter, then Sonnet, audit every outcome
- `function toQueuedTask(candidate: SuggestionCandidate, result: GateResult): QueuedTask` — throws unless result.decision === "approved"; contains the codebase's ONLY `as QueuedTask`

From src/pipeline/submit.ts (D-10 async-on-submission, D-02 halt gating):
- `function submitCandidate(deps, candidate: SuggestionCandidate): { accepted: true; id: string } | { accepted: false; reason: "halted" }` — when mode !== HALTED: returns { accepted: true, id } immediately, classification runs in the background and routes approved → pool.add, held-for-review → reviewQueue.insert, rejected → audit only. When mode === HALTED: refuses without classifying or pooling, records a submission_refused audit row, returns { accepted: false, reason: "halted" } (D-02 — Phase 2 chat feedback can surface "stream is paused")

From src/queue/pool.ts:
- `class CandidatePool { add(candidate, result): void; list(): ApprovedCandidate[]; remove(id): void }` — pre-screened pool rounds will draw from (D-10)

From src/queue/task-queue.ts:
- `class TaskQueue { enqueue(task: QueuedTask): void; list(): QueuedTask[]; remove(id: string): QueuedTask | undefined }` — parameter type is the brand; no overload accepts SuggestionCandidate

From src/state-machine/review-queue.ts (D-05/06/07):
- `insertHeld(db, candidate, result): number` — persists the FULL candidate identity (candidate_id, source, kind, submitted_at_ms, text, username) into the review_queue columns plan 01-01 created
- `approve(db, deps, reviewId): void` — resolves row to 'approved', records review_resolved audit row, reconstructs the SuggestionCandidate from the row ({ id: candidate_id, source, kind, twitchUsername, text, submittedAtMs }) and re-adds it to CandidatePool (D-06)
- `reject(db, reviewId): void` — resolves row to 'rejected' + audit row
- `expireStale(db, ttlMs): number` and `expireAllPending(db): number` — mark 'expired-unreviewed' + one audit row each (D-07)
- `listPending(db): ReviewItem[]`

From src/audit/record.ts (extended this plan — still insert-only):
- `recordSubmissionRefused(db, { candidate, streamMode })` — event_type "submission_refused"; the D-02 refusal trail
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate chokepoint + branded queue + pool — gate.test.ts goes GREEN</name>
  <files>src/compliance/gate.ts, src/queue/pool.ts, src/queue/task-queue.ts, src/queue/task-queue.test.ts</files>
  <read_first>
    - src/compliance/gate.test.ts (the RED contract from plan 01-02 to turn GREEN), src/compliance/prefilter.ts, src/compliance/classifier.ts
    - src/shared/types.ts (QueuedTask brand), src/audit/record.ts
    - .planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md (Pattern 1 single-funnel branded type incl. the honest caveat; architecture diagram)
  </read_first>
  <behavior>
    - Test: entire fixture suite passes through classify() with the fake classifier (gate.test.ts from plan 01-02 goes GREEN)
    - Test: classify() calls recordGateDecision exactly once per invocation, on every path (prefilter reject, classifier reject, approve, hold, fail-closed) — audit-before-return
    - Test: toQueuedTask() throws for decision rejected and held-for-review; returns a QueuedTask for approved
    - Test: TaskQueue.enqueue only compiles with a QueuedTask (type-level: a @ts-expect-error line passing a raw SuggestionCandidate must satisfy tsc)
  </behavior>
  <action>
    Implement src/compliance/gate.ts per `<interfaces>`: classify() runs prefilterCheck → early rejected return (audited) → classifyWithSonnet → D-12 coercion already handled in classifier → audit → return. toQueuedTask() contains the codebase's single sanctioned `as QueuedTask` assertion, with the comment "single-funnel invariant — enforced by tests/invariants/single-funnel.test.ts". Both functions take deps ({ db, prefilter, classifier, logger, streamModeProvider }) so tests inject fakes and Phase 2/3 callers share one wired instance.

    Implement src/queue/pool.ts and src/queue/task-queue.ts per `<interfaces>` (in-memory for Phase 1 — durable queue persistence is a later-phase concern; the audit ledger already persists the compliance record). task-queue.ts must not import anything from compliance/ (it only consumes the branded type from shared/types.ts) and must have no method accepting SuggestionCandidate. task-queue.test.ts includes the @ts-expect-error compile-level proof that enqueue(rawCandidate) is rejected by tsc.
  </action>
  <verify>
    <automated>npx vitest run src/compliance/gate.test.ts src/queue && npm run typecheck && grep -rn "as QueuedTask" src --include=*.ts | grep -cv "compliance/gate.ts" | grep -qx 0 && grep -c "as QueuedTask" src/compliance/gate.ts | grep -qx 1 && echo SINGLE_FUNNEL_OK</automated>
  </verify>
  <acceptance_criteria>
    - gate.test.ts fully GREEN including every fixture via the fake classifier; fail-closed fixture proves rejected/classifier-unavailable
    - `npm run typecheck` now passes project-wide (the TS2307 that plan 01-02 left in gate.test.ts is resolved by gate.ts existing)
    - `grep -rn "as QueuedTask" src --include=*.ts` shows exactly one hit, in src/compliance/gate.ts
    - task-queue.test.ts contains a passing @ts-expect-error proving enqueue(rawCandidate) is a compile error
  </acceptance_criteria>
  <done>A candidate can only become a QueuedTask through classify()+toQueuedTask(); every decision path is audited exactly once; the brand-typed queue is compile-provably unreachable with raw candidates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Review workflow + submission pipeline with D-02 HALTED intake refusal</name>
  <files>src/state-machine/review-queue.ts, src/state-machine/review-queue.test.ts, src/pipeline/submit.ts, src/pipeline/submit.test.ts, src/audit/record.ts</files>
  <read_first>
    - src/audit/schema.sql (review_queue columns incl. candidate_id/source/kind/submitted_at_ms from plan 01-01), src/audit/record.ts, src/audit/db.ts
    - src/state-machine/stream-mode.ts (mode provider for the D-02 gate), src/compliance/gate.ts (classify deps shape from Task 1)
    - .planning/phases/01-compliance-gate-kill-switch/01-CONTEXT.md (D-02, D-05, D-06, D-07, D-10)
  </read_first>
  <behavior>
    - Test: submitCandidate returns { accepted: true, id } synchronously before classification settles (D-10); after settle — approved lands in CandidatePool, held lands in review_queue table with status pending AND full candidate identity columns populated, rejected lands nowhere but audit_log
    - Test (D-02): with the machine forced to HALTED (via triggerHalt), submitCandidate returns { accepted: false, reason: "halted" }; the classifier spy is NEVER called; pool, review_queue, and task queue are unchanged; audit_log gains one submission_refused row carrying the suggestion text, username, and stream_mode HALTED
    - Test (D-02 recovery): after recover(machine, deps, "resume"), submitCandidate accepts again
    - Test: review-queue approve() flips status to approved, inserts a review_resolved audit row, reconstructs the SuggestionCandidate from the row's candidate_id/source/kind/submitted_at_ms/text/username and re-adds it to CandidatePool with its ORIGINAL id and source preserved (D-06); reject() flips + audits; expireAllPending() marks every pending row expired-unreviewed with one review_expired audit row each (D-07); the ORIGINAL gate_decision audit rows are never updated (assert original rows unchanged after resolution — two-table integrity, RESEARCH.md Pitfall 5)
  </behavior>
  <action>
    Extend src/audit/record.ts with ONE new insert-only helper: `recordSubmissionRefused(db, { candidate, streamMode })` writing event_type "submission_refused" with the candidate's text/username/source (COMP-05: refusals are decisions too). Keep the module's grep-gate hygiene: no UPDATE/DELETE words anywhere in the file, comments included.

    Implement src/state-machine/review-queue.ts against the review_queue table from plan 01-01's schema.sql, per `<interfaces>`. insertHeld persists candidate_id, source, kind, submitted_at_ms alongside text/username/category/rationale so approve() can rebuild the exact SuggestionCandidate (D-06 — no lossy reconstruction, no joins). Status transitions: pending → approved | rejected | expired-unreviewed, terminal thereafter. Every resolution INSERTS a new audit_log row (review_resolved / review_expired) via record.ts — never UPDATE audit_log (D-16 integrity). expireStale takes ttlMs computed from REVIEW_TTL_HOURS (default 4h); expireAllPending exists for start-of-session cleanup (D-07: "review queue starts clean each stream night") — plan 01-04 wires it into main.ts startup.

    Implement src/pipeline/submit.ts: submitCandidate validates the candidate shape with a zod schema (untrusted boundary), then checks deps.mode() (a streamModeProvider closure over the StreamModeMachine). If mode === "HALTED": call recordSubmissionRefused, return { accepted: false, reason: "halted" } — no classification, no pooling (D-02 "stop accepting input"; refusal is cheap, synchronous, and audit-visible). Otherwise return { accepted: true, id } immediately, then run classify() on a floating promise with .catch into pino error (the gate itself never throws, but keep the guard). Routing per `<interfaces>`. Note in a module doc comment: (a) this function is the ONLY intended entry point for all future ingestion sources (Phase 2 chat, Phase 4 paid); (b) a classification already in flight when a halt lands is allowed to settle and route normally — the pool is passive pre-screened storage, nothing executes from it while HALTED, and the frozen queue is triaged via D-04.
  </action>
  <verify>
    <automated>npx vitest run src/state-machine/review-queue.test.ts src/pipeline && npm run typecheck && grep -c "HALTED" src/pipeline/submit.ts && grep -vE "^\s*(//|\*|/\*)" src/audit/record.ts | grep -ciE "UPDATE|DELETE" | grep -qx 0 && echo APPEND_ONLY_STILL_OK</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` tests pass, including both D-02 tests (refusal while HALTED with audit row + classifier never called; acceptance resumes after recovery)
    - approve() re-adds the candidate with its ORIGINAL candidate id, source, kind, and submittedAtMs (test asserts field-level equality against the pre-hold candidate)
    - review_queue rows resolve without any UPDATE to audit_log (test asserts original gate_decision rows byte-identical after approve/reject/expire)
    - record.ts still passes the comment-filtered UPDATE/DELETE grep gate after the recordSubmissionRefused addition
  </acceptance_criteria>
  <done>Held items flow through a lossless review workflow; the intake pipeline is the single ingestion entry point, is async-on-submission, and provably stops accepting input while HALTED (D-02) with a full audit trail.</done>
</task>

<task type="auto">
  <name>Task 3: Live Sonnet gate-eval runner (Success Criterion 2)</name>
  <files>scripts/gate-eval.ts, package.json</files>
  <read_first>
    - src/compliance/gate.ts, src/compliance/fixtures/ (all three fixture files), src/compliance/classifier.ts
    - .planning/phases/01-compliance-gate-kill-switch/01-RESEARCH.md (Environment Availability — ANTHROPIC_API_KEY is metered, hard requirement for live eval)
  </read_first>
  <action>
    Implement scripts/gate-eval.ts (add package.json script `gate:eval` = `tsx scripts/gate-eval.ts`): loads ALL fixtures, runs each through the REAL gate with a live Anthropic client (concurrency 4 via a tiny promise pool — do not add p-queue this phase), prints a per-category table (fixture id, expected, actual, PASS/FAIL) and a summary. Exit rules: exit 2 with a clear "ANTHROPIC_API_KEY not set — live eval skipped" message when the key is missing; exit 1 (SAFETY FAIL) if ANY fixture whose expected decision is not approved receives approved, or if the canonical play-money-gambling fixture does not come back held-for-review; exit 0 otherwise, reporting over-rejections (expected approved, got rejected) as warnings only — lean-reject tolerance per D-12, but list them for prompt tuning. The executor should run the live eval once and iterate the classifier system prompt (in src/compliance/classifier.ts, plan 01-02's module) if it safety-fails; audit rows written during eval go to a throwaway DB path (data/eval.db), not the operator DB.
  </action>
  <verify>
    <automated>npx vitest run src/compliance src/queue src/pipeline src/state-machine/review-queue.test.ts && npm run typecheck && npm run lint && { npm run gate:eval; ev=$?; if [ $ev -eq 0 ]; then echo GATE_EVAL_PASS; elif [ $ev -eq 2 ]; then echo GATE_EVAL_SKIPPED_NO_KEY; else exit 1; fi; }</automated>
  </verify>
  <acceptance_criteria>
    - Full unit suite for compliance/queue/pipeline/review-queue GREEN; typecheck and lint clean
    - `npm run gate:eval` handled OUTSIDE any && chain: exit 0 (zero SAFETY FAILs across all 13 taxonomy categories + adversarial + feasibility fixtures, Success Criterion 2) prints GATE_EVAL_PASS; exit 2 (no ANTHROPIC_API_KEY in the execution environment) prints GATE_EVAL_SKIPPED_NO_KEY and is an ACCEPTABLE verify outcome — the executor must surface it as a user follow-up in the SUMMARY, since the phase is not verifiable-complete until a live eval has passed; exit 1 (safety fail) fails the verify and requires prompt iteration
    - Play-money gambling fixture comes back held-for-review in a passing live eval
  </acceptance_criteria>
  <done>The live Sonnet gate is evaluated against the full adversarial fixture suite with an explicit safety-fail bar, or the missing-key path is cleanly surfaced as a follow-up instead of a false failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Viewer suggestion text → submitCandidate → gate | Adversarial-by-design untrusted input entering the single funnel |
| Gate → build queue | The single-funnel invariant COMP-01 protects |
| gate-eval → Anthropic Messages API | Live metered calls carrying fixture text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Elevation of Privilege | Any module constructing QueuedTask outside gate.ts | mitigate | Branded unique-symbol type + exactly-one `as QueuedTask` grep/test gate + enqueue() brand-typed parameter with @ts-expect-error proof; full invariant test in plan 01-04; code review per Success Criterion 1 (brand is compile-time only — RESEARCH.md Pattern 1 caveat documented) |
| T-01-09 | Repudiation | Gate decisions or refusals unlogged | mitigate | recordGateDecision on every classify() return path incl. prefilter and fail-closed (test counts exactly one audit call); recordSubmissionRefused on every D-02 refusal |
| T-01-11 | Denial of Service | Suggestion flood burning metered Sonnet calls | accept (this phase) | Prefilter absorbs obvious junk free; D-02 halt refusal cuts intake to zero cost during incidents; per-user rate limits are Phase 2 ingestion concerns (no real input source exists yet); flagged for Phase 2 |
| T-01-21 | Tampering | Input accepted while HALTED (violating D-02 freeze semantics) | mitigate | submitCandidate checks mode before any classification/pooling; refusal returns a distinct result and writes a submission_refused audit row; tests prove classifier is never invoked and state is unchanged during a halt |
| T-01-SC | Tampering (supply chain) | No new package installs this plan | accept | All dependencies already installed and audited in plan 01-01 |
</threat_model>

<verification>
- `npm test` fully green (incl. gate.test.ts now GREEN); `npm run typecheck` clean project-wide
- Single-funnel grep: exactly one `as QueuedTask` in src, inside compliance/gate.ts
- D-02: submitCandidate refuses with audit trail while HALTED; accepts after recovery
- `npm run gate:eval` (with key): 0 SAFETY FAILs; play-money gambling fixture held-for-review; report attached to SUMMARY — or exit-2 missing-key follow-up recorded in SUMMARY
</verification>

<success_criteria>
- COMP-01 structurally enforced: one gate function, one brand assertion, brand-typed queue, one ingestion entry point
- D-02 (input freeze), D-05/06/07 (review lifecycle), D-08/10 (decision vocabulary + async submission) all implemented and test-asserted
- Success Criterion 2 satisfied by the live eval (or explicitly surfaced as the sole remaining follow-up if the key is unavailable)
</success_criteria>

<output>
Create `.planning/phases/01-compliance-gate-kill-switch/01-05-SUMMARY.md` when done.
</output>
