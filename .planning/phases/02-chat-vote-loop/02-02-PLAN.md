---
phase: 02-chat-vote-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/ingestion/command-parser.ts
  - src/ingestion/command-parser.test.ts
  - src/ingestion/chat-sender.ts
  - src/ingestion/chat-sender.test.ts
  - src/ingestion/twitch-auth.ts
  - src/ingestion/twitch-auth.test.ts
  - tests/invariants/scan-helpers.ts
  - tests/invariants/chat-sender.test.ts
autonomous: true
requirements: [INFRA-01]
user_setup:
  - service: twitch
    why: "OAuth app registration for chat read/write (EventSub + Helix sendChatMessage)"
    env_vars:
      - name: TWITCH_CLIENT_ID
        source: "dev.twitch.tv/console → Register Your Application → Client ID"
      - name: TWITCH_CLIENT_SECRET
        source: "dev.twitch.tv/console → your app → New Secret"
      - name: TWITCH_BROADCASTER_USER_ID
        source: "Broadcaster's numeric Twitch user id (username→id lookup)"
    dashboard_config:
      - task: "Register an application with OAuth Redirect URL http://localhost:4900/auth/callback, category Chat Bot"
        location: "dev.twitch.tv/console/apps"
must_haves:
  truths:
    - "!suggest and !vote messages parse into typed commands; anything else (including malformed variants) parses to null with no throw (D2-15 silence rule)"
    - "Every outbound chat message flows through one rate-limited sender queue budgeted safely under the verified 100 msgs/30s broadcaster tier (D2-08); a source scan fails the build if any other module calls sendChatMessage"
    - "Twitch tokens persist across process restarts via RefreshingAuthProvider + a token file, and refreshed tokens are re-persisted automatically (INFRA-01)"
    - "Token values never appear in logs"
  artifacts:
    - path: "src/ingestion/command-parser.ts"
      provides: "zod-validated !suggest/!vote parsing (pure function, no I/O)"
      exports: ["parseCommand"]
    - path: "src/ingestion/chat-sender.ts"
      provides: "p-queue-backed sole sendChatMessage caller (D2-08)"
      exports: ["createChatSender"]
    - path: "src/ingestion/twitch-auth.ts"
      provides: "RefreshingAuthProvider factory + token-file persistence + OAuth URL/exchange helpers (INFRA-01, D2-09/D2-10)"
      exports: ["createAuthProvider", "buildAuthorizeUrl", "completeAuthorization", "TWITCH_SCOPES"]
    - path: "tests/invariants/chat-sender.test.ts"
      provides: "source-scan invariant: sendChatMessage referenced only in src/ingestion/chat-sender.ts"
  key_links:
    - from: "src/ingestion/chat-sender.ts"
      to: "p-queue"
      via: "PQueue({ concurrency: 1, intervalCap, interval, strict: true })"
      pattern: "intervalCap"
    - from: "src/ingestion/twitch-auth.ts"
      to: "@twurple/auth"
      via: "RefreshingAuthProvider + exchangeCode + onRefresh persistence callback"
      pattern: "onRefresh"
---

<objective>
Install the audited Twitch client stack and build the three foundation modules chat ingestion needs: the zod-validated command parser, the single rate-limited chat sender (with its own machine-checked sole-caller invariant), and the persisted auto-refreshing token/auth provider.

Purpose: These are pure, injectable, network-free-under-test building blocks. Plan 02-04 wires them to a live EventSub listener; keeping them separate means every behavior here is unit-provable before any Twitch account exists.
Output: @twurple/* 8.1.4 + p-queue 9.3.1 installed; parser, sender, auth modules with full unit coverage; chat-sender source-scan invariant.
</objective>

## Phase Goal

**As a** Twitch viewer on stream night, **I want to** suggest ideas with `!suggest` and vote in timed rounds while watching the live tally on the stream overlay, **so that** chat genuinely picks what gets built next — with every suggestion still passing the compliance gate.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-chat-vote-loop/02-CONTEXT.md
@.planning/phases/02-chat-vote-loop/02-RESEARCH.md
@.planning/phases/02-chat-vote-loop/02-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES. Plan 02-04 wires these to the live listener. -->

From src/ingestion/command-parser.ts:
- `function parseCommand(messageText: string): { kind: "suggest"; text: string } | { kind: "vote"; option: number } | null`

From src/ingestion/chat-sender.ts:
- `interface ChatMessageSink { sendChatMessage(broadcasterId: string, text: string): Promise<unknown> }`
  (structural subset of twurple's ApiClient.chat — the REAL ApiClient satisfies it; tests inject a fake)
- `interface ChatSenderDeps { sink: ChatMessageSink; broadcasterId: string; logger?: Logger; intervalCap?: number; intervalMs?: number }`
- `interface ChatSender { send(text: string): Promise<void>; readonly pending: number }`
- `function createChatSender(deps: ChatSenderDeps): ChatSender`

From src/ingestion/twitch-auth.ts:
- `const TWITCH_SCOPES = ["user:read:chat", "user:write:chat"] as const`  // D2-09/D2-10: user access token acting as itself — user:bot/channel:bot NOT needed (RESEARCH.md Pattern 2)
- `interface TwitchAuthDeps { clientId: string; clientSecret: string; tokenPath: string; logger?: Logger }`
- `function createAuthProvider(deps: TwitchAuthDeps): RefreshingAuthProvider | null`  // null when no persisted token exists yet (bootstrap required)
- `function buildAuthorizeUrl(args: { clientId: string; redirectUri: string; state: string }): string`
- `async function completeAuthorization(deps: TwitchAuthDeps, code: string, redirectUri: string): Promise<void>`  // exchangeCode + persist + register on provider

From tests/invariants/scan-helpers.ts (adapted from single-funnel.test.ts's private helpers — that
file is owned by plan 02-03 this phase; do NOT edit it here):
- `function stripComments(source: string): string`
- `interface ScannedFile { rel: string; stripped: string }`
- `function collectFiles(rootAbs: string, opts?: { extensions?: string[]; includeTests?: boolean }): ScannedFile[]`
- `function allMatches(files: ScannedFile[], pattern: RegExp): Map<string, string[]>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install the audited Twitch stack and build the command parser</name>
  <files>package.json, package-lock.json, src/ingestion/command-parser.ts, src/ingestion/command-parser.test.ts</files>
  <read_first>
    - .planning/phases/02-chat-vote-loop/02-RESEARCH.md (Package Legitimacy Audit table; Code Examples: command-parser.ts — use as the starting point)
    - src/compliance/prefilter.ts (structural analog: pure fn, regex-first, discriminated result, never throws on no-match)
    - package.json (allowScripts block — the four new packages have no install scripts, nothing to add there)
  </read_first>
  <behavior>
    - Test: parseCommand("!suggest build a snake game") → { kind: "suggest", text: "build a snake game" }; case-insensitive ("!SUGGEST x" works); surrounding whitespace trimmed
    - Test: parseCommand("!vote 2") → { kind: "vote", option: 2 }; only 1-3 accepted — "!vote 4", "!vote 0", "!vote abc", "!vote" all → null
    - Test: parseCommand("!suggest") (no text) → null; a 2001-char suggestion body → null (max 2000, matching submitCandidate's CandidateSchema bound)
    - Test: plain chat ("hello"), other commands ("!lurk"), and empty string → null; no input ever throws (fuzz a few hostile strings: nulls, emoji, RTL marks)
  </behavior>
  <action>
    Run `npm install @twurple/auth@8.1.4 @twurple/api@8.1.4 @twurple/eventsub-ws@8.1.4 p-queue@9.3.1` — exact versions per RESEARCH.md's live-registry verification; all three @twurple/* packages MUST stay lockstep on the same minor (CLAUDE.md compatibility table). All four packages passed the Package Legitimacy Audit ([OK] slopcheck, zero postinstall scripts) — no legitimacy checkpoint required. If more than 2 weeks have passed since 2026-07-09, re-run `npm view @twurple/api version` first and install whatever the current lockstep 8.1.x is.

    Implement src/ingestion/command-parser.ts starting from the RESEARCH.md Code Example verbatim (zod schemas SuggestCommand/VoteCommand; regex-first extraction with `/^!suggest\s+(.+)$/i` and `/^!vote\s+([1-3])$/i`; return null for anything unrecognized — no feedback for invalid votes per D2-15's chat-noise rule). Two deltas from the example: (1) use zod safeParse, not parse, so this module NEVER throws on any input string — a malformed chat message must never crash the listener (fail-closed-to-null, per the project's never-throw-into-caller pattern); (2) cap suggestion text at 2000 chars (mirror of submitCandidate's CandidateSchema.text bound).
  </action>
  <verify>
    <automated>npx vitest run src/ingestion/command-parser.test.ts && node -e "const p=require('./package.json'); const v=['@twurple/auth','@twurple/api','@twurple/eventsub-ws'].map(k=>p.dependencies[k]); if(new Set(v).size!==1) throw new Error('twurple not lockstep: '+v); console.log('LOCKSTEP_OK',v[0],'p-queue',p.dependencies['p-queue'])"</automated>
  </verify>
  <acceptance_criteria>
    - `npm install` succeeds; package.json lists @twurple/auth, @twurple/api, @twurple/eventsub-ws at the identical 8.1.x version and p-queue at 9.x
    - All `<behavior>` parser tests pass, including the never-throws fuzz cases
    - `grep -c "safeParse" src/ingestion/command-parser.ts` returns >= 1 (no throwing parse at this boundary)
    - command-parser.ts imports nothing except zod (pure transform — no twurple, no db, no logger)
  </acceptance_criteria>
  <done>Twitch client stack installed lockstep; !suggest/!vote parsing is total (never throws) and fully unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rate-limited chat sender + sole-caller source-scan invariant</name>
  <files>src/ingestion/chat-sender.ts, src/ingestion/chat-sender.test.ts, tests/invariants/scan-helpers.ts, tests/invariants/chat-sender.test.ts</files>
  <read_first>
    - .planning/phases/02-chat-vote-loop/02-RESEARCH.md (Pattern 4: p-queue sender; verified 100/30s broadcaster tier — budget 15/30s)
    - tests/invariants/single-funnel.test.ts (stripComments/scanSources/allMatches helpers to ADAPT into scan-helpers.ts — do not edit single-funnel.test.ts itself; plan 02-03 owns it this phase)
    - src/queue/task-queue.ts (narrow-class + doc-comment sole-writer discipline to copy)
  </read_first>
  <behavior>
    - Test: createChatSender with a fake sink — send("hi") resolves after the sink was called once with (broadcasterId, "hi"); messages send in FIFO order
    - Test (fake timers): with intervalCap 2 / intervalMs 1000, three rapid send() calls deliver two immediately and the third only after the interval elapses (rate budget enforced)
    - Test: a sink that rejects does NOT propagate — send() resolves, the failure is logged (fail-closed: a failed chat message never throws into round-narration callers)
    - Test: messages longer than 500 chars are truncated to 500 before the sink is called (Twitch hard cap — UI-SPEC narration contract)
    - Test (invariant scan): tests/invariants/chat-sender.test.ts fails if a scratch file under src/ outside src/ingestion/chat-sender.ts contains `sendChatMessage(` (self-test via a temp fixture or by asserting the current tree passes with exactly one sanctioned location)
  </behavior>
  <action>
    Implement src/ingestion/chat-sender.ts per RESEARCH.md Pattern 4: a PQueue({ concurrency: 1, intervalCap: deps.intervalCap ?? 15, interval: deps.intervalMs ?? 30_000, strict: true }) wrapping every call to deps.sink.sendChatMessage(deps.broadcasterId, text). Defaults come from env in the composition root (CHAT_SEND_INTERVAL_CAP / CHAT_SEND_INTERVAL_MS, added to .env.example by plan 02-01) — this module takes plain numbers, no process.env reads. 15/30s leaves wide margin under the verified 100/30s broadcaster tier (RESEARCH.md Pattern 2). Truncate text to 500 chars before sending. Catch sink rejections inside the queued task: log via deps.logger, resolve normally (never-throw-into-caller pattern from gate.ts's classify). Expose `pending` (queue size + pending count) for observability. Doc comment at top: this file is the ONLY module allowed to reference sendChatMessage — enforced by tests/invariants/chat-sender.test.ts (D2-08).

    Create tests/invariants/scan-helpers.ts: extract/adapt stripComments, the recursive source collector (parameterized: root dir, extensions filter, include/exclude *.test.ts), and allMatches from tests/invariants/single-funnel.test.ts into exported helpers per this plan's `<interfaces>` block. Do NOT modify single-funnel.test.ts (plan 02-03 edits its allowlists; two plans must not touch the same file in the same wave).

    Create tests/invariants/chat-sender.test.ts using scan-helpers: scan all non-test .ts files under src/ and assert `sendChatMessage` (regex /sendChatMessage/) appears ONLY in src/ingestion/chat-sender.ts, and `@twurple/api` is imported only from src/ingestion/ or src/main.ts (composition root constructs the real ApiClient). Failure messages name file:line like the single-funnel test does.
  </action>
  <verify>
    <automated>npx vitest run src/ingestion/chat-sender.test.ts tests/invariants/chat-sender.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` tests pass, including the fake-timer rate-budget case and the 500-char truncation case
    - `grep -c "strict: true" src/ingestion/chat-sender.ts` returns 1 (p-queue window-boundary burst gotcha closed)
    - chat-sender.ts never throws from send(): test proves a rejecting sink still resolves
    - tests/invariants/chat-sender.test.ts passes against the current tree and its scan covers every non-test src/**/*.ts file
    - scan-helpers.ts exports stripComments, collectFiles, allMatches (reused by plan 02-05's DOM-safety scan)
  </acceptance_criteria>
  <done>One sanctioned, rate-budgeted path to Twitch chat exists and the build fails if anyone bypasses it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persisted auto-refreshing Twitch auth (token file + RefreshingAuthProvider)</name>
  <files>src/ingestion/twitch-auth.ts, src/ingestion/twitch-auth.test.ts</files>
  <read_first>
    - .planning/phases/02-chat-vote-loop/02-RESEARCH.md (Pattern 3: verified exchangeCode signature, AccessToken interface, RefreshingAuthProvider onRefresh/onRefreshFailure wiring — this is the authoritative source; no in-repo OAuth precedent exists)
    - src/audit/db.ts (bootstrap-and-persist factory idiom: setup-or-create, return ready handle)
    - src/main.ts armPanicHotkey (graceful-degradation pattern: init failure logs loudly, returns null, never crashes)
  </read_first>
  <behavior>
    - Test: createAuthProvider with a tokenPath pointing at a valid persisted AccessToken JSON returns a provider with the broadcaster user registered; with a missing/corrupt file returns null (bootstrap needed) without throwing
    - Test: buildAuthorizeUrl produces `https://id.twitch.tv/oauth2/authorize` with query params client_id, redirect_uri, response_type=code, scope=user:read:chat+user:write:chat (space-joined, URL-encoded), and the passed state value
    - Test: completeAuthorization persists the exchanged AccessToken JSON to tokenPath (inject the exchange function — tests never hit the network) and a subsequent createAuthProvider call returns a non-null provider
    - Test: the onRefresh callback writes the NEW token data to tokenPath (simulate by invoking the registered callback directly or via an injected provider factory)
    - Test: no log line produced by this module ever contains an accessToken or refreshToken value (assert on a capturing fake logger)
  </behavior>
  <action>
    Implement src/ingestion/twitch-auth.ts per RESEARCH.md Pattern 3 (verified API shapes). Token persistence: a JSON file at deps.tokenPath (default wired from TWITCH_TOKEN_PATH=./data/twitch-token.json — inside the already-gitignored data/ directory) holding the twurple AccessToken shape { accessToken, refreshToken, scope, expiresIn, obtainmentTimestamp } plus userId. D2-09 grants discretion between SQLite table and token file — token file chosen deliberately: keeps this plan's files disjoint from plan 02-01's schema.sql (wave-1 parallelism) and tokens are one mutable record, not append-only audit data. Write the file with mode 0o600 where the platform honors it and note in a comment that on Windows ACLs apply instead.

    createAuthProvider: read tokenPath; on missing/corrupt file log a warning ("no persisted Twitch token — run /auth/start to authorize") and return null (graceful degradation, armPanicHotkey pattern — the app must run without Twitch). Otherwise construct `new RefreshingAuthProvider({ clientId, clientSecret })`, wire `onRefresh((userId, newTokenData) => persist to tokenPath)` and `onRefreshFailure((userId, error) => logger.error — "Twitch token refresh failed; bot is going deaf — re-authorize at /auth/start")`, then `addUser(userId, persistedTokenData, ["chat"])` per the twurple docs example. For unit tests, accept an injectable provider-constructor seam (deps `makeProvider?`) so tests exercise persistence wiring without twurple internals.

    buildAuthorizeUrl: pure string builder over https://id.twitch.tv/oauth2/authorize with TWITCH_SCOPES and the caller-supplied state nonce (the state is generated/validated by plan 02-04's console routes — login-CSRF defense, T-02-16 in plan 02-04's threat register).

    completeAuthorization: call exchangeCode(clientId, clientSecret, code, redirectUri) (injectable as deps `exchange?` for tests; default the real @twurple/auth export), persist the returned AccessToken + userId to tokenPath, then register on the provider via addUserForToken(tokenData, ["chat"]).

    Never log token material: log userId, scope list, obtainmentTimestamp only. This module is the ONLY place that reads/writes tokenPath.
  </action>
  <verify>
    <automated>npx vitest run src/ingestion/twitch-auth.test.ts && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` tests pass with zero network calls (injected exchange/provider seams)
    - TWITCH_SCOPES equals exactly ["user:read:chat", "user:write:chat"] — no user:bot/channel:bot (RESEARCH.md Pattern 2)
    - `grep -cE "accessToken|refreshToken" src/ingestion/twitch-auth.ts` shows matches only in persistence/type code paths; the capturing-logger test proves no token value is ever logged
    - createAuthProvider returns null (not throw) on missing token file
    - `npm test` fully green including both invariant scans (single-funnel untouched, chat-sender new)
  </acceptance_criteria>
  <done>Restart-safe Twitch auth: once bootstrapped, tokens auto-refresh and re-persist with zero interaction; before bootstrap, the app degrades gracefully and says exactly how to fix it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Twitch chat text → parseCommand | First parse of raw viewer-authored message text (adversarial by design) |
| App → Twitch Helix (outbound chat) | Rate-limited external API; lockout = bot goes mute mid-show |
| Token file at rest → process | Long-lived refresh token grants chat read/write as the broadcaster |
| npm registry → node_modules | 4 new packages installed this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | command-parser input handling | mitigate | zod safeParse + regex-first extraction; module is total (never throws) so hostile strings cannot crash the listener; 2000-char cap mirrors the funnel's CandidateSchema bound |
| T-02-06 | Denial of Service | outbound chat rate lockout (30-min mute documented in PITFALLS.md) | mitigate | Single p-queue sender at 15/30s (strict sliding window) — 85% headroom under the verified 100/30s broadcaster tier; sole-caller enforced by tests/invariants/chat-sender.test.ts |
| T-02-07 | Information Disclosure | refresh token at rest + in logs | mitigate | Token file lives in gitignored data/ with 0o600 where honored; capturing-logger test proves no token value is ever logged; onRefreshFailure logs loudly WITHOUT the token (extends Phase 1's T-01-10 key-hygiene discipline to OAuth tokens) |
| T-02-08 | Spoofing | stolen client secret enabling token minting | accept | Client secret lives in .env (gitignored) on the streamer's own machine — same trust boundary as ANTHROPIC_API_KEY (T-01-10); no additional at-rest encryption for v1 single-machine deployment |
| T-02-SC | Tampering (supply chain) | npm installs (@twurple/auth, @twurple/api, @twurple/eventsub-ws, p-queue) | mitigate | All four [OK] in RESEARCH.md Package Legitimacy Audit (slopcheck pass, 2016-2021 first-publish, official repos, zero postinstall) — no blocking checkpoint required |
</threat_model>

<verification>
- `npm test` green: parser + sender + auth units, both invariant scans
- `npm run typecheck` and `npm run lint` pass
- `node -e` lockstep check: three @twurple versions identical
</verification>

<success_criteria>
- INFRA-01's token half proven: persisted auto-refreshing tokens survive restarts (unit-level; live proof in plan 02-06's checkpoint)
- D2-08 machine-enforced: one rate-budgeted chat writer, invariant-scanned
- Parser total over all inputs — the untrusted-boundary contract plan 02-04 builds on
</success_criteria>

<output>
Create `.planning/phases/02-chat-vote-loop/02-02-SUMMARY.md` when done.
</output>
