---
phase: 02-chat-vote-loop
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pipeline/round.ts
  - src/pipeline/round.test.ts
  - tests/invariants/single-funnel.test.ts
  - src/operator-console/server.ts
  - src/operator-console/public/index.html
  - src/operator-console/public/console.css
  - src/operator-console/public/console.js
  - src/main.ts
autonomous: true
requirements: [CHAT-02, CHAT-04]
must_haves:
  truths:
    - "The streamer clicks Start Round in the console and a timed round opens over up to 3 pool candidates; the button is disabled with the exact UI-SPEC reason copy when the pool has < 2 candidates, a round is already open, or the mode is not IDLE (D2-01/D2-04)"
    - "When a round closes, the winning already-approved candidate lands in the build queue ONLY via src/pipeline/round.ts's enqueueWinner — the single-funnel invariant suite passes with the new allowlist entry (CHAT-04, COMP-01)"
    - "A winner whose gate approval is older than the staleness bound is re-submitted through the compliance gate instead of enqueued directly (D2-05)"
    - "After a process restart mid-round, the console shows the restored round with the correct tally and remaining time before any new input is accepted (D2-14)"
    - "The console round panel shows live candidates/counts and a countdown; a frozen round in HALTED triage shows the UI-SPEC freeze line (D2-16)"
  artifacts:
    - path: "src/pipeline/round.ts"
      provides: "enqueueWinner — the ONE new funnel entry point: toQueuedTask() + taskQueue.enqueue() for round winners, with the D2-05 staleness branch"
      exports: ["enqueueWinner"]
    - path: "tests/invariants/single-funnel.test.ts"
      provides: "check (d) allowlist extended with src/pipeline/round.ts — same commit as the file it sanctions"
      contains: "src/pipeline/round.ts"
    - path: "src/operator-console/server.ts"
      provides: "POST /api/round/start (CSRF-covered, zod-validated) + ConsoleState.round"
      contains: "/api/round/start"
    - path: "src/main.ts"
      provides: "RoundManager construction + startup restore() wired into createApp"
      contains: "restore"
  key_links:
    - from: "src/state-machine/round.ts"
      to: "src/pipeline/round.ts"
      via: "enqueueWinner injected into RoundManagerDeps in main.ts"
      pattern: "enqueueWinner"
    - from: "src/operator-console/server.ts"
      to: "src/state-machine/round.ts"
      via: "POST /api/round/start calls round.startRound()"
      pattern: "startRound"
    - from: "src/operator-console/public/console.js"
      to: "/api/round/start"
      via: "fetch POST on Start Round click"
      pattern: "api/round/start"
    - from: "src/pipeline/round.ts"
      to: "src/compliance/gate.ts"
      via: "toQueuedTask(candidate, approvedResult) — brand construction stays in gate.ts"
      pattern: "toQueuedTask"
---

<objective>
Wire the winner→build-queue funnel entry point (the FIRST production caller of toQueuedTask/enqueue outside submit.ts) with its mandatory single-funnel allowlist update, and give the streamer the console Start Round control with the full round status panel, HALTED-freeze display, and startup crash-restore wiring.

Purpose: This makes rounds operable end-to-end from the operator's seat (with /api/dev/submit as the interim suggestion source): pool → Start Round → countdown → close → winner queued through the gate. CHAT-04's queue path and CHAT-02's timed-round operability land here; chat-driven votes arrive in plan 02-04.
Output: src/pipeline/round.ts + updated invariant allowlist, console round panel per UI-SPEC, main.ts round wiring with restore-before-listen ordering.
</objective>

## Phase Goal

**As a** Twitch viewer on stream night, **I want to** suggest ideas with `!suggest` and vote in timed rounds while watching the live tally on the stream overlay, **so that** chat genuinely picks what gets built next — with every suggestion still passing the compliance gate.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-chat-vote-loop/02-CONTEXT.md
@.planning/phases/02-chat-vote-loop/02-RESEARCH.md
@.planning/phases/02-chat-vote-loop/02-PATTERNS.md
@.planning/phases/02-chat-vote-loop/02-UI-SPEC.md
@.planning/phases/01-compliance-gate-kill-switch/01-UI-SPEC.md

<interfaces>
<!-- Contracts this plan CONSUMES (created by plan 02-01) and CREATES. -->

Consumed — from src/state-machine/round.ts (plan 02-01):
- `class RoundManager { startRound(): RoundSnapshot; recordVote(twitchUserId, option): boolean; closeRound(): void; snapshot(): RoundSnapshot | null; restore(): void; on(event, handler): void }`
- `class RoundStartError extends Error { readonly reason: "not-idle" | "pool-too-small" }`
- `type EnqueueWinnerResult = { queued: true } | { queued: false; reason: "stale-reclassified" | "halted" }`
- events: ROUND_OPENED / ROUND_CLOSED / VOTE_RECORDED from src/shared/events.ts
- `interface RoundSnapshot { roundId; status; frozen; candidates: RoundCandidate[]; openedAtMs; endsAtMs; remainingMs; winnerOption; tiebreak; totalVotes }` from src/shared/types.ts

Consumed — from src/compliance/gate.ts and src/queue/task-queue.ts (Phase 1, unchanged):
- `function toQueuedTask(candidate: SuggestionCandidate, result: GateResult): QueuedTask` — throws unless result.decision === "approved"
- `class TaskQueue { enqueue(task: QueuedTask): void; list(): QueuedTask[] }`

Consumed — from src/pipeline/submit.ts (Phase 1, unchanged):
- `function submitCandidate(deps: SubmitDeps, candidate: SuggestionCandidate): SubmitResult` — the re-classification path for stale winners

Created — from src/pipeline/round.ts:
- `interface EnqueueWinnerDeps { taskQueue: TaskQueue; db: Database.Database; mode: () => StreamMode; resubmit: (candidate: SuggestionCandidate) => SubmitResult; staleAfterMs?: number; logger?: Logger }`
- `function enqueueWinner(deps: EnqueueWinnerDeps, approved: ApprovedCandidate): EnqueueWinnerResult`
  (ApprovedCandidate = { candidate, result, addedAtMs } from src/queue/pool.ts)

Created — console state extension (src/operator-console/server.ts):
- `interface ConsoleState extends StateSnapshot { pool; queue; pendingReviewCount; review; round: RoundSnapshot | null }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/pipeline/round.ts funnel entry + single-funnel allowlist update (same commit)</name>
  <files>src/pipeline/round.ts, src/pipeline/round.test.ts, tests/invariants/single-funnel.test.ts</files>
  <read_first>
    - tests/invariants/single-funnel.test.ts (check (d)'s allowed Set at ~line 181 — the literal to extend; check (b) already permits src/pipeline/*)
    - src/pipeline/submit.ts (deps-injection shape + doc-comment style to copy; the resubmit target for stale winners)
    - src/compliance/gate.ts toQueuedTask (throws on non-approved — the contract this file relies on)
    - src/queue/pool.ts (ApprovedCandidate: { candidate, result, addedAtMs })
    - .planning/phases/02-chat-vote-loop/02-RESEARCH.md (Pitfall 1 — this task IS the pitfall fix)
  </read_first>
  <behavior>
    - Test: enqueueWinner with a fresh ApprovedCandidate calls toQueuedTask then taskQueue.enqueue exactly once and returns { queued: true }; the queued task's text is byte-identical to the pooled candidate's text (never re-derived from raw chat — RESEARCH.md Security Domain)
    - Test: enqueueWinner where now - addedAtMs > staleAfterMs does NOT enqueue; it calls deps.resubmit(candidate) once and returns { queued: false, reason: "stale-reclassified" } (D2-05 staleness branch — re-classification through the full gate)
    - Test: enqueueWinner while deps.mode() === "HALTED" refuses without enqueueing and returns { queued: false, reason: "halted" } (belt-and-suspenders — RoundManager never closes while HALTED, but the funnel must be independently safe)
    - Test: a non-approved GateResult reaching enqueueWinner propagates toQueuedTask's throw (COMP-01 — this must stay loud, not be swallowed)
    - Test (invariant): the full single-funnel suite passes with src/pipeline/round.ts present — check (d) allowlist now contains exactly gate.ts, submit.ts, round.ts
  </behavior>
  <action>
    Create src/pipeline/round.ts as a deliberately narrow module (separate from submit.ts — submit handles NEW unclassified candidates; this file promotes ALREADY-approved pool items; two different trust states, per RESEARCH.md Structure Rationale). Single export enqueueWinner(deps, approved) per `<interfaces>`: (1) if deps.mode() === "HALTED" → log + return { queued: false, reason: "halted" }; (2) staleness check: staleAfterMs defaults from env WINNER_STALENESS_MINUTES (default 360 — effectively never within one stream session since the pool starts clean each night per D2-13, but honest against multi-hour marathon pools; same env-knob idiom as roundDurationMs); stale → deps.resubmit(approved.candidate) and return { queued: false, reason: "stale-reclassified" }; (3) otherwise task = toQueuedTask(approved.candidate, approved.result); deps.taskQueue.enqueue(task); return { queued: true }. Doc comment mirrors submit.ts's: this file is a sanctioned single-funnel entry point, listed in the invariant allowlist.

    In THE SAME task/commit, edit tests/invariants/single-funnel.test.ts check (d): change `new Set(["src/compliance/gate.ts", "src/pipeline/submit.ts"])` to `new Set(["src/compliance/gate.ts", "src/pipeline/submit.ts", "src/pipeline/round.ts"])`. Do not touch checks (a), (b), (c), (e) — round.ts lives under src/pipeline/ so check (b) already permits its .enqueue( call, and it must NOT contain any `as QueuedTask` cast (check (a) stays at exactly one, in gate.ts).
  </action>
  <verify>
    <automated>npx vitest run src/pipeline/round.test.ts tests/invariants/single-funnel.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` tests pass; the single-funnel suite is green WITH src/pipeline/round.ts in the tree
    - `grep -c "as QueuedTask" src/pipeline/round.ts` returns 0 (brand construction stays in gate.ts)
    - `grep -c "src/pipeline/round.ts" tests/invariants/single-funnel.test.ts` returns 1 (allowlist extended, not weakened — Set still enumerates exact files)
    - enqueueWinner passes approved.result (the ORIGINAL stored GateResult) to toQueuedTask — no synthetic { decision: "approved" } literal anywhere in round.ts
  </acceptance_criteria>
  <done>The winner→queue path exists, is invariant-sanctioned in the same commit, and stale approvals re-run the gate instead of riding an old pass.</done>
</task>

<task type="auto">
  <name>Task 2: Console round control — POST /api/round/start, ConsoleState.round, round panel UI, HALTED freeze line</name>
  <files>src/operator-console/server.ts, src/operator-console/public/index.html, src/operator-console/public/console.css, src/operator-console/public/console.js</files>
  <read_first>
    - src/operator-console/server.ts (POST /api/halt route shape: zod safeParse → precondition → domain call → pushState → JSON; CSRF middleware placement — new routes on the same app inherit it; buildState/ConsoleState)
    - src/operator-console/public/console.js (el()/button() textContent-only helpers; renderAll wiring; ws reconnect)
    - .planning/phases/02-chat-vote-loop/02-UI-SPEC.md (Copywriting Contract — operator console table: EXACT disabled-reason strings, round-open heading, HALTED triage freeze line, empty states; console typography/color deltas: Start Round is the ONE new accent button)
    - .planning/phases/01-compliance-gate-kill-switch/01-UI-SPEC.md (inherited design system: tokens, 44px click target)
    - src/shared/types.ts (RoundSnapshot — the shape the panel renders)
  </read_first>
  <action>
    Server: add `round: RoundManager` to ConsoleServerDeps. Extend buildState() to include `round: deps.round.snapshot()`. Add POST /api/round/start following the /api/halt shape exactly: zod-validate the body with a strict empty-object schema (`z.object({}).strict()` on `req.body ?? {}`), then call deps.round.startRound() in a try/catch — RoundStartError maps to 409 JSON `{ error, reason }` with reason "not-idle" | "pool-too-small" (never a stack trace, T-01-03 discipline); success returns the RoundSnapshot JSON and calls pushState(). Subscribe pushState() to the round manager's ROUND_OPENED/ROUND_CLOSED/VOTE_RECORDED events (console push stays synchronous per-event — operator-frequency, not overlay-frequency; the 300ms debounce is the overlay's concern, plan 02-05). The CSRF middleware already covers the new POST (registered on the same app after the middleware); no extra CSRF work.

    UI (console.js/index.html/console.css, textContent-only via the existing el() helper — zero innerHTML): add a "Round" panel rendering from state.round:
    - No round + pool >= 2: accent "Start Round" button (Body 16/600, .button-accent, 44px min target, single click, NO modal) + UI-SPEC empty-state copy: heading "No round running", body "{n} candidates ready. Start a round when the show's ready for one."
    - Disabled states with the EXACT UI-SPEC strings: pool < 2 → "Need at least 2 approved candidates to start a round (have {n})."; round open → "Round in progress — {m:ss} left."; mode ≠ IDLE → "Rounds can only start from Standby (current mode: {mode})."
    - Empty pool: heading "Candidate pool is empty", body "Waiting for !suggest ideas from chat. Approved ideas appear here."
    - Round open: heading "Round open — {m:ss} left" (countdown computed CLIENT-side from round.endsAtMs on a 1s interval, resynced on every ws push — the server never streams timer frames) + numbered candidate list with live vote counts (the console MAY show tallies; only chat may not — D2-07)
    - HALTED with a frozen round: inside the existing triage panel render the UI-SPEC line "Round frozen at {m:ss} remaining — {n} votes recorded. Resume continues the round; Discard cancels it and returns candidates to the pool." (use round.remainingMs and round.totalVotes)
    - Start Round click: fetch POST /api/round/start with Content-Type application/json and body {}; render the 409 reason strings on failure.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && grep -c "innerHTML" src/operator-console/public/console.js | grep -qx 0 && echo NO_INNERHTML && grep -c "api/round/start" src/operator-console/public/console.js</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/round/start with a non-JSON Content-Type returns 403 (CSRF middleware inherited); with valid JSON from IDLE + 2 pooled candidates returns 200 with a RoundSnapshot; from non-IDLE returns 409 with reason "not-idle" (covered by a route test added to the existing console test surface or e2e in plan 02-06)
    - `grep -c "innerHTML" src/operator-console/public/console.js` returns 0
    - All four UI-SPEC copy strings above appear verbatim in console.js (grep for "Need at least 2 approved candidates" and "Round frozen at" each returns 1)
    - buildState() output includes a `round` key (null when no round)
    - Countdown renders client-side from endsAtMs — `grep -cE "setInterval|requestAnimationFrame" src/operator-console/public/console.js` >= 1 and no per-second server push exists in server.ts
  </acceptance_criteria>
  <done>The streamer can start, watch, and (via existing halt/recover) freeze-resume-discard rounds from the console, with exact UI-SPEC copy.</done>
</task>

<task type="auto">
  <name>Task 3: main.ts wiring — RoundManager construction, restore-before-listen, winner funnel injection</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (createApp composition order: db → machine → pool → taskQueue → console server; close() teardown; gateDeps/classify wiring — resubmit reuses it)
    - src/state-machine/round.ts (RoundManagerDeps — what to inject)
    - src/pipeline/round.ts (EnqueueWinnerDeps — what enqueueWinner needs)
  </read_first>
  <action>
    In createApp: construct the RoundManager after pool/taskQueue and BEFORE startConsoleServer, injecting { db, machine, pool, logger, enqueueWinner: (candidate, result) => enqueueWinner({ taskQueue, db, mode: () => machine.mode, resubmit: (c) => submitCandidate({ db, mode: () => machine.mode, pool, classify: (cand) => classify(gateDeps, cand), logger }, c), logger }, { candidate, result, addedAtMs }) } — note enqueueWinner receives the ApprovedCandidate shape; adapt the RoundManager's injected-callback signature from plan 02-01 (candidate + result; RoundManager supplies pooled_at_ms as addedAtMs from its stored round_candidates row).

    Call round.restore() immediately after construction and BEFORE startConsoleServer — the D2-14 ordering contract: persisted round state is live before any surface can accept input. Pass `round` into ConsoleServerDeps. Add `round` to AppHandle (plans 02-04/02-05 and the e2e suite need it). No teardown change needed beyond what close() already does IF the RoundManager's timer is unref'd (plan 02-01 requirement) — but explicitly clear any active round timer in close() if RoundManager exposes a dispose()/stop() method; if it doesn't, add one there-and-then is NOT this plan's job — instead call clearTimeout via a RoundManager.stop() only if plan 02-01 shipped it; otherwise rely on .unref() and note it in the summary.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && grep -c "restore()" src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "restore" src/main.ts` shows round.restore() called before startConsoleServer( in source order
    - AppHandle exports `round` (typed RoundManager)
    - `npm test` fully green — including single-funnel (new allowlist) and chat-sender invariants
    - A quick createApp-based test (extend tests/e2e/console-flows.e2e.test.ts only if trivial, else assert via unit test in round.test.ts's existing harness): starting the app with a db file containing an open round yields GET /api/state with round.status "open" — if adding this requires touching files owned by other plans, defer the assertion to plan 02-06's recovery e2e and say so in the summary
  </acceptance_criteria>
  <done>Rounds are fully wired into the app lifecycle: restore precedes serving, winners flow through the sanctioned funnel, and the console operates the whole loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Round winner (chat-derived text) → build queue | The ONE new funnel entry point this phase adds (COMP-01 surface) |
| Browser → POST /api/round/start | New state-changing console route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | winner→queue path bypassing the gate | mitigate | enqueueWinner is the only new toQueuedTask caller, allowlisted by name in single-funnel check (d) in the same commit; no `as QueuedTask` cast outside gate.ts (check (a) still exactly 1); winner text passed byte-identical from the pooled, already-gated candidate — never re-derived from raw chat |
| T-02-10 | Tampering | stale approval enqueued after conditions changed | mitigate | D2-05 staleness bound (WINNER_STALENESS_MINUTES, default 360): stale winners re-enter submitCandidate for full re-classification instead of direct enqueue |
| T-02-11 | Tampering | CSRF on POST /api/round/start | mitigate | Route registered on the existing console app behind the uniform Origin+Content-Type CSRF middleware (T-01-01/CR-01 pattern); strict zod empty-body schema; RoundStartError → terse 409, never a stack trace |
| T-02-12 | Denial of Service | round start spam wedging the state machine | mitigate | startRound guarded by machine.mode === "IDLE" (RoundStartError "not-idle"); operator-only surface (localhost console); state machine transition table rejects illegal re-entry |
</threat_model>

<verification>
- `npm test` green (single-funnel with new allowlist, all round/pipeline units)
- `npm run dev` → console shows the Round panel; with 2+ dev-submitted approved candidates, Start Round opens a 60s round; halt mid-round shows the freeze line; recover-resume continues the countdown
- Winner appears in the console queue list after close (via the gate funnel)
</verification>

<success_criteria>
- CHAT-02: timed rounds with configurable duration run end-to-end from the console (countdown visible; overlay rendering lands in 02-05)
- CHAT-04's queue half: the winning suggestion becomes the next build task exclusively via the sanctioned funnel (chat announcement lands in 02-04)
- Single-funnel invariant evolved correctly: extended by exact filename, never weakened
- Crash-restore ordering wired: restore() before any listener
</success_criteria>

<output>
Create `.planning/phases/02-chat-vote-loop/02-03-SUMMARY.md` when done.
</output>
