---
phase: 02-chat-vote-loop
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/queue/pool.ts
  - src/queue/pool.test.ts
  - src/ingestion/suggest-intake.ts
  - src/ingestion/suggest-intake.test.ts
  - src/ingestion/narration.ts
  - src/ingestion/narration.test.ts
  - src/ingestion/twitch-chat.ts
  - src/ingestion/twitch-chat.test.ts
  - src/operator-console/server.ts
  - src/operator-console/public/index.html
  - src/operator-console/public/console.js
  - src/main.ts
autonomous: true
requirements: [CHAT-01, CHAT-05, COMP-03]
must_haves:
  truths:
    - "A viewer's `!suggest <idea>` reaches submitCandidate() (the Phase 1 funnel, unmodified) and the viewer gets an @-reply outcome: rejected with a viewer-safe category label, held-for-review notice, duplicate notice, or silence on approval until the idea appears in a round (CHAT-01, COMP-03, D2-06)"
    - "Per-user limits run BEFORE classification: a second suggestion inside the cooldown or with one already pooled gets a quiet coalesced notice and never burns a Sonnet call (D2-11, closes T-01-11)"
    - "The bot narrates round open (with candidates), round close + winner/tiebreak/zero-votes, using the exact UI-SPEC templates, all through the single sender queue — live tallies NEVER go to chat (CHAT-05, D2-03, D2-07)"
    - "!vote N from chat records a vote keyed by the EventSub chatterId; EventSub disconnect/ready transitions are logged and reconnection triggers round-state reconciliation (D2-15, INFRA-02 hooks)"
    - "One-time OAuth bootstrap works via GET /auth/start → Twitch → GET /auth/callback with a state nonce; without a token the app runs degraded and the console shows the UI-SPEC auth-expired copy"
    - "The narrator, sender, round-event subscriptions, and startTwitchChat are all constructed inside createApp whenever a chatSource/chatSink pair is available — injected test fakes exercise the exact production wiring (the e2e proof in plan 02-06 needs no src edits)"
  artifacts:
    - path: "src/ingestion/twitch-chat.ts"
      provides: "EventSub listener wiring behind an injected ChatEventSource; !suggest/!vote dispatch; disconnect/ready observability + reconcile hook"
      exports: ["startTwitchChat", "ChatEventSource"]
    - path: "src/ingestion/suggest-intake.ts"
      provides: "Pre-classification intake policy: per-user cooldown, max-1-pooled, exact-duplicate check via prefilter normalize() (D2-11/D2-12)"
      exports: ["createSuggestIntake"]
    - path: "src/ingestion/narration.ts"
      provides: "UI-SPEC chat copy templates + burst coalescing, sole consumer of ChatSender (D2-06/D2-07)"
      exports: ["createNarrator"]
    - path: "src/queue/pool.ts"
      provides: "Bounded pool: maxSize + oldest-drop eviction with onEvict callback (D2-13)"
      contains: "onEvict"
    - path: "src/main.ts"
      provides: "CreateAppOptions chatSource/chatSink seams + in-createApp chat composition (sender, narrator, round-event subscriptions, startTwitchChat, reconcile)"
      contains: "chatSink"
  key_links:
    - from: "src/ingestion/twitch-chat.ts"
      to: "src/pipeline/submit.ts"
      via: "submitCandidate() — the ONLY intake path for !suggest (COMP-01)"
      pattern: "submitCandidate"
    - from: "src/ingestion/twitch-chat.ts"
      to: "src/state-machine/round.ts"
      via: "round.recordVote(chatterId, option) on !vote"
      pattern: "recordVote"
    - from: "src/ingestion/narration.ts"
      to: "src/ingestion/chat-sender.ts"
      via: "sender.send(text) — never sendChatMessage directly"
      pattern: "\\.send\\("
    - from: "src/main.ts"
      to: "src/ingestion/narration.ts"
      via: "createApp subscribes ROUND_OPENED → narrator.roundOpened / ROUND_CLOSED → narrator.roundClosed when a chatSource/chatSink pair exists"
      pattern: "roundOpened"
    - from: "src/operator-console/server.ts"
      to: "src/ingestion/twitch-auth.ts"
      via: "GET /auth/start builds authorize URL; GET /auth/callback calls completeAuthorization"
      pattern: "completeAuthorization"
---

<objective>
Make the loop live: wire EventSub chat ingestion (!suggest → intake limits → the unmodified Phase 1 funnel; !vote → the round ledger), COMP-03 category-level rejection feedback with burst coalescing, game-show narration of round transitions through the rate-limited sender, the bounded candidate pool, and the one-time OAuth bootstrap routes.

Purpose: After this plan, a real viewer can suggest and vote, and chat hears exactly the transition beats — the phase's first three success criteria become demoable against a live channel (proven in plan 02-06's checkpoint).
Output: twitch-chat/suggest-intake/narration modules with unit coverage, bounded pool, OAuth console routes with state-nonce CSRF defense, main.ts live wiring with graceful degradation.
</objective>

## Phase Goal

**As a** Twitch viewer on stream night, **I want to** suggest ideas with `!suggest` and vote in timed rounds while watching the live tally on the stream overlay, **so that** chat genuinely picks what gets built next — with every suggestion still passing the compliance gate.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-chat-vote-loop/02-CONTEXT.md
@.planning/phases/02-chat-vote-loop/02-RESEARCH.md
@.planning/phases/02-chat-vote-loop/02-PATTERNS.md
@.planning/phases/02-chat-vote-loop/02-UI-SPEC.md

<interfaces>
<!-- Consumed contracts (plans 02-01/02-02/02-03) and created contracts. -->

Consumed:
- `parseCommand(messageText)` from src/ingestion/command-parser.ts (02-02)
- `createChatSender(deps): ChatSender` with `send(text): Promise<void>` and `interface ChatMessageSink { sendChatMessage(broadcasterId: string, text: string): Promise<unknown> }` from src/ingestion/chat-sender.ts (02-02)
- `createAuthProvider / buildAuthorizeUrl / completeAuthorization / TWITCH_SCOPES` from src/ingestion/twitch-auth.ts (02-02)
- `submitCandidate(deps: SubmitDeps, candidate): SubmitResult` from src/pipeline/submit.ts (Phase 1, UNCHANGED — SubmitDeps.classify is per-call-site, which is how feedback hooks in)
- `RoundManager.recordVote(twitchUserId, option)` + ROUND_OPENED/ROUND_CLOSED events with RoundSnapshot payloads (02-01)
- `normalize(text)` exported from src/compliance/prefilter.ts (Phase 1 — D2-12 dedup reuses it; do not duplicate the function)
- `CATEGORY_META: Record<GateCategory, { label, disposition }>` from src/compliance/categories.ts (viewer-safe labels for COMP-03)

Created — src/ingestion/twitch-chat.ts:
- `interface ChatMessageEvent { chatterId: string; chatterDisplayName: string; messageText: string }`
- `interface ChatEventSource { onChannelChatMessage(broadcasterId: string, userId: string, handler: (e: ChatMessageEvent) => void): unknown; onUserSocketReady(handler: (userId: string, sessionId: string) => void): unknown; onUserSocketDisconnect(handler: (userId: string, error?: Error) => void): unknown; start(): void; stop(): void }`
  (minimal structural subset — the real EventSubWsListener satisfies it via a thin adapter in main.ts; tests inject a fake, mirroring hotkey.ts's KeyEventSource)
- `interface TwitchChatDeps { source: ChatEventSource; broadcasterUserId: string; intake: SuggestIntake; submit: (candidate: SuggestionCandidate) => SubmitResult; round: { recordVote(id: string, option: number): boolean }; narrator: Narrator; reconcile: () => void; logger: Logger }`
- `function startTwitchChat(deps: TwitchChatDeps): { stop(): void }`

Created — src/ingestion/suggest-intake.ts:
- `type IntakeVerdict = { ok: true } | { ok: false; reason: "cooldown" | "pending-exists" | "duplicate" }`
- `interface SuggestIntake { check(chatterId: string, rawText: string): IntakeVerdict; registerAccepted(chatterId: string, candidateId: string): void }`
- `function createSuggestIntake(deps: { pool: CandidatePool; cooldownMs: number; now?: () => number }): SuggestIntake`

Created — src/ingestion/narration.ts:
- `type FeedbackKind = "rejected" | "held" | "duplicate" | "cooldown" | "trim"`
- `interface Narrator { roundOpened(snap: RoundSnapshot): void; roundClosed(snap: RoundSnapshot): void; feedback(kind: FeedbackKind, displayName: string, categoryLabel?: string): void; error(text: string): void }`
- `function createNarrator(deps: { sender: ChatSender; coalesceMs?: number; logger?: Logger }): Narrator`

Created — src/queue/pool.ts (backward-compatible extension):
- `new CandidatePool(opts?: { maxSize?: number; onEvict?: (item: ApprovedCandidate) => void })` — existing zero-arg construction keeps working

Created — createApp seams (src/main.ts):
- `CreateAppOptions` gains BOTH `chatSource?: ChatEventSource` AND `chatSink?: ChatMessageSink` — when both are provided (test fakes or real twurple adapters), createApp itself constructs sender → narrator → round-event subscriptions → startTwitchChat; plan 02-06's e2e injects fakes through these two options and needs no src edits
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-classification intake policy + bounded pool (D2-11/D2-12/D2-13)</name>
  <files>src/queue/pool.ts, src/queue/pool.test.ts, src/ingestion/suggest-intake.ts, src/ingestion/suggest-intake.test.ts</files>
  <read_first>
    - src/queue/pool.ts (current Map-based implementation — extend constructor, keep every existing method signature)
    - src/compliance/prefilter.ts (normalize() — the canonical text-normalization path; import it, never copy it)
    - .planning/phases/01-compliance-gate-kill-switch/01-SECURITY.md (T-01-11 — the accepted risk this task closes)
    - src/state-machine/review-queue.ts (env-knob idiom for cooldown default)
  </read_first>
  <behavior>
    - Test (pool): with maxSize 3, adding a 4th approved candidate evicts the OLDEST (insertion order) and invokes onEvict exactly once with the evicted ApprovedCandidate; without opts, behavior is unchanged from Phase 1 (unbounded)
    - Test (intake): first check(chatterId, text) → ok; a second within cooldownMs → { ok: false, reason: "cooldown" }; after cooldownMs elapses (injected now()) → ok again
    - Test (intake): after registerAccepted(chatterId, candidateId) and that candidate landing in the pool, a new suggestion from the same chatterId → reason "pending-exists"; once the candidate leaves the pool (drawn into a round / evicted), the same user may suggest again
    - Test (intake, D2-12): a suggestion whose normalize()d text exactly matches any pooled candidate's normalize()d text → reason "duplicate" — including unicode/zero-width/hyphen-disguised variants of a pooled text (normalize() already folds these)
    - Test (ordering): check() is pure over injected state — it performs zero classification and touches no classifier; verdicts are computed without any Promise (synchronous — the whole point is running BEFORE the Sonnet call)
  </behavior>
  <action>
    Extend src/queue/pool.ts: constructor accepts optional { maxSize, onEvict }. In add(), after the existing approved-only guard and set(), if size > maxSize delete the oldest entry (first Map insertion-order key) and call onEvict(evicted). Keep list()/remove() untouched. The composition root wires onEvict → recordPoolDropped (audit row per D2-13) and maxSize from POOL_MAX_SIZE (default 50).

    Create src/ingestion/suggest-intake.ts per `<interfaces>`: an in-memory Map<chatterId, { lastSuggestAtMs, pendingCandidateIds: Set<string> }>. check(chatterId, rawText): (1) cooldown — now() - lastSuggestAtMs < cooldownMs → "cooldown"; (2) pending — any registered candidateId still present in pool.list() → "pending-exists" (max 1 pooled per user, D2-11; a rejected/held candidate is no longer pooled so it naturally frees the slot); (3) duplicate — normalize(rawText) equals normalize() of any pooled candidate text → "duplicate" (D2-12; import normalize from src/compliance/prefilter.ts). Key EVERYTHING by chatterId (numeric Twitch user id) — never display name (T-02-13; the intake layer sees chatterId directly from the EventSub payload, so no SuggestionCandidate type change is needed, per RESEARCH.md Open Question 1). registerAccepted records lastSuggestAtMs = now() and adds the candidateId. State is in-memory only — pool starts clean each night (D2-13) so intake state legitimately resets with the process; note this in the doc comment.
  </action>
  <verify>
    <automated>npx vitest run src/queue/pool.test.ts src/ingestion/suggest-intake.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` tests pass; pool eviction preserves the approved-only guard and existing method signatures (Phase 1 pool tests, if any exist inline, still pass)
    - `grep -c "from \"../compliance/prefilter.js\"" src/ingestion/suggest-intake.ts` returns 1 (normalize imported, not duplicated)
    - suggest-intake.ts contains no async/await/Promise and imports no classifier/gate module (grep for "classify" returns 0) — pre-classification by construction
    - Intake keyed by chatterId: no reference to twitchUsername/displayName in suggest-intake.ts verdict logic
  </acceptance_criteria>
  <done>Suggestion floods die at a synchronous in-memory check before any metered Sonnet call; the pool is bounded with audited eviction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EventSub chat listener + narration with UI-SPEC copy and burst coalescing</name>
  <files>src/ingestion/twitch-chat.ts, src/ingestion/twitch-chat.test.ts, src/ingestion/narration.ts, src/ingestion/narration.test.ts</files>
  <read_first>
    - src/kill-switch/hotkey.ts (KeyEventSource injected-interface pattern — ChatEventSource copies it)
    - .planning/phases/02-chat-vote-loop/02-UI-SPEC.md (Bot chat narration table — the templates are a BINDING copy contract, including 60-char title truncation and ≤500-char totals)
    - .planning/phases/02-chat-vote-loop/02-RESEARCH.md (Pattern 1: onUserSocketReady/Disconnect hooks; Pitfall 4: broadcaster id passed as BOTH args; Security Domain: zod at the EventSub payload boundary)
    - src/pipeline/submit.ts (SubmitDeps/SubmitResult — the funnel contract; the classify-wrapper feedback hook mirrors server.ts's classifyThenPush)
    - src/compliance/categories.ts (CATEGORY_META labels — chat sees ONLY these, never internal codes/rationale)
  </read_first>
  <behavior>
    - Test (listener): a fake ChatEventSource emitting { chatterId: "42", chatterDisplayName: "viewer1", messageText: "!suggest cool idea" } with intake ok → deps.submit called once with a well-formed SuggestionCandidate { source: "chat", kind: "suggestion", twitchUsername: "viewer1", text: "cool idea" } and intake.registerAccepted called with ("42", candidate.id)
    - Test (listener): intake verdict "cooldown"/"duplicate"/"pending-exists" → submit NOT called; narrator.feedback called with the matching kind ("pending-exists" maps to the cooldown notice)
    - Test (listener): "!vote 2" → round.recordVote("42", 2) called; non-command chatter → nothing called; a payload missing chatterId or messageText (zod-invalid) → handler returns without throwing and without calling anything (malformed EventSub payload never crashes the listener)
    - Test (listener): handler exceptions from downstream (submit throws) are caught and logged — the listener survives (fail-closed/never-crash)
    - Test (listener): onUserSocketDisconnect → logger.warn with the RESEARCH.md wording; onUserSocketReady → logger.info AND deps.reconcile() invoked (INFRA-02 observability + reconciliation hook)
    - Test (narrator): roundOpened with 3 candidates sends exactly one message matching the UI-SPEC template `Voting is OPEN — !vote 1, 2 or 3: [1] {title} [2] {title} [3] {title} — {duration}s on the clock.` with titles truncated to 60 chars; a 2-candidate round says "1 or 2"
    - Test (narrator): roundClosed variants — winner: `Round over — "{title}" wins with {n} votes. Queued for the build.`; tiebreak: `Dead heat! Coin flip says… "{title}". Queued for the build.`; zero votes: `No votes this round — candidates return to the pool. We'll run it back.`
    - Test (narrator, fake timers): three feedback() calls within coalesceMs produce ONE combined message of @-runs (D2-07 burst rule), each rendered from its UI-SPEC template (`@{user} that one can't run on stream: {category label}. Different idea?` for rejected, held/duplicate/cooldown variants per the table); total length ≤ 500
    - Test (narrator): NO method exists to send tallies/timers — the Narrator interface has no tally-shaped input (rate-budget doctrine is structural)
  </behavior>
  <action>
    Implement src/ingestion/narration.ts per `<interfaces>`: pure template rendering over the UI-SPEC Bot chat narration table (copy strings VERBATIM — they are a reviewed copy contract), 60-char title truncation with "…", feedback coalescing via a trailing setTimeout(coalesceMs, default 3000).unref() buffer that flushes one combined ≤500-char message (overflowing entries flush in a second message), all sends via deps.sender.send() only. Map FeedbackKind→template: rejected (needs categoryLabel), trim (`too big for a live build` D-13 variant — used when the category is feasibility), held (D-08 wording), duplicate, cooldown. error(text) sends the UI-SPEC error line.

    Implement src/ingestion/twitch-chat.ts per `<interfaces>`, copying hotkey.ts's injected-event-source shape: startTwitchChat registers onChannelChatMessage(broadcasterUserId, broadcasterUserId, handler) — the SAME id twice (Pitfall 4: condition needs both broadcaster_user_id and user_id; single-token self-subscription). The handler: zod-validate the event object shape ({ chatterId: nonempty string, chatterDisplayName: string, messageText: string }) via safeParse — types are compile-time fiction, EventSub payloads are network input; parseCommand(messageText); dispatch:
    - suggest: intake.check(chatterId, text) → not ok: narrator.feedback(mapped kind, displayName) and return; ok: build SuggestionCandidate { id: crypto.randomUUID(), source: "chat", kind: "suggestion", twitchUsername: chatterDisplayName, text, submittedAtMs: Date.now() }, call deps.submit(candidate) (which is submitCandidate pre-bound in main.ts — the ONLY intake path, COMP-01), then intake.registerAccepted. If submit returns { accepted: false, reason: "halted" } do nothing further (D-02 refusal is already audited; no chat noise while halted).
    - vote: round.recordVote(chatterId, option) — return value ignored (invalid votes silent, D2-15).
    Wrap the whole handler body in try/catch → logger.error (a hostile payload or downstream throw must never kill the listener). Register onUserSocketDisconnect/onUserSocketReady logging exactly per RESEARCH.md Pattern 1, with deps.reconcile() on ready. Return { stop } delegating to source.stop().

    Classification-outcome feedback (COMP-03) is wired in main.ts (Task 4) via the classify-wrapper seam, not here — this module only surfaces synchronous intake verdicts.
  </action>
  <verify>
    <automated>npx vitest run src/ingestion/twitch-chat.test.ts src/ingestion/narration.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` tests pass, including malformed-payload and downstream-throw survival
    - `grep -c "onChannelChatMessage" src/ingestion/twitch-chat.ts` returns >= 1 with broadcasterUserId passed twice (test asserts both args equal)
    - twitch-chat.ts imports neither @twurple/* nor task-queue/gate modules (grep returns 0 for each — injected source only; funnel untouched)
    - narration.ts message strings match the UI-SPEC table verbatim (grep for "Voting is OPEN" and "Dead heat! Coin flip says" each returns 1)
    - Every narrator send path routes through ChatSender.send — `grep -c "sendChatMessage" src/ingestion/narration.ts` returns 0 (chat-sender invariant scan stays green)
  </acceptance_criteria>
  <done>Chat commands drive the real funnel and round ledger through injected seams; the bot speaks only transition beats, in exact show copy, within budget.</done>
</task>

<task type="auto">
  <name>Task 3: OAuth bootstrap routes, state-nonce lifecycle, console Twitch connection indicator</name>
  <files>src/operator-console/server.ts, src/operator-console/public/index.html, src/operator-console/public/console.js</files>
  <read_first>
    - src/operator-console/server.ts (route registration order relative to CSRF middleware and error boundary)
    - src/ingestion/twitch-auth.ts (buildAuthorizeUrl/completeAuthorization/createAuthProvider signatures from plan 02-02)
    - .planning/phases/02-chat-vote-loop/02-UI-SPEC.md (console error-state copy: "Twitch connection lost" / "Twitch login expired" — exact strings)
  </read_first>
  <action>
    Console server: add optional deps `twitchAuth?: { authorizeUrl(state: string): string; complete(code: string): Promise<void> }` and `twitchStatus?: () => "connected" | "disconnected" | "unauthorized"`. Routes (GET routes — CSRF middleware passes GETs, so add the OAuth state-nonce defense): GET /auth/start — generate a crypto.randomBytes(32).toString("hex") nonce, store it in a module-scoped single-slot variable with a 10-minute expiry, redirect (302) to twitchAuth.authorizeUrl(nonce); when twitchAuth is absent return 503 `{ error: "Twitch auth not configured — set TWITCH_CLIENT_ID/SECRET" }`. GET /auth/callback — zod-validate query { code: string min 1, state: string min 1 }; reject 403 unless state exactly matches the stored unexpired nonce (login-CSRF defense, T-02-16: a GET route that writes credentials MUST NOT be triggerable by a foreign page — this is the state parameter doing the job the Origin check does for POSTs); clear the nonce (single use), await twitchAuth.complete(code), respond with a minimal textContent-safe HTML page "Twitch authorized — you can close this tab" and log success. Errors → terse 400/403 JSON, never a stack trace. Extend buildState() with `twitch: deps.twitchStatus?.() ?? "unauthorized"`.

    Console UI: add a small Twitch connection pill (connected green / reconnecting amber / unauthorized red-outline) fed from state.twitch, plus the UI-SPEC error blocks rendered when applicable: "Twitch connection lost" body copy and "Twitch login expired" body copy ("Re-authorize at /auth/start to reconnect chat. Rounds can't run until this is fixed.") — textContent only.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && grep -c "auth/callback" src/operator-console/server.ts && grep -c "Twitch login expired" src/operator-console/public/console.js</automated>
  </verify>
  <acceptance_criteria>
    - GET /auth/callback with a wrong/missing/reused state returns 403 and never calls complete() (route test with an injected fake twitchAuth)
    - GET /auth/start twice invalidates the first nonce (single-slot); /auth/callback with the first nonce fails
    - GET /auth/start with no twitchAuth dep returns 503 with the exact configured-hint copy
    - Console state JSON includes `twitch` status key; UI renders the two UI-SPEC error copies verbatim (grep console.js for "Twitch login expired" returns 1)
    - `grep -c "innerHTML" src/operator-console/public/console.js` still returns 0
  </acceptance_criteria>
  <done>The one-time OAuth bootstrap and re-auth path exist on the console server with a single-use expiring state nonce, and the operator can see Twitch connection health at a glance.</done>
</task>

<task type="auto">
  <name>Task 4: createApp composition — chatSource/chatSink seams, narrator + sender + round-event wiring, COMP-03 feedback hook, reconcile, isMain twurple adapters</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (armPanicHotkey guarded-import pattern at the isMain branch — Twitch adapter construction copies it exactly; createApp deps flow and close() teardown)
    - src/operator-console/server.ts (classifyThenPush wrapper — the model for the feedback hook; the new twitchAuth/twitchStatus deps from Task 3)
    - src/ingestion/twitch-chat.ts + src/ingestion/narration.ts + src/ingestion/chat-sender.ts (the contracts createApp composes: ChatEventSource, ChatMessageSink, Narrator, ChatSender)
    - src/ingestion/twitch-auth.ts (createAuthProvider/buildAuthorizeUrl/completeAuthorization signatures)
  </read_first>
  <action>
    CreateAppOptions gains BOTH `chatSource?: ChatEventSource` AND `chatSink?: ChatMessageSink` (plus `twitchAuth?` passthrough for the console dep). This is the seam contract plan 02-06's e2e relies on: tests inject a fake source and a capturing sink and get the FULL production chat pipeline.

    Inside createApp (unconditionally): construct pool as `new CandidatePool({ maxSize: POOL_MAX_SIZE env (default 50), onEvict: (item) => recordPoolDropped(db, { candidate: item.candidate, streamMode: machine.mode }) })`; construct the intake (createSuggestIntake with INTAKE_COOLDOWN_SECONDS env, default 60).

    Inside createApp, whenever BOTH a chatSource and a chatSink are available (injected via options, or built by the isMain branch below): (1) createChatSender({ sink: chatSink, broadcasterId, intervalCap/intervalMs from env }); (2) createNarrator({ sender, logger }); (3) subscribe round events — ROUND_OPENED → narrator.roundOpened(snapshot), ROUND_CLOSED → narrator.roundClosed(snapshot); (4) build the COMP-03 feedback hook by wrapping the classify function passed into the chat-path submitCandidate binding (mirror classifyThenPush): `classifyThenNotify = async (c) => { const r = await classify(gateDeps, c); if (r.decision === "rejected") narrator.feedback(r.category === "feasibility" ? "trim" : "rejected", c.twitchUsername ?? "viewer", CATEGORY_META[r.category]?.label); if (r.decision === "held-for-review") narrator.feedback("held", c.twitchUsername ?? "viewer"); return r; }` — approvals get NO chat message (silence until the round, D2-07 budget); the narrator must never receive r.rationale or the suggestion text — labels only (COMP-03 "without a lecture"; outbound chat is itself a compliance surface); (5) implement reconcile as a real db-vs-memory comparison: read the open round + tally fresh from SQLite and compare against round.snapshot(); log any divergence and re-sync the in-memory tally from the ledger (delegate to a RoundManager method if 02-01 shipped one; a log-only stub or TODO marker is NOT acceptable); (6) startTwitchChat({ source: chatSource, broadcasterUserId, intake, submit: submitCandidate pre-bound with classifyThenNotify, round, narrator, reconcile, logger }); (7) register createApp-owned onUserSocketReady/onUserSocketDisconnect handlers on the source that drive the twitchStatus value ("connected"/"disconnected") exposed to ConsoleServerDeps — "unauthorized" when no source/sink pair exists. Ensure close() stops the listener (the startTwitchChat return's stop()) when it was started. This code path is IDENTICAL for injected fakes and real adapters — that identity is what makes plan 02-06's e2e a proof of production wiring.

    The isMain entrypoint branch's ONLY job (armPanicHotkey pattern — vitest never loads twurple): dynamic-import @twurple/auth, @twurple/api, @twurple/eventsub-ws inside a try/catch; createAuthProvider from env (TWITCH_CLIENT_ID/SECRET/TWITCH_TOKEN_PATH); if provider is null log "TWITCH DISABLED — no token; authorize at http://127.0.0.1:4900/auth/start" and call createApp WITHOUT chatSource/chatSink (console + overlay keep running — graceful degradation; twitchAuth is still passed when client id/secret exist so /auth/start works); otherwise construct ApiClient + EventSubWsListener, adapt them behind the two seam interfaces (thin arrow-function adapters: EventSubWsListener → ChatEventSource mapping twurple's event object to { chatterId, chatterDisplayName, messageText }; ApiClient.chat → ChatMessageSink), build the twitchAuth object from buildAuthorizeUrl/completeAuthorization + env, and pass { chatSource, chatSink, twitchAuth } into createApp. NO narrator, sender, subscription, startTwitchChat, or reconcile logic may live in the isMain branch — construction of real adapters and option passing only.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && grep -c "chatSink" src/main.ts && grep -cE "import .*@twurple" src/ingestion/twitch-chat.ts | grep -qx 0 && echo NO_STATIC_TWURPLE_IN_LISTENER</automated>
  </verify>
  <acceptance_criteria>
    - CreateAppOptions declares both chatSource and chatSink; `grep -c "chatSink" src/main.ts` >= 2 (option + composition use)
    - startTwitchChat, createNarrator, createChatSender, and the ROUND_OPENED/ROUND_CLOSED narrator subscriptions are all invoked INSIDE createApp (not the isMain branch) — a createApp-level test with a fake source + capturing sink observes round-open narration end-to-end without touching twurple
    - The isMain branch contains only dynamic twurple imports, adapter construction, twitchAuth env wiring, and the createApp call — grep confirms no `createNarrator|startTwitchChat` call inside the isMain block
    - `npm test` fully green — including chat-sender invariant (no new sendChatMessage callers) and single-funnel (no new toQueuedTask/enqueue callers: !suggest goes through submitCandidate only)
    - vitest suite passes with zero network access; static `import ... from "@twurple/..."` appears nowhere under src/ except type-only imports in src/ingestion/twitch-auth.ts and dynamic imports in src/main.ts's entrypoint branch
    - reconcile performs a real db-vs-memory tally comparison with re-sync (source assertion: reads round_votes and compares against snapshot(); no TODO/log-only stub)
    - Rejected-suggestion feedback carries CATEGORY_META label only: classifyThenNotify never passes rationale or suggestion text to the narrator (source assertion)
  </acceptance_criteria>
  <done>With env + a one-time browser authorization, the app reads real chat, filters suggestions per-user before the gate, narrates rounds in show copy, and degrades gracefully when Twitch is absent — and the identical composition runs under test fakes, so plan 02-06 can prove it without src edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Twitch EventSub WS → message handler | Raw viewer-authored payloads (adversarial by design) enter the process here |
| Suggestion intake → metered Sonnet classifier | Each accepted suggestion costs a paid API call |
| Browser → GET /auth/callback | Credential-writing route reachable by any local browser page |
| Bot → public Twitch chat | Outbound messages are themselves a ToS compliance surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Spoofing | intake cooldown bypass via display-name change | mitigate | Intake state keyed by chatterId (immutable numeric id) exclusively; display name used only for @-reply rendering |
| T-02-14 | Denial of Service | !suggest flood burning metered Sonnet calls (closes Phase 1 accepted risk T-01-11) | mitigate | Synchronous per-user cooldown + max-1-pooled + exact-dup checks run BEFORE submitCandidate/classify; over-limit gets a coalesced quiet notice; POOL_MAX_SIZE bounds memory with audited eviction |
| T-02-15 | Tampering | malformed/hostile EventSub payload shape | mitigate | zod safeParse on the event object before any field use; whole handler wrapped in try/catch; listener never crashes on hostile input (twurple's TS types are not a runtime guarantee) |
| T-02-16 | Spoofing (login CSRF) | GET /auth/callback swapping in an attacker's token | mitigate | Single-use 10-min-expiry state nonce generated at /auth/start and required to match at callback; zod-validated query; terse errors |
| T-02-17 | Information Disclosure / Compliance | outbound bot messages echoing non-compliant text | mitigate | Narration renders fixed UI-SPEC templates; rejected/held feedback carries ONLY viewer-safe CATEGORY_META labels — never the rejected suggestion text or classifier rationale; candidate titles in round messages are already-gate-approved text, truncated to 60 chars |
| T-02-18 | Elevation of Privilege | chat text treated as instructions downstream | accept (out of phase scope) | Phase 3 owns the orchestrator prompt-injection boundary (SAND-04); this phase preserves the discipline — suggestion text is opaque data end-to-end, and the winner text enqueued is byte-identical to the gated text (T-02-09) |
</threat_model>

<verification>
- `npm test` green across all suites and both invariant scans
- With a fake source + capturing sink injected via CreateAppOptions: !suggest → feedback loop and !vote → tally mutation observable via GET /api/state (plan 02-06 formalizes this as e2e)
- `npm run dev` without Twitch env: app starts, console shows unauthorized pill, nothing crashes
</verification>

<success_criteria>
- CHAT-01: `!suggest <idea>` enters the candidate pipeline via submitCandidate only
- COMP-03: rejected suggestions get category-level @-reply feedback (label only, no lecture), held/duplicate/cooldown variants per UI-SPEC
- CHAT-05: round open/close/winner narrated in exact show copy through the single budgeted sender; tallies never in chat
- T-01-11 formally closed by D2-11 enforcement before classification
- The full chat pipeline composes inside createApp behind the chatSource/chatSink seams — test fakes and production adapters share one code path
</success_criteria>

<output>
Create `.planning/phases/02-chat-vote-loop/02-04-SUMMARY.md` when done.
</output>
