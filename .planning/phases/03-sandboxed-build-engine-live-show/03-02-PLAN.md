---
phase: 03-sandboxed-build-engine-live-show
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - package.json
  - src/shared/types.ts
  - src/orchestrator/types.ts
  - src/orchestrator/progress-events.ts
  - src/orchestrator/progress-events.test.ts
  - src/audit/record.ts
  - src/audit/schema.sql
  - tests/e2e/build-flow.e2e.test.ts
autonomous: true
requirements: [BUILD-02]
must_haves:
  truths:
    - "The orchestrator emits a small, stable status vocabulary: queued/researching/planning/building/done/failed/refused"
    - "Raw Agent SDK message/hook types never leak past the translation layer — only PipelineStage words cross into overlay/console/chat/audit"
    - "Every pipeline-stage transition can be written to the append-only audit ledger with the task id"
  artifacts:
    - path: "src/orchestrator/progress-events.ts"
      provides: "SDK message/hook stream → PipelineStage translation (BUILD-02)"
      min_lines: 30
    - path: "src/orchestrator/types.ts"
      provides: "Injected structural interfaces: AgentRunner/QueryFn, SandboxAdapter, DevServerProbe, BuildStatusView"
      min_lines: 40
    - path: "tests/e2e/build-flow.e2e.test.ts"
      provides: "The failing end-to-end happy-path spec the phase makes pass (MVP e2e-first)"
      min_lines: 30
  key_links:
    - from: "src/orchestrator/progress-events.ts"
      to: "PipelineStage vocabulary in src/shared/types.ts"
      via: "fixed Record translation table (PILL_BY_MODE analog)"
      pattern: "PipelineStage"
    - from: "src/audit/record.ts recordPipelineStage"
      to: "audit_log"
      via: "shared insert() helper, one row per stage transition"
      pattern: "pipeline_stage"
---

<objective>
Interface-first foundation for the whole phase: install the Agent SDK, define the shared status vocabulary and the injected structural interfaces every later plan builds against, ship the pure SDK→vocabulary translation layer (BUILD-02), add the append-only audit record functions for pipeline events, and write the FAILING end-to-end happy-path spec (MVP e2e-first) that waves 3–4 make pass. No agent actually runs yet — this plan defines the contracts so downstream plans receive blueprints, not a scavenger hunt.

Purpose: Give executors the exact type contracts and the stable public event vocabulary up front, so the orchestrator, sandbox adapter, overlay, and preview can all be built and unit-tested against injected fakes without real WSL2 / query() / network.
Output: `package.json` (SDK dep), extended `src/shared/types.ts`, new `src/orchestrator/types.ts` + `progress-events.ts` (+test), extended `src/audit/record.ts` + `schema.sql`, and a failing `tests/e2e/build-flow.e2e.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sandboxed-build-engine-live-show/03-CONTEXT.md
@.planning/phases/03-sandboxed-build-engine-live-show/03-RESEARCH.md
@.planning/phases/03-sandboxed-build-engine-live-show/03-PATTERNS.md
@.planning/phases/03-sandboxed-build-engine-live-show/03-UI-SPEC.md

<interfaces>
<!-- Existing contracts this plan extends. Executor uses these directly. -->

From src/shared/types.ts (extend, do not rename):
- `export type CandidateSource = "chat" | "channel_points" | "donation" | "chaos" | "operator";`  ← ADD "orchestrator" (03-RESEARCH.md Open Question 2)
- `export interface SuggestionCandidate { id; source; kind; twitchUsername; text; submittedAtMs; }`
- `export type StreamMode = ... | "BUILD_IN_PROGRESS" | ...;`

From src/overlay/server.ts (the PILL_BY_MODE analog to mirror for the stage table):
- `const PILL_BY_MODE: Record<StreamMode, OverlayPill>` — a fixed table; raw internal words never leak past it.

From src/audit/record.ts (mirror recordGateDecision's arg-object + insert() shape):
- `insert(db, { createdAtMs, eventType, source, twitchUsername, suggestionText, decision, category, rationale, streamMode, taskId })`
- `audit_log` has NO CHECK constraints on event_type/source/category (schema-safe to add vocabulary).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the Agent SDK and define the shared vocabulary + injected interfaces</name>
  <files>package.json, src/shared/types.ts, src/orchestrator/types.ts</files>
  <read_first>
    - package.json (dependencies + allowScripts blocks)
    - src/shared/types.ts (whole file — CandidateSource, SuggestionCandidate, StreamMode)
    - .planning/phases/03-sandboxed-build-engine-live-show/03-RESEARCH.md §Standard Stack (SDK version + Package Legitimacy Audit) and §Code Examples (query() shape, spawnClaudeCodeProcess, SpawnOptions/SpawnedProcess)
    - .planning/phases/03-sandboxed-build-engine-live-show/03-PATTERNS.md §Shared Patterns (injected-dependency + structural-interface seam)
  </read_first>
  <action>
    Install `@anthropic-ai/claude-agent-sdk@0.3.206` (pin exact per CLAUDE.md pre-1.0 guidance; disposition Approved [OK] in 03-RESEARCH.md Package Legitimacy Audit — no blocking checkpoint required). Add its native optional dep to `allowScripts` if the install prompts. Do NOT install dockerode (fallback-only).
    In src/shared/types.ts: add `"orchestrator"` to the `CandidateSource` union (per 03-RESEARCH.md Open Question 2 — cleaner audit trail than reusing "operator"). Add `export type PipelineStage = "queued" | "researching" | "planning" | "building" | "done" | "failed" | "refused";` and `export interface BuildStatusView { taskId: string; title: string; stage: PipelineStage; }` (the overlay/console-facing build status; `title` is the chat-derived task text, rendered textContent-only downstream).
    In new src/orchestrator/types.ts: define the INJECTED structural seams (mirroring KeyEventSource / ChatEventSource / OverlayModeSource so vitest injects fakes and never touches real WSL2/query()/network):
      - `AgentRunner` — a `run(spec)` interface wrapping the SDK `query()` call; `spec` carries { agent: "research" | "build", model: "sonnet" | undefined, systemPrompt: string, userPrompt: string, sandbox?, spawnClaudeCodeProcess?, abortController }, and returns an async iterable of a small internal message type. Keep the raw SDK types confined here.
      - `SandboxAdapter` — { spawn(opts): SpawnedProcessLike; terminate(): Promise<void> } behind which the real wsl.exe implementation (03-05) hides.
      - `DevServerProbe` — { reachable(): Promise<boolean> } for the preview manager (03-08).
      - `BuildSessionDeps` skeleton interface (taskQueue, db, machine, registry, agentRunner, sandboxAdapter, comp02, promptBoundary, progress sink, logger) — the DI shape 03-06 implements against (mirror EnqueueWinnerDeps).
    Export nothing that leaks a raw SDK type outside src/orchestrator/.
  </action>
  <acceptance_criteria>
    - `npm ls @anthropic-ai/claude-agent-sdk` shows `0.3.206` (exact).
    - `grep -n '"orchestrator"' src/shared/types.ts` matches inside the CandidateSource union.
    - `PipelineStage` and `BuildStatusView` are exported from src/shared/types.ts.
    - src/orchestrator/types.ts exports AgentRunner, SandboxAdapter, DevServerProbe, BuildSessionDeps as interfaces (no implementation).
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Progress-events translation layer + audit record functions (BUILD-02)</name>
  <files>src/orchestrator/progress-events.ts, src/orchestrator/progress-events.test.ts, src/audit/record.ts, src/audit/schema.sql</files>
  <behavior>
    - A SubagentStart/SubagentStop/task_progress SDK message for the research agent maps to stage `researching`; for the build agent to `building`; the plan step maps to `planning`.
    - A result message with a refusal subtype maps to `refused` (not `failed`).
    - An error/failed result maps to `failed`; a clean completion maps to `done`.
    - An unrecognized raw SDK message type maps to no stage change (returns null) — raw types never leak.
    - recordPipelineStage writes exactly one audit_log row with event_type `pipeline_stage`, source `orchestrator`, the stage in `decision`, and the task id.
  </behavior>
  <read_first>
    - src/overlay/server.ts lines 30, 84-129 (PILL_BY_MODE + buildOverlayState — the fixed-table precedent)
    - src/audit/record.ts (whole file — recordGateDecision + insert() shape to mirror)
    - src/audit/schema.sql (event_type/source/category comment vocabulary — comment-only change)
    - .planning/phases/03-sandboxed-build-engine-live-show/03-RESEARCH.md §Code Examples (SDK message stream) and §phase_requirements BUILD-02
  </read_first>
  <action>
    Create src/orchestrator/progress-events.ts: a PURE function `translate(sdkMessage): PipelineStage | null` backed by a fixed translation table (the PILL_BY_MODE analog) — the ONLY place raw SDK message/hook shapes are inspected. Nothing downstream imports the raw SDK message type. Fail-closed: an unknown message returns null, never throws.
    Extend src/audit/record.ts with the Phase 3 record functions, each mirroring recordGateDecision's arg-object → insert() shape (NO schema column changes): `recordPipelineStage` (event_type `pipeline_stage`), `recordComp02Decision` (event_type `comp02_decision`), `recordBuildRefusal` (`build_refused`), `recordBuildRetry` (`build_retry`), `recordBuildSkip` (`build_skip`), `recordSandboxTeardown` (`sandbox_teardown`). All carry `source: "orchestrator"` and the task id.
    Update src/audit/schema.sql: extend ONLY the descriptive comment listing event_type/source vocabulary (no new columns — audit_log has no CHECK constraints, confirmed in 03-PATTERNS.md).
    Write src/orchestrator/progress-events.test.ts covering the behavior cases above with hand-built fake SDK messages (no network, no real SDK session).
  </action>
  <acceptance_criteria>
    - `translate()` returns the correct PipelineStage for research/plan/build/done/failed/refused fixtures and null for unknown input (asserted in the test).
    - progress-events.ts imports no symbol that is re-exported to a non-orchestrator module (raw SDK types stay contained).
    - recordPipelineStage + the other new record fns compile and each write one row (unit-asserted or covered by the e2e later).
    - schema.sql changed comment-only (git diff shows no new column).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/orchestrator/progress-events.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Failing end-to-end happy-path spec (MVP e2e-first)</name>
  <files>tests/e2e/build-flow.e2e.test.ts</files>
  <read_first>
    - tests/e2e/round-flow.e2e.test.ts (createApp bootstrap + injected-fake pattern, ephemeral port, in-memory db)
    - tests/e2e/kill-switch.e2e.test.ts (abort/registry e2e shape)
    - src/main.ts lines 65-109, 133-149, 462-486 (CreateAppOptions, AppHandle, createApp composition + close())
    - .planning/phases/03-sandboxed-build-engine-live-show/03-UI-SPEC.md §Overlay build states + §Bot chat narration (the observable stages/messages the e2e asserts)
  </read_first>
  <action>
    Write tests/e2e/build-flow.e2e.test.ts describing the phase's target happy path, using injected fakes (fake AgentRunner, fake SandboxAdapter, fakeClassifier) — NO real WSL2/query()/network. Assert the observable end-to-end slice: a QueuedTask picked up → machine enters BUILD_IN_PROGRESS → stages emit researching → planning → (COMP-02 approves the plan) → building → done → machine returns to IDLE; and the overlay `GET /api/state` (or build-status push) reflects the current stage. Mark the suite so it FAILS/`.todo` until 03-06/03-09 land (this is the MVP contract the phase drives toward). Add a code comment naming the plans that make it green (03-06 core, 03-09 failure/veto).
    Keep the test authored against createApp's injected-fake seams (extend CreateAppOptions in 03-06 to accept the orchestrator fakes) so no src edits are needed later to prove production wiring — mirroring how 02-06 proved chat wiring.
  </action>
  <acceptance_criteria>
    - tests/e2e/build-flow.e2e.test.ts exists and encodes the research→plan→comp02→build→done observable sequence.
    - The suite is explicitly pending/failing (documented) — it does not falsely pass before the slice exists.
    - `npm test` still exits with the suite visible as todo/failing-by-design, not an accidental green.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/e2e/build-flow.e2e.test.ts 2>&1 | grep -Eiv '^#' | grep -Eiq 'todo|fail|pending'</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | New package `@anthropic-ai/claude-agent-sdk` enters the dependency tree |
| SDK message stream → overlay/console/chat/audit | Raw agent internals must not leak past the translation layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `@anthropic-ai/claude-agent-sdk` install | mitigate | Package Legitimacy Audit in 03-RESEARCH.md = Approved [OK] (7.2M/wk, first-party Anthropic repo); pin exact `0.3.206`; slopcheck already run. No [ASSUMED]/[SUS] packages, so no blocking human checkpoint. |
| T-03-06 | Information Disclosure | Raw SDK message types leaking classifier/agent internals to the stream | mitigate | progress-events.ts is the sole inspector of raw messages; only the fixed PipelineStage vocabulary crosses outward (PILL_BY_MODE discipline) |
| T-03-07 | Repudiation | Pipeline events not auditable | mitigate | recordPipelineStage + siblings append one row per event with task id (D3-13) |
</threat_model>

<verification>
- `npm run typecheck` and `npm test -- src/orchestrator/progress-events.test.ts` pass.
- The SDK is pinned exact; dockerode is NOT installed.
- The failing e2e spec exists and drives the phase.
</verification>

<success_criteria>
The status vocabulary, injected interfaces, translation layer, audit record functions, and the failing happy-path e2e all exist — downstream plans build against concrete contracts (BUILD-02 delivered), and the phase has an executable definition of done.
</success_criteria>

<output>
Create `.planning/phases/03-sandboxed-build-engine-live-show/03-02-SUMMARY.md` when done.
</output>
