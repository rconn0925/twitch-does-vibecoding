---
phase: 03-sandboxed-build-engine-live-show
plan: 04
type: tdd
wave: 2
depends_on: [03-02]
files_modified:
  - src/orchestrator/comp02.ts
  - src/orchestrator/comp02.test.ts
autonomous: true
requirements: [COMP-02]
must_haves:
  truths:
    - "The generated build plan is re-screened through the SAME Phase 1 gate before any code is written"
    - "A rejected plan aborts the build; a held plan routes to the existing console review queue; only approved proceeds"
    - "COMP-02 is a direct classify() call on the plan text — never submitCandidate(), never a parallel gate (single-funnel holds)"
  artifacts:
    - path: "src/orchestrator/comp02.ts"
      provides: "Second-pass compliance re-screen of the build plan via classify() (COMP-02, D3-06/D3-07)"
      min_lines: 30
  key_links:
    - from: "src/orchestrator/comp02.ts"
      to: "src/compliance/gate.ts classify()"
      via: "direct classify(gateDeps, planCandidate) call (enqueueWinner precedent)"
      pattern: "classify\\("
---

<objective>
Build the second compliance pass (COMP-02): after the plan agent produces a build plan, re-screen that plan text through the SAME Phase 1 gate via a direct `classify()` call — before any code is written — and again on output batches during execution (D3-06 pre-write, D3-07 in-flight). A rejection aborts the build with narrated feedback; a held-for-review routes to the existing console review queue; only approved proceeds. This is a second CALL to the single funnel, not a new gate — the single-funnel invariant must still hold.

Purpose: An approved-but-vague suggestion must not yield non-compliant output — the plan/output is checked, not just the raw suggestion.
Output: `src/orchestrator/comp02.ts` (+unit test), consumed by the orchestrator in 03-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sandboxed-build-engine-live-show/03-CONTEXT.md
@.planning/phases/03-sandboxed-build-engine-live-show/03-RESEARCH.md
@.planning/phases/03-sandboxed-build-engine-live-show/03-PATTERNS.md

<interfaces>
From src/compliance/gate.ts (the second-pass entry — call directly, NOT submitCandidate):
- `export async function classify(deps: GateDeps, candidate: SuggestionCandidate): Promise<GateResult>` — prefilter → classifier → D-12 guard → audit → return; never throws (fail-closed).
- `GateDeps { db?; fakeClassifier?; classifier?; prefilter?; logger?; streamModeProvider? }`
- `GateResult { decision: "approved"|"rejected"|"held-for-review"; category; rationale }`

From src/pipeline/round.ts (the direct-classify precedent named in 03-RESEARCH.md Pattern 1):
- enqueueWinner builds a candidate then calls the pre-bound classify — NOT submitCandidate (whose CandidateSchema hard-codes a source enum and is async fire-and-forget).

From src/shared/types.ts:
- `SuggestionCandidate { id; source; kind; twitchUsername; text; submittedAtMs }` — source now includes "orchestrator" (03-02).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: comp02.ts — synchronous plan re-screen via direct classify() (RED→GREEN→REFACTOR)</name>
  <files>src/orchestrator/comp02.ts, src/orchestrator/comp02.test.ts</files>
  <read_first>
    - src/compliance/gate.ts (whole file — classify() + GateDeps + fail-closed)
    - src/pipeline/round.ts lines 57-96 (enqueueWinner direct-classify pattern)
    - .planning/phases/03-sandboxed-build-engine-live-show/03-PATTERNS.md §Pattern 1 (COMP-02 as a direct classify() call)
    - .planning/phases/03-sandboxed-build-engine-live-show/03-RESEARCH.md §COMP-02 wiring + Open Question 2 (source "orchestrator")
  </read_first>
  <action>
    RED first, then GREEN. Export `screenBuildPlan(deps, { taskId, planText }): Promise<Comp02Outcome>` where deps carries a pre-bound `classify: (candidate) => Promise<GateResult>` (bound to the app's gateDeps in main.ts — NOT a fresh gate). Build a `SuggestionCandidate` with `id: ${taskId}-plan`, `source: "orchestrator"`, `kind: "suggestion"`, `twitchUsername: null`, `text: planText` (the build agent's OWN generated plan — NEVER raw chat text re-fed), `submittedAtMs: Date.now()`, and call `classify()` directly. Map the result: `approved` → `{ proceed: true }`; `rejected` → `{ proceed: false, disposition: "rejected", category }` (caller narrates + aborts, audit row already written by classify()); `held-for-review` → `{ proceed: false, disposition: "held" }` (caller routes to the existing review_queue D-08 flow). Do NOT call submitCandidate, do NOT call toQueuedTask, do NOT call .enqueue — comp02 only returns a decision. Never throw: a classify() fail-closed rejection is a normal `rejected` outcome. Also export a thin `screenOutputBatch(deps, { taskId, outputText })` reusing the same call shape for the D3-07 in-flight re-screen (cadence decided by the orchestrator in 03-06; discretion: re-screen on each Write/Edit batch or every 60s of building, whichever first).
    REFACTOR: keep the candidate-construction in one private helper shared by both exports.
  </action>
  <acceptance_criteria>
    - Test (injected fakeClassifier returning approved/rejected/held) asserts the three outcomes map correctly and that submitCandidate/toQueuedTask/.enqueue are never referenced.
    - Test asserts the candidate passed to classify has `source: "orchestrator"` and `text === planText` (never mutated).
    - Test asserts a throwing fakeClassifier still resolves to a `rejected`/fail-closed outcome (never throws out of comp02).
    - `grep -c 'submitCandidate\|toQueuedTask\|\.enqueue(' src/orchestrator/comp02.ts` returns 0 (after excluding comments).
    - `npm test -- src/orchestrator/comp02.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/orchestrator/comp02.test.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated plan → build execution | An approved-but-vague suggestion could produce a non-compliant plan; screened before code is written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Elevation of Privilege | Non-compliant build plan reaching execution unscreened | mitigate | comp02.ts re-screens the plan via classify() BEFORE the sandboxed build query() runs (D3-06); in-flight batch re-screen (D3-07) |
| T-03-11 | Tampering | COMP-02 bypassing the single funnel (parallel gate / submitCandidate) | mitigate | direct classify() only; single-funnel invariant (check c/d) still holds — no toQueuedTask/enqueue in orchestrator |
</threat_model>

<verification>
- comp02.test.ts passes; single-funnel.test.ts still green (orchestrator never enqueues or mints QueuedTask).
- classify() is the sole route to a decision — no parallel classifier constructed.
</verification>

<success_criteria>
The build plan is re-screened through the same gate before any code is written (COMP-02 / success criterion 1): rejected plans abort with narrated feedback, held plans route to the existing review queue, only approved plans proceed — and the single-funnel invariant is preserved.
</success_criteria>

<output>
Create `.planning/phases/03-sandboxed-build-engine-live-show/03-04-SUMMARY.md` when done.
</output>
