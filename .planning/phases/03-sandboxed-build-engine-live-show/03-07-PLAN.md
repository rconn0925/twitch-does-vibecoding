---
phase: 03-sandboxed-build-engine-live-show
plan: 07
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - src/overlay/server.ts
  - src/overlay/server.test.ts
  - src/overlay/public/overlay.js
  - src/overlay/public/overlay.css
autonomous: true
requirements: [PRES-02, PRES-04]
must_haves:
  truths:
    - "The overlay shows the suggestion queue, current build status, and the pipeline stage (researching → planning → building) in real time"
    - "The pill vocabulary is unchanged — the pill stays BUILDING for the whole pipeline; the build panel carries the fine-grained stage"
    - "The chat-voted task title renders textContent-only, truncated to 80 chars — no chat-derived markup reaches the DOM"
  artifacts:
    - path: "src/overlay/server.ts"
      provides: "OverlayState.buildStatus + OverlayBuildSource seam + immediate stage-diff push (PRES-02/04)"
      min_lines: 220
    - path: "src/overlay/public/overlay.js"
      provides: "Build-status panel renderer: NOW BUILDING header, task title, 3-step stepper, stage caption, 8s done beat"
      min_lines: 250
  key_links:
    - from: "src/overlay/public/overlay.js build panel"
      to: "OverlayState.buildStatus"
      via: "textContent-only render on ws push"
      pattern: "buildStatus"
    - from: "src/overlay/server.ts"
      to: "PipelineStage vocabulary"
      via: "BuildStatusView pushed as a state diff, immediate (not debounced)"
      pattern: "buildStatus"
---

<objective>
Extend the existing read-only OBS overlay so viewers watch the build live (PRES-02 queue + build status, PRES-04 pipeline stage). Additive to the Phase 2 full-state-on-connect + diff surface: add a `buildStatus` field to `OverlayState`, a structural `OverlayBuildSource` seam the orchestrator feeds, and a build-status panel in `overlay.js`/`overlay.css` per the approved UI-SPEC — a NOW BUILDING header, the chat-voted task title (textContent-only, 80-char truncated), a three-step Research → Plan → Build stepper, a fixed stage caption, and an 8-second "BUILT IT" done beat. The pill vocabulary is unchanged (stays BUILDING).

Purpose: "The AI process is part of the show" — the pipeline stage is a narrative feature, and the build panel reuses the vote panel's slot (mutually exclusive modes) with the same broadcast-safety rules.
Output: extended `src/overlay/server.ts` (+test) and `src/overlay/public/overlay.js` + `overlay.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sandboxed-build-engine-live-show/03-UI-SPEC.md
@.planning/phases/03-sandboxed-build-engine-live-show/03-PATTERNS.md

<interfaces>
From src/overlay/server.ts (extend, same file, additive — see PATTERNS.md §overlay MODIFIED):
- `export interface OverlayState { pill: OverlayPill; round: RoundSnapshot | null; nextUp: string[]; }`  ← ADD `buildStatus: BuildStatusView | null;`
- `PILL_BY_MODE` — UNCHANGED (no new pill words; BUILD_IN_PROGRESS → "BUILDING").
- `OverlayModeSource`/`OverlayRoundSource`/`OverlayQueueSource` seams — ADD an `OverlayBuildSource { snapshot(): BuildStatusView | null; on(event, handler) }` seam.
- pushState()/buildOverlayState() — extend to include buildStatus; lifecycle events push immediately (stage transitions are low-frequency → push immediately like ROUND_OPENED, NOT via the 300ms tally debounce).

From src/shared/types.ts (03-02): `BuildStatusView { taskId; title; stage: PipelineStage }`, `PipelineStage`.

From src/overlay/public/overlay.js: `el(tag, className, text)` textContent helper; `candidateRow()`/`renderVotePanel()`/`startWinnerBeat()` (WINNER_BEAT_MS=8000) — the direct template for the stepper + done beat; `truncate(text, 80)`.

DOM safety: tests/invariants/dom-safety.test.ts auto-discovers src/**/public/*.js — no innerHTML/insertAdjacentHTML/document.write/eval.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend OverlayState + OverlayBuildSource seam + immediate stage-diff push</name>
  <files>src/overlay/server.ts, src/overlay/server.test.ts</files>
  <read_first>
    - src/overlay/server.ts (whole file — OverlayState, seams, buildOverlayState, pushState, the ROUND_OPENED/CLOSED immediate-push wiring, VOTE_RECORDED debounce)
    - src/overlay/server.test.ts (existing test shape)
    - .planning/phases/03-sandboxed-build-engine-live-show/03-UI-SPEC.md §Overlay build states + §Motion & update cadence
  </read_first>
  <action>
    Add `buildStatus: BuildStatusView | null` to OverlayState. Add an `OverlayBuildSource` seam (mirrors OverlayRoundSource: `snapshot(): BuildStatusView | null` + `on(event, handler)`), added to OverlayServerDeps. In buildOverlayState(), populate `buildStatus` from the build source's snapshot (null when no build active). Subscribe to a build-stage-changed event on the build source and push immediately (like ROUND_OPENED/ROUND_CLOSED) — NOT through the tally debounce, since stage transitions are a handful per build. `nextUp` (queue) already exists — PRES-02's queue requirement is satisfied by the existing taskQueue.list() slice; keep it. Do NOT change PILL_BY_MODE. Keep the surface read-only (no new mutation route).
    Test: with a fake OverlayBuildSource, `GET /api/state` and a ws push both carry the current buildStatus; a stage change triggers an immediate push; buildStatus is null when no build is active.
  </action>
  <acceptance_criteria>
    - OverlayState includes buildStatus; GET /api/state returns it (asserted).
    - A build-stage change causes an immediate ws push (not debounced) — asserted with a fake source + timing.
    - PILL_BY_MODE is unchanged (git diff shows no edit to the table).
    - `npm test -- src/overlay/server.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/overlay/server.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build-status panel in overlay.js/overlay.css (PRES-02/04, UI-SPEC)</name>
  <files>src/overlay/public/overlay.js, src/overlay/public/overlay.css</files>
  <read_first>
    - src/overlay/public/overlay.js lines 1-35, 115-247 (el() helper, candidateRow/renderVotePanel, startWinnerBeat/WINNER_BEAT_MS, truncate, reconnect-with-backoff)
    - src/overlay/public/overlay.css (vote panel geometry — the build panel reuses the slot)
    - .planning/phases/03-sandboxed-build-engine-live-show/03-UI-SPEC.md §Overlay Build-Status Panel Layout Contract + §Color (overlay build panel) + §Copywriting (public overlay build panel)
    - tests/invariants/dom-safety.test.ts (the auto-discovered scan the new code must satisfy)
  </read_first>
  <action>
    Add a build-status panel renderer to overlay.js, mounted when `state.buildStatus` is non-null (the vote panel and build panel never render simultaneously — VOTING_ROUND vs BUILD_IN_PROGRESS are mutually exclusive). Per UI-SPEC: header "NOW BUILDING" (→ "BUILT IT" during the done beat); task title via `el()` textContent-only, JS-truncated to 80 chars + CSS ellipsis backstop (the single chat-derived string on this panel); a three-step stepper Research → Plan → Build with completed(green)/active(accent)/upcoming(secondary) badge treatment driven by `state.buildStatus.stage`; a fixed stage caption from the orchestrator-authored set (researching→"Digging into the idea", planning→"Drafting the build plan", building→"Writing the code", done→"Live on screen now", failed→"Regrouping…"(amber), refused→"Skipping this one"(amber)). On `done`: all steps green, header "BUILT IT", hold 8s (reuse the WINNER_BEAT_MS pattern) then collapse. On `failed`/`refused`: freeze the stepper at the current step, amber caption, no red, no error text, no "failed"/"error" word on stream. On ws disconnect: freeze last render, silent backoff retry (existing pattern). Add the CSS (reuse vote-panel anchor: lower-left 48px, 560px width; 300ms background-color transition on the active badge). No innerHTML anywhere.
  </action>
  <acceptance_criteria>
    - The build panel renders only when buildStatus != null and reuses the vote-panel slot geometry (48px lower-left, 560px).
    - Task title is set via el()/textContent and truncated to 80 chars; no innerHTML/insertAdjacentHTML/document.write/eval in overlay.js.
    - Stepper reflects the stage; done beat holds ~8s then collapses; failed/refused show amber caption, never red/error text.
    - `npm test -- tests/invariants/dom-safety.test.ts` passes (covers the new build-panel code path).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/invariants/dom-safety.test.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat-voted task title → OBS DOM | The one chat-derived string on the build panel must render as opaque text |
| overlay ws → OBS browser source | Read-only, loopback-only; already established Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Tampering (XSS) | Chat-voted title rendered on the overlay | mitigate | textContent-only via el(), 80-char truncation; dom-safety invariant auto-covers src/overlay/public/*.js |
| T-03-16 | Information Disclosure | Error/failure detail or classifier rationale reaching the broadcast surface | mitigate | UI-SPEC hard rule: no error text on stream; failed/refused show only the fixed amber caption; buildStatus carries only taskId/title/stage |
</threat_model>

<verification>
- overlay.server.test.ts + dom-safety.test.ts pass.
- PILL_BY_MODE unchanged; surface remains read-only (no mutation route added).
</verification>

<success_criteria>
The overlay shows the queue, current build status, and pipeline stage in real time (PRES-02/PRES-04, success criterion 4), broadcast-safe (no red, no error text, textContent-only), with the pill staying BUILDING across the whole pipeline.
</success_criteria>

<output>
Create `.planning/phases/03-sandboxed-build-engine-live-show/03-07-SUMMARY.md` when done.
</output>
