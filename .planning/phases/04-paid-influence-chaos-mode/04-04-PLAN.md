---
phase: 04-paid-influence-chaos-mode
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/overlay/server.ts
  - src/overlay/server.test.ts
  - src/overlay/public/overlay.js
  - src/overlay/public/overlay.css
autonomous: true
requirements: [PAID-01, PAID-02, CHAOS-01]
must_haves:
  truths:
    - "While a control window is active, the public overlay shows a free-reign banner (top-left, 48px): FREE REIGN label + truncated donor display name + m:ss countdown — and NOTHING else (no amount, no currency, no message text, no trigger type on the public wire)"
    - "The pill vocabulary becomes exactly six words: FREE_REIGN_WINDOW -> FREE REIGN (violet dot), CHAOS_MODE -> CHAOS (white dot); no other state word ever reaches the stream"
    - "The build panel gains a provenance chip (FREE REIGN violet / CHAOS PICK neutral) driven by BuildStatusView.source, so viewers see how the current build was picked"
    - "The donor display name is attacker-controlled and renders via textContent only, JS-truncated to 24 chars — the zero-innerHTML invariant covers the new banner/chip code paths"
  artifacts:
    - path: "src/overlay/server.ts"
      provides: "OverlayState.controlWindow coarse projection {donorDisplayName,endsAtMs} + OverlayControlWindowSource seam + PILL_BY_MODE FREE REIGN/CHAOS + immediate push"
      min_lines: 200
    - path: "src/overlay/public/overlay.js"
      provides: "free-reign banner renderer + provenance chip + client-side countdown from endsAtMs"
      min_lines: 200
  key_links:
    - from: "src/overlay/public/overlay.js banner"
      to: "OverlayState.controlWindow.endsAtMs"
      via: "client-side 1s countdown from absolute endsAtMs (server never streams timer frames)"
      pattern: "endsAtMs"
    - from: "src/overlay/public/overlay.js provenance chip"
      to: "OverlayState.buildStatus.source"
      via: "textContent-only fixed-vocabulary chip"
      pattern: "source"
    - from: "src/overlay/server.ts"
      to: "OverlayControlWindowSource seam"
      via: "coarse projection pushed immediately on window open/close (not debounced)"
      pattern: "controlWindow"
---

<objective>
Extend the read-only OBS overlay with the paid/chaos presentation per the APPROVED 04-UI-SPEC: a free-reign window banner (donor display name + m:ss countdown, top-left, coarse by design — no amount/message/trigger on the public wire), the one-time pill-vocabulary extension to six words (`FREE REIGN` violet / `CHAOS` neutral), and a build-panel provenance chip (`FREE REIGN` / `CHAOS PICK`) driven by `BuildStatusView.source`. Additive to the Phase 2/3 full-state-on-connect + diff surface, broadcast-safe (violet is paid-only, chaos gets no color, never red, textContent-only, ≥48px safe margins).

Purpose: the on-stream honesty beat — viewers always see when someone bought control and how the current build got picked, without ever exposing donor financials or message text.
Output: extended `src/overlay/server.ts` (+test), `src/overlay/public/overlay.js` + `overlay.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-paid-influence-chaos-mode/04-UI-SPEC.md
@.planning/phases/04-paid-influence-chaos-mode/04-PATTERNS.md

<interfaces>
From src/shared/types.ts (04-01): `BuildStatusView.source: "vote" | "donation" | "channel_points" | "chaos"`; `StreamMode`.
From src/overlay/server.ts (extend — PATTERNS.md §overlay/server.ts):
```typescript
export interface OverlayState { pill; round; nextUp; buildStatus; }  // ADD controlWindow: { donorDisplayName: string; endsAtMs: number } | null  (COARSE — name+deadline ONLY)
const PILL_BY_MODE: Record<StreamMode, OverlayPill> = { ... FREE_REIGN_WINDOW: "STANDBY"(placeholder), CHAOS_MODE: "STANDBY"(placeholder) ... }; // CHANGE these two -> "FREE REIGN" / "CHAOS"; OverlayPill union gains the two words
// ADD OverlayControlWindowSource { snapshot(): { donorDisplayName; endsAtMs } | null; on(event, handler) } + NULL_CONTROL_WINDOW_SOURCE default (mirrors OverlayBuildSource / NULL_BUILD_SOURCE)
```
Push cadence: window open/close/revoke are low-frequency show beats -> push IMMEDIATELY (like ROUND_OPENED / BUILD_STAGE_CHANGED), NOT through the vote-tally debounce (PATTERNS.md §overlay push-cadence).

From src/overlay/public/overlay.js: `el(tag,className,text)` textContent helper; the vote countdown renderer (client-side 1s tick from RoundSnapshot.endsAtMs — copy for the banner); `truncate()`; reconnect-with-backoff. Banner anchors top-left 48px (the only unused corner). Countdown digits -> Urgency amber (#F59E0B) for the final 10s. Provenance chip mounts on the build-panel header after NOW BUILDING when buildStatus.source is donation/channel_points (-> "FREE REIGN", violet #8B5CF6) or chaos (-> "CHAOS PICK", default). No chip for source "vote".

DOM safety: tests/invariants/dom-safety.test.ts auto-discovers src/**/public/*.js — no innerHTML/insertAdjacentHTML/document.write/eval. Donor name: el()/textContent + JS truncate 24 chars + CSS ellipsis backstop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OverlayState.controlWindow coarse projection + pill vocabulary + seam</name>
  <files>src/overlay/server.ts, src/overlay/server.test.ts</files>
  <read_first>
    - src/overlay/server.ts (whole file — OverlayState, PILL_BY_MODE, OverlayBuildSource/NULL_BUILD_SOURCE seam pattern, buildOverlayState, pushState, ROUND_OPENED immediate-push wiring)
    - src/overlay/server.test.ts (fake-source + GET /api/state + ws push test shape)
    - .planning/phases/04-paid-influence-chaos-mode/04-UI-SPEC.md §Overlay state model + §State-Contract Deltas
  </read_first>
  <action>
    Add `controlWindow: { donorDisplayName: string; endsAtMs: number } | null` to OverlayState — the COARSE public projection, name + deadline only (NO amount, currency, message, or trigger type on the public wire, T-04 narrow projection). Add an OverlayControlWindowSource seam (snapshot()/on()) + a NULL_CONTROL_WINDOW_SOURCE default, mirroring OverlayBuildSource, added to the server deps so the overlay composes before Phase 4 wiring lands. Populate controlWindow in buildOverlayState() from the source snapshot. Change PILL_BY_MODE: FREE_REIGN_WINDOW -> "FREE REIGN", CHAOS_MODE -> "CHAOS" (replacing the Phase 2 STANDBY placeholders); extend the OverlayPill union with the two words. Subscribe to WINDOW_OPENED/WINDOW_CLOSED/WINDOW_REVOKED (and chaos toggle if surfaced via the mode source) and push IMMEDIATELY — never through the tally debounce. Keep the surface read-only (no mutation route).
    Test: with a fake OverlayControlWindowSource, GET /api/state and a ws push carry the coarse controlWindow ({donorDisplayName,endsAtMs}) and NOTHING else (assert no amount/message/trigger keys); a window open triggers an immediate push; controlWindow is null when no window is active; PILL_BY_MODE maps FREE_REIGN_WINDOW/CHAOS_MODE to the new words.
  </action>
  <acceptance_criteria>
    - OverlayState.controlWindow is the coarse {donorDisplayName,endsAtMs} projection; the state object exposes no amount/currency/message/trigger field (asserted).
    - PILL_BY_MODE yields FREE REIGN / CHAOS; OverlayPill union updated.
    - A window lifecycle event causes an immediate (non-debounced) ws push — asserted with a fake source.
    - `npm test -- src/overlay/server.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/overlay/server.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Free-reign banner + provenance chip + pill dots (overlay.js/css)</name>
  <files>src/overlay/public/overlay.js, src/overlay/public/overlay.css</files>
  <read_first>
    - src/overlay/public/overlay.js (el() helper, the vote countdown client-tick from endsAtMs, truncate, pill renderer, reconnect-with-backoff, the build panel header from Phase 3)
    - src/overlay/public/overlay.css (pill variants, build-panel geometry, safe-margin conventions)
    - .planning/phases/04-paid-influence-chaos-mode/04-UI-SPEC.md §Overlay Free-Reign Banner + §Provenance Chip + §Color + §Typography + §New pill variants
    - tests/invariants/dom-safety.test.ts (the auto-discovered scan the new code must satisfy)
  </read_first>
  <action>
    Add a free-reign banner renderer to overlay.js, mounted whenever state.controlWindow !== null (banner independence rule: driven by controlWindow, NOT the pill — persists across FREE_REIGN_WINDOW -> BUILD_IN_PROGRESS while the donor's build runs, collapses on expiry/revoke). Single row, top-left 48px/48px: violet dot -> "FREE REIGN" (Label 20/600, violet #8B5CF6) -> donor display name via el()/textContent, JS-truncated to 24 chars with "…" (+ CSS nowrap/ellipsis backstop) -> m:ss countdown (Heading 32/700, tabular-nums), computed client-side from controlWindow.endsAtMs on the existing 1s tick (reuse the vote countdown machinery; digits -> amber #F59E0B in the final 10s). NEVER render amount/currency/message. On expiry/revoke the banner collapses on the next push — no "expired"/"revoked" text, no red, ever. On ws disconnect freeze the last render + silent backoff (existing pattern). Add the provenance chip to the build-panel header (after NOW BUILDING, 8px gap) when state.buildStatus.source is donation/channel_points ("FREE REIGN", violet text on Secondary bg) or chaos ("CHAOS PICK", default text); no chip for source "vote". Update the pill renderer so FREE REIGN uses a violet dot and CHAOS uses a slate-50 (#F8FAFC) white dot (chaos gets NO violet — mirrors D-08 visual separation). Add the CSS (.pill-freereign, .pill-chaos, banner, chip) — violet #8B5CF6 reserved exclusively for the paid-control elements, nothing else. No innerHTML/insertAdjacentHTML/document.write/eval anywhere.
  </action>
  <acceptance_criteria>
    - Banner renders only when controlWindow != null, anchored top-left 48px, showing name + countdown only; donor name via textContent, truncated to 24 chars.
    - Countdown ticks client-side from endsAtMs, amber in the final 10s; banner persists into BUILD_IN_PROGRESS and collapses on close — no red/error text ever.
    - Provenance chip reflects buildStatus.source (FREE REIGN violet / CHAOS PICK neutral / none for vote); CHAOS pill uses a white dot, FREE REIGN a violet dot.
    - No innerHTML/insertAdjacentHTML/document.write/eval in overlay.js; `npm test -- tests/invariants/dom-safety.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/invariants/dom-safety.test.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| donor display name -> OBS DOM | Attacker-controlled string must render as opaque truncated text |
| backend state -> public overlay wire | Only a coarse projection may cross; donor financials/message must never reach the public surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering (XSS) | Donor display name rendered on the overlay banner/chip | mitigate | el()/textContent only, JS truncate 24 chars + CSS ellipsis; dom-safety invariant auto-covers src/overlay/public/*.js |
| T-04-13 | Information Disclosure | Donation amount/currency/message text leaking onto the broadcast surface | mitigate | OverlayState.controlWindow is the coarse {donorDisplayName,endsAtMs} projection only; server test asserts no amount/message/trigger keys reach the wire (T-04 narrow public projection, coarse-surface rule) |
| T-04-14 | Information Disclosure | Error/failure/"revoked" text reaching the stream | mitigate | UI-SPEC hard rule: banner collapses silently on close, no red, no error string — chat/audit carry the never-silent duty, not the overlay |
</threat_model>

<verification>
- overlay server test + dom-safety invariant pass.
- Public wire carries name + endsAtMs only (no financial/message fields); violet is paid-only; no red on the overlay.
</verification>

<success_criteria>
The overlay surfaces active paid windows and build provenance honestly and coarsely (PAID-01/02 window presence, CHAOS-01 chaos-pick provenance) — six-word pill vocabulary, violet-for-paid / neutral-for-chaos, donor financials and message text never on stream, textContent-only, never red.
</success_criteria>

<output>
Create `.planning/phases/04-paid-influence-chaos-mode/04-04-SUMMARY.md` when done.
</output>
