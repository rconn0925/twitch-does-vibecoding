---
phase: 04-paid-influence-chaos-mode
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/operator-console/server.ts
  - src/operator-console/server.test.ts
  - src/operator-console/public/index.html
  - src/operator-console/public/console.js
  - src/operator-console/public/console.css
autonomous: true
requirements: [PAID-04, CHAOS-01]
must_haves:
  truths:
    - "The streamer can revoke an active window with a single click (destructive red, no modal) via POST /api/control-window/revoke, and toggle chaos mode via POST /api/chaos/toggle — both inheriting the existing Origin+Content-Type CSRF + DNS-rebinding middleware (no new middleware)"
    - "The console shows the FULL honest window detail the overlay hides: donor name (untruncated), trigger, and the amount->duration mapping line ($5.00 -> 1:00 window (capped at 5:00)) — the PAID-04 ledger view"
    - "Precedence (D-05) is surfaced: chaos toggle disabled while a window is active; Start Round disabled while chaos is on; chaos only from IDLE — each with a muted reason line"
    - "A donations connection pill and a Twitch missing-channel-points-scope pill state prevent silent-failure of the donation feed / redemption subscription (04-RESEARCH Open Question 1)"
  artifacts:
    - path: "src/operator-console/server.ts"
      provides: "ConsoleState.controlWindow/chaos/donations + missing-scope; POST /api/control-window/revoke + /api/chaos/toggle routes with typed-error->status mapping"
      min_lines: 60
    - path: "src/operator-console/public/console.js"
      provides: "window panel + Revoke + chaos toggle + donations/scope pills + new audit event-type filters; textContent-only donor render"
      min_lines: 120
  key_links:
    - from: "console.js Revoke Window"
      to: "POST /api/control-window/revoke"
      via: "single click, optional D-18 reason tag, inherits CSRF middleware"
      pattern: "control-window/revoke"
    - from: "console.js chaos toggle"
      to: "POST /api/chaos/toggle"
      via: "single click, drives machine transition via injected seam"
      pattern: "chaos/toggle"
    - from: "src/operator-console/server.ts"
      to: "ControlWindowSnapshot (04-01)"
      via: "injected ConsoleControlWindowSource seam, full-detail render"
      pattern: "ControlWindowSnapshot"
---

<objective>
Extend the operator console with the Phase 4 control surface per the APPROVED 04-UI-SPEC: a control-window panel showing the FULL honest amount->duration ledger the overlay hides (donor, trigger, mapping line), a single-click **Revoke Window** (destructive), a **chaos-mode toggle** in the round panel, a **Donations** connection pill + a Twitch **missing-channel-points-scope** pill state, and the new audit event-type filters. Two new POST routes (`/api/control-window/revoke`, `/api/chaos/toggle`) that inherit the existing uniform CSRF + DNS-rebinding middleware unchanged (no new security surface) and map typed errors to HTTP status like the veto/round-start routes.

Purpose: give the streamer full paid-window visibility + one-tap revoke (PAID-04 revocable/logged) and the chaos toggle (CHAOS-01), with precedence (D-05) surfaced so the controls never conflict.
Output: extended `src/operator-console/server.ts` (+test), `index.html`, `console.js`, `console.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-paid-influence-chaos-mode/04-UI-SPEC.md
@.planning/phases/04-paid-influence-chaos-mode/04-PATTERNS.md

<interfaces>
From src/shared/types.ts (04-01): `ControlWindowSnapshot { donorDisplayName; trigger; amountLabel; durationMs; endsAtMs }`, `WindowTrigger`, `StreamMode`.
From src/operator-console/server.ts (extend — PATTERNS.md §operator-console/server.ts):
```typescript
export interface ConsoleState extends StateSnapshot { pool; queue; pendingReviewCount; review; round; twitch; build; }
// ADD: controlWindow: ControlWindowSnapshot | null; chaos: boolean;
//      donations: "connected" | "reconnecting" | "unconfigured";  // <-- reconcile with copy: use "reconnecting" (UI-checker flag a — align union to the "Donations: reconnecting" label, NOT "disconnected")
//      twitch status union gains "missing-scope"
// Inject: ConsoleControlWindowSource { snapshot(): ControlWindowSnapshot | null; revoke(): void } + a chaos seam { enabled(): boolean; toggle(): void } (structural — real impls wired in 04-07; test with fakes)
```
Route templates (copy exactly, PATTERNS.md §veto/round-start routes):
- veto route: safeParse params/body -> 400 on fail; act; recordVeto; pushState(); json. Optional D-18 reasonTag.
- round-start route: try { snap = startRound(); pushState(); json } catch (RoundStartError) { 409 with reason }.
- Shared DNS-rebinding + Origin/Content-Type CSRF middleware (app.use, lines ~179-236) already covers EVERY route registered after it — NO new middleware (T-03-25 precedent).

New routes:
- POST /api/control-window/revoke: strict-empty-body schema; call deps.controlWindow.revoke(); write recordWindowRevoked; pushState(); return snapshot. (If no window active -> 409 reason "no-window".)
- POST /api/chaos/toggle: strict-empty-body; call deps.chaos.toggle() (drives machine FREE... no — CHAOS_MODE/IDLE via the machine); pushState(); return {chaos:boolean}. 409 if mode not togglable (window active / not IDLE) with reason.

DOM safety: donor display name untruncated but textContent-only (`overflow-wrap: anywhere`); dom-safety invariant covers console.js. Audit event-type filter dropdown gains: window_opened, window_expired, window_revoked, window_denied, chaos_toggled, chaos_pick.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConsoleState extension + revoke/chaos routes (typed-error->status, inherited CSRF)</name>
  <files>src/operator-console/server.ts, src/operator-console/server.test.ts</files>
  <read_first>
    - src/operator-console/server.ts (ConsoleState + buildState(); the veto route ~549-582; round-start route ~441-458; the DNS-rebinding + CSRF middleware ~179-236; the injected RoundManager/OverlayBuildSource seam pattern)
    - src/operator-console/server.test.ts (route + CSRF + state test shape)
    - .planning/phases/04-paid-influence-chaos-mode/04-UI-SPEC.md §Operator Console + §State-Contract Deltas
  </read_first>
  <action>
    Extend ConsoleState with: controlWindow: ControlWindowSnapshot | null; chaos: boolean; donations: "connected" | "reconnecting" | "unconfigured" (RECONCILE UI-checker flag a — the union value is "reconnecting" to match the "Donations: reconnecting" copy; do NOT use "disconnected"); and add "missing-scope" to the Twitch status union. Add injected ConsoleControlWindowSource ({ snapshot(): ControlWindowSnapshot | null; revoke(): void }) and a chaos seam ({ enabled(): boolean; toggle(): void }) to the server deps (structural seams, defaulting to null-object/no-op so the console composes before 04-07 wiring). Populate controlWindow/chaos/donations in buildState() from the seams.
    Add POST /api/control-window/revoke: strict-empty body schema (mirror RoundStartBodySchema); on an active window call deps.controlWindow.revoke() + recordWindowRevoked + pushState() -> return the (now null) snapshot; if no window active return 409 { reason: "no-window" }. Add POST /api/chaos/toggle: strict-empty body; try deps.chaos.toggle() + pushState() -> return { chaos }; catch a typed ControlWindowError/precedence error -> 409 with reason (window active or not-IDLE). Register both AFTER the existing middleware block (they inherit CSRF + DNS-rebinding — add NO new middleware). Support the optional D-18 reasonTag on revoke (kind "revoke-window"), never blocking.
    Test: revoke with valid Origin/Content-Type succeeds + writes a window_revoked row + pushes state; revoke with no active window -> 409 no-window; chaos toggle flips state; a cross-origin or wrong-content-type POST to either route is rejected by the inherited middleware (403) — asserting NO new middleware was needed; ConsoleState carries controlWindow/chaos/donations/missing-scope.
  </action>
  <acceptance_criteria>
    - Both routes exist, inherit the existing CSRF/DNS-rebinding middleware (cross-origin POST -> 403 asserted; no app.use added).
    - Revoke writes a window_revoked audit row and returns the updated snapshot; no-window -> 409.
    - ConsoleState.donations union is "connected"|"reconnecting"|"unconfigured" (reconciled with copy); Twitch union has "missing-scope".
    - `npm test -- src/operator-console/server.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/operator-console/server.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Window panel + chaos toggle + pills + audit filters (index.html/console.js/console.css)</name>
  <files>src/operator-console/public/index.html, src/operator-console/public/console.js, src/operator-console/public/console.css</files>
  <read_first>
    - src/operator-console/public/index.html (panel structure, round panel, status-bar pills, audit filter dropdown)
    - src/operator-console/public/console.js (el()/textContent render helpers, the round-panel countdown machinery, veto button + D-18 reason-row flow, pill render, fetch-with-CSRF-headers pattern)
    - src/operator-console/public/console.css (--accent/--destructive/status-pill tokens, 44px click targets, .round-reason muted line)
    - .planning/phases/04-paid-influence-chaos-mode/04-UI-SPEC.md §Operator Console + §Copywriting Contract + §Color (console additions)
    - tests/invariants/dom-safety.test.ts
  </read_first>
  <action>
    index.html: add `<section id="window-panel" class="panel" hidden>` directly above the round panel; add the Donations status pill next to the Twitch pill; add the six new options to the audit event-type filter dropdown (window_opened/window_expired/window_revoked/window_denied/chaos_toggled/chaos_pick); add the chaos toggle button inside the round panel.
    console.js: render the window panel only when state.controlWindow != null (hidden-not-empty, like the build panel): heading "Free-reign window — {m:ss} left" (client 1s tick from endsAtMs, reuse round-panel countdown); donor line "{displayName} — donation"/"— channel points" via el()/textContent, untruncated, overflow-wrap anywhere; mapping line (muted) = controlWindow.amountLabel (the honest PAID-04 math the overlay hides); a **Revoke Window** button (.button-destructive, 44px min, single click, NO modal) that POSTs /api/control-window/revoke with CSRF headers, then shows the existing D-18 optional reason row (kind "revoke-window", non-blocking). Render the chaos toggle: label "Enable Chaos Mode"/"Disable Chaos Mode" (.button-accent, 44px, single click, POST /api/chaos/toggle) with the disabled reasons — "Chaos can only start from Standby (current mode: {mode})." when mode != IDLE, "A free-reign window is active — it takes precedence." when a window is active (D-05); disable Start Round with "Voting is off while chaos mode is on." when chaos is on; add the chaos-on body line "Chaos mode on — the next task is a random pick from the approved pool. No vote runs." Render the Donations pill (connected green / reconnecting amber / not configured amber) and the Twitch missing-channel-points-scope pill (amber) + the two error-state boxes (copy from UI-SPEC §Copywriting: "Donation feed disconnected" / "Channel-points windows need a re-authorization"). NO violet anywhere on the console (paid violet is overlay-only). No innerHTML/insertAdjacentHTML/document.write/eval.
    console.css: window-panel styling reusing existing tokens; Revoke = --destructive #DC2626 (extends the red inventory by exactly one — Halt/Veto/Reject/Revoke); chaos toggle = --accent #3B82F6; new pill variants use existing status-approved/status-held colors. No new color.
  </action>
  <acceptance_criteria>
    - Window panel hidden when no window; when active shows donor (untruncated, textContent), trigger, amountLabel mapping line, and Revoke Window (single click, optional reason tag, no modal).
    - Chaos toggle present with all three D-05/precedence disabled-reason states; Start Round disabled while chaos on.
    - Donations pill + Twitch missing-scope pill + the two error boxes present with the exact UI-SPEC copy.
    - No violet on the console; no innerHTML/etc; `npm test -- tests/invariants/dom-safety.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/invariants/dom-safety.test.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> console POST routes | State-changing revoke/toggle from the localhost console — CSRF + DNS-rebinding boundary |
| donor display name -> console DOM | Attacker-controlled string rendered (untruncated) in the private console |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Spoofing (CSRF) | Forged cross-origin POST to revoke/toggle | mitigate | new routes registered after the existing Origin+Content-Type CSRF + DNS-rebinding middleware — inherited uniformly, NO new middleware; cross-origin POST -> 403 asserted (T-03-25 precedent) |
| T-04-16 | Tampering (XSS) | Donor display name in the console DOM | mitigate | el()/textContent only, overflow-wrap anywhere; dom-safety invariant covers console.js |
| T-04-17 | Denial of Service | Chaos toggle + window colliding (precedence violation) | mitigate | typed-error -> 409 mapping + disabled-with-reason UI (D-05): window > chaos > vote surfaced, never a conflicting active pair |
</threat_model>

<verification>
- console server test (incl. CSRF-inherited 403) + dom-safety invariant pass.
- No new middleware; donations union reconciled to "reconnecting"; no violet on console.
</verification>

<success_criteria>
The streamer has full honest paid-window visibility + single-click revoke (PAID-04) and a chaos toggle with correct precedence surfacing (CHAOS-01), plus donation-feed / missing-scope pills that prevent silent failure — all inheriting the existing CSRF posture with zero new security surface.
</success_criteria>

<output>
Create `.planning/phases/04-paid-influence-chaos-mode/04-05-SUMMARY.md` when done.
</output>
