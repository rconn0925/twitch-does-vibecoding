---
phase: 04-paid-influence-chaos-mode
plan: 08
type: execute
wave: 5
depends_on: [04-07]
files_modified:
  - .planning/phases/04-paid-influence-chaos-mode/04-LIVE-GATE.md
  - docs/OPERATIONS.md
autonomous: false
requirements: [PAID-01, PAID-02]
user_setup:
  - service: streamelements
    why: "Live donation (tip) ingestion for free-reign windows (PAID-01)"
    env_vars:
      - name: STREAMELEMENTS_JWT
        source: "StreamElements Dashboard -> Account -> Show secrets / API tokens (JWT)"
    dashboard_config:
      - task: "Confirm SE.Pay tipping is enabled and note the tip currency"
        location: "StreamElements Dashboard -> Monetization -> Tips"
  - service: twitch
    why: "channel:read:redemptions scope + a custom channel-points reward for micro-windows (PAID-02)"
    env_vars: []
    dashboard_config:
      - task: "Re-authorize the broadcaster via /auth/start to grant channel:read:redemptions"
        location: "Local app OAuth flow (/auth/start) — same posture as Phase 2 bootstrap"
      - task: "Create a dedicated custom channel-points reward that requires viewer text input"
        location: "Twitch Creator Dashboard -> Viewer Rewards -> Channel Points"
must_haves:
  truths:
    - "A runbook exists that the streamer follows to bind the real StreamElements account/JWT, re-authorize the broadcaster for channel:read:redemptions, and create the custom reward — all the steps the code cannot do itself (D-10 deferred live gates)"
    - "A real (small) test tip opens a live free-reign window and a real channel-points redemption opens a live window — verified on the overlay banner, the console panel, and the chat narration — with a working revoke, before Phase 4 is declared live-ready"
    - "The MEDIUM-confidence platform claims flagged in research (Bits AUP verbatim text A6, StreamElements JWT rotation A2 / chargeback policy A4) are manually re-read and the verdict recorded"
  artifacts:
    - path: ".planning/phases/04-paid-influence-chaos-mode/04-LIVE-GATE.md"
      provides: "the batched live-gate runbook + a GO/NO-GO verdict record (mirrors SANDBOX-SETUP.md)"
      contains: "GO"
  key_links:
    - from: "STREAMELEMENTS_JWT env"
      to: "connectStreamElements(jwt)"
      via: "server-side secret loaded at boot, never logged"
      pattern: "STREAMELEMENTS_JWT"
    - from: "/auth/start re-auth"
      to: "channel:read:redemptions"
      via: "broadcaster token gains the scope; redemption subscription succeeds"
      pattern: "channel:read:redemptions"
---

> **DESCOPE NOTE (2026-07-10):** The channel-points (PAID-02) steps in this plan — broadcaster custom-reward creation and the real redemption smoke test — are DESCOPED for v1. The real channel is not Twitch affiliate; Helix returned 403 on the custom-rewards endpoint. Paid influence for v1 is tips-only (StreamElements). The `channel:read:redemptions` re-auth and StreamElements JWT binding were completed 2026-07-10. See .planning/PROJECT.md Key Decisions. Execute the remaining tips-only + AUP-re-read steps as written; skip reward creation and redemption checks.

<objective>
The batched live-gate wave (D-10): everything built in 04-01..04-07 runs against injected fakes; this plan performs the human-only steps the code cannot do — binding the real StreamElements account + JWT, re-authorizing the broadcaster for `channel:read:redemptions`, creating the custom channel-points reward, and running a real small tip + real redemption smoke test against the live pipeline — plus the manual re-read of the MEDIUM-confidence platform/AUP claims research flagged. It writes a runbook + records a GO/NO-GO verdict, mirroring Phase 3's `SANDBOX-SETUP.md` and Phase 2's live-OAuth checkpoint.

Purpose: close the gap between fake-tested code and a live-broadcast-ready paid/chaos loop without blocking any of the buildable slices on account setup.
Output: `04-LIVE-GATE.md` runbook + verdict; a `docs/OPERATIONS.md` section for the StreamElements/redemption setup + JWT rotation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-paid-influence-chaos-mode/04-RESEARCH.md
@.planning/phases/04-paid-influence-chaos-mode/04-CONTEXT.md
@.planning/phases/03-sandboxed-build-engine-live-show/SANDBOX-SETUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the live-gate runbook + OPERATIONS section</name>
  <files>.planning/phases/04-paid-influence-chaos-mode/04-LIVE-GATE.md, docs/OPERATIONS.md</files>
  <read_first>
    - .planning/phases/03-sandboxed-build-engine-live-show/SANDBOX-SETUP.md (the runbook + verdict-record format to mirror)
    - docs/OPERATIONS.md (existing section structure — the Phase 2 live-OAuth / §6 pattern to extend)
    - .planning/phases/04-paid-influence-chaos-mode/04-RESEARCH.md §Research Question 1/2/3/4 + §Assumptions Log (A2 JWT rotation, A4 chargeback, A6 Bits AUP)
  </read_first>
  <action>
    Create 04-LIVE-GATE.md as a step-by-step runbook with an empty GO/NO-GO verdict block at the top (status: PENDING). Sections: (1) StreamElements — create/confirm account, enable SE.Pay tips, retrieve the JWT, set STREAMELEMENTS_JWT (server-side only, never commit); note the A2 ~2-week JWT rotation claim as UNVERIFIED — the streamer confirms the actual rotation cadence at the dashboard and records it. (2) Twitch redemption scope — run /auth/start, grant channel:read:redemptions, confirm the console Twitch pill clears the "missing channel-points scope" state; create a dedicated custom reward requiring text input, note its reward id. (3) Live smoke test — send a real small tip and confirm: overlay free-reign banner (donor + countdown, NO amount/message), console window panel (full amount->duration mapping), chat narration (donation wording), a donor !build instruction reaching the queue through the gate, and a working Revoke; then redeem the custom reward and confirm the redemption wording + window. (4) Compliance re-read — manually re-read the live Bits AUP page (A6) and StreamElements chargeback/refund + JWT policy (A4/A2), record any delta from research. Add a docs/OPERATIONS.md section covering StreamElements setup, the re-auth step, and JWT rotation handling for ongoing operation.
  </action>
  <acceptance_criteria>
    - 04-LIVE-GATE.md exists with a PENDING verdict block and all four runbook sections.
    - docs/OPERATIONS.md has a StreamElements + channel-points-scope + JWT-rotation section.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04-paid-influence-chaos-mode/04-LIVE-GATE.md && grep -qi "PENDING\|GO" .planning/phases/04-paid-influence-chaos-mode/04-LIVE-GATE.md</automated>
  </verify>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The runbook (Task 1) lists the exact steps. This checkpoint is the account/token setup the code cannot perform.</what-built>
  <how-to-verify>
    Follow 04-LIVE-GATE.md sections 1-2: create/confirm the StreamElements account, retrieve the JWT and set STREAMELEMENTS_JWT locally (do not commit it); run /auth/start to grant channel:read:redemptions and confirm the console Twitch pill no longer shows "missing channel-points scope"; create the dedicated custom channel-points reward (text input required) and record its reward id in the runbook.
  </how-to-verify>
  <resume-signal>Type "setup done" once the JWT is set, the scope is granted, and the reward exists — or describe what blocked.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full paid/chaos pipeline (04-07) is wired and running against the now-live StreamElements socket + EventSub redemption subscription.</what-built>
  <how-to-verify>
    Follow 04-LIVE-GATE.md section 3 with the app running: (1) send a REAL small tip -> confirm the overlay free-reign banner shows donor + countdown ONLY (no amount/message on stream), the console window panel shows the full amount->duration mapping, and chat narrates the donation-worded open. (2) As the donor, issue a !build instruction -> confirm it reaches the queue through the gate (or is narrated-rejected if it fails the filter, with the window staying open). (3) Click Revoke Window -> confirm the window closes and reverts. (4) Redeem the custom channel-points reward -> confirm the redemption-worded narration + a window opens. (5) Toggle chaos mode -> confirm a random pick from the pool builds with no vote. Then record GO or NO-GO (with the failing check) in the runbook verdict block, and complete section 4's compliance re-read.
  </how-to-verify>
  <resume-signal>Type "GO" (all checks pass) or "NO-GO: <failing check>" — the verdict is recorded in 04-LIVE-GATE.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| streamer machine -> StreamElements/Twitch dashboards | Human-only credential retrieval; JWT + broadcaster token are live secrets |
| live tip/redemption -> production pipeline | First real untrusted-money/redemption events hit the gate + window FSM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Information Disclosure | STREAMELEMENTS_JWT committed/leaked | mitigate | runbook instructs server-side-only env, never committed; the secrets-isolation invariant + capturing-logger discipline (Phase 2) already forbid logging tokens |
| T-04-27 | Repudiation | Untested live compliance claims (Bits AUP / chargeback) treated as load-bearing | mitigate | section 4 manual re-read of the flagged MEDIUM-confidence claims (A2/A4/A6) with a recorded verdict before live use |
| T-04-28 | Elevation of Privilege | A live gate-bypass slipping through only under real conditions | accept | code paths are fake-proven (04-06 invariants) + the smoke test exercises a real gated instruction; residual live-only risk is accepted for a single-streamer v1 and recorded in the verdict |
| T-04-SC | Tampering (supply chain) | (carried) socket.io-client — already gated at 04-02 | mitigate | no new install in this plan |
</threat_model>

<verification>
- 04-LIVE-GATE.md written with a recorded GO/NO-GO verdict; OPERATIONS section present.
- Human checkpoints confirm a real tip + redemption drive live windows with a working revoke and correct overlay/console/chat surfacing.
</verification>

<success_criteria>
The real StreamElements + channel-points integration is bound and smoke-tested end-to-end (PAID-01 live donation window, PAID-02 live redemption window), the flagged platform/AUP claims are re-verified, and a GO/NO-GO verdict is recorded — closing the D-10 deferred live gates without having blocked any buildable slice.
</success_criteria>

<output>
Create `.planning/phases/04-paid-influence-chaos-mode/04-08-SUMMARY.md` when done.
</output>
