---
phase: 05-build-history-stream-night-dry-run
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/history/server.ts
  - src/history/server.test.ts
  - src/history/public/history.html
  - src/history/public/history.css
  - src/history/public/history.js
  - src/main.ts
  - tests/e2e/build-history.e2e.test.ts
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "GET /history serves the read-only changelog shell (history.html + history.css + history.js), loopback-bound, no ws, no POST/PUT/DELETE/PATCH"
    - "GET /api/history returns a HistoryPage: nights grouped by stream-night (calendar-day bucket of created_at_ms), reverse-chronological, 10 nights per page, with hasOlder and a ?before= cursor"
    - "The wire carries only { buildId, title, provenance(coarsened to vote|paid|chaos), result, timeLabel } plus server-formatted nightKey/nightLabel/entryCountLabel — NO donor identity, amount, trigger-type, rationale, category, or host internals"
    - "history.js renders suggestion titles via textContent only (zero innerHTML), JS-truncated to 100 chars, with provenance chips (VOTE / FREE REIGN violet / CHAOS PICK) and result badges (Built green / Refused, Failed amber — never red)"
    - "The page shows the empty state ('No builds yet' / 'Suggest one in chat!') with no history, and a calm no-red error state ('History's taking a break') on a load failure"
    - "A completed build persisted by 05-01 appears end-to-end on /api/history in its correct night group with correct provenance and result"
  artifacts:
    - path: "src/history/server.ts"
      provides: "loopback-bound read-only Express surface: static shell + one validated paginated GET /api/history that coarsens provenance and groups by stream night"
      contains: "127.0.0.1"
    - path: "src/history/public/history.js"
      provides: "textContent-only client render: night groups, provenance chips, result badges, empty/error/pagination states"
    - path: "src/history/public/history.html"
      provides: "static shell + single <script src> — no inline JS/handlers"
    - path: "src/history/public/history.css"
      provides: "reuses the shipped console type scale + dark palette + Phase-4 provenance/result tokens (no new colors, no red)"
  key_links:
    - from: "src/history/server.ts GET /api/history"
      to: "listBuildHistory (src/audit/record.ts)"
      via: "reads build_history, buckets rows into stream-nights, coarsens provenance donation|channel_points→paid at the wire boundary"
      pattern: "listBuildHistory"
    - from: "src/history/public/history.js"
      to: "/api/history"
      via: "fetch on load + on Load-older-nights click"
      pattern: "/api/history"
    - from: "src/main.ts"
      to: "startHistoryServer"
      via: "composition — started alongside the overlay/preview/console servers"
      pattern: "startHistoryServer"
---

<objective>
The served, audience-facing half of HIST-01: a new read-only changelog page ("the audience built this together"). It reads the durable build_history table (05-01) and presents a reverse-chronological list grouped by stream night, coarsening the 4-value DB provenance to the 3-value public projection (vote|paid|chaos) and dropping donor identity, amount, and trigger-type entirely at the wire boundary (D-03/D-04 coarse-projection discipline). It is a fourth localhost surface alongside overlay/preview/console — loopback-bound, read-only by construction, no WebSocket. The page must be safe to screen-share.

This is the composition/e2e wave: it wires the surface into main.ts and proves the full vertical slice (build completes → row persists → page renders it).

Purpose: turn the durable record into a shareable, honest, injection-safe public changelog.
Output: src/history/ server + public page + a wired composition + an end-to-end test against fake ledger data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-build-history-stream-night-dry-run/05-CONTEXT.md
@.planning/phases/05-build-history-stream-night-dry-run/05-PATTERNS.md
@.planning/phases/05-build-history-stream-night-dry-run/05-UI-SPEC.md

<interfaces>
<!-- Contracts extracted by the Phase 5 pattern map + the APPROVED UI-SPEC. Use directly. -->

Served-surface analog (src/preview/server.ts, full file 1-110): loopback-bound Express, DNS-rebinding host
  guard as FIRST middleware (isLoopbackHostHeader from src/shared/loopback.ts → 403 "forbidden host"),
  express.static(publicDir), server.listen(port, "127.0.0.1"), close() = closeAllConnections + server.close.
  NO express.json(), NO POST/PUT/DELETE/PATCH, NO WebSocketServer (unlike overlay/server.ts). Read-only by
  construction is the load-bearing control (D-04).

Query-validation analog (src/operator-console/server.ts AuditQuerySchema ~lines 215-219, /api/audit ~782-795):
  zod z.coerce.number().int().min(1).max(N).default(...); safeParse(req.query) → 400 "invalid ..." on failure.

Data source (from 05-01): listBuildHistory(db, { limit, beforeMs? }) → BuildHistoryRow[]
  { id, taskId, title, provenance: 'vote'|'donation'|'channel_points'|'chaos', result: 'built'|'refused'|'failed', createdAtMs }.

APPROVED wire shape (05-UI-SPEC.md "Served-Page State Shape"):
  GET /history                                   → history.html + history.css + history.js
  GET /api/history?before={nightKey}&limit=10    → HistoryPage (first call omits `before`)
  HistoryPage  { nights: HistoryNight[]; hasOlder: boolean }
  HistoryNight { nightKey: string; nightLabel: string; entries: HistoryEntry[]; entryCountLabel: string }
  HistoryEntry { buildId: string; title: string; provenance: 'vote'|'paid'|'chaos';
                 result: 'built'|'refused'|'failed'; timeLabel: string }
  Coarsening at the boundary: donation|channel_points → 'paid'. nightLabel/entryCountLabel/timeLabel are
  SERVER-formatted (no client-side timezone/date/plural math). nightKey e.g. "2026-07-08".

Provenance chip vocab to reuse (src/overlay/public/overlay.js 327-337): vote→"VOTE" (neutral #F8FAFC),
  paid→"FREE REIGN" (violet #8B5CF6), chaos→"CHAOS PICK" (neutral). dom-safety helpers el()/truncate()
  (overlay.js 85-96) — textContent only, never innerHTML.

Result badge vocab (05-UI-SPEC.md): built→"Built" (green #22C55E), refused→"Refused", failed→"Failed"
  (both amber #F59E0B — NO red anywhere on this surface).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: read-only /history server with grouped, coarsened, paginated GET + main.ts wiring</name>
  <files>src/history/server.ts, src/history/server.test.ts, src/main.ts</files>
  <read_first>
    - src/preview/server.ts (full file — the whole loopback/read-only/static/no-ws structure to copy; its close() teardown)
    - src/shared/loopback.ts (isLoopbackHostHeader — the DNS-rebinding host guard)
    - src/operator-console/server.ts (AuditQuerySchema + /api/audit ~lines 215-219, 782-795 — the zod query-validation precedent)
    - src/main.ts (where startPreviewServer / startOverlayServer / console server are composed and closed — add startHistoryServer to the same lifecycle)
    - 05-UI-SPEC.md "Served-Page State Shape" + "Pagination / Scroll-Cap" (the exact wire + 10-nights-per-page + per-night 50-entry defensive cap)
  </read_first>
  <behavior>
    - GET /history with a loopback Host serves history.html (200); a non-loopback Host → 403 "forbidden host".
    - Only GET is mounted for /api/history; there is no express.json() and no state-changing route on the app.
    - GET /api/history (no before) returns the 10 most-recent stream-nights (calendar-day buckets of createdAtMs), reverse-chronological, entries newest-first within a night, hasOlder true when an 11th older night exists.
    - GET /api/history?before={nightKey} returns the next 10 nights strictly older than that night; hasOlder reflects whether more remain.
    - Each HistoryEntry coarsens provenance: donation→paid, channel_points→paid, vote→vote, chaos→chaos; and carries no donor/amount/trigger/rationale/category field.
    - nightLabel ("Tuesday, July 8, 2026"), entryCountLabel ("3 builds"/"1 build" server-pluralized), and timeLabel ("9:42 PM") are server-formatted strings.
    - A night with >50 entries returns the first 50 plus an overflowCount so the client can render "+N more that night".
    - Invalid query (limit out of range, malformed before) → 400.
  </behavior>
  <action>
    Create src/history/server.ts by copying src/preview/server.ts's structure: the isLoopbackHostHeader 403 guard as the first middleware, express.static(publicDir, { index: "history.html" }), createServer + server.listen(port, "127.0.0.1"), and a close() that closes connections then the server. Add exactly ONE dynamic route, GET /api/history, validated by a ChangelogQuerySchema (zod: limit as z.coerce.number().int().min(1).max(50).default(10); before as an optional string matching a YYYY-MM-DD nightKey) via safeParse → 400 on failure. In the handler: call listBuildHistory(db, { limit: a generous row cap, beforeMs: derived from `before`'s day start when present }), bucket rows into stream-nights by local-calendar-day of createdAtMs, take up to `limit` nights, and for each night build a HistoryNight with server-formatted nightKey/nightLabel/entryCountLabel and entries mapped to HistoryEntry (coarsening provenance donation|channel_points→paid, formatting timeLabel; cap 50 entries/night with an overflow indicator). Compute hasOlder by checking whether any night older than the returned set exists. Do NOT mount express.json() or any non-GET method — read-only by construction (D-04). Add startHistoryServer(deps) exporting a { server, port, close } handle mirroring startPreviewServer, and wire it into src/main.ts composition next to the other served surfaces (start on boot, close on shutdown). Write server.test.ts driving the behavior block against an in-memory db seeded with fake build_history rows spanning multiple days.
  </action>
  <verify>
    <automated>npx vitest run src/history/server.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>src/history/server.ts is loopback-bound, read-only (no json/POST/PUT/DELETE/PATCH, no ws), returns night-grouped reverse-chrono HistoryPage with 10-nights pagination + hasOlder + coarsened provenance + server-formatted labels; started/closed in main.ts; server.test.ts + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: the changelog page (html/css/js) — textContent-only render, chips, badges, states</name>
  <files>src/history/public/history.html, src/history/public/history.css, src/history/public/history.js</files>
  <read_first>
    - src/preview/public/preview.html (static shell + single <script src>, no inline JS/handlers — the shell idiom)
    - src/overlay/public/overlay.js (el()/truncate() dom-safety helpers lines 85-96; provenance-chip block lines 327-337)
    - src/operator-console/public/console.js (renderRound's <ol>/<li> list block ~lines 308-330 — the "render an array as a DOM list" shape)
    - src/overlay/public/overlay.css + src/operator-console/public/console.css (the shipped dark palette + console type scale + .provenance-chip pattern to reuse)
    - 05-UI-SPEC.md in full (layout contract, provenance chip mapping, result-state treatment, empty state, error state, pagination, copywriting — this is the APPROVED contract; match it exactly)
  </read_first>
  <action>
    Create history.html as a static shell: page header (title "Build History" Display 28/600; tagline "The audience built this together." Body 16/400 muted), an empty container the JS fills, and a single <script src="history.js"> — no inline JS, no inline event handlers. Create history.css reusing the shipped tokens ONLY (dark palette #0F172A/#1E293B/#F8FAFC, console 14/16/20/28 type scale, spacing multiples of 4, Phase-4 provenance violet #8B5CF6 + result green #22C55E / amber #F59E0B); declare no new colors and NO red token on this surface. Reserve accent #3B82F6 exclusively for the "Load older nights" button (44px min height). Address the two non-blocking UI-checker flags: (a) declare the page focal point — the most-recent night group at the top, reinforced by the Display-size title — via source layout order + a brief CSS comment; (b) give the error-state box an explicit non-red calm treatment (Secondary #1E293B background, muted #94A3B8 text). Create history.js: on load, fetch GET /api/history and render night groups (each a Heading-20 nightLabel + muted entryCountLabel, a 1px Secondary hairline, then entry rows). Each entry row: provenance chip (VOTE neutral / FREE REIGN violet / CHAOS PICK neutral, reusing overlay's .provenance-chip class names), the title via el()/textContent JS-truncated to 100 chars, a result badge (● Built green / ● Refused amber / ● Failed amber — never red), and a tabular-nums timeLabel. Reuse overlay.js's el()/truncate() helpers (copy them — textContent ONLY, never innerHTML/insertAdjacentHTML/document.write/eval; the tests/invariants/dom-safety.test.ts scan auto-covers src/**/public/*.js). Render the "+N more that night" muted line when a night overflows 50. Show the empty state ("No builds yet" / "Suggest one in chat!") when nights is empty; show the calm error state ("History's taking a break" / "Couldn't load build history right now. Refresh in a bit.") on a fetch failure — no stack trace, no HTTP code, no red. Show a centered "Load older nights" button only when hasOlder is true; on click, fetch the next page with ?before=<oldest loaded nightKey> and append, hiding the button when hasOlder becomes false.
  </action>
  <verify>
    <automated>npx vitest run tests/invariants/dom-safety.test.ts</automated>
  </verify>
  <done>history.html/css/js exist matching the APPROVED UI-SPEC (chips, result badges, empty/error/pagination states, focal point + non-red error box declared); dom-safety invariant passes (zero innerHTML in history.js); no new color tokens, no red.</done>
</task>

<task type="auto">
  <name>Task 3: end-to-end vertical-slice test (build completes → persists → renders on /api/history)</name>
  <files>tests/e2e/build-history.e2e.test.ts</files>
  <read_first>
    - src/orchestrator/build-session.ts (finalize() — the completion beat that now writes build_history, from 05-01)
    - src/audit/record.ts (recordBuildHistory / listBuildHistory)
    - src/history/server.ts (startHistoryServer + GET /api/history)
    - existing e2e tests under tests/e2e/ (the harness/fixtures pattern for driving the pipeline against injected fakes — no live channel)
  </read_first>
  <action>
    Write an end-to-end test that, against injected fakes (fake db, no live Twitch/StreamElements/sandbox), drives builds to completion through the finalize() path for each provenance (a vote build, a donation window build, a channel_points build, a chaos build) and each result (built, failed, refused), then starts the history server (or calls the /api/history handler) and asserts: (1) each completed build appears exactly once; (2) an aborted build (finalizeAborted) does NOT appear; (3) donation and channel_points builds both surface as provenance 'paid' on the wire while vote/chaos pass through; (4) results map done→built, failed→failed, refused→refused; (5) entries are grouped into the correct stream-night bucket and returned reverse-chronological; (6) the wire object contains none of: donor identifier, amount, trigger-type, rationale, category. Seed multi-day fake rows to exercise the 10-nights pagination + hasOlder + ?before= cursor.
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/build-history.e2e.test.ts</automated>
  </verify>
  <done>The full vertical slice is proven against fakes: completed builds persist and render on /api/history with correct night grouping, coarsened provenance, and honest results; aborts never appear; the wire leaks no donor/financial/trigger detail; pagination works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_history (server) → public /api/history wire | coarse projection: 4-value provenance collapses to 3; donor identity/amount/trigger-type dropped entirely |
| chat-derived title → audience browser DOM | untrusted text rendered on a screen-shareable page — textContent-only, truncated |
| network → /history server | a fourth localhost surface must stay loopback-bound and read-only (no second write path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | XSS via a chat-derived (approved) suggestion title on the audience page | mitigate | history.js renders titles via el()/textContent only (never innerHTML), JS-truncated to 100 chars + CSS ellipsis backstop; tests/invariants/dom-safety.test.ts auto-scans src/**/public/*.js and fails the build on any innerHTML/insertAdjacentHTML/document.write/eval |
| T-05-05 | Information Disclosure | Donor identity / donation amount / trigger-type leaking onto the public, long-lived, screen-shareable changelog | mitigate | provenance coarsened donation\|channel_points→'paid' at the server boundary; the HistoryEntry wire type carries ONLY buildId/title/provenance/result/timeLabel — donor, amount, rationale, category, host internals are never selected into the response; e2e test asserts their absence |
| T-05-06 | Information Disclosure | Re-broadcasting pre-gate / banned suggestion text on the audience page (D-03) | mitigate | the page reads ONLY build_history, which (per 05-01) contains solely gate-APPROVED task.text with honest outcomes (built\|refused\|failed); there is no query path to pre-gate audit_log text, and refused/failed entries render the WORD honestly but never raw rejected-at-intake text |
| T-05-07 | Elevation of Privilege | A second write path / the changelog surface being anything but read-only + loopback-bound | mitigate | copied from preview/server.ts: isLoopbackHostHeader 403 guard as first middleware; listen bound to 127.0.0.1; no express.json(), no POST/PUT/DELETE/PATCH, no WebSocketServer — read-only by construction |
| T-05-08 | Denial of Service | An adversarially-large history or a single huge night degrading the page | mitigate | 10-nights-per-page pagination + a defensive 50-entry-per-night cap with an overflow indicator; bounded zod limit (max 50) |
| T-05-SC | Tampering (supply chain) | package installs | accept | no new package installs — reuses express/zod already vetted in Phases 1-4 |
</threat_model>

<verification>
- server.test.ts, dom-safety.test.ts, and build-history.e2e.test.ts green; tsc + biome clean.
- Manual: GET /api/history from a non-loopback Host returns 403; the app exposes no non-GET route on the history surface.
- The rendered page matches the APPROVED 05-UI-SPEC (chips, badges, empty/error/pagination states, no red, no new colors).
</verification>

<success_criteria>
The audience-facing changelog is live on localhost: a browsable, reverse-chronological, night-grouped, screen-shareable page that renders every completed build honestly and safely (textContent-only, coarse public projection, no donor/financial detail, no red), reads solely gate-approved records, and stays read-only + loopback-bound — completing HIST-01's browsable-page half and closing success criterion 1.
</success_criteria>

<output>
Create `.planning/phases/05-build-history-stream-night-dry-run/05-02-SUMMARY.md` when done.
</output>
