---
phase: 05-build-history-stream-night-dry-run
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - .planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md
  - docs/OPERATIONS.md
autonomous: false
requirements: [HIST-01]
user_setup:
  - service: preconditions
    why: "The dry run exercises the two deferred human gates end-to-end; they must be cleared FIRST"
    dashboard_config:
      - task: "Record Phase 3 Wave 0 WSL2 go/no-go = GO in SANDBOX-SETUP.md (real filesystem-escape / dev-server-exposure / wsl --terminate veto / billing / latency proofs)"
        location: ".planning/phases/03-sandboxed-build-engine-live-show/SANDBOX-SETUP.md"
      - task: "Clear the Phase 4 live gate = GO in 04-LIVE-GATE.md (StreamElements JWT bound, channel:read:redemptions re-auth, custom reward, real tip + redemption smoke test)"
        location: ".planning/phases/04-paid-influence-chaos-mode/04-LIVE-GATE.md"
must_haves:
  truths:
    - "A dry-run runbook exists with a PENDING GO/NO-GO verdict block and numbered sections consolidating both deferred human gates (Phase 3 Wave 0, Phase 4 04-08) as recorded preconditions plus a full end-to-end test-channel rehearsal"
    - "The rehearsal exercises every mode on a low-stakes test channel: suggest → filter → vote → build → live preview, a real small donation free-reign window, and a chaos-mode round"
    - "The rehearsal exercises the kill switch against a GENUINELY in-progress build and confirms it halts cleanly"
    - "The rehearsal includes an audit-log review confirming zero unfiltered inputs reached an agent AND every rejection produced chat feedback, and confirms completed builds appear on the changelog page"
    - "docs/OPERATIONS.md gains a dry-run section in the existing calm, no-red, 'if X fails do Y' voice"
  artifacts:
    - path: ".planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md"
      provides: "the stream-night dry-run runbook + a GO/NO-GO verdict record (mirrors SANDBOX-SETUP.md / 04-LIVE-GATE.md)"
      contains: "GO"
  key_links:
    - from: "05-DRY-RUN.md preconditions"
      to: "SANDBOX-SETUP.md GO + 04-LIVE-GATE.md GO"
      via: "the runbook blocks the rehearsal on both earlier gates being recorded GO first"
      pattern: "GO"
    - from: "in-progress build"
      to: "kill switch"
      via: "veto/halt aborts a genuinely running agent session cleanly (finalizeAborted, no changelog row)"
      pattern: "halt"
---

<objective>
The v1 finish-line human gate (success criteria 2/3/4). Everything buildable in Phase 5 (05-01/05-02) and the earlier phases runs against injected fakes or is gated behind deferred human proofs; this plan is the ONE end-to-end rehearsal on a low-stakes test channel that ties them together. It is NOT autonomous code — it is a runbook + checklist that consolidates the two deferred human gates (Phase 3 Wave 0 WSL2 GO, Phase 4 04-08 live gate) as preconditions, then walks the streamer through a full-loop dry run: suggest → filter → vote → build → live preview, a real small donation window, a chaos round, a kill-switch-against-in-progress-build, and an audit-log review proving zero unfiltered inputs reached an agent and every rejection produced chat feedback — plus confirming the completed builds land on the new changelog page.

Mirrors Phase 4's 04-08-PLAN.md (its 04-LIVE-GATE.md artifact) and Phase 3's SANDBOX-SETUP.md: a PENDING verdict block → numbered sections → blocking human-action + human-verify checkpoints ending in GO / NO-GO.

Purpose: prove the format is ready for the first real stream night with zero compliance incidents.
Output: 05-DRY-RUN.md runbook + verdict; a docs/OPERATIONS.md dry-run section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-build-history-stream-night-dry-run/05-CONTEXT.md
@.planning/phases/05-build-history-stream-night-dry-run/05-PATTERNS.md
@.planning/phases/04-paid-influence-chaos-mode/04-08-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: write the dry-run runbook + OPERATIONS section</name>
  <files>.planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md, docs/OPERATIONS.md</files>
  <read_first>
    - .planning/phases/04-paid-influence-chaos-mode/04-08-PLAN.md (the runbook + PENDING-verdict-block + numbered-section + human-action/human-verify structure to mirror)
    - .planning/phases/03-sandboxed-build-engine-live-show/SANDBOX-SETUP.md (the sibling GO/NO-GO verdict-record format; the Wave 0 proofs this runbook lists as a precondition)
    - docs/OPERATIONS.md (existing section structure ~lines 1-223, especially the numbered §1-6 layout and the "Known Limitation Log" table ~lines 99-115 — extend in the same calm, no-red, "if X fails do Y" voice)
    - .planning/ROADMAP.md §"Phase 5" success criteria 2/3/4 (the exact things the rehearsal must prove)
  </read_first>
  <action>
    Create 05-DRY-RUN.md as a step-by-step runbook with an empty GO/NO-GO verdict block at the top (status: PENDING). Sections: (1) Preconditions — confirm Phase 3 Wave 0 WSL2 GO is recorded in SANDBOX-SETUP.md and the Phase 4 04-LIVE-GATE.md verdict is GO (StreamElements JWT bound, channel:read:redemptions granted, custom reward created); if either is not GO, the dry run is blocked. (2) Test-channel setup — a low-stakes test channel/account, the app running with the overlay/preview/console/history surfaces open, a small real tip ready to send. (3) Full-loop rehearsal — run suggest → filter → vote → build → live preview end to end: confirm a !suggest enters the pool or gets category-level chat rejection feedback; a timed vote round tallies on the overlay; the winner queues through the gate; the sandboxed build runs and the app-under-construction preview renders. (4) Paid + chaos — send a real small donation and confirm a free-reign window opens (overlay banner donor+countdown only, no amount on stream), issue a gated instruction during it; then toggle chaos mode and confirm a random pick from the filtered pool builds with no vote. (5) Kill switch vs. in-progress build — start a build, wait until it is GENUINELY mid-session (agent turn running), trigger the veto/halt, and confirm it aborts cleanly with no false "BUILT IT" and no changelog row (finalizeAborted path). (6) Audit + changelog review — open the audit page and confirm zero unfiltered inputs reached an agent and every rejection produced chat feedback; open the /history changelog page and confirm the night's completed builds appear with correct provenance chips and honest results, and that NO rejected-at-intake / pre-gate text and no donor/amount detail is visible. Record any anomaly in a Known Limitation Log row. End with the GO/NO-GO verdict instructions. Add a docs/OPERATIONS.md dry-run section covering the rehearsal procedure and the changelog surface's operational note (localhost route, read-only, safe to screen-share), in the existing voice.
  </action>
  <acceptance_criteria>
    - 05-DRY-RUN.md exists with a PENDING verdict block and all six numbered sections (preconditions, test-channel setup, full-loop, paid+chaos, kill-switch-vs-in-progress, audit+changelog review).
    - docs/OPERATIONS.md has a new dry-run + changelog-surface section.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md && grep -qi "PENDING" .planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md && grep -qi "dry run" docs/OPERATIONS.md</automated>
  </verify>
  <done>05-DRY-RUN.md written with a PENDING verdict block + six sections; docs/OPERATIONS.md extended with the dry-run + changelog operational section.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The runbook (Task 1) lists the exact preconditions. This checkpoint confirms the two deferred human gates the dry run depends on are cleared — the code cannot do this.</what-built>
  <how-to-verify>
    Follow 05-DRY-RUN.md section 1: confirm Phase 3 Wave 0 WSL2 go/no-go is recorded GO in SANDBOX-SETUP.md (real filesystem-escape isolation, dev-server-only exposure, wsl --terminate veto, billing, latency), and the Phase 4 04-LIVE-GATE.md verdict is GO (StreamElements JWT set, channel:read:redemptions re-auth done, custom reward created, real tip + redemption smoke-tested). Then follow section 2: set up the low-stakes test channel, start the app, open the overlay/preview/console/history surfaces, and have a small real tip ready.
  </how-to-verify>
  <resume-signal>Type "preconditions GO" once both earlier gates read GO and the test channel + app are ready — or describe what is still NO-GO/blocked.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full v1 system — the Phase 1 gate/kill-switch, Phase 2 chat loop, Phase 3 sandboxed build + preview, Phase 4 paid/chaos, and the new Phase 5 changelog — running live on a test channel.</what-built>
  <how-to-verify>
    Follow 05-DRY-RUN.md sections 3-6 with the app running on the test channel: (1) Run the full loop — !suggest (confirm pool-entry or category-level chat rejection feedback), a timed vote round (overlay tally + timer), the winner queuing through the gate, the sandboxed build, and the live preview of the app under construction. (2) Send a REAL small donation → confirm a free-reign window opens (overlay banner shows donor + countdown ONLY, no amount), issue a gated instruction during it; then toggle chaos mode → confirm a random pick builds with no vote. (3) Start a build, let it get GENUINELY in-progress (an agent turn running), then trigger the veto/halt → confirm it aborts cleanly within seconds, with no false "BUILT IT" on the overlay and NO row added to the changelog. (4) Open the audit page → confirm zero unfiltered inputs reached an agent and every rejection produced chat feedback. (5) Open the /history changelog → confirm the night's completed builds appear with correct provenance chips (VOTE/FREE REIGN/CHAOS PICK) and honest results (Built/Refused/Failed, no red), and that NO pre-gate/rejected text and no donor/amount detail is visible. Record GO or NO-GO (with the failing check) in the verdict block, and log any anomaly in the Known Limitation Log.
  </how-to-verify>
  <resume-signal>Type "GO" (all checks pass — v1 is dry-run-ready) or "NO-GO: <failing check>" — the verdict is recorded in 05-DRY-RUN.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-channel chat/donations → live v1 pipeline | first full end-to-end exercise of the real gate + sandbox + paid loop together under live conditions |
| in-progress agent build → streamer kill switch | the veto must halt a genuinely running session cleanly, from any state |
| completed builds → public changelog | the night's honest outcomes must appear without re-broadcasting pre-gate text or donor detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Elevation of Privilege | A live-only gate-bypass or sandbox-escape surfacing only under real conditions | mitigate | the dry run runs on a LOW-STAKES test channel with both isolation gates (Phase 3 Wave 0) already GO; section 6's audit review explicitly proves zero unfiltered inputs reached an agent before any real stream night |
| T-05-10 | Denial of Service | The kill switch failing against a genuinely in-progress build (public dead air) | mitigate | section 5 exercises veto against a real mid-session build and requires a clean abort within seconds (COMP-04 / BUILD-04) as a GO condition |
| T-05-11 | Repudiation | A false "built" record or re-broadcast rejected text slipping onto the public changelog under live conditions | mitigate | section 5 confirms the aborted build adds NO changelog row; section 6 confirms only gate-approved titles + honest results + no donor/amount detail render (D-03/D-04) |
| T-05-12 | Information Disclosure | Donor identity/amount visible on stream during the real donation window | mitigate | section 4 confirms the overlay banner shows donor + countdown only (no amount/message), carrying the Phase-4 coarse-projection discipline |
| T-05-SC | Tampering (supply chain) | package installs | accept | no code / no package installs in this plan — it is a human-gate runbook only |
</threat_model>

<verification>
- 05-DRY-RUN.md written with a recorded GO/NO-GO verdict; the OPERATIONS dry-run section present.
- The two blocking checkpoints confirm both earlier gates are GO and the full-loop + kill-switch + audit + changelog rehearsal passes on a test channel.
</verification>

<success_criteria>
The v1 format is proven ready for the first real stream night: a documented dry-run rehearsal exercises every mode end-to-end on a test channel, halts a genuinely in-progress build with the kill switch, and confirms from the audit log zero unfiltered inputs reached an agent and every rejection produced chat feedback — with completed builds honestly and safely surfaced on the changelog. Closes Phase 5 success criteria 2, 3, and 4 and consolidates the deferred Phase 3 / Phase 4 human gates into one recorded GO/NO-GO verdict.
</success_criteria>

<output>
Create `.planning/phases/05-build-history-stream-night-dry-run/05-03-SUMMARY.md` when done.
</output>
