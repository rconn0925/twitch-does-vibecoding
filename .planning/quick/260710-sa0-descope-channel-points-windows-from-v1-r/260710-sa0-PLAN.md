---
phase: quick
plan: 260710-sa0
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md
  - .planning/phases/04-paid-influence-chaos-mode/04-08-PLAN.md
autonomous: true
requirements: [PAID-02]
must_haves:
  truths:
    - "PROJECT.md Key Decisions logs the 2026-07-10 descope decision with the Helix 403 rationale and the revisit-at-affiliate condition"
    - "REQUIREMENTS.md marks PAID-02 deferred/descoped for v1 without deleting it — annotation states it is built + fake-tested but not live-verifiable on a non-affiliate channel"
    - "STATE.md gate batch sections D and E no longer demand a custom channel-points reward or a real redemption smoke test; StreamElements JWT + broadcaster re-auth are shown as DONE (2026-07-10)"
    - "05-DRY-RUN.md channel-points steps are marked DESCOPED with a pointer to the PROJECT.md decision — the dry run is no longer blocked on an impossible precondition"
    - "04-08-PLAN.md carries a banner note that its channel-points steps are descoped; the plan body is otherwise untouched"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row for the PAID-02 v1 descope"
      contains: "affiliate"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PAID-02 annotated as deferred for v1 (not deleted)"
      contains: "PAID-02"
    - path: ".planning/STATE.md"
      provides: "Updated gate batch D/E, pending todos, blockers, quick-task row"
      contains: "260710-sa0"
  key_links:
    - from: ".planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md"
      to: ".planning/PROJECT.md Key Decisions"
      via: "one-line DESCOPED pointer on each channel-points step"
      pattern: "DESCOPED"
---

<objective>
Descope channel-points (PAID-02) control windows from v1 across all planning/tracking docs. Docs-only change — NO code edits.

User decision 2026-07-10: the authorized broadcaster account is the REAL channel and it is NOT Twitch affiliate. Helix returned 403 "The broadcaster must have partner or affiliate status" on the custom-rewards endpoint, so custom channel-points rewards cannot exist on this channel yet. Paid influence for v1 is tips-only (StreamElements). Channel points return to scope when the channel reaches affiliate.

The built PAID-02 code stays in place, dormant: src/main.ts:1311-1320 already degrades gracefully (failed redemption subscription logs loudly, surfaces a degraded console pill, disables points-triggered windows, show continues). Do not touch it.

Honesty rule for every edit: descoped means "not live-verifiable on this channel yet" — never "done". No checkbox that represents a live verification may be marked [x] by this task unless the key facts say that specific item was actually completed (StreamElements JWT bound + broadcaster re-auth with channel:read:redemptions WERE completed 2026-07-10).

Purpose: unblock the v1 human-gate batch (04-08 live gate + 05 dry run) from a precondition that is physically impossible on this channel.
Output: five updated .planning/ docs, committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md
@.planning/phases/04-paid-influence-chaos-mode/04-08-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Log the descope decision (PROJECT.md) and defer PAID-02 (REQUIREMENTS.md)</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>
    PROJECT.md — three edits:
    1. Key Decisions table: append a row. Decision: "Descope channel-points control windows (PAID-02) from v1 — paid influence is tips-only (StreamElements)". Rationale: "The authorized broadcaster account is the real channel and is NOT Twitch affiliate; Helix returned 403 'The broadcaster must have partner or affiliate status' on the custom-rewards endpoint (verified 2026-07-10). Custom rewards cannot exist on this channel yet. Code stays built + dormant behind the existing degradation path (src/main.ts:1311-1320)." Outcome: "✅ Decided 2026-07-10 — revisit when the channel reaches affiliate".
    2. Active requirements, "Paid influence" bullet "Channel points redemptions offer a similar (smaller-scale) direct-influence mechanic": leave the checkbox unchecked and append an italic annotation — *(DESCOPED for v1, 2026-07-10 — channel not affiliate, Helix 403; built + fake-tested, dormant; see Key Decisions)*.
    3. Update the "Last updated" footer line to reflect this change (2026-07-10, channel-points descope).

    REQUIREMENTS.md — two edits:
    1. PAID-02 line: keep the checkbox unchecked (it is NOT done). Rewrite as: "- [ ] **PAID-02** *(DEFERRED — v1 descope 2026-07-10)*: A channel points redemption grants a smaller-scale direct-influence window via native Twitch EventSub redemptions — *built and fake-tested, but not live-verifiable: the real channel is not Twitch affiliate (Helix 403 on custom-rewards endpoint), so no custom reward can exist yet. Tips-only paid influence for v1; returns to scope at affiliate. See PROJECT.md Key Decisions.*"
    2. Traceability table: change the PAID-02 row Status from "Pending" to "Deferred (v1 descope — non-affiliate channel)". Do not delete the row; leave the Coverage line at 31/31 mapped.
  </action>
  <verify>
    <automated>grep -c "affiliate" .planning/PROJECT.md && grep -q "DEFERRED" .planning/REQUIREMENTS.md && grep -q "Deferred (v1 descope" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>PROJECT.md Key Decisions has the dated descope row with Helix-403 rationale and revisit-at-affiliate condition; REQUIREMENTS.md shows PAID-02 unchecked, annotated as deferred (not deleted), traceability row says Deferred.</done>
</task>

<task type="auto">
  <name>Task 2: Align the gate batch (STATE.md), dry-run runbook (05-DRY-RUN.md), and live-gate plan banner (04-08-PLAN.md)</name>
  <files>.planning/STATE.md, .planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md, .planning/phases/04-paid-influence-chaos-mode/04-08-PLAN.md</files>
  <action>
    STATE.md — gate batch section D ("Phase 4 — live gate 04-08 + CR-03"):
    1. Mark "[x] StreamElements account + JWT bound (JWT never logged)" DONE with "(done 2026-07-10)".
    2. Mark "[x] channel:read:redemptions broadcaster RE-AUTH" DONE with "(done 2026-07-10 — token now carries the scope)".
    3. Split the combined line "A custom channel-points reward created + a real tip + a real redemption smoke-tested" into two lines: "- [ ] A real tip smoke-tested (free-reign window opens live)" stays PENDING; "- ~~A custom channel-points reward created + a real redemption smoke-tested~~ — **N/A, DESCOPED for v1** (channel not affiliate, Helix 403 2026-07-10; see PROJECT.md Key Decisions)". Bits AUP re-read and CR-03 lines stay pending, unchanged.
    4. Headline gates paragraph (the "Phase 4 — Live gate 04-08" bullet near the top): update "real tip/redemption smoke test" to "real tip smoke test (channel-points redemption DESCOPED for v1 — non-affiliate)" and note the JWT + re-auth items are done.
    5. Section E (Phase 5 dry run): on the "Real small donation free-reign window + channel-points window" line, keep the donation window part pending and strike the channel-points part: "+ ~~channel-points window~~ (DESCOPED v1)".
    6. Pending Todos: edit the "[Phase 4 — BLOCKING...] Live gate 04-08" todo to reflect reality — StreamElements JWT + broadcaster re-auth done 2026-07-10; remaining: real tip smoke test, Bits AUP/chargeback re-read, CR-03 human-check; channel-points reward/redemption items removed as DESCOPED for v1.
    7. Blockers/Concerns: update the Phase 4 blocker line — donation platform is settled (StreamElements bound 2026-07-10); what remains is the verbatim Bits AUP re-read at the live gate. Note channel points are descoped for v1 (non-affiliate) so the Channel Points AUP re-verification is N/A until affiliate.
    8. Quick Tasks Completed table: append row "| 2026-07-10 | `260710-sa0` — Descope channel-points (PAID-02) windows from v1 (docs/tracking only) | ✅ Done. Real channel is non-affiliate — Helix 403 on custom-rewards verified 2026-07-10. Tips-only paid influence for v1; PAID-02 code stays dormant behind the main.ts degradation path; revisit at affiliate. |".
    9. Frontmatter: refresh last_updated; append to stopped_at/last_activity only if trivially consistent (do not restructure frontmatter).

    05-DRY-RUN.md — three edits (do NOT change the PENDING verdict or any other check):
    1. Section 1, Phase 4 live-gate precondition parenthetical: change "(StreamElements account + JWT bound; channel:read:redemptions broadcaster re-auth done; a custom channel-points reward created; a real tip + a real redemption smoke-tested; Bits AUP / chargeback claims manually re-read)" so the reward-creation and redemption items read "~~a custom channel-points reward created; a real redemption smoke-tested~~ (DESCOPED v1 — see PROJECT.md Key Decisions)" while the tip smoke test and AUP re-read remain required.
    2. Section 2: on the line "A small real tip is ready to send through the donation platform, and the custom channel-points reward is redeemable" — strike the channel-points clause with the same one-line DESCOPED pointer.
    3. Section 4: mark the step "Redeem the channel-points reward → a smaller window opens the same way." as "~~...~~ **DESCOPED for v1** — channel not affiliate (Helix 403); see PROJECT.md Key Decisions" (strike the checkbox line, do not delete it).

    04-08-PLAN.md — one edit only: insert a blockquote banner immediately after the frontmatter closing "---", before <objective>: "> **DESCOPE NOTE (2026-07-10):** The channel-points (PAID-02) steps in this plan — broadcaster custom-reward creation and the real redemption smoke test — are DESCOPED for v1. The real channel is not Twitch affiliate; Helix returned 403 on the custom-rewards endpoint. Paid influence for v1 is tips-only (StreamElements). The channel:read:redemptions re-auth and StreamElements JWT binding were completed 2026-07-10. See .planning/PROJECT.md Key Decisions. Execute the remaining tips-only + AUP-re-read steps as written; skip reward creation and redemption checks." Do not modify the frontmatter, tasks, or any other section of 04-08-PLAN.md.
  </action>
  <verify>
    <automated>grep -q "260710-sa0" .planning/STATE.md && grep -q "DESCOPED" .planning/STATE.md && grep -q "DESCOPED" .planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md && grep -q "DESCOPE NOTE" .planning/phases/04-paid-influence-chaos-mode/04-08-PLAN.md && grep -qi "PENDING" .planning/phases/05-build-history-stream-night-dry-run/05-DRY-RUN.md</automated>
  </verify>
  <done>STATE.md section D shows JWT + re-auth done, tip smoke pending, reward/redemption struck as descoped; section E strikes only the channel-points window; todos/blockers match; quick-task row added. 05-DRY-RUN.md channel-points steps struck with pointers, verdict still PENDING. 04-08-PLAN.md has only the banner added, everything else byte-identical.</done>
</task>

</tasks>

<verification>
- All five files edited; `git diff --stat` shows ONLY the five .planning/ files listed in files_modified — zero src/ changes.
- No live-verification checkbox was newly marked [x] except the two items the key facts confirm done (StreamElements JWT bound; channel:read:redemptions re-auth), both dated 2026-07-10.
- Every DESCOPED marker points (directly or via "see PROJECT.md Key Decisions") to the logged decision.
- 05-DRY-RUN.md final verdict remains ⏳ PENDING; 04-08-PLAN.md tasks/frontmatter unchanged.
</verification>

<success_criteria>
The PAID-02 descope is consistently recorded across PROJECT.md (decision), REQUIREMENTS.md (deferred, not deleted), STATE.md (gate batch D/E, todos, blockers, quick-task row), 05-DRY-RUN.md (steps struck with pointers), and 04-08-PLAN.md (banner only). The v1 human-gate batch no longer requires a channel-points reward or redemption on a non-affiliate channel, while honestly recording that PAID-02 is built-but-not-live-verifiable and returns to scope at affiliate.
</success_criteria>

<output>
Create `.planning/quick/260710-sa0-descope-channel-points-windows-from-v1-r/260710-sa0-SUMMARY.md` when done.
</output>
