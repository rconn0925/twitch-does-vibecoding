---
phase: quick
plan: 260710-v4e
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/events.ts
  - src/queue/pool.ts
  - src/overlay/server.ts
  - src/overlay/public/queue.html
  - src/overlay/public/queue.css
  - src/overlay/public/queue.js
  - src/main.ts
  - src/overlay/server.test.ts
  - src/queue/pool.test.ts
autonomous: true
requirements: [QUICK-V4E]
must_haves:
  truths:
    - "An OBS browser source pointed at http://127.0.0.1:4901/queue renders a dedicated page with a SUGGESTION POOL section (approved candidates + suggester usernames) and an UP NEXT section (full build queue with position numbers, up to 10)"
    - "Pool add/evict/remove (candidate drawn into a round) pushes fresh overlay state to connected ws clients without waiting for the next unrelated event"
    - "No GateResult field (rationale, category, decision) and no donor/payment data ever appears in the serialized overlay wire payload's new fields"
    - "The page reconnects silently with backoff after an OBS scene-switch reload and reconstructs full UI from the first ws message"
  artifacts:
    - path: "src/overlay/public/queue.html"
      provides: "The what's-coming page markup (two empty sections + script/css links)"
    - path: "src/overlay/public/queue.js"
      provides: "textContent-only renderer + reconnect-with-backoff ws client"
    - path: "src/overlay/public/queue.css"
      provides: "Page layout in the overlay design language (Dominant backing panels)"
    - path: "src/overlay/server.ts"
      provides: "OverlayState.pool + OverlayState.queue display-only projections, OverlayPoolSource seam, GET /queue route, POOL_CHANGED push wiring"
      contains: "OverlayPoolSource"
    - path: "src/queue/pool.ts"
      provides: "CandidatePool emits POOL_CHANGED on add/evict/remove"
      contains: "POOL_CHANGED"
  key_links:
    - from: "src/queue/pool.ts"
      to: "src/overlay/server.ts"
      via: "POOL_CHANGED event -> pushState()"
      pattern: "POOL_CHANGED"
    - from: "src/main.ts"
      to: "startOverlayServer"
      via: "pool dep + queueDisplayMax wiring"
      pattern: "pool:"
    - from: "src/overlay/public/queue.js"
      to: "ws://<host>"
      via: "full-state-on-connect push, same reconnect pattern as overlay.js"
      pattern: "new WebSocket"
---

<objective>
Add a dedicated "what's coming" page to the EXISTING public overlay server (port 4901) at GET /queue — a second OBS browser-source URL showing (top) the live approved-suggestion pool awaiting the next vote round and (bottom) the full vote-winner build queue up to VOTE_QUEUE_MAX (10) with position numbers.

Purpose: the main overlay's queue strip shows only 3 items and the pool is invisible on stream; this page gives viewers the full pipeline picture as one optional OBS source.
Output: extended OverlayState projection (display fields ONLY), POOL_CHANGED push wiring, /queue route + static page, tests, boot log line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/overlay/server.ts
@src/overlay/server.test.ts
@src/overlay/public/index.html
@src/overlay/public/overlay.js
@src/overlay/public/overlay.css
@src/queue/pool.ts
@src/shared/events.ts

<interfaces>
Key contracts already in the codebase (no exploration needed):

From src/queue/pool.ts:
```typescript
export interface ApprovedCandidate {
  candidate: SuggestionCandidate; // { id, source, kind, twitchUsername: string | null, text, submittedAtMs }
  result: GateResult;             // { decision, category, rationale } — MUST NEVER cross the wire
  addedAtMs: number;
}
export class CandidatePool {
  add(candidate, result): void;   // throws unless result.decision === "approved" (COMP-01 invariant)
  list(): ApprovedCandidate[];    // insertion order
  remove(id: string): void;
}
```

From src/overlay/server.ts:
```typescript
export interface OverlayState { pill; round; nextUp: string[]; buildStatus; controlWindow; suggestPhase; }
export interface OverlayQueueSource { list(): readonly { text: string }[]; }
// Seam pattern to mirror (snapshot/list + .on): OverlayBuildSource, OverlayControlWindowSource
// Narrowing exemplar: buildOverlayState() re-projects controlWindow down to {donorDisplayName,endsAtMs}
```

From src/main.ts (composition, ~line 1187):
```typescript
const overlay = await startOverlayServer({ machine, round, taskQueue, port: opts.overlayPort ?? 0, ...build/controlWindow/autoCycle seams..., logger });
// voteQueueMax = envPositive(process.env.VOTE_QUEUE_MAX, 10) already computed earlier (~line 643)
// pool = new CandidatePool({ maxSize, onEvict }) at ~line 269
```

From src/main.ts entrypoint boot-log style (~line 1213):
```typescript
logger.info({ port: overlay.port }, "public overlay listening at http://127.0.0.1:%d — add as OBS browser source at 1920x1080", overlay.port);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the overlay projection with pool + full queue, add the /queue route and POOL_CHANGED push wiring</name>
  <files>src/shared/events.ts, src/queue/pool.ts, src/overlay/server.ts, src/main.ts</files>
  <action>
1. **src/shared/events.ts** — add `POOL_CHANGED = "pool:changed" as const` (doc comment: emitted by CandidatePool after add/evict/remove so the overlay's what's-coming page pushes fresh state; low-frequency — adds are serialized behind classifier calls, removes are round-open/chaos beats). Add it to the `AppEvent` union.

2. **src/queue/pool.ts** — make `CandidatePool extends EventEmitter` (import from `node:events`; call `super()` in the constructor). Emit `POOL_CHANGED` exactly once at the END of `add()` (a single emit covers the add AND any eviction it caused) and at the end of `remove()` ONLY when the id was actually present (`this.#items.delete(id)` returns boolean — no phantom pushes). Update the file doc comment: the pool stays PASSIVE storage (nothing executes from it); the event is a read-only change notification for broadcast projections.

3. **src/overlay/server.ts**:
   - Add seam (mirrors OverlayQueueSource + the `.on` pattern of OverlayBuildSource):
     ```
     export interface OverlayPoolSource {
       list(): readonly { candidate: { text: string; twitchUsername: string | null } }[];
       on(event: string, handler: (...args: unknown[]) => void): void;
     }
     ```
     plus a `NULL_POOL_SOURCE` (empty list, no-op on) and optional dep `pool?: OverlayPoolSource` in `OverlayServerDeps`, defaulted like `build`/`controlWindow`. Also add optional `queueDisplayMax?: number` dep (default 10, constant `DEFAULT_QUEUE_DISPLAY_MAX = 10` with a comment tying it to VOTE_QUEUE_MAX).
   - Extend `OverlayState` with two fields, each with a security doc comment naming the binding constraints:
     - `pool: { text: string; username: string | null }[]` — the approved-suggestion pool awaiting the next round. DISPLAY FIELDS ONLY: this projection is the EXEMPLAR narrowing (per the STATE.md residual about RoundSnapshot over-projection — do NOT copy that pattern). Never GateResult, never rationale/category/decision, never addedAtMs. `username` = `candidate.twitchUsername` (already what RoundSnapshot exposes for vote candidates — no MORE than the main overlay's wire).
     - `queue: string[]` — the FULL build queue in FIFO order, first `queueDisplayMax` texts (same source and semantics as `nextUp`, larger slice; position numbers are client-rendered).
   - In `buildOverlayState()`, build both with EXPLICIT narrowing (the controlWindow re-projection idiom): `pool: poolSource.list().map((item) => ({ text: item.candidate.text, username: item.candidate.twitchUsername }))` and `queue: taskQueue.list().slice(0, queueDisplayMax).map((task) => task.text)`. Even a richer source object can then never leak extra keys.
   - Subscribe `pool.on(POOL_CHANGED, () => pushState())` — immediate push like the lifecycle events, with a comment: adds are rate-limited by classifier latency + intake cooldown, removes are round-open/chaos beats; never the vote-tally debounce.
   - Add the route ABOVE `/api/state`, GET only (the read-only posture is untouched — no mutation route exists):
     ```
     app.get("/queue", (_req, res) => { res.sendFile(path.join(publicDir, "queue.html")); });
     ```
     (import `node:path`; `publicDir` already exists). The Host-allowlist middleware is app-level FIRST middleware, so /queue inherits the CR-02 posture automatically — note this in a comment.

4. **src/main.ts** — pass `pool` and `queueDisplayMax: voteQueueMax` in the `startOverlayServer({...})` deps (~line 1187; `voteQueueMax` is already in scope from the auto-cycle block). CandidatePool now structurally satisfies OverlayPoolSource (it has list() and EventEmitter.on). After the existing "public overlay listening" logger.info (~line 1213), add the boot line in the same style:
   ```
   logger.info({ port: overlay.port }, "what's-coming page at http://127.0.0.1:%d/queue — optional OBS browser source", overlay.port);
   ```
  </action>
  <verify>
    <automated>npx vitest run src/overlay/server.test.ts src/queue/pool.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>GET /api/state carries `pool` (display fields only) and `queue` (up to 10 texts, FIFO); GET /queue serves HTML; POOL_CHANGED emits from CandidatePool and triggers an overlay push; existing 724-test suite still green (no existing overlay/pool test broken).</done>
</task>

<task type="auto">
  <name>Task 2: Build the what's-coming client page (queue.html/css/js) in the overlay design language</name>
  <files>src/overlay/public/queue.html, src/overlay/public/queue.css, src/overlay/public/queue.js</files>
  <action>
1. **queue.html** — mirror index.html's shape: doctype, `<link rel="stylesheet" href="/overlay.css" />` (palette tokens + transparent body) THEN `<link rel="stylesheet" href="/queue.css" />`, a header comment (dedicated OBS browser source, two sections, quiet placeholder empty-states allowed on THIS page — unlike the main overlay), two empty containers:
   ```
   <section id="pool-panel" class="wc-panel"></section>
   <section id="queue-panel" class="wc-panel"></section>
   <script src="/queue.js"></script>
   ```
   No text authored in HTML beyond the comment — all rendering is JS textContent.

2. **queue.css** — page-specific layout only (palette vars come from overlay.css `:root`). Two stacked panels: pool panel top, queue panel bottom, `var(--safe-margin)` page padding, `var(--block-gap)` between, `background: var(--dominant)`, `border-radius`, `padding: var(--panel-pad)` — no bare text over video (every text node sits on a backed panel). Type scale per the binding contract: section labels 24px/700, rows 20px/600, nothing under 20px. Classes: `.wc-panel`, `.wc-label` (muted, letter-spaced section header), `.wc-row` (flex row), `.wc-pos` (Secondary-background number badge, `tabular-nums`), `.wc-text`, `.wc-user` (muted, smaller-weight but >= 20px), `.wc-empty` (muted placeholder line). No red anywhere; no new accent colors — Secondary badges only, staying inside the 60/30/10 budget.

3. **queue.js** — IIFE, vanilla JS, structured exactly like overlay.js:
   - Copy the `el(tag, className, text)` textContent-only helper and `truncate(text, max)` verbatim (T-02-19 rule restated in the header comment: chat-derived strings — pool text, usernames, queue texts — render via textContent ONLY, never innerHTML; JS-truncation with CSS ellipsis backstop).
   - Constants: `TEXT_MAX = 80` (the main overlay's 80-char vote-row pattern), `USER_MAX = 24` (the donor-name precedent for attacker-controlled names), `QUEUE_MAX = 10`.
   - `renderPool(state)`: clear panel via `replaceChildren()`; append `el("div","wc-label","SUGGESTION POOL")`; if `state.pool` is empty/absent append `el("div","wc-empty","waiting for suggestions…")`; else one `.wc-row` per item: `.wc-text` = truncate(item.text, TEXT_MAX), and when `item.username` is non-null append `.wc-user` = truncate(item.username, USER_MAX).
   - `renderQueue(state)`: label "UP NEXT"; empty → `el("div","wc-empty","queue is empty")`; else for `state.queue.slice(0, QUEUE_MAX)`: `.wc-row` with `.wc-pos` = String(index + 1) and `.wc-text` = truncate(text, TEXT_MAX).
   - `handleState` stores latest and re-renders both; then the SAME ~30-line reconnect-with-backoff block as overlay.js (new WebSocket(`ws://${location.host}`), open resets attempts, message → try/parse/handleState with silent catch, close → setTimeout(connect, Math.min(500 * 2 ** attempts, 8000)), error → socket.close()). Broadcast rule comment: on disconnect freeze the last render, retry silently, no error text ever.
   - No countdowns, no timers — this page is push-driven only.
  </action>
  <verify>
    <automated>npx biome check src/overlay/public/queue.js src/overlay/public/queue.css && node -e "const s=require('fs').readFileSync('src/overlay/public/queue.js','utf8'); if(/innerHTML/.test(s)) { throw new Error('innerHTML forbidden on broadcast surface'); }"</automated>
  </verify>
  <done>Opening http://127.0.0.1:4901/queue in a browser (or the served page in a test fetch) renders both sections from the connect push; empty pool/queue show the quiet placeholder lines; zero innerHTML usage; biome clean.</done>
</task>

<task type="auto">
  <name>Task 3: Projection-security + ordering + route tests, then the full gate</name>
  <files>src/overlay/server.test.ts, src/queue/pool.test.ts</files>
  <action>
Extend **src/overlay/server.test.ts** using its existing fake-source idioms (makeFakeBuild / makeFakeControlWindow patterns, `start()` helper gains `pool?: FakePool` and `queueDisplayMax?`):

1. **makeFakePool** — EventEmitter-backed, `list()` returns DELIBERATELY-RICH ApprovedCandidate-shaped items (the RichWindowSnapshot trick): full candidate `{ id, source: "chat", kind, twitchUsername: "viewer9", text: "build a drum machine", submittedAtMs }` PLUS `result: { decision: "approved", category: "weapons", rationale: "classifier-rationale-sentinel" }` and `addedAtMs`. Expose `setItems(next)` that mutates then emits `POOL_CHANGED`.

2. **Display-fields-only projection test**: GET /api/state with the rich fake pool → `state.pool` equals `[{ text: "build a drum machine", username: "viewer9" }]`; `Object.keys(state.pool[0]).sort()` equals `["text","username"]`; and against the RAW serialized JSON of the new fields assert the forbidden tokens never appear: `"rationale"`, `"classifier-rationale-sentinel"`, `"weapons"`, `"addedAtMs"`, `"decision"`. NOTE: assert on `JSON.stringify(state.pool)` + `JSON.stringify(state.queue)` (not the whole state — `round` still carries GateResult per the known STATE.md residual; do not widen the test to whole-payload or it fails on the pre-existing leak this task is explicitly NOT fixing).

3. **Approved-only invariant**: in **src/queue/pool.test.ts**, verify (add if not already present) that `add()` throws for `decision: "rejected"` and `"held-for-review"` — the pool can only ever contain approved candidates, which is what makes the /queue pool section gate-approved by construction. Also add: `add()` emits POOL_CHANGED once (including when it evicts), `remove()` of a present id emits, `remove()` of an absent id does NOT emit.

4. **POOL_CHANGED push test** (server.test.ts): connect ws, initial push has `pool: []`; `fakePool.setItems([...])` → an IMMEDIATE second push (no fake timers, the `until()` helper) carrying the new pool projection.

5. **Full-queue ordering test**: `start({ nextUpTexts: ["t1"..."t11"] })` (11 texts) → `state.queue` equals exactly t1..t10 in order (cap 10), while `state.nextUp` still equals t1..t3 (the main overlay strip is unchanged).

6. **Route test**: GET /queue returns 200 with content-type containing text/html; POST/PUT/DELETE/PATCH to /queue all 404 (extend the existing mutation-method loop's path list with "/queue"); GET /queue with a foreign Host header returns 403 (reuse the raw `http.request` rebound-GET pattern from the CR-02 test).

7. Run the FULL gate and fix anything it surfaces:
   `npx vitest run && npx tsc --noEmit && npx biome check .`
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <done>All new tests pass alongside the existing suite (724+ tests); pool projection provably display-fields-only; approved-only invariant asserted; queue capped at 10 in FIFO order with nextUp untouched; /queue serves GET-only under the Host allowlist; tsc + biome clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → broadcast wire | OverlayState JSON is screen-shared to Twitch viewers via OBS — anything projected is public |
| chat → DOM | Pool texts, usernames, and queue texts are attacker-controlled chat strings rendered in OBS CEF |
| network → localhost server | New GET /queue route on the loopback-only overlay server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v4e-01 | Information Disclosure | OverlayState.pool projection | mitigate | Explicit map to `{text, username}` only in buildOverlayState (controlWindow narrowing idiom); test asserts key set + absence of rationale/category/decision/addedAtMs sentinels in the serialized fields |
| T-v4e-02 | Information Disclosure | pre-gate / rejected text on stream | mitigate | Pool source is CandidatePool, whose add() throws for any non-approved GateResult (COMP-01) — approved-only by construction, asserted in pool.test.ts |
| T-v4e-03 | Tampering (stored XSS on broadcast) | queue.js rendering | mitigate | textContent-only el() helper, zero innerHTML (automated grep in Task 2 verify), 80-char text / 24-char username JS truncation with CSS backstop |
| T-v4e-04 | Information Disclosure | DNS-rebound read of /queue | mitigate | Route sits behind the existing app-level Host-allowlist middleware (CR-02); rebound-GET 403 test added for /queue |
| T-v4e-05 | Tampering | new mutation surface | mitigate | GET-only route added; mutation-method 404 loop extended to /queue (the no-route-exists control stays intact) |
| T-v4e-06 | Denial of Service | POOL_CHANGED push flood | accept | Adds are serialized behind classifier latency (+60s per-user intake cooldown); removes are round-open/chaos beats — worst burst is a handful of small JSON frames to localhost CEF |
| T-v4e-07 | Information Disclosure | existing RoundSnapshot GateResult over-projection | accept (out of scope) | Pre-existing STATE.md residual; this plan does NOT widen it (new fields are the narrowing exemplar) and the fix stays tracked in STATE.md pending todos |
</threat_model>

<verification>
- `npx vitest run` — full suite green (existing overlay/pool tests + new projection/route/push tests)
- `npx tsc --noEmit` — clean
- `npx biome check .` — clean
- Manual (optional, not gating): `npm run dev`, open http://127.0.0.1:4901/queue — both sections render; !suggest an idea on the fake path or dev-submit → pool row appears live
</verification>

<success_criteria>
- GET /queue on the existing overlay server (no new server/port) serves the what's-coming page behind the same Host-allowlist posture
- Page shows SUGGESTION POOL (approved candidates: text + username, textContent-only, truncated) and UP NEXT (full FIFO queue up to 10 with position numbers), each with a quiet placeholder when empty
- OverlayState gains `pool` + `queue` display-only projections; no GateResult/donor/pre-gate data crosses the wire in the new fields (test-asserted)
- Pool mutations push overlay state live via POOL_CHANGED; queue mutations already push via existing triggers
- Boot log line "what's-coming page at http://127.0.0.1:%d/queue — optional OBS browser source" in the existing style
- Full gate green: vitest + tsc + biome
</success_criteria>

<output>
Create `.planning/quick/260710-v4e-dedicated-whats-coming-overlay-page-sugg/260710-v4e-SUMMARY.md` when done.
</output>
