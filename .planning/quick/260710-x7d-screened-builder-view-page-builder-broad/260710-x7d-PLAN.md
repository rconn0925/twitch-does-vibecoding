---
phase: quick
plan: 260710-x7d
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/events.ts
  - src/overlay/builder-feed.ts
  - src/overlay/builder-feed.test.ts
  - src/orchestrator/build-session.ts
  - src/orchestrator/build-session.test.ts
  - src/overlay/server.ts
  - src/overlay/server.test.ts
  - src/main.ts
  - src/overlay/public/builder.html
  - src/overlay/public/builder.css
  - src/overlay/public/builder.js
autonomous: true
requirements: [QUICK-X7D]
must_haves:
  truths:
    - "An OBS browser source at http://127.0.0.1:4901/builder renders the live build feed: a NOW BUILDING title line, fixed stage captions, 'Writing <path>' / 'Editing <path>' activity lines, and capped code snippets — snippets and paths ONLY from output batches screenOutputBatch APPROVED"
    - "A batch that screenOutputBatch rejects contributes ZERO builder-feed lines: its path and content never appear in the serialized wire payload (structurally unreachable — the feed call sits after the proceed guard, on the approved branch only)"
    - "Only fixed orchestrator-authored vocabulary + the already-broadcast build title + approved paths/snippets ever appear on the /builder wire — no raw SDK tool names or tool input keys, no classifier rationale, no donor data, no pre-gate suggestion text, no red anywhere (failed/refused captions are amber)"
    - "The feed is a bounded ring buffer (50 lines) that replays in full on ws connect (OBS scene-switch reload reconstructs the page) and clears when the next build starts — a killed build's lines never leak into the next build's feed"
    - "No build active → a quiet 'standing by' placeholder; a halted/vetoed build's feed just stops (no false BUILT IT line, no error text)"
  artifacts:
    - path: "src/overlay/builder-feed.ts"
      provides: "createBuilderFeed(): ring-buffer projection — sink side (buildStarted/stage/batchApproved) for the build session, source side (list/on) for the overlay server; fixed caption table; snippet capping"
      contains: "createBuilderFeed"
    - path: "src/orchestrator/build-session.ts"
      provides: "optional builderFeed dep + tap points (buildStarted at pipeline entry, stage in emitStage, batchApproved strictly after screen.proceed) + pure extractApprovedBatchDisplays extractor"
      contains: "extractApprovedBatchDisplays"
    - path: "src/overlay/server.ts"
      provides: "OverlayState.builderFeed display projection, OverlayBuilderFeedSource seam + NULL default, GET /builder route, BUILDER_FEED_CHANGED push wiring"
      contains: "OverlayBuilderFeedSource"
    - path: "src/shared/events.ts"
      provides: "BUILDER_FEED_CHANGED event constant in the AppEvent union"
      contains: "BUILDER_FEED_CHANGED"
    - path: "src/overlay/public/builder.html"
      provides: "The builder view page markup (single THE AI panel + script/css links)"
    - path: "src/overlay/public/builder.js"
      provides: "textContent-only renderer + reconnect-with-backoff ws client + auto-scroll to newest"
    - path: "src/overlay/public/builder.css"
      provides: "Terminal-ish layout in the overlay design language (Dominant panel, monospace snippets, 20px+ type, amber warn class, no red)"
  key_links:
    - from: "src/orchestrator/build-session.ts"
      to: "src/overlay/builder-feed.ts"
      via: "deps.builderFeed?.batchApproved(...) placed AFTER the `if (!screen.proceed) break` guard in consumeTurn — the rejected branch breaks before the call"
      pattern: "batchApproved"
    - from: "src/overlay/builder-feed.ts"
      to: "src/overlay/server.ts"
      via: "BUILDER_FEED_CHANGED event -> pushState() (immediate, like POOL_CHANGED)"
      pattern: "BUILDER_FEED_CHANGED"
    - from: "src/main.ts"
      to: "createBuilderFeed / createBuildSession / startOverlayServer"
      via: "one feed instance created unconditionally; sink handed to the build session, source handed to the overlay server"
      pattern: "createBuilderFeed"
    - from: "src/overlay/public/builder.js"
      to: "ws://<host>"
      via: "full-state-on-connect push, same reconnect pattern as queue.js"
      pattern: "new WebSocket"
---

<objective>
Add a broadcast-safe "watch the AI build" page to the EXISTING public overlay server (port 4901) at GET /builder — an OBS browser source for the stream's "THE AI" slot, replacing a raw terminal window-capture. It streams the sandboxed build agent's activity as a terminal-ish feed: the build title, stage transitions, fixed "Writing <path>" activity lines, and small capped code snippets.

The binding safety property is STRUCTURAL: nothing chat-derived or agent-derived reaches this wire unless it already passed existing screening — the title is the same 80-char-truncated string the overlay build panel already broadcasts, and paths/snippets come ONLY from output batches screenOutputBatch (COMP-02) APPROVED. The tap sits after the approval guard in the existing pipeline; a rejected or not-yet-screened batch's content is unreachable by construction. screenOutputBatch/screenBuildPlan (src/orchestrator/comp02.ts) and halt.ts are NOT modified.

Purpose: viewers see the AI actually working, with zero risk of un-screened content hitting the broadcast.
Output: BuilderFeed ring-buffer projection + build-session tap points, OverlayState.builderFeed field + /builder route, static page (builder.html/css/js), tests, boot log line.
</objective>

<execution_context>
@C:/Users/ross/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ross/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/overlay/server.ts
@src/overlay/server.test.ts
@src/orchestrator/build-session.ts
@src/orchestrator/comp02.ts
@src/orchestrator/progress-events.ts
@src/shared/events.ts
@src/shared/types.ts
@src/overlay/public/queue.js
@src/overlay/public/queue.css
@src/overlay/public/queue.html
@src/overlay/public/overlay.js
@src/overlay/public/overlay.css
@src/main.ts

<interfaces>
<!-- Key contracts already in the codebase. Use these directly. -->

From src/shared/types.ts:
```typescript
export type PipelineStage =
  | "queued" | "researching" | "planning" | "building"
  | "done" | "failed" | "refused";
export interface BuildStatusView { taskId: string; title: string; stage: PipelineStage; source?: ... }
```

From src/orchestrator/build-session.ts (the in-flight COMP-02 site inside consumeTurn — the tap goes AFTER this guard):
```typescript
if (inFlightScreen) {
  const batch = extractWriteEditText(message);
  if (batch !== null) {
    const screen = await screenOutputBatch(deps.comp02, { taskId: task.id, outputText: batch });
    recordComp02Decision(deps.db, { ... });
    if (!screen.proceed) { outcome = "compliance-rejected"; break; }
    // <<< TAP POINT: only reachable when screen.proceed === true >>>
  }
}
```
Also in build-session.ts: `const WRITE_EDIT_TOOLS = new Set(["Write", "Edit", "MultiEdit", "NotebookEdit"])`, the private `contentBlocks(message)` helper, `emitStage(task, stage, summary?)` (fires for every stage incl. finalize/enterDecision terminals), `runPipeline()` entry (where `active = { task, ac }` is set), and `finalizeAborted()` (which must make NO feed call).

From src/overlay/server.ts (the seam idiom to mirror — OverlayPoolSource + NULL_POOL_SOURCE + explicit wire narrowing in buildOverlayState + the /queue route):
```typescript
export interface OverlayPoolSource { list(): readonly {...}[]; on(event: string, handler: (...args: unknown[]) => void): void; }
app.get("/queue", (_req, res) => { res.sendFile("queue.html", { root: publicDir }); });
poolSource.on(POOL_CHANGED, () => { pushState(); });
```

From src/overlay/public/overlay.js (fixed stage captions to REUSE verbatim; failed/refused are amber, never red):
```javascript
const STAGE_CAPTION = {
  researching: "Digging into the idea",
  planning: "Drafting the build plan",
  building: "Writing the code",
  done: "Live on screen now",
  failed: "Regrouping…",
  refused: "Skipping this one",
};
const AMBER_STAGES = new Set(["failed", "refused"]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BuilderFeed projection + build-session tap points (the structural screening gate)</name>
  <files>src/shared/events.ts, src/overlay/builder-feed.ts, src/overlay/builder-feed.test.ts, src/orchestrator/build-session.ts, src/orchestrator/build-session.test.ts</files>
  <action>
**1. src/shared/events.ts** — add `export const BUILDER_FEED_CHANGED = "builder-feed:changed" as const;` with a POOL_CHANGED-style doc comment (emitted by the BuilderFeed after append/clear; low-frequency by construction — batch approvals are serialized behind classifier calls) and add it to the `AppEvent` union.

**2. src/overlay/builder-feed.ts (new)** — the ring-buffer projection between the build session and the overlay wire. Zero SDK types in this file; it receives only display-shaped data.

- Wire line shape: `export interface BuilderFeedLine { kind: "title" | "stage" | "stage-warn" | "activity" | "snippet"; text: string; }` — `text` is ALWAYS server-composed (fixed copy + already-screened data); the kind set is closed, and `stage-warn` (failed/refused) is what the client renders amber.
- `export interface BuilderFeedSink { buildStarted(title: string): void; stage(stage: PipelineStage): void; batchApproved(displays: ApprovedBatchDisplay[]): void; }` and `export interface ApprovedBatchDisplay { verb: "Writing" | "Editing"; path: string; text: string; }`.
- `export function createBuilderFeed(opts?: { maxLines?: number }): BuilderFeed` where `BuilderFeed` implements the sink AND the source side the overlay server consumes: `list(): readonly BuilderFeedLine[]` and `on(event, handler)` (a Node EventEmitter, like CandidatePool). Constants: `DEFAULT_MAX_LINES = 50`, `TITLE_MAX = 80`, `SNIPPET_MAX_CHARS = 200`, `SNIPPET_MAX_LINES = 3`.
- Behavior:
  - `buildStarted(title)`: CLEAR the buffer (security-model point 5 — the next build starts clean; a killed build's lines can never leak forward), then append `{ kind: "title", text: "NOW BUILDING: " + title }` with the title JS-truncated to 80 chars (the SAME cap the overlay build panel applies to this SAME already-broadcast string). Emit BUILDER_FEED_CHANGED once.
  - `stage(stage)`: append one line from a FIXED caption table reusing overlay.js's STAGE_CAPTION wording verbatim (researching → "Digging into the idea", planning → "Drafting the build plan", building → "Writing the code", done → "Live on screen now", failed → "Regrouping…", refused → "Skipping this one"). failed/refused use kind `"stage-warn"`; all others `"stage"`. `"queued"` (and any unknown value — fail closed) appends NOTHING. Emit on append.
  - `batchApproved(displays)`: for each display append `{ kind: "activity", text: `${verb} ${path}` }` and, when `text` is non-empty, one `{ kind: "snippet", text }` with the snippet capped server-side: first `SNIPPET_MAX_LINES` lines, then hard-sliced to `SNIPPET_MAX_CHARS` chars with a trailing `…` when truncated (JS truncation is authoritative; the CSS backstop comes in Task 3). One BUILDER_FEED_CHANGED emit per batchApproved call.
  - Ring bound: after any append, drop oldest lines beyond `maxLines` (50).
- NO clear-on-terminal-stage and NO feed method for aborts: a halted/vetoed build's feed simply stops (consistent with no-false-BUILT-IT); the clear happens at the NEXT buildStarted.

**3. src/orchestrator/build-session.ts** — tap the already-safe signals. Do NOT touch comp02.ts, halt.ts, or any screening logic.

- Add optional dep `builderFeed?: BuilderFeedSink` to `BuildSessionDeps` (type-only import from `../overlay/builder-feed.js` — precedent: this file already imports BUILD_STAGE_CHANGED from `../overlay/server.js`). Document: this is the broadcast /builder feed; every call site is post-screening by construction.
- Add pure exported extractor `extractApprovedBatchDisplays(message: unknown): ApprovedBatchDisplay[]` next to `extractWriteEditText` (same `contentBlocks` + `WRITE_EDIT_TOOLS` machinery — build-session.ts is the declared SDK-shape containment boundary, so shape narrowing belongs HERE, keeping builder-feed.ts SDK-free). Per qualifying tool_use block: `verb = block.name === "Write" ? "Writing" : "Editing"` (Edit/MultiEdit/NotebookEdit all map to "Editing" — raw tool names NEVER cross out of this function); `path` from `input.file_path` or `input.notebook_path` (skip the block if neither is a string); `text` from `input.content` ?? `input.new_string` ?? `input.new_source` ?? first `edits[].new_string`, else "".
- Tap points (exactly three; finalizeAborted gets NO call):
  1. In `runPipeline()`, immediately after `active = { task, ac }` inside the try: `deps.builderFeed?.buildStarted(task.text);`
  2. In `emitStage()`, after `deps.progress.push(view)`: `deps.builderFeed?.stage(stage);` — this single line covers researching/planning/building plus the terminal done/failed/refused beats from finalize() and enterDecision(), and the retryBuild path's "building" re-emit, with zero new call sites.
  3. In `consumeTurn()`, inside the `inFlightScreen` block, strictly AFTER the `if (!screen.proceed) { outcome = "compliance-rejected"; break; }` guard: `deps.builderFeed?.batchApproved(extractApprovedBatchDisplays(message));` — the rejected branch `break`s before this line executes, making rejected content unreachable on the feed by control flow, not by convention. Add a comment stating exactly that (T-x7d structural gate).

**4. Tests.** `src/overlay/builder-feed.test.ts` (new, vitest, no fakes needed — pure module):
- ring bound: 60 appends → `list().length === 50`, oldest evicted (assert first surviving line).
- clear-on-new-build: populate, `buildStarted("next")` → list is exactly the one new title line.
- title truncation at 80 chars + "NOW BUILDING: " prefix.
- fixed vocabulary: every caption comes from the fixed table; `stage("queued")` and `stage("bogus" as never)` append nothing; failed/refused → kind "stage-warn"; kinds across all methods ⊆ the closed 5-value set.
- snippet capping: 10-line/1000-char input → ≤ 3 lines and ≤ 201 chars (200 + ellipsis).
- BUILDER_FEED_CHANGED emitted on buildStarted/stage/batchApproved.

`src/orchestrator/build-session.test.ts` additions (mirror the file's existing injected fakes — fake agentRunner yielding scripted AgentMessage streams, fake comp02 classify, fake machine/registry/queue/db):
- (a) STRUCTURAL GATE: build turn stream yields an assistant message with a Write tool_use `{ file_path: "sandbox/evil.txt", content: "FORBIDDEN-PAYLOAD" }`; comp02 classify fake REJECTS. Attach a real createBuilderFeed() as builderFeed. After the run: `JSON.stringify(feed.list())` contains NEITHER "evil.txt" NOR "FORBIDDEN-PAYLOAD" (assert on the serialized wire shape). The feed still contains the amber refused caption ("Skipping this one") from the narrated compliance-failure path.
- (b) approved batch: same stream, classify APPROVES → feed contains an activity line exactly `"Writing sandbox/evil.txt"`-shaped (`Writing <path>`) plus a snippet line with the capped content.
- (c) fixed-vocabulary wire: in the approved case, the serialized feed contains NONE of the raw tokens `"tool_use"`, `"file_path"`, `"new_string"`, `"MultiEdit"`, `"NotebookEdit"`, `"input"` — tool names map to fixed copy only. Use a MultiEdit tool_use in the scripted stream and assert its line reads `Editing <path>`.
- (d) abort semantics: abort mid-build (the existing finalizeAborted test pattern) → no "Live on screen now" line ever appears; feed lines from before the abort remain (frozen); a subsequent startBuild clears them.
  </action>
  <verify>npx vitest run src/overlay/builder-feed.test.ts src/orchestrator/build-session.test.ts && npx tsc --noEmit</verify>
  <done>BuilderFeed exists as a bounded, clear-on-new-build ring buffer with a closed line vocabulary; build-session taps it at exactly buildStarted/emitStage/post-approval; the rejected-batch test proves path+content are absent from the serialized wire; comp02.ts and halt.ts untouched (git diff shows no changes to them).</done>
</task>

<task type="auto">
  <name>Task 2: /builder route + OverlayState.builderFeed projection + main.ts wiring</name>
  <files>src/overlay/server.ts, src/overlay/server.test.ts, src/main.ts</files>
  <action>
**1. src/overlay/server.ts** — mirror the quick-v4e /queue + OverlayPoolSource pattern exactly:

- New seam: `export interface OverlayBuilderFeedSource { list(): readonly { kind: string; text: string }[]; on(event: string, handler: (...args: unknown[]) => void): void; }` + `NULL_BUILDER_FEED_SOURCE` (empty list, no-op on) + optional `builderFeed?: OverlayBuilderFeedSource` in `OverlayServerDeps` (doc: quick-x7d builder view; defaults to an empty feed → the page shows its standing-by state).
- New `OverlayState` field `builderFeed: { kind: string; text: string }[]` with a doc comment stating the safety contract: every line is orchestrator-authored fixed copy or COMP-02-APPROVED path/snippet content, appended only past the screenOutputBatch proceed guard (build-session.ts) — the raw SDK stream is never tapped pre-screening.
- In `buildOverlayState()`: explicit re-projection `builderFeed: feedSource.list().map((l) => ({ kind: l.kind, text: l.text }))` — the T-v4e-01/controlWindow narrowing idiom, so even a richer source object can never leak extra keys onto the wire.
- Route, directly below the /queue route with the same comment rationale: `app.get("/builder", (_req, res) => { res.sendFile("builder.html", { root: publicDir }); });` — inherits the app-level Host-allowlist middleware and the no-mutation-routes posture automatically.
- Push wiring, next to the POOL_CHANGED block: `feedSource.on(BUILDER_FEED_CHANGED, () => { pushState(); });` — immediate push (batch approvals are serialized behind classifier latency; stage beats are a handful per build — never the vote-tally debounce). Import BUILDER_FEED_CHANGED from ../shared/events.js.

**2. src/main.ts:**
- Import createBuilderFeed. Create ONE instance unconditionally (just before the `if (opts.agentRunner && opts.sandboxAdapter)` orchestrator block, ~line 976): `const builderFeed = createBuilderFeed();` — the page must exist and show standing-by even when no orchestrator is composed.
- Inside the orchestrator block, pass `builderFeed` into `createBuildSession({ ... })` deps.
- Pass `builderFeed` into `startOverlayServer({ ... })` (always — the same instance is both sink and source).
- Boot log line after the existing /queue log, same shape: `logger.info({ port: overlay.port }, "builder view at http://127.0.0.1:%d/builder — OBS browser source for THE AI slot", overlay.port);`

**3. src/overlay/server.test.ts** — mirror the /queue tests one-for-one:
- `GET /builder` → 200, content-type text/html (after Task 3 lands builder.html; write the test to fetch the route — it will pass once the file exists; if executing this task before Task 3, assert 200 via a temporary stub is NOT allowed — instead order the work so builder.html exists first or run this test at the end of Task 3's gate. Simplest: add the test here; the Task 3 full-suite gate is the binding verification).
- DNS-rebound raw-HTTP GET /builder with foreign Host → 403 (copy the existing rebound-request helper pattern at the CR-02 test).
- Add "/builder" to the existing mutation-method 404 loop (`POST/PUT/DELETE/PATCH` all 404).
- `/api/state` includes `builderFeed: []` by default (NULL source).
- Wire-shape narrowing: inject a fake builderFeed source whose list() items carry EXTRA keys (`{ kind, text, secret: "donor", rationale: "x" }`) → serialized `/api/state` and ws-push payloads contain only `kind`/`text` (assert `"secret"`/`"rationale"` absent from the raw JSON string — the pool-wire forbidden-strings test at ~line 785 is the pattern).
- Push + replay: fake source emits BUILDER_FEED_CHANGED → connected ws client receives a fresh full-state push; a NEW ws connection's FIRST message already contains the source's current lines (full-buffer replay on OBS reload).
  </action>
  <verify>npx vitest run src/overlay/server.test.ts && npx tsc --noEmit</verify>
  <done>GET /builder is served by the existing 4901 server behind the Host allowlist with zero mutation routes; OverlayState.builderFeed carries exactly {kind,text} lines regardless of source richness; BUILDER_FEED_CHANGED pushes full state immediately; main.ts composes one feed instance into both the build session and the overlay server and logs the boot line.</done>
</task>

<task type="auto">
  <name>Task 3: Builder page client (terminal-ish, textContent-only) + full gate</name>
  <files>src/overlay/public/builder.html, src/overlay/public/builder.css, src/overlay/public/builder.js</files>
  <action>
Mirror queue.html/queue.css/queue.js structure and comments (they are the newest client pattern):

**builder.html** — minimal shell like queue.html: loads overlay.css THEN builder.css (palette tokens come from overlay.css :root), one `<main id="feed-panel" class="ai-panel">`, script builder.js. Title "THE AI".

**builder.css** — page-specific layout only, overlay design language:
- Single Dominant backing panel (`background: var(--dominant)`, radius 12px, panel padding) filling the source frame; body transparent (inherited), safe-margin padding, box-sizing border-box.
- `.ai-label` section header "THE AI" — 24px/700, letter-spacing, var(--muted) (the .wc-label recipe).
- Feed lines ≥ 20px: `.ai-stage` 20px/600; `.ai-title` 22px/700; `.ai-activity` 20px/600 monospace (`font-family: ui-monospace, Consolas, monospace`); `.ai-snippet` 20px/500 monospace, var(--muted), `white-space: pre-wrap`, plus the CSS backstop behind the JS cap: `overflow: hidden; max-height: calc(3 * 1.5em); overflow-wrap: anywhere;`.
- `.ai-warn` — amber via the overlay's existing amber/accent token (check overlay.css for the token the .build-caption-amber rule uses and reuse it; NEVER any red).
- `.ai-empty` — the standing-by placeholder, 20px/600 var(--muted).
- Feed container: `display: flex; flex-direction: column; overflow: hidden;` with the panel a fixed-height column so overflow clips at the top (oldest) edge.

**builder.js** — copy queue.js's IIFE skeleton verbatim where applicable:
- Same `el(tag, className, text)` textContent-only helper and header comment (T-02-19 rendering rule: never assigns HTML strings; all wire strings are opaque plain text). Zero innerHTML anywhere.
- `handleState(state)`: read `state.builderFeed`; if not a non-empty array → render the "THE AI" label + `.ai-empty` "standing by…". Otherwise render label + one node per line, class by fixed kind map: title→ai-title, stage→ai-stage, stage-warn→"ai-stage ai-warn", activity→ai-activity, snippet→ai-snippet; UNKNOWN kind → skip the line entirely (fail closed client-side too). JS-truncate every line's text as a defensive backstop (title/activity/stage at 120 chars; snippet at 200) even though the server already caps.
- Auto-scroll to newest: after render, `panel.scrollTop = panel.scrollHeight` (with the CSS overflow config this pins the newest line visible after every push and after the on-connect full replay).
- WS connect/reconnect: copy queue.js's connect() with the same backoff (500ms * 2^attempts, max 8000), silent freeze-on-disconnect, JSON.parse in try/catch. Full state arrives on connect → the whole feed reconstructs after an OBS scene switch.
- No timers, no countdowns — push-driven only. No syntax highlighting, zero dependencies.

Then run the FULL gate (this is the plan's binding verification): `npx vitest run && npx tsc --noEmit && npx biome check .` — fix anything the suite/type-check/lint surfaces across all three tasks' files.
  </action>
  <verify>npx vitest run && npx tsc --noEmit && npx biome check .</verify>
  <done>GET /builder renders the terminal-ish feed page: standing-by when idle, live title/stage/activity/snippet lines during a build, amber (never red) for failed/refused, auto-scrolled to newest, reconnecting silently with backoff. Full test suite (existing ~733 + new), tsc, and biome all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent SDK stream → /builder wire | Agent-generated file content (derived from chat-controlled tasks) crosses to a SCREEN-SHARED broadcast surface |
| chat-derived title → /builder wire | task.text is viewer-authored (already gate-approved + already broadcast on the overlay build panel) |
| localhost network → overlay HTTP/ws | DNS-rebinding / foreign-Origin reads of broadcast state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-x7d-01 | Information Disclosure | consumeTurn tap point | mitigate | batchApproved() call sits AFTER the `!screen.proceed → break` guard — rejected/unscreened batch content is unreachable by control flow; test (a) asserts absence on the serialized wire |
| T-x7d-02 | Information Disclosure | builder-feed wire shape | mitigate | Closed 5-kind line vocabulary, fixed caption table, tool names mapped to "Writing"/"Editing" inside extractApprovedBatchDisplays — raw SDK tool names/input keys never cross; test (c) |
| T-x7d-03 | Tampering (stored XSS on broadcast) | builder.js renderer | mitigate | textContent-only el() construction, zero innerHTML, JS truncation + CSS backstop — same posture as queue.js (T-02-19) |
| T-x7d-04 | Information Disclosure | OverlayState.builderFeed projection | mitigate | Explicit {kind,text} re-projection in buildOverlayState (T-v4e-01 idiom); richer-source leak test in server.test.ts |
| T-x7d-05 | Information Disclosure | cross-build leakage | mitigate | buildStarted() clears the ring buffer — a killed/vetoed build's lines never survive into the next build's feed; finalizeAborted makes no feed call (no false BUILT IT line) |
| T-x7d-06 | Spoofing/ID | /builder route reads | mitigate | Inherits app-level Host-allowlist (CR-02), ws Origin+Host check (WR-01), GET-only-by-construction; mirrored route tests |
| T-x7d-07 | DoS | unbounded feed memory / push floods | mitigate | 50-line ring bound; pushes gated behind classifier latency + stage-beat cadence (POOL_CHANGED precedent) — accepted as low-frequency by construction |
| T-x7d-SC | Tampering | npm installs | accept | Zero new dependencies (constraint); no install tasks exist in this plan |
</threat_model>

<verification>
1. `npx vitest run && npx tsc --noEmit && npx biome check .` — full gate green.
2. Structural gate test passes: a screenOutputBatch-rejected batch's path/content absent from `JSON.stringify(feed.list())` and from the overlay wire.
3. `git diff --stat` shows NO changes to src/orchestrator/comp02.ts, src/compliance/**, or src/kill-switch/halt.ts.
4. Grep sanity: the only `batchApproved` call site in build-session.ts appears after the `!screen.proceed` break inside consumeTurn.
</verification>

<success_criteria>
- GET /builder on 4901 serves the builder view page behind the existing Host allowlist; POST/PUT/DELETE/PATCH 404; DNS-rebound GET 403.
- Feed content is exclusively: 80-char-truncated already-broadcast build title, fixed stage captions (overlay.js wording, amber for failed/refused, no red), fixed "Writing/Editing <path>" activity lines, and ≤3-line/≤200-char snippets — all sourced strictly from COMP-02-approved batches.
- Rejected batches produce zero feed bytes (test-proven on serialized wire shape); ring buffer bounded at 50 and cleared on the next buildStarted; full replay on ws connect; standing-by placeholder when idle.
- Zero new npm dependencies; compliance screening logic and halt paths untouched; boot log announces the /builder URL.
</success_criteria>

<output>
Create `.planning/quick/260710-x7d-screened-builder-view-page-builder-broad/260710-x7d-SUMMARY.md` when done.
</output>
