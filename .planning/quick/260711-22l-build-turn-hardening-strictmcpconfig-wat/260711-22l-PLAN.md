---
phase: quick-260711-22l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/turn-options.ts
  - src/orchestrator/turn-options.test.ts
  - src/orchestrator/sdk-runner.ts
  - src/orchestrator/gallery-publisher.ts
  - src/orchestrator/gallery-publisher.test.ts
  - src/orchestrator/build-session.ts
  - src/orchestrator/build-session.test.ts
  - src/audit/record.ts
  - src/main.ts
  - tests/e2e/build-flow.e2e.test.ts
  - .env.example
autonomous: true
requirements: [QUICK-22L-MCP-LOCKDOWN, QUICK-22L-WATCHDOG, QUICK-22L-GALLERY-PUBLISH]

must_haves:
  truths:
    - "The sandboxed build turn's query() Options carry strictMcpConfig: true AND mcpServers: {} — chat-derived prompts structurally cannot invoke the builder account's claude.ai connectors (Gmail/Drive/HubSpot/Railway/GitHub)"
    - "The single build turn is bounded by env-tunable BUILD_TURN_TIMEOUT_SECONDS (default 900s), no longer the hardcoded 5-minute constant; the fail-closed watchdog abort path is unchanged"
    - "A build that finalizes done triggers a host-side snapshot commit+push of the workspace generation dir to the gallery repo; failed/refused/comp02-rejected/aborted builds NEVER publish"
    - "A publish failure NEVER throws into the build pipeline, finalize, or halt paths — it is a loud pino error + one audit row, and the show loop continues untouched"
    - "No chat-derived text ever reaches a shell (execFile arg arrays only, no shell: true anywhere) and no .env secret, dotfile, or node_modules content enters a gallery commit"
  artifacts:
    - path: "src/orchestrator/turn-options.ts"
      provides: "Pure SDK-free options assembly with MCP lockdown (unit-testable — sdk-runner.ts is never loaded in tests)"
      exports: ["assembleSandboxedBuildOptions", "assembleHostTurnOptions"]
    - path: "src/orchestrator/gallery-publisher.ts"
      provides: "Host-side snapshot publisher (git via execFile arg arrays, injected exec/fs fakes)"
      exports: ["createGalleryPublisher", "resolveGalleryConfig", "workspaceCopyFilter", "sanitizeCommitTitle"]
    - path: "src/audit/record.ts"
      provides: "recordGalleryPublish audit helper following the insert() pattern"
      contains: "gallery_publish"
    - path: ".env.example"
      provides: "BUILD_TURN_TIMEOUT_SECONDS, GALLERY_REPO_URL, GALLERY_PUBLISH_ENABLED documentation"
      contains: "BUILD_TURN_TIMEOUT_SECONDS"
  key_links:
    - from: "src/orchestrator/sdk-runner.ts"
      to: "src/orchestrator/turn-options.ts"
      via: "assembleSandboxedBuildOptions(spec, buildSandboxOptions())"
      pattern: "strictMcpConfig"
    - from: "src/orchestrator/build-session.ts finalize()"
      to: "deps.onBuildDone"
      via: "called ONLY when stage === 'done', try/catch-wrapped, next to workspace.markBuilt()"
      pattern: "onBuildDone"
    - from: "src/main.ts orchestrator composition"
      to: "galleryPublisher.publishNow"
      via: "onBuildDone closure — fire-and-forget, audits via recordGalleryPublish"
      pattern: "publishNow"
    - from: "src/main.ts createBuildSession deps"
      to: "BUILD_TURN_TIMEOUT_SECONDS"
      via: "turnTimeoutMs: envPositive(...) * 1000"
      pattern: "BUILD_TURN_TIMEOUT_SECONDS"
---

<objective>
Three streamer-approved build-turn hardening follow-ups now that straight-to-build is merged:

1. **MCP lockdown** — the builder account's claude.ai connectors (Gmail/Drive/HubSpot/Railway/GitHub) are reachable from sandboxed build turns today. Lock the build turn's SDK Options so chat-derived prompts structurally cannot invoke them.
2. **WR-07 watchdog budget** — the 5-minute per-turn timeout aborted a live, progressing build. Make the single build turn's budget env-tunable (`BUILD_TURN_TIMEOUT_SECONDS`, default 900).
3. **GitHub auto-commit** — after each `done` build, the HOST-side orchestrator commits + pushes a snapshot of the persistent workspace generation to https://github.com/rconn0925/vibecoding-gallery (already created, private). The chat/build agent never gets GitHub access — publishing is a controlled host-side snapshot using the streamer's own git/gh credentials, which never enter the sandbox.

Purpose: close the connector-exposure flag from the 2026-07-11 sandbox-spawn debug session, stop the watchdog from killing healthy live builds, and give every completed build a durable public artifact trail.
Output: hardened sdk-runner options assembly (unit-tested), env-tunable watchdog, new `src/orchestrator/gallery-publisher.ts` + `recordGalleryPublish` audit helper + `onBuildDone` seam, all wired in main.ts, documented in .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/orchestrator/sdk-runner.ts
@src/orchestrator/build-session.ts
@src/orchestrator/workspace.ts
@src/audit/record.ts
@src/main.ts
@.env.example

**READ-ONLY (verify, never modify):** src/orchestrator/sandbox-process.ts (+ its test), src/compliance/**, src/state-machine/halt.ts, all kill-switch paths, src/orchestrator/classifier-runner.ts (CLASSIFIER_SYSTEM_PROMPT byte-untouched).

**Zero new npm dependencies.** Host git + gh are assumed present (they are). Use node builtins only (`node:child_process` execFile, `node:fs/promises`).
</context>

<interfaces>
<!-- Verified against the INSTALLED node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts -->

From sdk.d.ts (Options type, verified 2026-07-11):
```typescript
// line 1668
mcpServers?: Record<string, McpServerConfig>;
// line 1919 — "Only use MCP servers passed via the mcpServers option ... ignoring
// all other MCP configurations: project .mcp.json, user settings, plugins, and
// on-disk agent frontmatter. Maps to the CLI --strict-mcp-config flag."
strictMcpConfig?: boolean;
// line 1835
settings?: string | Settings;
```

From sdk.d.ts (`export declare interface Settings`, line 4787; field at 4950):
```typescript
// "When true in any settings source, claude.ai MCP cloud connectors are not
// auto-fetched or connected. ... Any-source-true wins."
disableClaudeAiConnectors?: boolean;
```

From src/main.ts:240 (the envPositive idiom to reuse):
```typescript
function envPositive(raw: string | undefined, fallback: number): number {
  const parsed = raw === undefined ? Number.NaN : Number.parseFloat(raw);
  return Number.isFinite(parsed) && parsed > 0 ? parsed : fallback;
}
```

From src/orchestrator/build-session.ts:
- `BuildSessionDeps.turnTimeoutMs?: number` already exists (line ~142); `DEFAULT_TURN_TIMEOUT_MS = 5 * 60_000` at line 194 is the deps-absent fallback.
- `finalize(task, stage)` at line ~422: the `stage === "done"` branch (`deps.workspace.markBuilt()`) is the ONLY correct done seam. `finalizeAborted()`, `enterDecision()`, and `skipTask()` are the failure/abort paths — the hook must be unreachable from them.
- `onHeldForReview?: (task, planText) => void` (line ~120) is the existing optional-callback dep pattern to mirror.

From src/orchestrator/sandbox-process.ts:64-65 (read-only — duplicate these defaults with a comment, do not modify):
```typescript
distroName: env.BUILD_DISTRO_NAME ?? "vibecoding-build",
distroUser: env.BUILD_DISTRO_USER ?? "builder",
```

From src/audit/record.ts — the insert() pattern every audit helper follows (arg object -> insert with eventType/source/decision/rationale/streamMode/taskId; audit_log has NO CHECK constraints, so new event_type values are schema-safe).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP lockdown on the build turn + env-tunable watchdog budget</name>
  <files>src/orchestrator/turn-options.ts, src/orchestrator/turn-options.test.ts, src/orchestrator/sdk-runner.ts, src/main.ts, .env.example</files>
  <behavior>
    - Test: assembleSandboxedBuildOptions(spec, sandboxOpts) returns Options with strictMcpConfig === true, mcpServers deep-equal {}, and settings.disableClaudeAiConnectors === true
    - Test: it passes through systemPrompt by IDENTITY (options.systemPrompt === spec.systemPrompt — SAND-04 bare-reference invariant), abortController, sandbox = sandboxOpts, spawnClaudeCodeProcess = spec.spawnClaudeCodeProcess, cwd = spec.workspaceDir
    - Test: assembleHostTurnOptions(spec) returns allowedTools: [], disallowedTools containing WebFetch/WebSearch/Write/Edit/MultiEdit/NotebookEdit/Bash, AND the same three lockdown fields (defense-in-depth on the unreachable host branch)
    - Test: neither function mutates the input spec
  </behavior>
  <action>
    **New module `src/orchestrator/turn-options.ts`** — pure options assembly, extracted so the lockdown is unit-testable: sdk-runner.ts statically imports the real SDK and is deliberately NEVER loaded in a vitest run, so the assertions must live against an SDK-free module. Use `import type { Options } from "@anthropic-ai/claude-agent-sdk"` (type-only — erased at runtime, safe for tests) and `import type { AgentRunSpec } from "./types.js"`.

    Export two functions:
    - `assembleSandboxedBuildOptions(spec: AgentRunSpec, sandboxOptions: Options["sandbox"]): Options` — the sandboxed build-turn options: systemPrompt (bare reference), abortController, sandbox, spawnClaudeCodeProcess, cwd = spec.workspaceDir, PLUS the MCP lockdown triple:
      - `strictMcpConfig: true` (belt: ignores user settings / .mcp.json / plugin MCP config — verified at sdk.d.ts:1919)
      - `mcpServers: {}` (braces: even strict mode loads only what's passed here — nothing)
      - `settings: { disableClaudeAiConnectors: true }` (third layer: sdk.d.ts:4950 — this key specifically gates the AUTO-FETCHED claude.ai cloud connectors, which is exactly the Gmail/Drive/HubSpot/Railway/GitHub exposure; strictMcpConfig's doc covers file/settings-based MCP config, this covers the account-level connector fetch. Type the object against the SDK's `Settings` interface via type-only import.)
    - `assembleHostTurnOptions(spec: AgentRunSpec): Options` — the unreachable-from-live-pipeline host branch: systemPrompt, abortController, `allowedTools: []`, `disallowedTools: HOST_TURN_DISALLOWED` (move the constant into this module and export it), plus the SAME lockdown triple (structural defense-in-depth, CR-02/WR-01 doctrine — never depend on an SDK default for a safety boundary).

    Document in the module header WHY three layers (memory: mcp-lockdown-github-autocommit; the builder account's claude.ai connectors were reachable from sandboxed build turns).

    **`src/orchestrator/sdk-runner.ts`** — replace the inline Options assembly in `run()` with calls to the two new functions (`assembleSandboxedBuildOptions(spec, buildSandboxOptions())` on the sandbox branch, `assembleHostTurnOptions(spec)` on the fallback). Remove the now-moved HOST_TURN_DISALLOWED constant (import it if the comment wants to reference it). Behavior otherwise identical — `query({ prompt: spec.userPrompt, options })` and the message loop unchanged.

    **Classifier read-verify (NO edits):** grep `src/orchestrator/classifier-runner.ts` and confirm its transport already disables tools (tools-disabled single-turn per quick-if0). Do NOT add lockdown there, do NOT touch the file — CLASSIFIER_SYSTEM_PROMPT stays byte-identical. Note the verification result in the SUMMARY.

    **Watchdog budget (`src/main.ts`)**: in the `createBuildSession({...})` composition block (~line 1002), add `turnTimeoutMs: envPositive(process.env.BUILD_TURN_TIMEOUT_SECONDS, DEFAULT_BUILD_TURN_TIMEOUT_SECONDS) * 1_000` with a new module-scope constant `const DEFAULT_BUILD_TURN_TIMEOUT_SECONDS = 900;` (15 min). Keep `DEFAULT_TURN_TIMEOUT_MS` in build-session.ts untouched — it remains the deps-absent fallback (tests and any future non-build turn). Update the WR-07 comment on `DEFAULT_TURN_TIMEOUT_MS` to note the live composition now injects BUILD_TURN_TIMEOUT_SECONDS (default 900s). The watchdog abort path itself (abort + fail-closed finalize) is UNCHANGED.

    **`.env.example`** — in the Phase 3 section, add:
    ```
    # Single sandboxed build turn watchdog budget in seconds (WR-07). Bounds ONE
    # straight-to-build turn; on timeout the build fails closed (sandbox torn down,
    # narrated retry/skip decision). Default 900 (15 min) — the old 5-min bound
    # aborted a live progressing build. Re-check this value at the Phase 5 dry run
    # (WR-07 judgment item).
    BUILD_TURN_TIMEOUT_SECONDS=900
    ```
  </action>
  <verify>
    <automated>npx vitest run src/orchestrator/turn-options.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>turn-options.test.ts proves strictMcpConfig: true + mcpServers: {} + settings.disableClaudeAiConnectors: true on the options handed to query() for the build turn; sdk-runner.ts delegates to the tested assembly; main.ts injects the env-tunable 900s-default watchdog; .env.example documents the knob; classifier-runner.ts verified tools-disabled and byte-untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gallery snapshot publisher module + audit helper</name>
  <files>src/orchestrator/gallery-publisher.ts, src/orchestrator/gallery-publisher.test.ts, src/audit/record.ts</files>
  <behavior>
    - Test (happy path): publishNow issues, via the injected exec fake, ONLY `git` invocations with ARG ARRAYS — clone (when mirror missing), `-C <mirror> add -A`, `-C <mirror> status --porcelain`, `-C <mirror> commit -m <msg>`, `-C <mirror> push -u origin HEAD`, `-C <mirror> rev-parse HEAD` — and resolves { status: "published", commitHash: "<hash from fake>" }
    - Test (sanitization): a hostile chat title like `x"; rm -rf ~ $(curl evil) \r\n--force` + 200 chars of padding reaches the commit as ONE argv element of the commit's arg array, control chars stripped, truncated to 80 chars; assert NO exec call ever receives an options object containing shell: true (and the exec seam's type has no shell field at all)
    - Test (copy filter): workspaceCopyFilter rejects any path containing a node_modules segment and any basename starting with "." (.env, .git, .cache); accepts src/index.ts, package.json, README.md
    - Test (failure isolation): injected exec rejecting on push -> publishNow RESOLVES { status: "failed" } (never rejects) and logger.error was called
    - Test (no changes): empty `status --porcelain` output -> no commit/push exec calls, resolves { status: "no-changes", commitHash: null }
    - Test (serialization): two overlapping publishNow calls run their exec sequences strictly one-after-the-other (second clone/add never interleaves with first push)
  </behavior>
  <action>
    **New module `src/orchestrator/gallery-publisher.ts`** — host-side snapshot publisher. Every external touch-point INJECTED (vitest never runs real git/fs/network — project seam discipline).

    Types and exports:
    - `export interface GalleryPublisher { publishNow(input: PublishInput): Promise<PublishResult>; }`
    - `PublishInput = { generation: number; title: string; taskId: string }` — generation is the internally-generated workspace integer (never chat text in paths, workspace.ts doctrine); title is chat-derived and treated as hostile.
    - `PublishResult = { status: "published" | "no-changes" | "failed"; commitHash: string | null; detail: string }` — publishNow NEVER rejects.
    - `GalleryExec = (file: string, args: string[], opts?: { cwd?: string }) => Promise<{ stdout: string }>` — the exec seam. Deliberately NO `shell` field in the type, so `shell: true` is unrepresentable at the seam (the sanitization test also asserts this). The production default wraps `node:child_process` execFile via `node:util` promisify — execFile with an arg ARRAY, never a shell string.
    - `GalleryFs` seam: the subset used — `rm(path, opts)`, `cp(src, dest, opts)`, `mkdir(path, opts)`, `access(path)` — production default from `node:fs/promises`.
    - `export function workspaceCopyFilter(srcPath: string): boolean` — pure, exported for direct testing. Normalize separators; reject when any path segment === "node_modules" OR the basename starts with "." (dotfiles AND dotdirs: .env, .git, .cache, .config). This is the secrets gate: nothing from the workspace's dotfiles can enter a commit.
    - `export function sanitizeCommitTitle(title: string): string` — strip all control chars (including \r \n \t -> single space), collapse whitespace, trim, truncate to 80 chars, fallback "untitled build" when empty. (Shell injection is already impossible via arg arrays — this is message hygiene, defense-in-depth.)
    - `export function resolveGalleryConfig(env: NodeJS.ProcessEnv): GalleryConfig | null`:
      - `repoUrl` = trimmed `GALLERY_REPO_URL` or default `"https://github.com/rconn0925/vibecoding-gallery.git"`
      - enabled unless `GALLERY_PUBLISH_ENABLED` trimmed === exact string `"false"` (the AUTO_ROUND_ENABLED strict-string idiom, inverted default) — AND only when repoUrl is non-empty; return null when disabled
      - `mirrorDir` = `"data/gallery-mirror"` (already covered by the repo's `data/` gitignore — verified; call this out in a comment)
      - `workspaceRootUnc` = `\\\\wsl.localhost\\${distroName}\\home\\${distroUser}\\projects` with distroName = `env.BUILD_DISTRO_NAME ?? "vibecoding-build"`, distroUser = `env.BUILD_DISTRO_USER ?? "builder"` — duplicate these two defaults with a comment pointing at sandbox-process.ts:64-65 (that file is READ-ONLY; do not import it — importing would drag the spawn adapter into any consumer).
    - `export function createGalleryPublisher(deps: { config: GalleryConfig; exec?: GalleryExec; fsx?: GalleryFs; logger: Logger }): GalleryPublisher`

    publishNow algorithm (all inside one try/catch; internal promise-chain serialization so overlapping publishes never interleave — builds are concurrency-1 but a slow push can outlive the next short build):
    1. Ensure mirror: `access(mirrorDir/.git)` fails -> `mkdir` parent recursive, then `git clone <repoUrl> <mirrorDir>` (arg array).
    2. Snapshot copy: source = `${workspaceRootUnc}\\app-${generation}` (host reaches the distro workspace via the \\wsl.localhost UNC share), dest = `<mirrorDir>/app-<generation>`. REPLACE-copy: `rm(dest, { recursive: true, force: true })` then `cp(src, dest, { recursive: true, filter: workspaceCopyFilter-based })` so files deleted in the workspace disappear from the snapshot. **PLAN NOTE (verified reasoning, re-verify live at first real publish):** automount=off in the distro governs distro->host drive mounts only; host->distro access via `\\wsl.localhost\vibecoding-build\...` is served by WSL's Plan 9 server and works regardless — and accessing the UNC path auto-starts a stopped distro. If the copy still fails, that is a FAILED publish (audited + logged), never a pipeline error.
    3. `git -C <mirror> add -A`
    4. `git -C <mirror> status --porcelain` -> empty stdout -> return `{ status: "no-changes", commitHash: null }` (skip commit/push; still audited by the caller).
    5. `git -C <mirror> commit -m` + message `app-<generation>: <sanitizeCommitTitle(title)>` as ONE argv element.
    6. `git -C <mirror> push -u origin HEAD` (`-u origin HEAD` handles a freshly-cloned empty repo with no upstream branch).
    7. `git -C <mirror> rev-parse HEAD` -> `{ status: "published", commitHash }`.
    - catch (anything) -> `logger.error({ err, generation, taskId }, "gallery publish FAILED — show loop unaffected")` -> resolve `{ status: "failed", commitHash: null, detail: message }`. Loud, never thrown.
    - Secrets discipline in the header comment: the streamer's git/gh credentials live in the HOST credential manager only and NEVER enter the sandbox; nothing from the orchestrator's .env is written into the mirror; the mirror dir sits under gitignored `data/`.

    **`src/audit/record.ts`** — add `recordGalleryPublish(db, args: { taskId: string; generation: number; status: "published" | "no-changes" | "failed"; commitHash: string | null; detail: string | null; streamMode: StreamMode })` following the exact insert() pattern: eventType `"gallery_publish"`, source `"orchestrator"`, decision = status, rationale = `commit <hash>` / detail / null as appropriate (embed the generation in the rationale text — internally-generated integer only), taskId. audit_log has no CHECK constraints — schema-safe addition; extend the record.ts section comment like the Phase 3/4 blocks do.
  </action>
  <verify>
    <automated>npx vitest run src/orchestrator/gallery-publisher.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Publisher unit-tested entirely against injected exec/fs fakes (zero real git/network in tests); hostile titles provably never reach a shell (arg-array seam with no shell field, sanitizer tested); node_modules + dotfiles provably excluded; publishNow never rejects; recordGalleryPublish follows the ledger pattern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Done-hook wiring, entrypoint composition, docs + full gate</name>
  <files>src/orchestrator/build-session.ts, src/orchestrator/build-session.test.ts, src/main.ts, tests/e2e/build-flow.e2e.test.ts, .env.example</files>
  <behavior>
    - Test (build-session): a build reaching finalize(task, "done") calls deps.onBuildDone exactly once with the task; onBuildDone absent -> no error
    - Test (build-session): onBuildDone is NOT called on: failed (enterDecision), refused, in-flight COMP-02 rejection (finalize "refused"), watchdog timeout, finalizeAborted (halt/veto), skipTask
    - Test (build-session): an onBuildDone that THROWS never breaks finalize — the machine still returns to IDLE, the task is still dequeued, the build_history row still lands (publisher-failure isolation is structural)
    - Test (e2e wiring): createApp with fake runner + a fake galleryPublisher — a done build invokes publishNow once with { generation, title: task.text, taskId }; a publishNow fake that REJECTS still leaves the app healthy (build finalized done, no unhandled rejection); no galleryPublisher injected -> nothing happens (default test path stays inert)
  </behavior>
  <action>
    **`src/orchestrator/build-session.ts`**: add optional dep `onBuildDone?: (task: QueuedTask) => void` to BuildSessionDeps (mirror onHeldForReview's doc style). Call it inside `finalize()` ONLY in the `stage === "done"` branch, immediately after `deps.workspace.markBuilt()`, wrapped in its own try/catch that logs `deps.logger?.error` and continues — a publisher callback error must NEVER disturb finalize, the IDLE transition, or the dequeue (must-have truth). Do NOT touch finalizeAborted, enterDecision, skipTask, or any halt/kill-switch path. Note in the comment: called synchronously while the machine is still BUILD_IN_PROGRESS, so a workspace rotation (console 409s mid-build) cannot race the caller's `workspace.generation()` read.

    **`src/main.ts`**:
    1. `CreateAppOptions` gains `galleryPublisher?: GalleryPublisher` (type-only import from `./orchestrator/gallery-publisher.js`) with a doc comment following the existing seam idiom: injected fake in tests, real publisher from the entrypoint helper; ABSENT -> no publishing (which is why the fake-runner e2e suite stays inert by default).
    2. In the orchestrator composition block (inside `if (opts.agentRunner && opts.sandboxAdapter)`), pass to createBuildSession:
       `onBuildDone: (task) => { ... }` — the closure: return immediately when `!opts.galleryPublisher`; else read `const generation = workspace.generation();` synchronously, then fire-and-forget:
       `void opts.galleryPublisher.publishNow({ generation, title: task.text, taskId: task.id }).then((result) => { if (db.open) recordGalleryPublish(db, { taskId: task.id, generation, status: result.status, commitHash: result.commitHash, detail: result.detail, streamMode: machine.mode }); }).catch((err) => logger.error({ err, taskId: task.id }, "gallery publish hook failed — show loop unaffected"));`
       One audit row per publish ATTEMPT (published / no-changes / failed all audited). The `db.open` guard mirrors auditIfOpen (WR-05 shutdown drain). `task.text` is the gate-APPROVED title (D-03) — same string build_history records.
    3. New entrypoint-only helper `buildGalleryPublisher(logger: Logger): GalleryPublisher | undefined` (buildDonationAdapter pattern — but a STATIC import of gallery-publisher.js is fine, it has no SDK/native deps): `const config = resolveGalleryConfig(process.env);` null -> `logger.warn("GALLERY PUBLISHING DISABLED — set GALLERY_REPO_URL / unset GALLERY_PUBLISH_ENABLED=false to enable post-build snapshots")` and return undefined; else `logger.info("GALLERY PUBLISHER ARMED — done builds snapshot to <repoUrl> via host-side git")` and return `createGalleryPublisher({ config, logger })`. Wire it in the isMain branch (synchronous call is fine alongside the Promise.all results) and spread `...(galleryPublisher ? { galleryPublisher } : {})` into createApp.
    4. publishNow is NOT wired to any timer — per-build commits ARE the periodic cadence (v1 decision); the exported publishNow on the seam is the future manual-use hook.

    **`tests/e2e/build-flow.e2e.test.ts`**: add the wiring cases from <behavior> using the existing fake-runner harness (drive one build to done; assert the fake publisher's recorded call; assert app close is clean with a rejecting publisher).

    **`.env.example`** — new section after Phase 5:
    ```
    # --- Gallery auto-publish (quick-22l): host-side post-build snapshots ---

    # After each successfully COMPLETED build, the orchestrator (HOST side — the
    # streamer's own git credentials, which never enter the sandbox) commits and
    # pushes a snapshot of the workspace generation dir to this repo. The build
    # agent NEVER gets GitHub access. Local mirror: data/gallery-mirror (gitignored).
    GALLERY_REPO_URL=https://github.com/rconn0925/vibecoding-gallery.git

    # STRICT string check (AUTO_ROUND_ENABLED idiom): ONLY the exact value "false"
    # disables publishing — any other value (including blank) leaves it ENABLED.
    GALLERY_PUBLISH_ENABLED=
    ```

    **Full gate:** `npx vitest run && npx tsc --noEmit && npx biome check .` — the whole suite (784+ tests), types, lint all green. Also re-run the invariant suites implicitly included (prompt-injection-boundary, single-funnel, secrets-isolation) — none of this task may move those.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <done>onBuildDone fires on done only (test-proven across all six non-done exits), a throwing/rejecting publisher provably cannot disturb finalize or the pipeline, main.ts composes the real publisher entrypoint-only (tests stay inert unless a fake is injected), knobs documented, full gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat -> build prompt -> SDK query() | Chat-derived text drives the sandboxed agent; anything reachable from that turn is chat-controllable |
| chat title -> host git commit | Gate-approved but still viewer-authored text enters a host-side git invocation |
| sandbox workspace -> host mirror -> GitHub | Workspace files (agent-written) cross onto the host and into a remote repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22l-01 | Elevation of privilege | build turn query() -> claude.ai connectors (Gmail/Drive/HubSpot/Railway/GitHub) | mitigate | strictMcpConfig: true + mcpServers: {} + settings.disableClaudeAiConnectors: true on every sdk-runner turn; unit-tested in turn-options.test.ts |
| T-22l-02 | Tampering | chat title -> publisher git commit | mitigate | execFile arg arrays ONLY; exec seam type has no shell field; sanitizeCommitTitle strips control chars + truncates 80; hostile-title test |
| T-22l-03 | Information disclosure | workspace dotfiles/.env/node_modules -> gallery repo | mitigate | workspaceCopyFilter rejects node_modules segments + dot-basenames (tested); mirror under gitignored data/; nothing from orchestrator .env written to mirror |
| T-22l-04 | Denial of service | publisher failure -> build pipeline / halt paths | mitigate | publishNow never rejects; onBuildDone try/catch inside finalize; fire-and-forget hook with .catch; failure = pino error + audit row only (tested: throwing hook, rejecting publisher) |
| T-22l-05 | Information disclosure | streamer git/gh credentials -> sandbox | mitigate | publishing is host-process-only; sandbox spawn env (sandbox-process.ts, untouched) never carries credentials; structural — no new sandbox surface added |
| T-22l-06 | Repudiation | silent/unrecorded publishes | mitigate | recordGalleryPublish: one append-only audit row per attempt (published / no-changes / failed + commit hash) |
| T-22l-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies in this plan — node builtins + already-installed packages only; no install tasks exist |
</threat_model>

<verification>
- `npx vitest run` — full suite green (784+ pre-existing + new turn-options, gallery-publisher, build-session done-hook, e2e wiring tests)
- `npx tsc --noEmit` clean; `npx biome check .` clean
- `git diff --stat` shows NO changes under src/compliance/, src/orchestrator/sandbox-process.ts, src/state-machine/halt.ts, src/kill-switch/ (read-only constraint held); classifier-runner.ts byte-identical
- Grep gates: `grep -rn "shell" src/orchestrator/gallery-publisher.ts` shows no `shell: true`; `grep -n "strictMcpConfig" src/orchestrator/turn-options.ts src/orchestrator/sdk-runner.ts` shows the lockdown wired into both assembly paths
</verification>

<success_criteria>
- Build-turn query() options provably (unit test) carry strictMcpConfig: true, mcpServers: {}, and settings.disableClaudeAiConnectors: true — chat prompts cannot reach the builder account's connectors
- BUILD_TURN_TIMEOUT_SECONDS (envPositive, default 900) bounds the live build turn; DEFAULT_TURN_TIMEOUT_MS kept as the deps-absent fallback; watchdog abort path untouched
- A done build fire-and-forgets a host-side snapshot: mirror clone/copy(filtered)/add/commit(sanitized title, arg arrays)/push to rconn0925/vibecoding-gallery, one audit row per attempt; failed/refused/aborted builds never publish; a publish failure can never reach the pipeline or halt paths
- Zero new npm deps; full gate (vitest + tsc + biome) green
</success_criteria>

<output>
Create `.planning/quick/260711-22l-build-turn-hardening-strictmcpconfig-wat/260711-22l-SUMMARY.md` when done. Record in the SUMMARY: the classifier-runner read-verify result, and a reminder to observe the FIRST real publish live (UNC-path readability + empty-repo first push) at the Phase 5 dry run.
</output>
