---
phase: quick-260711-l2a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state-machine/round.ts
  - src/state-machine/round.test.ts
  - src/state-machine/auto-cycle.ts
  - src/state-machine/auto-cycle.test.ts
  - src/ingestion/command-parser.ts
  - src/ingestion/command-parser.test.ts
  - src/main.ts
  - src/overlay/public/overlay.js
  - src/overlay/public/overlay.css
  - src/overlay/public/queue.js
  - src/overlay/public/queue.css
  - scripts/overlay-harness.ts
  - .env.example
autonomous: true
requirements: [QUICK-260711-L2A]

must_haves:
  truths:
    - "With ROUND_DURATION_SECONDS unset, a voting round runs 30 seconds (not 60)"
    - "A round can present up to 10 vote options and '!vote 4'..'!vote 10' are counted"
    - "When the pool reaches 10 approved suggestions mid-suggest-phase, voting starts immediately through the SAME code path as timer expiry (eligibility re-checked; halt parks it; no compliance-gate bypass)"
    - "The main overlay renders 10 vote options legibly at 1920x1080; /queue renders all 10 pool suggestions and all 10 queued builds with no clipped info"
    - "Phase-banner instructions match the real parsed command set (!suggest free text; !vote 1..N with N = live option count)"
  artifacts:
    - path: "scripts/overlay-harness.ts"
      provides: "Dev-only visual harness serving the real overlay pages with injected 10-item state"
    - path: "src/state-machine/auto-cycle.ts"
      provides: "Pool-full early close of the suggestion phase via #onPhaseEnd"
      contains: "POOL_CHANGED"
    - path: "src/state-machine/round.ts"
      provides: "30s default duration + ROUND_MAX_OPTIONS draw cap"
      contains: "ROUND_MAX_OPTIONS"
  key_links:
    - from: "src/state-machine/auto-cycle.ts"
      to: "src/queue/pool.ts"
      via: "injected pool sliver + POOL_CHANGED subscription"
      pattern: "POOL_CHANGED"
    - from: "src/state-machine/round.ts"
      to: "startRound draw"
      via: "roundMaxOptions() replacing the hardcoded slice(0, 3)"
      pattern: "roundMaxOptions"
    - from: "src/overlay/public/overlay.js"
      to: "round.candidates.length"
      via: "voteHint() compressed form for >3 options"
      pattern: "voteHint"
---

<objective>
Overlay + round-flow polish: (1) verify/fix the phase-banner command instructions, (2) make 10 suggestions / 10 vote options render legibly, (3) default round duration to 30s, (4) early-close the suggestion phase the moment the pool hits 10 — through the exact timer-expiry code path.

Purpose: the live cadence should never idle on a full pool, and the broadcast surfaces must stay legible at the configured maxima.
Output: updated round/auto-cycle/parser logic with unit tests, compacted overlay/queue-page rendering for 10 items, a dev visual harness, and updated .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/state-machine/round.ts
@src/state-machine/auto-cycle.ts
@src/queue/pool.ts
@src/ingestion/command-parser.ts
@src/overlay/server.ts
@src/overlay/public/overlay.js
@src/overlay/public/overlay.css
@src/overlay/public/queue.js
@src/overlay/public/queue.css
@src/main.ts

HARD CONSTRAINTS (read-only, do not touch): src/compliance/**, src/state-machine/halt.ts, src/kill-switch/**, CLASSIFIER_SYSTEM_PROMPT. Every suggestion still routes through the gate — early close changes WHEN the vote starts, never what enters the pool.

`.env` is UNTRACKED — edit it locally for the new values but NEVER `git add` it. Commit `.env.example` instead. Use explicit file paths in every commit, never `git add -A`.

<interfaces>
Key contracts (extracted; no exploration needed):

From src/queue/pool.ts:
```typescript
export class CandidatePool extends EventEmitter {
  add(candidate, result): void;   // THROWS for non-approved (COMP-01) — pool is approved-only by construction
  list(): ApprovedCandidate[];    // insertion order
  remove(id: string): void;
}
// emits POOL_CHANGED (from shared/events.js) on add AND remove
```

From src/state-machine/auto-cycle.ts:
```typescript
// #onPhaseEnd() is the timer-expiry path: clears phaseEndsAtMs, re-checks FULL
// eligibility (enabled, no round loaded, mode IDLE|BUILD_IN_PROGRESS, no control
// window, no chaos, vote queue not full), then startRound("auto") with
// pool-too-small -> restart window. HALT_TRIGGERED -> #park() clears timer+phase.
// Early close MUST funnel through this method — that IS the "same code path" rule.
```

From src/state-machine/round.ts:
```typescript
export function roundDurationMs(): number; // env ROUND_DURATION_SECONDS, DEFAULT_ROUND_DURATION_SECONDS = 60 (-> 30)
// startRound(): const drawn = pooled.slice(0, 3);  <- the draw cap to widen
```

From src/ingestion/command-parser.ts:
```typescript
// /^!vote\s+([1-3])$/i  + zod .min(1).max(3)  <- widen to 1..10
// recordVote() already silently ignores option > round.options.length (D2-15)
```

From src/overlay/server.ts (harness composes THIS — no new server code):
```typescript
export function startOverlayServer(deps: OverlayServerDeps): Promise<OverlayServerHandle>;
// deps: machine {mode, on}, round {snapshot, on}, taskQueue {list}, pool? {list, on},
//       autoCycle? {snapshot, on}, build?, controlWindow?, builderFeed?, port
// Serves / (overlay), /queue, /builder, /api/state on 127.0.0.1 only.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 30s round default, 10-option draw cap, !vote 1-10 parsing, env docs</name>
  <files>src/state-machine/round.ts, src/state-machine/round.test.ts, src/ingestion/command-parser.ts, src/ingestion/command-parser.test.ts, .env.example</files>
  <behavior>
    - roundDurationMs() with ROUND_DURATION_SECONDS unset returns 30_000 (was 60_000); garbage/negative env still falls back to 30_000
    - New exported roundMaxOptions(): env ROUND_MAX_OPTIONS (envPositive idiom, integer-floored), default 10
    - startRound() with 12 pooled candidates draws 10 (insertion order); with 2 pooled draws 2 (D2-04 minimum unchanged)
    - parseCommand("!vote 10") -> {kind:"vote", option:10}; "!vote 4" works; "!vote 0", "!vote 11", "!vote 1.5" -> null
  </behavior>
  <action>
    In src/state-machine/round.ts: change DEFAULT_ROUND_DURATION_SECONDS 60 -> 30. Add `const DEFAULT_ROUND_MAX_OPTIONS = 10` and export `roundMaxOptions()` following the exact roundDurationMs() env-parse pattern (positive finite or default; apply Math.floor so "7.9" draws 7). Replace `pooled.slice(0, 3)` with `pooled.slice(0, roundMaxOptions())`. Update the startRound doc comment ("first min(3, pool)" -> the knob).

    In src/ingestion/command-parser.ts: widen the vote regex to `/^!vote\s+(\d{1,2})$/i` and the zod VoteCommand bound to .max(10). Keep the parser TOTAL (null on anything else). Comment why 10: matches DEFAULT_ROUND_MAX_OPTIONS; recordVote() remains the authoritative bound against the live option count (an out-of-range vote is silently ignored per D2-15), so a custom ROUND_MAX_OPTIONS below 10 stays safe.

    Update round.test.ts default-duration expectations (60_000 -> 30_000) and add draw-cap cases; add command-parser.test.ts cases per the behavior list.

    In .env.example: set ROUND_DURATION_SECONDS=30 and rewrite its stale comment (drop "set 20 for the 40/20 cadence" — the cadence is now 40s suggest / 30s vote by default); add a documented ROUND_MAX_OPTIONS=10 entry ("max candidates drawn into a vote round; ALSO the pool size that ends the suggestion phase early — see auto-cycle"). Locally edit .env to ROUND_DURATION_SECONDS=30 (DO NOT COMMIT .env).
  </action>
  <verify>
    <automated>npx vitest run src/state-machine/round.test.ts src/ingestion/command-parser.test.ts</automated>
  </verify>
  <done>Default round length is 30s, rounds draw up to 10 options, !vote accepts 1-10, .env.example documents both knobs; .env untracked and unstaged (`git status --porcelain` shows no .env)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Early-close the suggestion phase when the pool hits ROUND_MAX_OPTIONS</name>
  <files>src/state-machine/auto-cycle.ts, src/state-machine/auto-cycle.test.ts, src/main.ts</files>
  <behavior>
    - Pool reaches the cap mid-suggest-phase -> pending timer cancelled, startRound("auto") fires immediately via #onPhaseEnd (assert the fake startRound was called BEFORE the phase deadline; assert AUTO_CYCLE_CHANGED emitted)
    - Early close re-checks eligibility: pool hits cap but isVoteQueueFull() -> true at that moment -> NO startRound, scheduler parks (same as timer-expiry parking)
    - HALT parks first: HALT_TRIGGERED then POOL_CHANGED at cap -> nothing fires (no active phase)
    - Pool events below the cap -> timer path completely unchanged (startRound only at deadline)
    - A suggest phase that BEGINS with the pool already at cap closes immediately through the same path
    - No pool dep injected (existing tests) -> behavior identical to today
  </behavior>
  <action>
    In src/state-machine/auto-cycle.ts: add an OPTIONAL dep `pool?: { size(): number; on(event: string, handler: (...args: unknown[]) => void): void }` and `earlyCloseSize?: number` to AutoCycleDeps. Import POOL_CHANGED from shared/events.js. In the constructor, when pool is provided, subscribe to POOL_CHANGED; the handler calls a new private `#maybeEarlyClose()`: no-op unless a suggest phase is ACTIVE (`#phaseEndsAtMs !== null && #timer !== null`) AND `pool.size() >= earlyCloseSize`; otherwise `#clearTimer(); #onPhaseEnd();` — the EXACT method the timer expiry calls, so halt/mode/window/chaos/queue-full eligibility and the pool-too-small restart all apply unmodified (the "same code path" requirement — never call startRound directly). Also invoke #maybeEarlyClose() at the end of #beginSuggestPhase so a phase that opens onto an already-full pool closes immediately. Update the module doc comment (add the early-close rule to the safety-posture list). POOL_CHANGED also fires on remove — the size>=cap guard makes that harmless.

    In src/main.ts (~line 699, AutoCycleScheduler composition): wire `pool: { size: () => pool.list().length, on: (e, h) => pool.on(e, h) }` and `earlyCloseSize: roundMaxOptions()` (import from state-machine/round.js). One shared knob keeps "pool full" and "options drawn" the same number, so an early-closed round votes on the entire pool.

    Add the behavior tests to auto-cycle.test.ts using the existing fake-deps pattern (fake pool = plain EventEmitter-ish object with settable size). Do NOT touch halt.ts, the gate, or intake — early close only changes WHEN startRound fires.
  </action>
  <verify>
    <automated>npx vitest run src/state-machine/auto-cycle.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Pool reaching 10 mid-phase transitions straight to voting through #onPhaseEnd; halt/eligibility semantics proven unchanged by tests; suite green</done>
</task>

<task type="auto">
  <name>Task 3: 10-item legible rendering, banner copy check, dev visual harness</name>
  <files>src/overlay/public/overlay.js, src/overlay/public/overlay.css, src/overlay/public/queue.js, src/overlay/public/queue.css, scripts/overlay-harness.ts</files>
  <action>
    overlay.js — (a) voteHint(): keep the explicit "type !vote 1 / !vote 2 / !vote 3" form for counts <= 3; for larger counts emit the compressed fixed copy `type !vote 1–${count}` (the per-option list at 10 options overflows the 900px banner). (b) In renderVotePanel(), when the round has more than 5 candidates add a `vote-panel-compact` class to the panel (remove it otherwise). (c) Banner copy verification (part 1): confirm against command-parser.ts that the two banners cover the full parsed command set — `!suggest <text>` and `!vote <n>` are the ONLY commands; the SUGGESTIONS OPEN hint ("type !suggest — new idea or a tweak to what's on screen") and VOTE NOW hint are current; fix nothing else unless a mismatch is found (document the check in the summary). All strings remain fixed orchestrator-authored copy, textContent-only.

    overlay.css — add `.vote-panel-compact` overrides so 10 rows fit the lower-left slot at 1080p without colliding with the top banners: tighten .vote-rows gap (16->8px), candidate title 24->20px, badge 36->28px (font stays 20px), tally track 12->8px with 8->4px top margin. Respect the binding type contract: NOTHING below 20px, no new colors, no red. Keep the 440px width. Target: 10 compact rows + padding <= ~740px tall (bottom-anchored at the 48px safe margin, top edge stays below the ~160px top-banner band).

    queue.js / queue.css — show ALL 10: raise POOL_SHOW and QUEUE_SHOW to 10 (keep the "+N more" lines for anything beyond — POOL_MAX_SIZE is 50, so honesty still matters past 10). Compact the rows so 10 stacked pool entries + 10 queue rows fit one 1080-tall column: reduce .wc-row margin-top 12->6px, keep all font sizes >= 20px, tighten .wc-row-stacked line gap. Update the queue.css header comment: the page now targets a ~460x1080 full-height browser source (was 440x420) — state the new recommended OBS source dimensions in the comment.

    scripts/overlay-harness.ts (NEW, dev-only, never imported by src/) — compose the REAL startOverlayServer from src/overlay/server.js with fake sources on port 4999: mode source (settable), a round source whose snapshot is an open RoundSnapshot with 10 candidates (mixed short + full 80-char texts, varied vote counts incl. a unique leader, endsAtMs = now + 30s), a task-queue source with 10 long queued texts, a pool source with 10 items (long texts + 24-char usernames, some null username), autoCycle source for the suggest state. CLI switch `--mode=vote|suggest` (default vote): vote = open round + VOTING_ROUND mode (10-option vote panel + VOTE NOW banner); suggest = no round, suggestPhase ending in 40s + pool of 10 (SUGGESTIONS OPEN banner; /queue shows the full pool). All fake strings are FIXED harness-authored copy (no real chat data). Print the three URLs on boot. Run via `npx tsx scripts/overlay-harness.ts --mode=vote`. If a scripts/ dir convention or tsconfig include would flag it, keep it type-clean so `npx tsc --noEmit` stays green.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npx biome check src scripts</automated>
    <human-check>Run `npx tsx scripts/overlay-harness.ts --mode=vote`, open http://127.0.0.1:4999 in a browser (or an OBS browser source at 1920x1080): all 10 vote rows legible in the lower-left panel, no clipping/collision with the top banners, banner reads "VOTE NOW — type !vote 1–10". Then `--mode=suggest`: banner reads "SUGGESTIONS OPEN — type !suggest …". Open http://127.0.0.1:4999/queue (OBS source ~460x1080): all 10 pool suggestions with usernames and all 10 queued builds visible, nothing cut off.</human-check>
  </verify>
  <done>All 10 vote options and all 10 pool/queue items render legibly; voteHint compresses above 3 options; banner copy verified against the parser; harness boots and serves both modes; full suite + tsc + biome green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat -> parser | viewer-authored !vote/!suggest text (adversarial) |
| server -> OBS broadcast surface | chat-derived strings rendered on stream |
| pool -> round start | early-close trigger driven by pool size |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l2a-01 | Tampering | command-parser vote widening | mitigate | regex `\d{1,2}` + zod int 1..10; recordVote still bounds against live options.length (silent ignore, D2-15) |
| T-l2a-02 | Elevation | early-close path | mitigate | pool is approved-only by construction (CandidatePool.add throws, COMP-01); early close ONLY reroutes through #onPhaseEnd — full eligibility re-check incl. HALT parking; compliance/** and halt.ts untouched |
| T-l2a-03 | Info Disclosure | 10-item overlay rendering | mitigate | textContent-only el() helper unchanged; JS truncation caps kept (80/60/24); display-fields-only wire unchanged |
| T-l2a-04 | DoS | POOL_CHANGED early-close handler | mitigate | O(1) size check guarded by active-phase + cap; adds already rate-limited by classifier latency + intake cooldown |
| T-l2a-SC | Tampering | package installs | accept | no new dependencies installed by this plan (tsx/vitest/biome already present) |
</threat_model>

<verification>
- `npx vitest run` — full suite green (baseline 845 tests + new cases)
- `npx tsc --noEmit` and `npx biome check src scripts` clean
- `git status --porcelain` contains no `.env` entry at commit time
- Human visual check via the harness (Task 3 human-check) — record outcome in the SUMMARY
</verification>

<success_criteria>
- Part 1: banner copy verified against command-parser (the only commands are !suggest/!vote); voteHint stays correct up to 10 options
- Part 2: 10 vote options + 10 pool suggestions + 10 queue items render legibly (>=20px type, no clipped info) at 1920x1080; recommended /queue source dimensions documented
- Part 3: DEFAULT_ROUND_DURATION_SECONDS = 30 with tests; .env.example committed at 30; local .env updated but never committed
- Part 4: pool-at-10 early close unit-tested through #onPhaseEnd (immediate transition, eligibility re-check, halt parking, no-pool-dep back-compat)
</success_criteria>

<output>
Create `.planning/quick/260711-l2a-overlay-command-instructions-10-item-ren/260711-l2a-SUMMARY.md` when done.
</output>
