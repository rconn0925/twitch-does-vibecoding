---
phase: quick-260711-t8k-command-layer-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ingestion/command-parser.ts
  - src/ingestion/command-parser.test.ts
  - src/ingestion/twitch-chat.ts
  - src/ingestion/twitch-chat.test.ts
  - src/ingestion/narration.ts
  - src/ingestion/narration.test.ts
  - src/shared/types.ts
  - src/orchestrator/types.ts
  - src/orchestrator/workspace.ts
  - src/orchestrator/workspace.test.ts
  - src/orchestrator/gallery-publisher.ts
  - src/orchestrator/sandbox-process.ts
  - src/orchestrator/sandbox-process.test.ts
  - src/audit/schema.sql
  - src/audit/record.ts
  - src/audit/record.test.ts
  - src/main.ts
  - src/preview/dev-server-supervisor.ts
  - src/preview/dev-server-supervisor.test.ts
  - tests/e2e (swap/info/preview e2e specs)
  - docs/OPERATIONS.md
autonomous: true
requirements: [QUICK-T8K]
must_haves:
  truths:
    - "A viewer can type !swapbuild \"name\" (or unquoted) and, if it wins the mixed vote, the show SHIPS the current project (confirmed published|no-changes) and then activates the EXISTING target generation — its existing repo binding reused, no new repo ever created for a swap"
    - "A failed/unconfirmable ship NEVER activates the swap target — amber narration, audit row, head removed, next round opens (never a dead round)"
    - "An unresolvable swap name at drain time is a narrated amber failure via the failed-head idiom — no activation, no crash, next round opens; a swap naming the CURRENT project gets its own honest amber line (never a misleading 'couldn't find it')"
    - "Generation monotonicity survives backward swaps: after activating an OLDER generation, the next new-project allocates ABOVE the all-time high-water mark — a fresh generation number can NEVER collide with an existing app-N dir or an existing project_repos row (the new-repo rule holds post-swap)"
    - "!projects / !current / !repo / !help / !commands reply instantly from chat with NO gate call, NO vote, NO state change — one reply per command per global cooldown window (INFO_COMMAND_COOLDOWN_SECONDS, default 30)"
    - "!projects lists ONLY post-gate public data: sanitized project_repos.repo_name slugs + their github.com links — never raw chat/prompt text"
    - "The orchestrator owns the 5555 preview dev server: started at boot rooted at the ACTIVE app-N dir, killed+restarted (re-rooted) on EVERY generation change (console new-project, project-switch winner, swap activation), health-checked via the existing TCP probe; a start failure NEVER crashes the app (fail-open to standing-by, loud log)"
    - "Swap text is gate-screened through the ONE funnel before pooling (tier-1); info commands are read-only tier-2 with no funnel contact"
  artifacts:
    - path: "src/preview/dev-server-supervisor.ts"
      provides: "Orchestrator-owned preview dev-server lifecycle (reroot: stop → start → probe → retry-once → fail-open)"
    - path: "src/ingestion/command-parser.ts"
      provides: "swapbuild (quoted/unquoted) + projects/current/repo/help/commands parse arms"
    - path: "src/orchestrator/workspace.ts"
      provides: "top_generation high-water mark (newProject allocates above it); activateExisting(generation) — non-destructive pointer move with IN-METHOD validation (integer, >=1, <= top_generation, !== current), scaffolded=1, no deletion"
    - path: "src/audit/schema.sql"
      provides: "workspace_state.top_generation column (fresh DBs) — paired with the guarded runtime migration in workspace.ts for existing DBs"
    - path: "src/audit/record.ts"
      provides: "recordSwapOutcome (swap_activated from/to generation + unresolved/ship-failed/already-current failure statuses)"
    - path: "docs/OPERATIONS.md"
      provides: "§11 orchestrator-owned preview server; manual start retired; end-anchored pkill-by-port idiom documented"
  key_links:
    - from: "src/ingestion/twitch-chat.ts"
      to: "src/pipeline/submit.ts"
      via: "swap rides the SAME shared !suggest/!build/!revert path: intake.check → deps.submit (COMP-01 single funnel)"
      pattern: "kind === \"swapbuild\""
    - from: "src/main.ts drainVoteQueue"
      to: "runSwapWinner"
      via: "new case \"swap\" arm in the kind-router switch"
      pattern: "case \"swap\""
    - from: "runSwapWinner"
      to: "galleryPublisher.publishNow"
      via: "confirmed-push gate: activate ONLY on published|no-changes (LOCKED — same rule as shipThenRotate)"
      pattern: "published|no-changes"
    - from: "workspace.newProject"
      to: "workspace_state.top_generation"
      via: "monotonic allocation: top_generation = top_generation + 1, generation = top_generation — never generation + 1 from the (possibly swapped-back) pointer"
      pattern: "top_generation"
    - from: "src/main.ts"
      to: "src/preview/dev-server-supervisor.ts"
      via: "late-bound reRootPreview seam invoked at boot + all three generation-change sites"
      pattern: "reRootPreview"
    - from: "src/orchestrator/sandbox-process.ts"
      to: "wsl.exe"
      via: "EXISTING execFileFn seam only — startPreviewDevServer/stopPreviewDevServer are new methods on the same adapter, zero new process-spawn paths"
      pattern: "execFileFn\\(wslExePath"
---

<objective>
Command layer B: portfolio commands + preview ownership.

1. `!swapbuild <name>` — tier-1 VOTED command (new CandidateKind `"swap"`) that ships the current project (confirmed-push gated, LOCKED) then activates an EXISTING generation non-destructively, reusing its repo binding. Generation allocation moves to a monotonic high-water mark so a backward swap can never make a later new-project collide with an existing generation/repo (checker BLOCKER 1).
2. Instant tier-2 info commands (`!projects` `!current` `!repo` `!help`/`!commands`) — read-only, NO gate, NO vote, global per-command cooldown. `!projects` is a HARD DEP of `!swapbuild`; both ship here.
3. Orchestrator-owned preview dev server (fixes the real 260711 outage: the manually-started 5555 http.server died silently) — start at boot, kill+re-root on every generation change, TCP-probe health check, never crash the app.

Purpose: chat can navigate the portfolio of already-built apps, and the preview surface stops depending on a human remembering to start a server.
Output: parser/dispatch/narration/audit/router/workspace/supervisor changes + tests + OPERATIONS.md §11.

OUT OF SCOPE: command reference overlay panel (task C); any new CandidateKind beyond "swap".

EXECUTOR NOTES (checker-accepted 21-file span): the `<interfaces>` block below is AUTHORITATIVE — implement against it, do not re-derive contracts from exploration. Run each task's own verify gate before moving on; Task 3's full-suite regression is the LAST gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/ingestion/command-parser.ts
@src/ingestion/twitch-chat.ts
@src/ingestion/narration.ts
@src/shared/types.ts
@src/orchestrator/types.ts
@src/orchestrator/workspace.ts
@src/orchestrator/gallery-publisher.ts
@src/orchestrator/sandbox-process.ts
@src/preview/preview-manager.ts
@src/audit/schema.sql
@src/audit/record.ts
@src/main.ts
@docs/OPERATIONS.md
@.planning/phases/03-sandboxed-build-engine-live-show/SANDBOX-SETUP.md

<interfaces>
<!-- AUTHORITATIVE contracts — use directly, no codebase exploration needed. -->

From src/shared/types.ts:
- `type CandidateKind = "suggestion" | "project-switch" | "revert"` → gains `"swap"`.

From src/orchestrator/types.ts:
- `interface WorkspaceView { dir(); scaffolded(); markBuilt(); newProject(): number; generation(): number }` → gains `activateExisting(generation: number): void` (validating — see Task 2).
- `interface SandboxAdapter { spawn; terminate; ensureWorkspaceDir?; workspaceHasFiles?; workspaceHasCommittableFiles? }` — OPTIONAL-method precedent (existing test fakes need no change) → gains `startPreviewDevServer?(dir: string, port: number): Promise<void>` and `stopPreviewDevServer?(port: number): Promise<void>`.

From src/audit/schema.sql (workspace_state, line ~165):
- `workspace_state (id CHECK(id=1), generation, scaffolded, updated_at_ms)` — `CREATE TABLE IF NOT EXISTS`, so an existing streaming-PC DB will NOT pick up a new column from schema.sql alone; the runtime migration in workspace.ts (Task 2) covers existing DBs.

From src/orchestrator/gallery-publisher.ts:
- `GalleryPublisher.publishNow(input): Promise<PublishResult>` — NEVER rejects; `status: "published" | "no-changes" | "failed"`.
- `ProjectRepoStore { lookup(generation): string|null; record; knownNames(): Set<string> }` backed by `project_repos (generation INTEGER PK, repo_name TEXT, created_at_ms)`.
- `scaffoldRepo` (module-internal) runs ONLY when `store.lookup(generation) === null` — a target generation with a project_repos row routes to `continueRepo` automatically; a FRESH high-water generation has no row → new repo (the new-repo rule). Module-level const `DEFAULT_OWNER = "TwitchVibecodes"` (currently unexported).

From src/main.ts (quick-q5n kind router, ~line 1319–1514):
- `shipThenRotate(head)` — the LOCKED confirmed-push-before-rotation pattern to mirror: (i) `!workspace.scaffolded()` → nothing to ship; (ii) publishNow with server-composed title `app-${generation} final snapshot`; (iii) rotate ONLY on `published|no-changes`; (iv) failure → `taskQueue.remove(head.id)` + amber beat + audit + guarded `machine.transition("IDLE")`.
- `runRevertWinner(head)` — the remove-head-FIRST no-build arm pattern.
- `drainVoteQueue` switch on `head.kind`: `"project-switch"` / `"revert"` / default.
- Late-bound narrator idiom: `windowNarrator?.method()` (composed only when chat pipeline exists).
- Console gets `workspace` at line ~1203 (BEFORE the orchestrator block) — console new-project re-root therefore needs a late-bound seam, not a supervisor reference.

From src/ingestion/twitch-chat.ts:
- Dispatch: `chaosVote?: (chatterId) => void` optional-seam precedent (called before the shared path, no intake, returns).
- Shared path: `const text = ...; const kind: CandidateKind = ...` then `intake.check` → `deps.submit`.

From src/preview/preview-manager.ts:
- `PreviewManager.reachable(): Promise<boolean>` — the existing fail-closed TCP probe (127.0.0.1:port, 750ms). `resolvePreviewDevServerPort(process.env.PREVIEW_DEV_SERVER_PORT)` → default 5555.

From src/orchestrator/sandbox-process.ts:
- `execFileFn(wslExePath, ["-d", distroName, "-u", distroUser, "--", "sh", "-lc", script])` — the EXISTING in-distro exec idiom (`workspaceHasFiles` uses it). Distro/user from `sandboxConfigFromEnv` (defaults vibecoding-build / builder).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tier-2 instant info commands — !projects / !current / !repo / !help / !commands with global per-command cooldown</name>
  <files>src/ingestion/command-parser.ts, src/ingestion/command-parser.test.ts, src/ingestion/twitch-chat.ts, src/ingestion/twitch-chat.test.ts, src/ingestion/narration.ts, src/ingestion/narration.test.ts, src/orchestrator/gallery-publisher.ts, src/main.ts, tests/e2e (new info-commands e2e spec)</files>
  <behavior>
    - Parser: "!projects" / "!current" / "!repo" / "!help" / "!commands" (case-insensitive, strict no-arg per the RevertCommand idiom — trailing text → null) parse to a new discriminated result kind "info" with an `info` field of type InfoCommandKind = "projects" | "current" | "repo" | "help". "!commands" aliases to "help". All other strings keep parsing exactly as today (byte-identical existing cases).
    - Dispatch: an info command calls a new optional seam `deps.infoCommand?: (kind: InfoCommandKind) => void` and RETURNS — provably NO intake.check, NO submit, NO gate call, NO cooldown charged (assert the fake intake/submit were never invoked).
    - Cooldown: main.ts closure holds a per-command lastSent map; a second identical command inside INFO_COMMAND_COOLDOWN_SECONDS (envPositive, default 30) is a SILENT no-op (D2-15); a different command inside the window still replies (per-command windows are independent). Silent while machine.mode === "HALTED".
    - Replies: !projects lists repo_name slugs + https://github.com/{owner}/{name} links, most-recent-generation first, greedily packed to Twitch's 500-char cap with a "(+N more)" tail when truncated; empty table → fixed "no projects shipped yet" line. !current names the ACTIVE generation's repo (store lookup on workspace.generation()); null → fixed "current project hasn't shipped yet" line. !repo replies with just the current link (same null fallback). !help is a FIXED-COPY command list covering !suggest, !build, !swapbuild, !vote 1-5, !revert, !chaos, !projects, !current, !repo — server-composed, zero interpolation.
  </behavior>
  <action>
    1. command-parser.ts: add the five regex arms + InfoCommandKind, extend ParsedCommand with `{ kind: "info"; info: InfoCommandKind }`. Keep the never-throw total-parse contract.
    2. twitch-chat.ts: add `infoCommand?` to TwitchChatDeps; handle `command.kind === "info"` immediately after the vote branch (before !chaos, before the shared tier-1 path) — call seam, return. Document tier-2 contract in the module header: read-only, NO gate, NO state change, per D2-15 silence on suppression.
    3. narration.ts: add Narrator methods `infoProjects(entries: Array<{name: string; url: string}>)`, `infoCurrent(entry: {name: string; url: string} | null)`, `infoRepo(url: string | null)`, `infoHelp()`. All send via the existing `deps.sender.send` (the single rate-budgeted path — D2-08). The ONLY dynamic tokens are repo_name slugs and their derived github.com URLs (already-public post-gate data). Keep every new string clear of the copy-separation scan vocabularies (no chance/luck/odds/roll words, no money/tip/donation/points/pay words) — the literal command token "!chaos" in the help line is fine (the scans target the paid/chaos beat methods, but do not tempt fate with prose).
    4. gallery-publisher.ts: export the owner default as `DEFAULT_GALLERY_OWNER` (rename-in-place of the private DEFAULT_OWNER const, keep resolveGalleryConfig using it) so main.ts composes info links without needing a configured token.
    5. main.ts: inside the chat-pipeline block (where `narrator` is in scope), build the `infoCommand` closure: owner = trimmed GALLERY_GITHUB_OWNER env or DEFAULT_GALLERY_OWNER; prepared statement `SELECT generation, repo_name FROM project_repos ORDER BY generation DESC` (read-only, prepared once); workspace.generation() for current; per-command cooldown map + HALTED silence; route to the four narrator methods. Wire `infoCommand` into the startTwitchChat deps. NOTE: info links are composed from project_repos + a public owner string ONLY — never from GateResult, donor data, or raw candidate text.
    6. Tests: parser cases (all five commands, aliases, trailing-text → null, case-insensitivity); dispatch test proving zero intake/submit/gate calls on the info path; narration copy tests incl. 500-char packing + "(+N more)"; e2e: two !projects inside 30s → exactly ONE chat reply; !projects then !current inside the window → two replies (independent windows); !projects lists only repo_name slugs (feed a hostile raw prompt into the pool and assert it never appears in any info reply); HALTED → silent.
  </action>
  <verify>
    <automated>npx vitest run src/ingestion/command-parser.test.ts src/ingestion/twitch-chat.test.ts src/ingestion/narration.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>All five info commands reply instantly via the narrator/sender path with post-gate public data only; global per-command cooldown enforced (default 30s, INFO_COMMAND_COOLDOWN_SECONDS knob); zero funnel contact proven by test; existing parser/dispatch behavior byte-identical; copy-separation scans green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: !swapbuild — kind "swap" through the ONE funnel, ship-gated activate-existing swap arm, monotonic high-water generation allocation</name>
  <files>src/shared/types.ts, src/ingestion/command-parser.ts, src/ingestion/command-parser.test.ts, src/ingestion/twitch-chat.ts, src/ingestion/twitch-chat.test.ts, src/ingestion/narration.ts, src/ingestion/narration.test.ts, src/orchestrator/types.ts, src/orchestrator/workspace.ts, src/orchestrator/workspace.test.ts, src/audit/schema.sql, src/audit/record.ts, src/audit/record.test.ts, src/main.ts, tests/e2e (new swap e2e spec)</files>
  <behavior>
    - Parser: `!swapbuild "name"` AND `!swapbuild name with spaces` both parse to `{ kind: "swapbuild", text }` — strip ONE pair of surrounding double quotes if present, trim, min 1 / max 2000 (funnel-bound mirror). `!swapbuild` bare → null.
    - Gate-before-pool: a swap candidate reaches the pool ONLY through intake.check → submitCandidate → classify (the swap text is a project-name reference but is chat-derived and gate-screened like all tier-1 text). Approved chat swap gets a "pooled-swap" confirmation beat.
    - MONOTONIC ALLOCATION (checker BLOCKER 1): workspace_state gains a `top_generation` high-water mark. newProject() allocates `top_generation + 1` and moves BOTH columns; activateExisting moves ONLY the `generation` pointer. Pinned regression: activate 3→1, then newProject() → returns 4 (NEVER 2 — the pre-fix `generation + 1` bug would collide with app-2's dir, flip the new project into continue mode on the wrong codebase via the HI-01 probe, and publish it into app-2's OLD repo via store.lookup(2)).
    - Post-swap new-repo rule: after a backward swap, a project-switch winner's post-rotation publish targets the FRESH high-water generation (store.lookup(fresh) === null → scaffoldRepo → NEW repo) — test-pinned.
    - IN-METHOD VALIDATION (checker BLOCKER 2): activateExisting throws on non-integer, < 1, > top_generation, and === current generation. Caller-side project_repos resolution remains the PRIMARY resolver; the method guard is belt-and-braces because it is a public WorkspaceView method (an unknown target would park the pointer at a dir-less generation with scaffolded=1 — continue mode into nothing). Both rejection classes unit-tested.
    - Kind router swap arm ordering (LOCKED): resolvable target → SHIP current first; activation happens ONLY after publishNow resolves published|no-changes. Test BOTH orderings: (a) publish resolves failed → generation pointer UNCHANGED, no activation, amber beat, audit row, machine back to IDLE; (b) publish resolves published (and separately no-changes) → activateExisting runs, pointer moves to target, scaffolded=1.
    - Repo binding REUSED: after a swap, the next publishNow on the now-active target generation routes through continueRepo — assert ZERO `gh repo create` exec calls (the new-repo rule applies only to NEW projects).
    - Unresolvable name at drain time → failed-head idiom: head removed, amber narration, audit failure row, no activation, machine returns to IDLE, next round opens. A swap that matches ONLY the current project gets a DISTINCT honest amber line ("already on it"), never the misleading "couldn't find that project" (checker INFO b).
    - Chaos/paid invariants untouched: console chaos.pick still filters kind === "suggestion" (swap never reaches the raw build agent); chaosModePick's no-filter path routes swap through the kind router where it executes correctly; paid-window interceptor does not consume !swapbuild (falls through to the parser → pools for the next vote).
  </behavior>
  <action>
    1. types.ts: `CandidateKind` gains `"swap"` (doc comment: quick-t8k — chat-voted portfolio swap; text is a gate-screened project-name reference, resolution happens at drain time).
    2. command-parser.ts: SwapbuildCommand zod schema + regex arm (match `/^!swapbuild\s+(.+)$/i`, then strip surrounding quotes via a second regex, re-trim, reject empty). Extend ParsedCommand.
    3. twitch-chat.ts: extend the shared tier-1 path — `command.kind === "swapbuild"` maps to candidate kind `"swap"` with text = command.text (chat-derived → screened). Update the dispatch-contract header comment.
    4. HIGH-WATER MARK (BLOCKER 1 fix) — schema.sql + workspace.ts:
       - schema.sql: add `top_generation INTEGER NOT NULL DEFAULT 0` to the workspace_state CREATE TABLE (fresh DBs) with a doc comment: all-time allocation high-water mark; newProject allocates ABOVE it so a backward swap (activateExisting) can never make a later new-project collide with an existing generation dir or project_repos row.
       - workspace.ts createWorkspaceState: guarded additive migration for EXISTING DBs (the table is IF NOT EXISTS, so schema.sql alone can't add the column): check `PRAGMA table_info(workspace_state)` for top_generation; absent → `ALTER TABLE workspace_state ADD COLUMN top_generation INTEGER NOT NULL DEFAULT 0`. Then ALWAYS seed idempotently: `UPDATE workspace_state SET top_generation = generation WHERE top_generation < generation` (covers both migration and the fresh-insert path, where the seed row should end with top_generation = generation = 1).
       - newProject(): single UPDATE — `top_generation = top_generation + 1, generation = top_generation + 1, scaffolded = 0, updated_at_ms = @now` (compute once; the two columns land equal). Returns the new generation as before.
       - activateExisting(target: number): read the row; throw Error on !Number.isInteger(target), target < 1, target > top_generation, or target === current generation (BLOCKER 2 — descriptive messages, e.g. "activateExisting: target N exceeds top_generation M"); else UPDATE setting generation = @target, scaffolded = 1, updated_at_ms (top_generation UNTOUCHED). Non-destructive by construction: no filesystem touch, no deletion path.
       - orchestrator/types.ts: add `activateExisting(generation: number): void` to WorkspaceView with the validation contract in the doc comment.
    5. narration.ts: feedback kind `"pooled-swap"` (`PROJECT SWAP request is in — it competes in the next vote.` @-line); roundOpened listing arm for kind "swap": `[N] SWAP TO: <truncateTitle(text)>` (gate-approved text — same display rule as NEW/TWEAK); FOUR new beats: `swapActivated(name: string)` (SWAP winner beat — the resolved repo_name slug is the ONLY dynamic token; at most two sends: a shipping line + a landed line), `swapShipFailed()` (amber regroup: couldn't ship the current project, staying on it, no ERROR/red wording — D2-18), `swapUnresolved()` (amber: couldn't find that project by name, try !projects for the list), `swapAlreadyCurrent()` (amber, distinct honest line: that's the project already on screen — nothing to swap; checker INFO b). Keep all strings clear of copy-separation vocabularies.
    6. main.ts — classifyThenNotify: approved chat swap → `narrator.feedback("pooled-swap", viewer)`.
    7. audit/record.ts: `recordSwapOutcome(db, { taskId, fromGeneration, toGeneration, repoName, status: "activated" | "unresolved" | "already-current" | "ship-failed", detail, streamMode })` — status "activated" writes the swap_activated event (from/to generation in the payload); the three failure statuses write swap_failed with the status in the detail/decision field. Follow the recordRevertOutcome shape/doc conventions.
    8. main.ts — `runSwapWinner(head: QueuedTask)` next to runRevertWinner, and `case "swap": await runSwapWinner(head); break;` in drainVoteQueue's switch:
       a. `taskQueue.remove(head.id)` FIRST (no-build arm — the runRevertWinner idiom).
       b. Resolve: read `SELECT generation, repo_name FROM project_repos` (prepared statement); normalize head.text with the sanitizeRepoName transform minus the dated fallback (lowercase, non-[a-z0-9] runs → "-", trim hyphens); match precedence exact → prefix → substring (all case-insensitive against repo_name); multiple matches at the winning tier → pick the HIGHEST generation (deterministic, most recent wins). Branch on the result: winning match === current generation → narrator swapAlreadyCurrent + recordSwapOutcome already-current + guarded IDLE return (checker INFO b — resolution runs over ALL rows so "current" is detected honestly, then rejected); no match at all → narrator swapUnresolved + recordSwapOutcome unresolved + guarded IDLE return. NOTE: when the current generation wins a tier but an OLDER generation also matched that same tier, prefer the non-current match (swapping is the intent); only an exclusively-current match triggers swapAlreadyCurrent.
       c. Ship gate (mirror shipThenRotate ii–iv EXACTLY, LOCKED): if `workspace.scaffolded()` — require galleryPublisher AND publishNow({ generation: current, title: server-composed `app-${current} final snapshot`, taskId: head.id }) to resolve published|no-changes (recordGalleryPublish audit row as shipThenRotate does; no publisher configured = unconfirmable = ship failure with the audit row written here). Failure → swapShipFailed amber + recordSwapOutcome ship-failed + guarded IDLE; NO activation. If NOT scaffolded → nothing to ship (empty fresh generation is abandoned in place, archive-by-construction), proceed.
       d. Activate: `workspace.activateExisting(target.generation)` + recordSwapOutcome activated (from/to, repoName) + narrator swapActivated(repo_name). Call the late-bound `reRootPreview?.()` seam — DECLARE `let reRootPreview: (() => void) | null = null` early in createApp in THIS task (Task 3 assigns it; a null seam is a silent no-op, the windowNarrator idiom).
       e. Guarded return to IDLE (`if (machine.mode === "BUILD_IN_PROGRESS") machine.transition("IDLE")` in try/catch — halt mid-swap leaves HALTED). The existing drain continuation handles the next winner. State machine itself untouched.
    9. Tests: parser (quoted, unquoted multi-word, bare-null, quote-stripping, 2000 cap); gate-before-pool for swap (rejected swap never pools; intake precedes classify); workspace unit tests: MONOTONICITY PIN — seed generations 1..3 (top_generation 3), activateExisting(1), newProject() → 4 and top_generation → 4; migration test — open a db whose workspace_state lacks top_generation (create the legacy shape directly), createWorkspaceState → column added + seeded to current generation; activateExisting rejections — non-integer, 0, > top_generation, === current all throw and leave the row untouched; router e2e with fake publisher + fake adapter: ship-failure → no activation + pointer unchanged + amber + audit; published AND no-changes → activation + pointer moved + scaffolded=1; POST-SWAP NEW-REPO PIN — swap 3→1, then a project-switch winner: confirmed ship → newProject() yields 4 → its publish calls `gh repo create` (store.lookup(4) null → scaffoldRepo) and NEVER touches app-2/app-3's repos; post-swap publish on the swapped-to generation asserts NO `gh repo create` (repo REUSED — grep the fake GalleryExec call list); unresolvable → swapUnresolved amber; exclusively-current match → swapAlreadyCurrent amber (distinct copy asserted); resolution precedence table (exact vs prefix vs substring vs highest-generation tie-break vs prefer-non-current); chaos console pick still excludes swap kind; paid-window !swapbuild falls through to pooling. Run `npx vitest run tests/invariants` to prove single-funnel + paid-chaos-separation stay green.
  </action>
  <verify>
    <automated>npx vitest run src/ingestion src/orchestrator/workspace.test.ts src/audit/record.test.ts tests/invariants && npx tsc --noEmit</automated>
  </verify>
  <done>!swapbuild pools through the ONE gate funnel, wins the mixed vote, ships-then-activates with the LOCKED confirmed-push gate (both orderings test-pinned), reuses the existing repo binding (no-scaffoldRepo asserted); generation allocation is monotonic over top_generation (swap 3→1 then new-project → 4, post-swap new project gets a NEW repo — both pinned); activateExisting validates in-method (all rejection classes unit-tested); exclusively-current swaps get their own honest amber line; swap_activated audit rows written; invariant suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Orchestrator-owned preview dev server — boot start, re-root on every generation change, probe health check, OPERATIONS.md §11</name>
  <files>src/orchestrator/types.ts, src/orchestrator/sandbox-process.ts, src/orchestrator/sandbox-process.test.ts, src/preview/dev-server-supervisor.ts, src/preview/dev-server-supervisor.test.ts, src/main.ts, tests/e2e (re-root e2e assertions), docs/OPERATIONS.md</files>
  <behavior>
    - Boot: when the build engine is composed with an adapter that implements the new methods, the supervisor starts the in-distro http.server rooted at workspace.dir() (the ACTIVE app-N) without any manual step.
    - Re-root fires on EVERY generation change: console POST /api/workspace/new-project, project-switch winner rotation (shipThenRotate step iii), and swap activation (Task 2's reRootPreview seam) — assert one stop+start pair per event, rooted at the NEW dir.
    - Health check: after start, the existing preview-manager TCP probe is consulted; probe false → exactly ONE stop/start retry; still false → loud pino error and RESOLVE (fail-open — the preview page's standing-by state covers it). reroot() NEVER rejects and NEVER throws into any caller; a supervisor failure can never crash the app or touch the halt path.
    - Kill scoping: stop targets ONLY the http.server process for the configured port via an END-ANCHORED pkill -f pattern (`http\.server <port>$` — port 5555 can never match a hypothetical 55555; checker INFO a) — NEVER wsl --terminate (that is the halt path's tool; assert terminate() is never called by the supervisor).
    - Adapter absence (test fakes without the optional methods): supervisor logs once and no-ops — every existing e2e suite keeps passing unchanged.
  </behavior>
  <action>
    1. orchestrator/types.ts: add OPTIONAL SandboxAdapter methods `startPreviewDevServer?(dir: string, port: number): Promise<void>` and `stopPreviewDevServer?(port: number): Promise<void>` (the ensureWorkspaceDir? precedent — existing fakes untouched). Doc comments: quick-t8k preview ownership; port is an internally-derived integer (resolvePreviewDevServerPort), never chat text.
    2. sandbox-process.ts: implement both via the EXISTING execFileFn(wslExePath, ["-d", distroName, "-u", distroUser, "--", "sh", "-lc", script]) idiom — NO new spawn/exec paths (same constraint as q5n's revert).
       - stopPreviewDevServer: script `pkill -f 'http\.server <port>$' || true` — END-ANCHORED on the port token so ONLY the exact-port preview server matches (checker INFO a: unanchored `http\.server 5555` would also match `http.server 55555`; python's argv ends with the port, so `$` is correct). Documented robust idiom — pkill-by-anchored-port survives supervisor restarts and stale pidfiles, which a pidfile approach would not. Non-zero pkill exit (no match) is success by construction (`|| true`).
       - startPreviewDevServer: script `mkdir -p <dir> && cd <dir> && nohup python3 -m http.server <port> >/dev/null 2>&1 &` — the SANDBOX-SETUP.md-proven pattern, detached inside the distro (nohup + background; survives the wsl.exe invocation returning; the port is argv-final, matching the stop anchor). Rejects on wsl exec failure (the supervisor catches — the adapter stays honest like ensureWorkspaceDir).
       - `<dir>`/`<port>` interpolations come from workspace.dir() (internally-generated integer path) and the resolved port — assert in a test that the script strings contain no chat-derived bytes and that buildSandboxEnv/spawn() are untouched (secrets-isolation invariant must stay green).
    3. src/preview/dev-server-supervisor.ts (new): `createDevServerSupervisor({ adapter, port, workspaceDir: () => string, probeReachable: () => Promise<boolean>, logger, settleMs? })` returning `{ reroot(): Promise<void> }`. reroot serializes on an internal promise chain (the gallery-publisher chain idiom — overlapping reroots run strictly one-after-another and the chain never breaks because reroot never rejects): stop → start(workspaceDir()) → wait settleMs (default ~1500ms, injectable 0 in tests) → probe; false → one retry cycle; still false → logger.error loud + resolve. Adapter missing either method → single logger.warn (unsupported) + resolve. Every failure path resolves — fail-open to the standing-by page, NEVER crash the app.
    4. main.ts wiring (inside the `if (opts.agentRunner && opts.sandboxAdapter)` block, after previewManager is created): build the supervisor with adapter = opts.sandboxAdapter, port = the SAME resolvePreviewDevServerPort value previewManager uses, probeReachable = () => previewManager.reachable(); assign `reRootPreview = () => void supervisor.reroot()` (the Task 2 late-bound seam); fire `void supervisor.reroot()` once at composition (boot start). Add `void reRootPreview?.()` after `workspace.newProject()` in shipThenRotate (step iii). For the console: pass startConsoleServer a THIN wrapper of workspace whose newProject() calls the real workspace.newProject() then `reRootPreview?.()` before returning the generation (the build session keeps the UNWRAPPED instance; wrapper is spread + one override, no WorkspaceView change). Do NOT kill the dev server in close() — leaving it serving across host restarts is deliberate availability (today's outage was the server dying, not living too long).
    5. docs/OPERATIONS.md: new §11 "App-Preview Dev Server (Orchestrator-Owned)": manual `python3 -m http.server 5555` start is RETIRED; lifecycle (boot start at active app-N, auto re-root on new-project/project-switch/swap); the end-anchored pkill-by-port stop idiom and why wsl --terminate is never used; troubleshooting (probe failing → `wsl -d vibecoding-build -u builder -- sh -lc "pgrep -af http.server"`; blank preview = standing-by fail-open, check pino for the loud supervisor error). Cross-reference §10 stays intact.
    6. Tests: supervisor unit tests with a fake adapter (stop-then-start order; re-root uses the CURRENT workspaceDir() at call time; probe-fail → exactly one retry → loud resolve; never rejects; missing methods → warn no-op; serialized overlapping reroots); sandbox-process tests asserting exact wsl argv for both new methods (distro/user flags, sh -lc script shape, `|| true` on pkill, END-ANCHORED port token in the pkill pattern — assert the literal `$` anchor) and that terminate()/spawn()/buildSandboxEnv are untouched; e2e: boot with method-bearing fake adapter → one start rooted at app-<boot generation>; console new-project → stop+start at app-<g+1>; project-switch winner (confirmed publish) → re-root; swap activation (Task 2 flow) → re-root at the TARGET dir; failed swap ship → NO re-root; adapter start rejection → app boots fine, no throw. Full regression: `npx vitest run` (1035+ tests) + `npx biome check src tests` — this is the plan's LAST gate.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npx biome check src tests</automated>
  </verify>
  <done>The orchestrator starts, re-roots (all three generation-change sites + boot), health-checks, and fail-opens the 5555 preview dev server via the EXISTING sandbox adapter exec seam only; pkill end-anchored to the exact port; wsl --terminate never involved; OPERATIONS.md §11 retires the manual start; full suite + tsc + biome green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat → parser | !swapbuild text and info-command tokens are adversarial viewer input |
| host → WSL distro | preview dev-server start/stop scripts execute inside the sandbox distro |
| project_repos → broadcast chat | info replies expose repo names/links on stream |
| swap pointer → workspace/repo integrity | a backward pointer move must never corrupt future generation/repo allocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t8k-01 | Tampering | swap candidate text | mitigate | swap rides the ONE funnel (intake → classify → pool); resolution never interpolates chat text into exec/SQL — prepared statements + normalized in-memory matching only |
| T-t8k-02 | Elevation | runSwapWinner activation | mitigate | LOCKED confirmed-push gate: activateExisting is reachable ONLY after publishNow resolves published\|no-changes; both orderings test-pinned |
| T-t8k-03 | Information Disclosure | info command replies | mitigate | replies compose ONLY project_repos.repo_name slugs + owner-derived URLs (already-public post-gate data); hostile-prompt-never-appears test |
| T-t8k-04 | DoS | info command spam | mitigate | global per-command cooldown (default 30s) upstream of the rate-budgeted sender; suppressed repeats are silent |
| T-t8k-05 | Tampering | preview server start/stop scripts | mitigate | dir/port are internally-generated (integer generation path + validated port); scripts built in sandbox-process.ts via the existing execFileFn arg-array seam; secrets-isolation invariant re-run |
| T-t8k-06 | DoS | pkill scope | mitigate | pkill -f pattern END-ANCHORED on `http\.server <port>$` (5555 can never match 55555); wsl --terminate structurally absent from the supervisor path (asserted) |
| T-t8k-07 | Spoofing | swap name resolution ambiguity | accept | deterministic precedence (exact > prefix > substring, highest generation ties, prefer non-current) — a wrong-but-existing target is a visible, revertible on-show outcome, not a safety issue |
| T-t8k-08 | Tampering | generation allocation after backward swap | mitigate | monotonic top_generation high-water mark: newProject allocates above the all-time max, so a post-swap new project can never inherit an existing app-N dir (HI-01 continue-mode corruption) or an existing project_repos binding (old-repo publish corruption); pinned by the 3→1→newProject()→4 and post-swap-new-repo tests |
| T-t8k-09 | Elevation | activateExisting as a public WorkspaceView method | mitigate | in-method validation (integer, >=1, <= top_generation, !== current) throws before any write — an unknown target can never park the pointer at a dir-less generation with scaffolded=1 |
</threat_model>

<verification>
1. `npx vitest run` — full suite green (1035+ pre-existing + new parser/dispatch/narration/workspace/audit/supervisor/e2e tests), including the monotonicity pin (swap 3→1 → newProject → 4) and the post-swap new-repo pin.
2. `npx vitest run tests/invariants` — single-funnel, paid-chaos-separation, secrets-isolation, chat-sender, dom-safety all green.
3. `npx tsc --noEmit` and `npx biome check src tests` clean.
4. Grep gates: `grep -rn "case \"swap\"" src/main.ts` hits the kind router; `grep -rn -- "--terminate" src/preview/` → zero hits; `grep -c "scaffoldRepo" src/main.ts` → 0 (activation never scaffolds); `grep -n "top_generation" src/orchestrator/workspace.ts src/audit/schema.sql` hits both.
</verification>

<success_criteria>
- !swapbuild (quoted + unquoted) pools as kind "swap" through the ONE gate funnel and executes via the kind router: ship (confirmed published|no-changes, LOCKED) → activateExisting (validated pointer move, repo binding REUSED, no new repo) → preview re-root; every failure branch is amber-narrated, audited, head-removed, never a dead round; an exclusively-current target gets its own honest amber line.
- Generation allocation is monotonic: top_generation high-water mark migrated (schema.sql + guarded runtime ALTER for existing DBs), newProject allocates above it, activateExisting moves only the pointer and validates in-method — the backward-swap collision (continue-mode on the wrong codebase, publish into the old repo) is structurally impossible and test-pinned.
- !projects/!current/!repo/!help/!commands reply instantly (no gate, no vote, no state change) with post-gate repo_name slugs/links only, one reply per command per INFO_COMMAND_COOLDOWN_SECONDS window.
- The preview dev server is orchestrator-owned end to end: boot start, re-root on console new-project + project-switch + swap, probe-checked, fail-open — via the existing sandbox adapter exec seam, pkill end-anchored to the exact port, never wsl --terminate; OPERATIONS.md §11 retires the manual start.
- Paid↔chaos invariant, halt/state machine, and the single-funnel invariant untouched and test-green; copy-separation scans green; no new dependencies.
</success_criteria>

<output>
Create `.planning/quick/260711-t8k-command-layer-b/260711-t8k-SUMMARY.md` when done.
</output>
