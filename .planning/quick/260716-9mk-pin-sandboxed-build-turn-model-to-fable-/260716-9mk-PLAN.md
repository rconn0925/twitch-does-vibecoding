---
phase: quick-260716-9mk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/turn-options.ts
  - src/orchestrator/turn-options.test.ts
  - src/orchestrator/sdk-runner.ts
  - src/orchestrator/types.ts
  - src/orchestrator/build-session.ts
  - src/orchestrator/build-session.test.ts
  - .env.example
autonomous: true
requirements: [QUICK-260716-9mk]

must_haves:
  truths:
    - "Every sandboxed build turn's Options carries an explicit model — Twitch-chat builds provably run on Fable, never on the builder account's ambient default"
    - "BUILD_MODEL env var overrides the pinned model at assembly-call-time (GATE_MODEL idiom); a blank/whitespace value falls back to claude-fable-5"
    - "Chat-derived AgentRunSpec still structurally cannot request a model (no model field on the spec) — only the orchestrator-authored options assembly pins it"
    - "No comment in the orchestrator still claims the build 'inherits the Fable session default'"
  artifacts:
    - path: "src/orchestrator/turn-options.ts"
      provides: "model pin in assembleSandboxedBuildOptions (and assembleHostTurnOptions)"
      contains: "BUILD_MODEL"
    - path: "src/orchestrator/turn-options.test.ts"
      provides: "default-pin, override, and blank-fallback tests"
      contains: "claude-fable-5"
    - path: ".env.example"
      provides: "BUILD_MODEL knob documentation"
      contains: "BUILD_MODEL"
  key_links:
    - from: "src/orchestrator/turn-options.ts"
      to: "process.env.BUILD_MODEL"
      via: "call-time read inside assembleSandboxedBuildOptions"
      pattern: "process\\.env\\.BUILD_MODEL"
    - from: "src/orchestrator/turn-options.test.ts"
      to: "vi.stubEnv"
      via: "override test mirroring classifier-runner.test.ts GATE_MODEL tests"
      pattern: "stubEnv\\(\"BUILD_MODEL\""
---

<objective>
Pin the sandboxed build turn's model to Fable explicitly. Today `assembleSandboxedBuildOptions` sets NO `model` field, so the build inherits whatever the Claude Code engine inside the WSL2 distro defaults to (the builder account's ambient setting) — codebase comments claim a "Fable session default" but nothing enforces it. After this change, every chat-driven build turn provably runs on `claude-fable-5` (env-overridable via `BUILD_MODEL`), and all stale "inherits the session default" comments are corrected.

Purpose: Enforce the project's LOCKED model policy ("all agent work except research runs on Fable") structurally, not by accident of account configuration. CR-02/WR-01 doctrine: no boundary may depend on an SDK default — the model policy is such a boundary.
Output: model pin + tests in turn-options.{ts,test.ts}; corrected comments in sdk-runner.ts, types.ts, build-session.ts, build-session.test.ts; BUILD_MODEL documented in .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/orchestrator/turn-options.ts
@src/orchestrator/turn-options.test.ts
@src/orchestrator/classifier-runner.ts   # GATE_MODEL idiom, line ~167
@src/orchestrator/classifier-runner.test.ts  # GATE_MODEL test idiom, lines ~101-112
</context>

<locked_decisions>
- D-1 exception UNTOUCHED: the classifier gate stays on Sonnet via GATE_MODEL. Do not modify classifier-runner.ts or its tests.
- AgentRunSpec keeps NO model field. The structural invariant test in build-session.test.ts (`expect(spec && "model" in spec).toBe(false)`, line ~325) stays untouched and green — chat-derived specs still cannot request a model; only orchestrator-authored options assembly pins it.
- turn-options.ts stays SDK-free at runtime (only `import type`) so it remains vitest-loadable. A `model: string` field on the returned Options object requires no new imports.
- vitest + `tsc --noEmit` must stay green.
</locked_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin build-turn model in turn-options.ts with tests</name>
  <files>src/orchestrator/turn-options.ts, src/orchestrator/turn-options.test.ts</files>
  <behavior>
    - Test 1: `assembleSandboxedBuildOptions(makeSpec(), fakeSandboxSettings()).model` === "claude-fable-5" by default (no BUILD_MODEL set)
    - Test 2: with `vi.stubEnv("BUILD_MODEL", "claude-fable-5-custom")`, options.model === "claude-fable-5-custom" (mirrors classifier-runner.test.ts:107-112 GATE_MODEL test)
    - Test 3: with `vi.stubEnv("BUILD_MODEL", "   ")` (whitespace-only, the `.env`-file-with-blank-value case), options.model falls back to "claude-fable-5" — this is why the expression uses `?.trim() ||`, a deliberate hardening over GATE_MODEL's bare `??`
    - Test 4: `assembleHostTurnOptions(makeSpec()).model` === "claude-fable-5" by default (see pin-both decision below)
    - All existing tests (lockdown triple, acceptEdits, SAND-04 identity, no-mutation, host denylist) stay untouched and green
  </behavior>
  <action>
    In `assembleSandboxedBuildOptions` (src/orchestrator/turn-options.ts, ~line 65), add to the returned Options object:
    `model: process.env.BUILD_MODEL?.trim() || "claude-fable-5",`

    Read env at assembly-call-time (inline in the function body, NOT a module-top-level const) — this matches the GATE_MODEL idiom in classifier-runner.ts:167 where `process.env.GATE_MODEL` is read per consume() call, and it keeps `vi.stubEnv` tests deterministic without module-cache resets. The `?.trim() ||` form (vs GATE_MODEL's `??`) additionally treats a blank/whitespace `.env` entry as unset — pinning must not be defeated by `BUILD_MODEL=` left empty in a .env file.

    ALSO pin `assembleHostTurnOptions` (~line 100) with the same expression. Decision, per the planning scope's pick-one-and-say-why: PIN IT. Rationale to record in a comment there: the host branch is unreachable structural defense-in-depth, and CR-02/WR-01 doctrine says no boundary — including the Fable model policy — may depend on an SDK/account default, even on an unreachable branch (exactly the argument the function's existing lockdown-triple comment already makes for MCP config). Pinning is harmless, consistent, and keeps the two assembly functions covered by one doctrine.

    Add a short comment above the sandboxed-branch `model:` line: the explicit pin replaces the former reliance on the builder account's session default; the D-1 Sonnet classifier gate (GATE_MODEL) is a separate, untouched surface; chat-derived AgentRunSpec still carries no model field.

    Confirm no new runtime import is needed (model is a plain string on Options) — turn-options.ts must keep only `import type` from the SDK.

    In src/orchestrator/turn-options.test.ts: add `vi`, `afterEach` to the vitest import; add `afterEach(() => vi.unstubAllEnvs())` (top-level or inside the relevant describe) so stubbed BUILD_MODEL never leaks between tests; implement behaviors 1-4 above. Do not modify existing assertions.
  </action>
  <verify>
    <automated>npx vitest run src/orchestrator/turn-options.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>All new model-pin tests pass; every pre-existing turn-options test passes unchanged; tsc green; turn-options.ts still has zero runtime SDK imports (grep confirms only `import type` from @anthropic-ai/claude-agent-sdk).</done>
</task>

<task type="auto">
  <name>Task 2: Correct stale "session default" comments and document BUILD_MODEL</name>
  <files>src/orchestrator/sdk-runner.ts, src/orchestrator/types.ts, src/orchestrator/build-session.ts, src/orchestrator/build-session.test.ts, .env.example</files>
  <action>
    Update every comment claiming the build "inherits the Fable session default" to state the model is now explicitly pinned in assembleSandboxedBuildOptions (`BUILD_MODEL` env override, hard default `claude-fable-5`). Exact sites (verified by grep):

    1. src/orchestrator/sdk-runner.ts header, lines ~15-17: replace "The build inherits the Fable session default (AgentRunSpec carries no model field...)" with: the build model is explicitly pinned to Fable in turn-options.ts (BUILD_MODEL override, default claude-fable-5); AgentRunSpec still carries no model field so the pipeline structurally cannot request an override; the Sonnet classifier gate remains a separate surface.
    2. src/orchestrator/types.ts, AgentRunSpec doc comment lines ~43-48: same correction — keep the "No `model` field exists, so the pipeline structurally cannot request an override" sentence (that invariant is unchanged and load-bearing), change only the "inherits the Fable session default" claim to "the orchestrator pins the model explicitly in turn-options.ts".
    3. src/orchestrator/build-session.ts line ~816: rewrite the "The build inherits the Fable session default (...)" sentence the same way.
    4. src/orchestrator/build-session.ts line ~1011: change "(Fable session default, SANDBOXED, persistent workspace)" to "(Fable — explicitly pinned, SANDBOXED, persistent workspace)".
    5. src/orchestrator/build-session.test.ts line ~321: update the comment "(Fable session default)" to reflect the pin lives in options assembly; DO NOT touch the assertions themselves — `expect(spec && "model" in spec).toBe(false)` at ~line 325 stays byte-identical.

    Comment-only changes in these four source files — no behavior edits outside what Task 1 already made.

    .env.example: add a `BUILD_MODEL` entry. Place it in the build-turn section next to `BUILD_TURN_TIMEOUT_SECONDS` (~line 130), following the GATE_MODEL precedent at line 21. Suggested form — a 2-3 line comment noting: model for the sandboxed build turn, explicitly pinned so chat-driven builds provably run on Fable regardless of the builder account's ambient default; blank/unset → claude-fable-5; then `BUILD_MODEL=claude-fable-5`. docs/OPERATIONS.md has no central env-knob table (verified), so no change there.
  </action>
  <verify>
    <automated>cd /c/Users/ross/Projects/twitch-does-vibecoding && ! grep -rn "session default" src/orchestrator --include="*.ts" && grep -q "BUILD_MODEL" .env.example && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Zero remaining "session default" mentions in src/orchestrator; BUILD_MODEL documented in .env.example; full vitest suite green (including build-session.test.ts's untouched `"model" in spec === false` structural invariant); tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat → build turn | Chat-derived task text drives the sandboxed build; model selection must never be chat-influenceable |
| builder-account ambient config → build turn | The WSL2 Claude Code engine's default model comes from account state outside the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9mk-01 | Tampering | build-turn model selection | mitigate | Explicit `model` pin in orchestrator-authored options assembly; AgentRunSpec structurally carries no model field, so chat-derived specs cannot influence it (existing invariant test stays green) |
| T-9mk-02 | Elevation of privilege | ambient account default overriding project policy | mitigate | Hard default `claude-fable-5` with `?.trim() ||` so even a blank .env entry cannot silently un-pin |
| T-9mk-03 | Repudiation | "which model built this?" auditability | accept | BUILD_MODEL is a streamer-controlled env knob on the host machine, same trust level as GATE_MODEL; no further logging added in this quick task |
</threat_model>

<verification>
- `npx vitest run` — full suite green, including untouched lockdown-triple, SAND-04, and `"model" in spec === false` invariants
- `npx tsc --noEmit` — green
- `grep -rn "session default" src/orchestrator --include="*.ts"` — zero hits
- `grep -n "import .*@anthropic-ai/claude-agent-sdk" src/orchestrator/turn-options.ts` — only `import type` (SDK-free at runtime)
- classifier-runner.ts and classifier-runner.test.ts have zero diff (D-1 exception untouched)
</verification>

<success_criteria>
- assembleSandboxedBuildOptions returns `model: "claude-fable-5"` by default and honors BUILD_MODEL (trimmed, blank → default), read at call-time
- assembleHostTurnOptions carries the same pin, with a doctrine comment explaining why the unreachable branch is pinned too
- AgentRunSpec unchanged; build-session.test.ts structural invariant assertions byte-identical
- All four stale-comment sites corrected; BUILD_MODEL documented in .env.example
- vitest + tsc green
</success_criteria>

<output>
Create `.planning/quick/260716-9mk-pin-sandboxed-build-turn-model-to-fable-/260716-9mk-SUMMARY.md` when done
</output>
