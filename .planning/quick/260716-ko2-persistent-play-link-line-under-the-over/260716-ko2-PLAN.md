---
phase: quick-260716-ko2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/overlay/server.ts
  - src/overlay/server.test.ts
  - src/overlay/public/overlay.js
  - src/overlay/public/overlay.css
  - src/overlay/overlay-copy.test.ts
  - src/main.ts
  - tests/e2e/build-flow.e2e.test.ts
autonomous: true
requirements: [QUICK-260716-KO2]
tags: [overlay, play-link, phase-banner, wire-narrowing, github-pages]

must_haves:
  truths:
    - "The main overlay's top-center phase banner shows a persistent play-link sub-line directly under the timer/guidance in ALL live phases (suggestions open, vote now, building/vote-waiting, free-reign, standby) whenever the ACTIVE generation has a published repo"
    - "With generation 6 (VOIDFARER, repo a-space-simulation-where-we-fly-around-the-galaxy-exploring-beautiful-planets-st) active, the line reads `▶ PLAY IT: twitchvibecodes.github.io/a-space-simulation-where-we-fly-around-the-galaxy-exploring-beautiful-planets-st/` immediately after deploy — no build-done beat required"
    - "playUrl null (no repo row for the active generation) → NO line and NO empty shell; HALTED (ON HOLD pill) → no play line (never new UI in HALTED)"
    - "Project switch/swap/rotation flips the URL on the next state push with ZERO new event plumbing — playUrl is recomputed inside buildOverlayState on every push/connect"
    - "The URL is NEVER ellipsized/truncated on screen — the 80-char slug stays fully legible at 1080p via a smaller monospace sub-line"
  artifacts:
    - path: "src/overlay/server.ts"
      provides: "OverlayState.playUrl: string | null closed wire field + OverlayPlayUrlSource pull seam + NULL default"
      contains: "playUrl"
    - path: "src/main.ts"
      provides: "playUrl source wiring: active-generation project_repos lookup via galleryPlayUrl(galleryOwner, repo_name), fail-closed"
      contains: "playUrl"
    - path: "src/overlay/public/overlay.js"
      provides: "phase-play sub-line rendered in renderPhaseBanner via el()/textContent only"
      contains: "phase-play"
    - path: "src/overlay/public/overlay.css"
      provides: ".phase-play small monospace single-line style, no ellipsis"
      contains: "phase-play"
  key_links:
    - from: "src/main.ts"
      to: "project_repos"
      via: "prepared SELECT repo_name WHERE generation = workspace.generation(), composed through the EXISTING galleryPlayUrl helper"
      pattern: "galleryPlayUrl\\(galleryOwner"
    - from: "src/overlay/server.ts buildOverlayState"
      to: "wire"
      via: "playUrl narrowed to string|null (fail-closed on non-string)"
      pattern: "playUrl"
    - from: "src/overlay/public/overlay.js renderPhaseBanner"
      to: ".phase-play"
      via: "el() textContent-only append under the phase content"
      pattern: "phase-play"
---

<objective>
Add a PERSISTENT play-link line to the main overlay's top-center phase banner, rendered directly under the phase timer: always show the CURRENT project's public play URL (`https://twitchvibecodes.github.io/<repo>/`) so viewers can play the on-screen app at any time — not just during the transient 8s build-done beat (quick-g8p's `playable` field, which stays untouched).

User's exact ask (Ross, 2026-07-16): "right under the timer, there should be a link. set it now for this voidfarer project." Active generation is 6, repo `a-space-simulation-where-we-fly-around-the-galaxy-exploring-beautiful-planets-st` — the durable `project_repos` row already exists, so the dynamic lookup satisfies "set it now" with zero hardcoding: after deploy the line shows the VOIDFARER URL on the first state push.

Purpose: the play link currently appears only in chat and in the 8s BUILT IT beat — anyone tuning in mid-stream has no visible way to play the app on screen.
Output: new closed wire field `playUrl: string | null`, pull-based server composition (fresh at every push), client sub-line + CSS, narrowing/raw-bytes/copy-pin tests. TDD: failing-test commit → green commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260716-g8p-now-building-attribution-suggester-usern/260716-g8p-SUMMARY.md

Source files (read once, extract everything):
@src/overlay/server.ts        — OverlayState, buildOverlayState, existing `playable` seam (lines ~280-310, ~447-520, ~658-663)
@src/overlay/public/overlay.js — renderPhaseBanner (lines ~327-381), el() helper, PLAY IT done-beat precedent (~527-537)
@src/overlay/public/overlay.css — .phase-banner block (~226-265), .build-play precedent (~615-625)
@src/main.ts                  — galleryOwner (~468), playableRepoStmt + announcePlayable (~1544-1577), overlay deps wiring (~2224-2240), workspace.generation()
@src/overlay/server.test.ts   — playable narrowing trio (~1147-1194) + raw-bytes forbidden-key idiom (the template for the new tests)

<interfaces>
Existing contracts the executor uses directly — no exploration needed:

From src/orchestrator/gallery-publisher.ts (quick-g8p consolidated helpers — the SINGLE URL-construction point, DO NOT duplicate):
```typescript
export function galleryPlayUrl(owner: string, repoName: string): string; // https://<owner-lowercased>.github.io/<repo>/
export const DEFAULT_GALLERY_OWNER: string; // "TwitchVibecodes"
```

From src/main.ts (all already in createApp scope where the overlay deps object is built):
```typescript
const galleryOwner: string;                       // resolved once at createApp scope (~line 468)
const playableRepoStmt = db.prepare("SELECT repo_name FROM project_repos WHERE generation = @generation"); // ~line 1544
workspace.generation(): number;                   // the ACTIVE generation
```

From src/overlay/server.ts (the seam pattern to mirror):
```typescript
// Existing push-based seam (g8p `playable`) — the NEW seam is PULL-based instead:
export interface OverlayPlayableSource { snapshot(): { url: string } | null; on(...): void; }
// OverlayServerDeps optional-dep + NULL_*_SOURCE default idiom (playable/chaosMode/pool all follow it)
```

From src/overlay/public/overlay.js:
```javascript
function el(tag, className, text)   // textContent-only DOM construction — the ONLY way to render
// renderPhaseBanner(): three early-return branches (liveRound / voteWaiting / suggestPhase) then hidden=true
// latest.pill ∈ { "STANDBY", "VOTING OPEN", "BUILDING", "ON HOLD", "FREE REIGN", "CHAOS" }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for the playUrl wire field, freshness, fail-closed narrowing, and client copy pins</name>
  <files>src/overlay/server.test.ts, src/overlay/overlay-copy.test.ts, tests/e2e/build-flow.e2e.test.ts</files>
  <behavior>
    server.test.ts (mirror the `playable` trio at ~1147-1194 and the raw-bytes forbidden-key idiom):
    - Test 1: `playUrl` defaults to null with no source injected — both `/api/state` and the ws connect frame carry `playUrl: null` (closed field always present, never undefined-dropped from JSON... assert `"playUrl" in state` is true and value null).
    - Test 2: a fake pull source `{ current: () => "https://twitchvibecodes.github.io/a-space-simulation-where-we-fly-around-the-galaxy-exploring-beautiful-planets-st/" }` → state.playUrl is exactly that string AND matches `/^https:\/\/twitchvibecodes\.github\.io\/[a-z0-9-]+\/$/` (decision-1 shape assertion, exercised with the real 80-char VOIDFARER slug).
    - Test 3 (fail-closed narrowing, defence-in-depth): a MISBEHAVING source returning a rich object (e.g. `{ url: "...", repoRowSecret: "leak-me", donorAmount: 500 }` — i.e. `current()` returns a non-string) → state.playUrl is null; raw serialized wire bytes must not contain "repoRowSecret"/"donorAmount"/"leak-me" (the g8p raw-bytes pattern).
    - Test 4 (freshness, decision 4): the fake source's returned value CHANGES between two pushes (repo A → repo B, flip a closure variable, then trigger any existing push path e.g. ROUND_OPENED or the pool-changed event already used in this file) → the second ws frame carries repo B's URL with NO dedicated playUrl event emitted. Proves per-push recomputation, zero new event plumbing.

    overlay-copy.test.ts (existing read-overlay.js-as-text pin pattern):
    - Test 5: overlay.js contains the exact copy `▶ PLAY IT: ` exactly ONCE in renderPhaseBanner territory (index-ordered AFTER the `"type a command to jump in"` suggest-hint pin — the phase-banner region), distinct from the done-beat `PLAY IT → ` pin which must ALSO still exist (both pins asserted).
    - Test 6: overlay.js contains the `phase-play` class token and a `https://` strip for display (pin the replace pattern, e.g. `replace("https://", "")` or equivalent substring — display drops the scheme, trailing path kept intact).

    tests/e2e/build-flow.e2e.test.ts (extend the existing g8p announce matrix minimally):
    - Test 7: in the existing built/published matrix case, the overlay state ALSO carries `playUrl` equal to the active generation's `galleryPlayUrl` (persistent field, present even OUTSIDE the done beat); in the no-project_repos-row case, `playUrl` is null.
  </behavior>
  <action>
    Write the failing tests above. Follow the file's existing fake/idiom conventions exactly: server.test.ts already has `start({ ... })` optional-dep injection and raw-bytes helpers — add an optional `playUrl?: { current: () => unknown }` injection to the local start() harness. Do NOT touch production code in this task. Run the three test files and confirm the new tests FAIL for the right reason (playUrl undefined / copy pin absent), while all pre-existing tests still pass. Commit: `test(quick-260716-ko2): failing tests for the persistent phase-banner play link`.
  </action>
  <verify>
    <automated>npx vitest run src/overlay/server.test.ts src/overlay/overlay-copy.test.ts tests/e2e/build-flow.e2e.test.ts 2>&1 | tail -20 — new tests fail, zero pre-existing failures</automated>
  </verify>
  <done>All 7 new behaviors captured as failing tests; baseline tests untouched and green; RED commit exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — playUrl wire field + main.ts active-generation source + phase-banner sub-line + CSS</name>
  <files>src/overlay/server.ts, src/main.ts, src/overlay/public/overlay.js, src/overlay/public/overlay.css</files>
  <action>
    **src/overlay/server.ts** — add the closed field + pull seam (mirror the playable/chaosMode idiom):
    - `OverlayState.playUrl: string | null` with a doc comment distinguishing it from `playable` (playable = transient just-published done-beat link, g8p; playUrl = PERSISTENT active-generation link recomputed at every push from the durable project_repos routing — server-composed config owner + post-gate sanitizeRepoName slug, never chat text).
    - New `export interface OverlayPlayUrlSource { current(): string | null; }` — PULL-based, NO subscription: freshness comes from buildOverlayState being called on every push/connect (decision 4, zero new events). `NULL_PLAY_URL_SOURCE = { current: () => null }` default; optional `playUrl?: OverlayPlayUrlSource` on OverlayServerDeps.
    - In buildOverlayState: `const pu = playUrlSource.current();` then `playUrl: typeof pu === "string" ? pu : null` — fail-closed narrowing so a misbehaving/rich source can never put a non-string on the broadcast wire (Task 1 Test 3).

    **src/main.ts** — wire the source in the startOverlayServer deps object (~line 2224 block, next to `playable`):
    - `playUrl: { current: () => { try { if (!db.open) return null; const row = playableRepoStmt.get({ generation: workspace.generation() }) as { repo_name: string } | undefined; return row ? galleryPlayUrl(galleryOwner, row.repo_name) : null; } catch (err) { logger.error({ err }, "playUrl lookup failed — line stays absent"); return null; } } }`.
    - REUSE `playableRepoStmt` if it is in scope at the deps object (both live in createApp — verify); if not, prepare an identical statement adjacent to the wiring. REUSE `galleryPlayUrl` + `galleryOwner` — never duplicate URL construction (quick-1ki/g8p doctrine). No new events, no new holders, no changes to the existing `playable` announce path.

    **src/overlay/public/overlay.js** — restructure renderPhaseBanner from early-returns to fall-through so every branch shares one play-line tail:
    - Convert the three content branches (liveRound / voteWaiting / suggestPhase) into an if/else-if chain that appends content and sets `hasPhaseContent = true` instead of returning. Rendering inside each branch stays byte-equivalent (the exact-string copy pins on "VOTE NOW", "BUILDING — vote opens when it's done", "Suggestions open:", "type a command to jump in" etc. must keep passing).
    - After the chain: `const playUrl = typeof latest?.playUrl === "string" && latest.playUrl.length > 0 && latest?.pill !== "ON HOLD" ? latest.playUrl : null;` — the ON HOLD gate is the never-new-UI-in-HALTED rule (decision 3). If non-null, append `el("div", "phase-play", `▶ PLAY IT: ${playUrl.replace("https://", "")}`)` — textContent-only via el() (dom-safety invariant), display drops the scheme for compactness, trailing path kept whole.
    - `phaseBanner.hidden = !hasPhaseContent && playUrl === null;` — the line is PERSISTENT: with no phase content but a live playUrl the banner shows the play line alone (covers FREE REIGN/CHAOS/standby/winner-beat gaps); with neither, silent absence as today. NEVER a placeholder when playUrl is null.
    - No changes to the 1s tick interval (the line is static between pushes) and none to renderBuildPanel's done-beat PLAY IT (it stays).

    **src/overlay/public/overlay.css** — new `.phase-play` rule after `.phase-hint`:
    - Smaller monospace single line: `font-family: ui-monospace, monospace; font-size: 14px; font-weight: 600; margin-top: 6px; white-space: nowrap; color: var(--accent);` (accent like .build-play — never red). NO `overflow: hidden`, NO `text-overflow: ellipsis` — a truncated URL is useless; the 80-char slug at ~14px monospace (~700px + label) must fit fully. If the banner has a max-width that would clip it, widen/remove it for this element (banner box may grow, decision 2); shrink font (down to ~12px) rather than ever ellipsizing. overlay.css has pre-existing deferred biome failures — do not fix, do not worsen.

    Run the Task 1 test files → all green. Then full gates: `npx vitest run` (baseline 1289 + new), `npx tsc --noEmit`, `npx biome check src`. Invariant suites (dom-safety, single-funnel) must stay green — the new client line is el()-only. If any out-of-plan test fakes break on the widened OverlayServerDeps (they should not — the dep is optional), apply the mechanical Rule-3 fix and note it in the summary. Commit: `feat(quick-260716-ko2): persistent play-link line under the phase-banner timer`.

    Summary MUST carry the LIVE-DEPLOY FLAG (g8p precedent): overlay.js/overlay.css are OBS CEF-cached client files — deploy requires app restart AND `npm run obs -- refresh`; after that the VOIDFARER URL (generation 6 row already in project_repos) appears on the first push with no further action.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5 && npx tsc --noEmit && npx biome check src</automated>
  </verify>
  <done>All Task 1 tests green; full suite ≥ 1289 + new tests passing; tsc + biome clean (overlay.css pre-existing warnings unchanged); playUrl flows project_repos → galleryPlayUrl → wire → phase-banner sub-line; GREEN commit exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → broadcast wire | OverlayState JSON reaches the OBS browser source (public screen) |
| DB (post-gate slugs) → URL composition | repo_name rows feed a rendered on-screen string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ko2-01 | Information Disclosure | buildOverlayState playUrl | mitigate | Server-composed ONLY from config owner + post-gate sanitizeRepoName slug (already public on gallery/!current) via the existing galleryPlayUrl helper; `typeof === "string"` fail-closed narrowing + raw-bytes forbidden-key test — a rich source can never leak routing/donor/gate detail |
| T-ko2-02 | Tampering (stored XSS on broadcast) | overlay.js phase-play line | mitigate | el()/textContent-only rendering (dom-safety invariant suite); the string is never chat text — fixed label + server-composed URL |
| T-ko2-03 | DoS (per-push DB read) | main.ts playUrl.current() | accept | One prepared indexed SELECT per push on better-sqlite3 (synchronous, µs-scale); pushes are low-frequency show beats; try/catch fail-closed keeps a DB error off the broadcast path |
| T-ko2-SC | Tampering | package installs | accept | Zero new dependencies in this plan |
</threat_model>

<verification>
- `npx vitest run` — full suite green (baseline 1289 + ~7 new)
- `npx tsc --noEmit` clean; `npx biome check src` no new errors
- Narrowing proof: playUrl ∈ {null, string matching `^https://twitchvibecodes\.github\.io/[a-z0-9-]+/$`} on the wire; raw-bytes forbidden keys absent
- Copy pins: `▶ PLAY IT: ` (phase banner, once) AND `PLAY IT → ` (done beat, unchanged) both present
- Behavior matrix: null → no line/no shell; ON HOLD → no line; content-less phases → banner shows the line alone; generation flip → next push flips the URL with no new events
</verification>

<success_criteria>
- Persistent `▶ PLAY IT: twitchvibecodes.github.io/<repo>/` sub-line under the phase-banner timer in all live phases, never ellipsized, textContent-only
- playUrl recomputed inside buildOverlayState per push from the active generation's project_repos row via the existing galleryPlayUrl helper — zero new events, zero URL-construction duplication
- After deploy (restart + OBS refresh) the VOIDFARER (generation 6) URL shows immediately from the existing DB row — no hardcoding
- TDD: RED commit then GREEN commit, both scoped quick-260716-ko2; suite/tsc/biome gates green
</success_criteria>

<output>
Create `.planning/quick/260716-ko2-persistent-play-link-line-under-the-over/260716-ko2-SUMMARY.md` when done (include the LIVE-DEPLOY FLAG: app restart + `npm run obs -- refresh` required for the client files).
</output>
