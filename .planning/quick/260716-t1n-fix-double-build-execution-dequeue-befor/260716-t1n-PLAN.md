---
phase: quick-260716-t1n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/build-session.ts
  - src/orchestrator/build-session.test.ts
  - src/main.ts
  - tests/e2e/double-build-regression.e2e.test.ts
autonomous: true
requirements: [QUICK-260716-t1n]
must_haves:
  truths:
    - "A task that reaches a terminal stage (done/aborted/skipped) is dequeued exactly once and NEVER re-executed"
    - "The live fingerprint (parked fdl vote + pool-of-1 + solo pick landing synchronously on the BUILD->IDLE emit) does NOT rebuild the finished task; the solo winner builds next"
    - "No task_id ever gets a second build_history row with result='built'"
    - "The deferred drain (main.ts STATE_CHANGED setImmediate) still starts the NEXT queued task after a build finishes"
    - "HALTED stays frozen through finalizeAborted; no false BUILT-IT beat on aborted builds"
  artifacts:
    - path: "src/orchestrator/build-session.ts"
      provides: "dequeue+unregister BEFORE the IDLE transition in finalize, finalizeAborted, and skipTask"
    - path: "src/main.ts"
      provides: "drainVoteQueue head-selection guard skipping already-started head ids (belt layer)"
    - path: "tests/e2e/double-build-regression.e2e.test.ts"
      provides: "live-fingerprint double-run regression + kind matrix + history-uniqueness sweep"
    - path: "src/orchestrator/build-session.test.ts"
      provides: "ordering-invariant unit tests: queue no longer contains the task at the instant of the IDLE transition, for all three terminal paths"
  key_links:
    - from: "src/orchestrator/build-session.ts finalize()"
      to: "deps.taskQueue.remove / deps.registry.unregister"
      via: "reordered to execute BEFORE deps.machine.transition('IDLE')"
      pattern: "taskQueue\\.remove[\\s\\S]{0,400}transition\\(\"IDLE\"\\)"
    - from: "src/main.ts drainVoteQueue"
      to: "startedVoteHeads bounded Set"
      via: "head selection skips ids already handed to a build"
      pattern: "startedVoteHeads"
---

<objective>
Fix the double-build-execution bug confirmed live twice on stream 2026-07-16 (tasks 6bb0528e and 12dff1cb each re-executed after `done`). Root cause is fully diagnosed in `.planning/debug/260716-double-build-execution.md`: `finalize()` transitions BUILD_IN_PROGRESS->IDLE BEFORE dequeuing; the fdl AutoCycleScheduler's undeferred STATE_CHANGED handler resumes a parked vote synchronously inside that emit; with a 1-candidate pool the chain `#resumeFromWait -> soloPick -> enqueueWinner -> onWinnerQueued -> drainVoteQueue` runs inside `transition("IDLE")` while the finished task is still queue head — the drain captures and re-executes it.

Purpose: a finished build must be dequeued exactly once and never re-run — this double-ran builds, double-published gallery apps, burned workspace generations, and polluted build_history live on stream.
Output: reordered terminal paths (dequeue before transition) in all three exits, a positive re-run guard in drainVoteQueue, and four regression tests pinning the fix — including the exact live fingerprint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/260716-double-build-execution.md
@./CLAUDE.md
@src/orchestrator/build-session.ts
@src/state-machine/auto-cycle.ts
@src/main.ts

<interfaces>
<!-- Key facts the executor needs — extracted from current code; no exploration needed. -->

**build-session.ts — the three terminal paths (all currently transition BEFORE dequeue):**
- `finalize(task, stage, summary?)` (~:504-558): emitStage -> onBuildDone hook (done only, fire-and-forget) -> recordBuildHistory -> `current = null; active = null;` -> try { if mode===BUILD_IN_PROGRESS transition("IDLE"); setActiveTask(null,null) } -> `registry.unregister(task.id)` -> `taskQueue.remove(task.id)`.
- `finalizeAborted(task)` (~:575-604): recordSandboxTeardown -> `current=null; active=null;` -> `emitter.emit(BUILD_STAGE_CHANGED)` -> try { if mode===BUILD_IN_PROGRESS transition("IDLE") — HALTED deliberately left alone; setActiveTask(null,null) } -> unregister -> remove. Writes NO build_history row (CR-01) and never emits a `done` stage (no false BUILT-IT).
- `skipTask(taskId, reasonTag?)` (~:1098-1125): recordBuildSkip -> narrator.buildSkipped -> `current=null; active=null;` -> try { transition("IDLE") if BUILD_IN_PROGRESS; setActiveTask(null,null) } -> unregister -> remove -> emit BUILD_STAGE_CHANGED.

**stream-mode.ts `transition()`:** sets `#mode` then emits STATE_CHANGED **synchronously** (plain EventEmitter) — every subscriber runs inline inside the caller.

**auto-cycle.ts:** STATE_CHANGED handler (:200-204) calls `#maybeBegin("fresh")` with NO deferral; with `#waitingForBuild` set it routes to `#resumeFromWait` -> `#attemptRoundStart` -> pool-of-1 -> `soloPick()` -> `enqueueWinner` -> `onWinnerQueued` -> `drainVoteQueue` — all synchronous. DO NOT change this file: the fdl deadlock-matrix tests must stay unmodified and green (LOCKED rail).

**main.ts:**
- `drainVoteQueue` (~:1978-2047): guards are `mode !== BUILD_IN_PROGRESS` refusal, live-window refusal, chaos refusal, then `const head = taskQueue.list().find(isVoteOrigin)`. NO per-task started/finished mark exists. On commit it transitions VOTING_ROUND|IDLE -> BUILD_IN_PROGRESS then routes by `head.kind` (project-switch -> shipThenRotate, revert, swap, default -> `buildSession.startBuild(head, "vote")`) with a completion continuation `if (machine.mode === "IDLE") drainVoteQueue()`.
- `onWinnerQueued` (~:2056-2058): calls `drainVoteQueue()` synchronously — this is the undeferred route that fired live.
- Call site (b) (~:2068-2076): STATE_CHANGED -> `setImmediate` deferred drain, skipping prev===HALTED. Its comment documents the exact hazard. After the reorder this deferral remains correct and MUST still start the next queued task.

**Concurrent-work constraint (quick-260716-rll, merges BEFORE this executes):** rll adds a `dispatchBuild` interceptor wrapping the 5 `buildSession.startBuild` call sites in main.ts plus a grep-gate pinning `buildSession.startBuild(` to exactly 1 occurrence. Your guard MUST NOT add another raw `startBuild` call — the head-selection guard below adds none, and any startBuild-adjacent edits must go through whatever `dispatchBuild` shape exists in merged main.ts at execution time.

**p-queue note (why the in-window drain is safe after the reorder):** `startBuild` enqueues on a concurrency-1 PQueue whose current slot is still occupied by run 1's `runPipeline` (finalize runs inside it), so a drain firing inside the IDLE emit only transitions the mode + queues the pipeline — the new build's turn starts after finalize returns. Equivalent to closeRound's deliberate synchronous start.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing regression tests for the four shapes from the debug report</name>
  <files>tests/e2e/double-build-regression.e2e.test.ts, src/orchestrator/build-session.test.ts</files>
  <behavior>
    All four test shapes from the report's "Regression tests to pin it" section. Written FIRST; the double-run and ordering tests MUST fail against current code (the kind-matrix/history sweeps may partially fail — that is acceptable at RED as long as the core double-run assertions fail for the diagnosed reason, not for harness bugs).

    - Test 1 (e2e, THE LIVE FINGERPRINT): fake agent runner, auto-cycle (fdl) on, exactly 1 pooled approved candidate, suggest phase ends mid-build so the scheduler parks (`#waitingForBuild`). Drive the running build to `done`. Assert: exactly ONE `pipeline_stage` row with stage='building' per task_id, exactly ONE build_history row per task_id, and the NEXT build started is the solo-picked winner's task — NOT the finished task. (Reuse the harness pattern from tests/e2e/solo-auto-build.e2e.test.ts / tests/e2e/auto-cycle.e2e.test.ts — they already fake the runner, drive phases, and read the audit db.)
    - Test 2 (unit, ORDERING INVARIANT): in src/orchestrator/build-session.test.ts, wrap the fake machine's `transition` so that when called with "IDLE" it asserts `deps.taskQueue.list()` no longer contains the finished task id AND the registry no longer holds its controller. Three variants: (a) finalize via a done build, (b) finalizeAborted via an abort with mode still BUILD_IN_PROGRESS (shutdown-style — the HALTED arm never transitions, so it cannot be pinned this way; instead add assertion (b2): abort while mode=HALTED -> machine stays HALTED and the task is still dequeued+unregistered), (c) skipTask from a failed decision-pending build.
    - Test 3 (e2e, KIND MATRIX): repeat Test 1 with head.kind='suggestion' (plain rebuild path) and head.kind='project-switch' — for project-switch assert exactly ONE workspace_reset audit row per win (each live double-run burned an extra generation).
    - Test 4 (e2e, HISTORY UNIQUENESS): sweep at the end of the fingerprint scenarios asserting no task_id has more than 1 build_history row with result='built'.
  </behavior>
  <action>
    Read the debug report first (@.planning/debug/260716-double-build-execution.md) — it is the spec. Create tests/e2e/double-build-regression.e2e.test.ts using the existing e2e harness idioms (createApp with fakes, audit-db row queries). Extend src/orchestrator/build-session.test.ts with the ordering-invariant unit tests using its existing fake deps (fake TaskQueue with list/remove, fake BuildMachineView, fake AbortRegistry). Do NOT modify src/state-machine/auto-cycle.test.ts or any fdl deadlock-matrix tests (LOCKED). Do not touch production code in this task. Run the new tests, confirm they fail with the diagnosed signature (the finished head re-captured / still queued at transition time), and commit the RED state scoped quick-260716-t1n: `test(quick-260716-t1n): failing regression tests for the double-build-execution window`.
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/double-build-regression.e2e.test.ts src/orchestrator/build-session.test.ts 2>&1 | tail -20 — new tests FAIL (double-run + ordering), pre-existing build-session tests still PASS</automated>
  </verify>
  <done>Four regression-test shapes exist and fail for the diagnosed root cause; RED commit landed; no production code or locked test files touched.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — dequeue-before-transition in all three terminal paths + drainVoteQueue started-head guard</name>
  <files>src/orchestrator/build-session.ts, src/main.ts</files>
  <action>
    Layer 1 — ORDERING (primary), in src/orchestrator/build-session.ts. In ALL THREE terminal paths, move `deps.registry.unregister(...)` and `deps.taskQueue.remove(...)` ABOVE the try-block containing `machine.transition("IDLE")`:
    - finalize(): place them immediately after `current = null; active = null;` (i.e., after recordBuildHistory, before the transition try-block). Everything else — emitStage, onBuildDone hook, markBuilt, history row — stays exactly where it is.
    - finalizeAborted(): place them after `current = null; active = null;` and BEFORE `emitter.emit(BUILD_STAGE_CHANGED)` so the queue is clean before ANY emit. Do NOT touch the HALTED guard: `if (mode === "BUILD_IN_PROGRESS") transition("IDLE")` stays byte-identical — HALTED stays frozen, no build_history row is added, no `done` stage is ever emitted (LOCKED rails).
    - skipTask(): same move — unregister + remove above the transition try-block; the trailing `emitter.emit(BUILD_STAGE_CHANGED)` stays last.
    Update each path's doc comment to state the invariant: "a task is never discoverable in the queue once its terminal record is written / the machine leaves BUILD_IN_PROGRESS — dequeue MUST precede the IDLE transition because STATE_CHANGED handlers run synchronously inside it (quick-260716-t1n)."

    Side-effect checks (from the report — verify, do not assume):
    - grep main.ts for every `taskQueue.list()` consumer reachable from a synchronous STATE_CHANGED handler; confirm none besides drainVoteQueue reads the head with pre-dequeue expectations. Known-benign: isVoteQueueFull's deliberate off-by-one (~:1034) merely re-opens one emit earlier; the h73 pending-window promote and scheduler eligibility don't read the queue head.
    - Confirm the deferred drain at main.ts (~:2068 setImmediate) still starts the NEXT queued task (Test 1's "solo winner builds next" assertion covers this).

    Layer 2 — GUARD (belt), in src/main.ts, per the report's started-mark shape, implemented WITHOUT mutating the branded QueuedTask and WITHOUT any new `buildSession.startBuild` call (rll grep-gate pins `buildSession.startBuild(` to exactly 1 occurrence — compose with whatever dispatchBuild interceptor exists in merged main.ts):
    - Add a module-local bounded Set near drainVoteQueue: `const startedVoteHeads = new Set<string>()` with FIFO eviction above ~128 entries (queue ids are unique per task; the set only needs to outlive the finalize window).
    - Change head selection to `taskQueue.list().find((t) => isVoteOrigin(t) && !startedVoteHeads.has(t.id))`.
    - Stamp `startedVoteHeads.add(head.id)` at the moment drainVoteQueue COMMITS to the head: for the chaos-winner arm right before `runChaosWinner(head)`, and for the build arms immediately after the successful `transition("BUILD_IN_PROGRESS")` (before the async kind-router IIFE). A refused drain (mode/window/chaos guard) must NOT stamp.
    - Comment it as the belt layer defending ALL future synchronous drain routes (the chaosModePick "picked" arm is the same latent shape), citing quick-260716-t1n and the debug report.
    Do NOT defer onWinnerQueued via setImmediate (explicitly rejected by the report — it would change closeRound's deliberate synchronous VOTING_ROUND->BUILD_IN_PROGRESS semantics).

    Then: all Task 1 tests green, full suite green (baseline 1326 + rll's additions — zero failures, fdl deadlock-matrix and single-funnel invariant tests untouched and passing), `npx tsc --noEmit` clean, `npx biome check` clean. Commit scoped quick-260716-t1n: `fix(quick-260716-t1n): dequeue before the IDLE transition in all terminal paths + drain started-head guard`.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5 — full suite green (all files, 0 failures) AND npx tsc --noEmit AND npx biome check src/orchestrator/build-session.ts src/main.ts tests/e2e/double-build-regression.e2e.test.ts</automated>
  </verify>
  <done>All four regression shapes pass; unregister+remove precede the IDLE transition in finalize, finalizeAborted, and skipTask; drainVoteQueue skips started heads via the bounded set; HALTED/no-false-BUILT-IT semantics byte-preserved; fdl deadlock-matrix + single-funnel tests unmodified and green; grep-gate for `buildSession.startBuild(` (1 occurrence) still holds; GREEN commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal race-condition fix: reorders existing dequeue/transition statements and adds an in-process Set guard. No new external input, no new packages, no new network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t1n-01 | Elevation of Privilege (re-execution of an already-consumed approved task) | drainVoteQueue / finalize window | mitigate | Dequeue-before-transition invariant + startedVoteHeads positive guard; pinned by 4 regression tests incl. the live fingerprint |
| T-t1n-02 | Repudiation (duplicate build_history rows polluting the public changelog) | build_history ledger | mitigate | History-uniqueness sweep test (no task_id >1 result='built' row) |
</threat_model>

<verification>
- Full suite: `npx vitest run` — 0 failures (baseline 1326 + rll additions + Task 1's new tests).
- LOCKED rails: `git diff --stat` shows NO changes to src/state-machine/auto-cycle.ts, src/state-machine/auto-cycle.test.ts, or tests/invariants/single-funnel.test.ts; those suites pass.
- rll compose check: `grep -c 'buildSession.startBuild(' src/main.ts` still satisfies rll's gate (exactly 1 occurrence in merged code).
- Types + lint: `npx tsc --noEmit` and `npx biome check` clean.
</verification>

<success_criteria>
- The live fingerprint scenario (parked fdl vote + pool-of-1 + build done) provably does not rebuild the finished task and does build the solo winner next.
- Ordering invariant holds structurally: at the instant of every terminal IDLE transition (and the HALTED-preserving abort), the finished task is no longer in the queue or registry.
- Exactly one build_history 'built' row and one workspace_reset per win in the kind matrix.
- Two atomic commits (RED then GREEN) scoped quick-260716-t1n; no scope creep into auto-cycle.ts or event plumbing.
</success_criteria>

<output>
Create `.planning/quick/260716-t1n-fix-double-build-execution-dequeue-befor/260716-t1n-SUMMARY.md` when done.
</output>
