---
phase: quick-260717-093
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.ts
  - src/orchestrator/build-session.ts
  - src/orchestrator/build-session.test.ts
  - src/orchestrator/types.ts
  - src/orchestrator/workspace.ts
  - src/orchestrator/workspace.test.ts
  - tests/e2e/preview-reroot.e2e.test.ts
autonomous: true
requirements: [QUICK-260717-093]
tags: [preview, dev-server, reroot, holdover, teardown, obs]

must_haves:
  truths:
    - "A COMP-02 in-flight refusal of a TWEAK build resurrects the 5555 dev server at the CURRENT generation dir — the current project stays visible, never 'Between builds'"
    - "A refused/failed/skipped PROJECT-SWITCH build (holdover armed) resurrects the server at the HOLDOVER generation dir — the PREVIOUS project stays visible; the holdover stays armed and only a later successful done discharges it"
    - "While HALTED no reroot fires; leaving HALTED (recovery) fires exactly one holdover-aware reroot"
    - "A done build behaves exactly as before: single tqz discharge reroot when a holdover exists, zero reroot otherwise"
  artifacts:
    - path: "src/orchestrator/build-session.ts"
      provides: "onBuildTeardown optional dep fired on every non-done settle + the watchdog-teardown branch"
      contains: "onBuildTeardown"
    - path: "src/orchestrator/workspace.ts"
      provides: "dirFor(generation) — the single-sourced app-<N> path template exposed for holdover-aware reroots"
      contains: "dirFor"
    - path: "src/main.ts"
      provides: "holdover-aware supervisor workspaceDir + teardown-resurrection handler + HALTED-exit recovery reroot"
    - path: "tests/e2e/preview-reroot.e2e.test.ts"
      provides: "refused-tweak / refused-switch-under-holdover / halt-recovery / done-unchanged matrix"
  key_links:
    - from: "src/main.ts (createBuildSession deps)"
      to: "src/orchestrator/build-session.ts"
      via: "onBuildTeardown hook"
      pattern: "onBuildTeardown"
    - from: "src/main.ts (devServerSupervisor composition)"
      to: "src/orchestrator/workspace.ts"
      via: "workspace.dirFor(previewHoldoverGeneration ?? ...)"
      pattern: "dirFor\\("
    - from: "src/main.ts (STATE_CHANGED subscription)"
      to: "reRootPreview"
      via: "lastMode === HALTED && now !== HALTED"
---

<objective>
The app-under-construction preview (5555 → OBS LIVE BUILD slot) must NEVER drop to "Between builds" because a build was refused/failed/aborted. Ross's verbatim directive (2026-07-17): "when the second safety check goes off and prevents the current build from finishing, it replaces the current project with 'Between builds. Next one's coming right up'. This shouldn't happen. the previously approved project should stay on the screen."

Root cause (diagnosed live 2026-07-16, 20:25 twitch-clone refusal): every non-done teardown that terminates the WSL sandbox kills the python dev server on 5555 with it, and NOTHING restarts the server until the next generation-change reroot — the preview sits unreachable → standing-by card indefinitely.

Purpose: after any sandbox-killing teardown, resurrect the dev server serving `previewHoldoverGeneration ?? workspace.generation()` — during a project-switch holdover the PREVIOUS project comes back; for a tweak-build refusal the current project comes back.
Output: `onBuildTeardown` hook in build-session, holdover-aware supervisor `workspaceDir`, `workspace.dirFor()`, HALTED-exit recovery reroot, e2e matrix. Suite grows from the 1386 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260716-tqz-preview-holdover-keep-the-previous-proje/260716-tqz-SUMMARY.md
@src/preview/dev-server-supervisor.ts
@src/orchestrator/workspace.ts

Key source anchors (verified 2026-07-17):
- src/main.ts:465-481 — `reRootPreview` late-bound seam, `previewHoldoverGeneration`, `rerootPreviewNow()` clearing helper
- src/main.ts:1644-1655 — onBuildDone holdover discharge (nulls flag THEN calls raw `reRootPreview()`)
- src/main.ts:1716-1757 — saveAndCloseProject (immediate-reroot pattern reference)
- src/main.ts:2338-2354 — devServerSupervisor composition: `workspaceDir: () => workspace.dir()`, `reRootPreview = () => void devServerSupervisor.reroot()`, boot reroot
- src/main.ts:2408-2426 — holdover-aware playUrl (`previewHoldoverGeneration ?? workspace.generation()`)
- src/orchestrator/build-session.ts:145-152 — BuildSessionDeps.onBuildDone declaration (mirror for the new hook)
- src/orchestrator/build-session.ts:507-567 — finalize() (done branch fires onBuildDone at 526-533; dequeue-before-IDLE t1n invariant at 551-566)
- src/orchestrator/build-session.ts:584-617 — finalizeAborted() (halt/veto/shutdown; leaves HALTED alone)
- src/orchestrator/build-session.ts:790-799 — enterDecision() (freeze, NOT terminal)
- src/orchestrator/build-session.ts:868-898 — abort path (873-876) + WATCHDOG teardown branch (880-898: terminate → audit → enterDecision "failed")
- src/orchestrator/build-session.ts:901-916 — COMP-02 in-flight → abortForCompliance → finalize(refused); agent refusal → enterDecision (NO teardown)
- src/orchestrator/build-session.ts:1111-1142 — skipTask() (decision-freeze terminal)
- src/state-machine/stream-mode.ts:77-109 — transition/forceTransition/recoverTo all emit STATE_CHANGED with snapshot(); recovery exits HALTED via recoverTo
- tests/e2e/preview-reroot.e2e.test.ts — gatedRunner (`releaseNext("success"|"failed")`, outcome queue), fake sandbox recording `starts: {dir, port}[]`, `until()` helper; HOLDOVER describe from tqz
- tests/e2e/build-flow.e2e.test.ts:36-48 — writeBatch/resultSuccess/modelRefusal SDK-ish fixtures (COMP-02 in-flight rejection driving pattern)
- tests/e2e/auto-cycle.e2e.test.ts:331 — recovery idiom: `postJson(app, "/api/recover", { action: "reset-to-idle" })`

<interfaces>
From src/orchestrator/types.ts:82-84 (WorkspaceView — gains dirFor):
```typescript
export interface WorkspaceView {
  dir(): string;            // `/home/builder/projects/app-${generation}` — internally-generated integer only
  generation(): number;
  scaffolded(): boolean;
  markBuilt(): void;
  newProject(): number;
  activateExisting(target: number): void;
}
```

From src/preview/dev-server-supervisor.ts (DO NOT EDIT — D3-12):
```typescript
export interface DevServerSupervisorDeps {
  adapter: SandboxAdapter;
  port: number;
  workspaceDir: () => string;   // late-bound, read at cycle time — the holdover-awareness seam lives in main.ts's closure
  probeReachable: () => Promise<boolean>;
  logger: DevServerSupervisorLogger;
  settleMs?: number;
}
export interface DevServerSupervisor { reroot(): Promise<void>; } // serialized, never rejects, fail-open
```
</interfaces>
</context>

<pinned_decisions>
These are locked (planning-context design intent + planner resolutions within its discretion). Reference them in commit messages/comments as D093-1..5.

- **D093-1 (holdover-aware dir):** holdover-awareness is implemented in main.ts's supervisor `workspaceDir` closure — `previewHoldoverGeneration !== null ? workspace.dirFor(previewHoldoverGeneration) : workspace.dir()` — plus a new `WorkspaceView.dirFor(generation)` in workspace.ts (the path template stays single-sourced there; never duplicate `/home/builder/projects/app-${n}` in main.ts). `src/preview/**` stays byte-identical (D3-12). This makes EVERY reroot holdover-aware automatically; it is safe for all existing call sites because every immediate path clears the holdover first (`rerootPreviewNow`) and the done-discharge nulls the flag before calling `reRootPreview()`.
- **D093-2 (hook + call sites):** new optional `onBuildTeardown?: (task: QueuedTask) => void` in BuildSessionDeps, fired via a try/catch-wrapped helper from EXACTLY four places: (a) finalize() when stage !== "done" (covers COMP-02 in-flight refusal, plan-screen refusals, unexpected-error failed), (b) finalizeAborted() (main.ts guards HALTED), (c) skipTask() (decision-freeze terminal), (d) the WATCHDOG-timeout branch after `terminate()` (build-session.ts:880-898 — the sandbox is literally dead there and the machine then freezes on a decision for possibly minutes; per design intent "ANY build teardown that kills the sandbox" this fires even though enterDecision is not terminal). It does NOT fire on the agent-refusal enterDecision at :907-915 (no teardown happened — the server is healthy; avoid a pointless stop/start blip) and NEVER on done.
- **D093-3 (halt behavior — pinned per design intent):** no reroot while HALTED — main.ts's `onBuildTeardown` handler returns early when `machine.mode === "HALTED"` (the machine is frozen; the preview may stay dark). Reroot fires on ANY exit from HALTED via a STATE_CHANGED subscription with last-mode tracking (recoverTo emits STATE_CHANGED; recovery is a rare operator action — rerooting an already-healthy server is an acceptable one-time blip in exchange for a guaranteed-live preview after every recovery). The recovery reroot does NOT discharge the holdover (raw `reRootPreview()`, never `rerootPreviewNow()`).
- **D093-4 (tqz pins superseded):** quick-tqz's e2e assertions "failed/skipped project-switch builds never reroot" are SUPERSEDED by this directive. The on-screen outcome is unchanged (previous project stays visible) but the mechanism inverts: those paths now reroot AT THE HOLDOVER DIR. Update those specific tqz test expectations (start observed at `app-<holdoverGen>`, holdover still armed via playUrl assertion) — do not treat them as locked. tqz's discharge semantics ARE preserved: only done discharges.
- **D093-5 (fire-and-forget discipline):** the hook body in main.ts is a mode-guard + raw `reRootPreview()` only (a void fire-and-forget onto the supervisor's serialized never-rejecting chain). Nothing synchronous/throwing rides finalize/skip/abort teardown or the IDLE transition (t1n ordering). Place each build-session call site at the END of its function, after the dequeue + guarded transition.
</pinned_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing matrix for teardown-resurrection (e2e + unit pins)</name>
  <files>tests/e2e/preview-reroot.e2e.test.ts, src/orchestrator/build-session.test.ts, src/orchestrator/workspace.test.ts</files>
  <behavior>
New describe "preview TEARDOWN RESURRECTION e2e (quick-260717-093)" in tests/e2e/preview-reroot.e2e.test.ts, reusing the existing gatedRunner + recording fake sandbox + `until()` helper (fresh app per the tqz collision lesson — project_repos generation is PRIMARY KEY):

- Test 1 — refused TWEAK build resurrects at the CURRENT gen: extend the harness to drive a COMP-02 in-flight rejection — add a `releaseNext("comp02")` outcome whose build yields a writeBatch containing a marker string (build-flow.e2e.test.ts:36-41 fixture shape) and configure the app's `fakeClassifier` to reject exactly when the candidate text contains that marker (approve everything else — intake stays approved). Drive a suggest (tweak) build on the scaffolded current gen → release "comp02" → build finalizes refused → assert exactly ONE new stop+start pair lands at `/home/builder/projects/app-<currentGen>`; playUrl unchanged.
- Test 2 — refused PROJECT-SWITCH during holdover resurrects at the HOLDOVER gen: gen-1 active + seeded repo rows, project-switch winner → shipThenRotate rotates to gen 2 + arms the holdover → release "comp02" → assert the resurrection start is at `/home/builder/projects/app-1` (the HOLDOVER dir — this REPLACES the tqz "no reroot on failed switch" expectation per D093-4), playUrl still the gen-1 URL (holdover armed); then a successful done build → single discharge reroot at the active gen, playUrl flips. Also update the existing tqz FAILED-path test in the HOLDOVER describe: `releaseNext("failed")`×2 through the auto-retry into the frozen decision → `app.orchestrator.skipTask(...)` → expect a reroot at the holdover dir (was: expect none).
- Test 3 — HALT: no reroot while HALTED, one reroot on recovery: start a gated build, halt (the same force-halt driving the existing e2e suites use), record `startsBefore` → assert NO new start while HALTED (give the drain a tick); recover via `postJson(app, "/api/recover", { action: "reset-to-idle" })` (auto-cycle.e2e.test.ts:331 idiom) → assert exactly ONE new start; if a holdover was armed pre-halt it is STILL armed after recovery (playUrl stays previous-gen).
- Test 4 — done path unchanged: a plain successful tweak build produces ZERO resurrection reroots (starts length unchanged); a holdover done-discharge produces exactly ONE reroot (no double). The existing tqz HOLDOVER CORE test keeps passing.

Unit pins in src/orchestrator/build-session.test.ts (existing harness):
- onBuildTeardown fires on: finalize(refused) via compliance-rejected; finalizeAborted (non-halt abort); skipTask; the watchdog-timeout branch (tiny turnTimeoutMs via the existing injectable dep, runner that never yields after start).
- onBuildTeardown does NOT fire on: a done finalize; the agent-refusal enterDecision freeze (fires only later at skipTask).
- A THROWING onBuildTeardown never disturbs finalize/skip (machine still reaches IDLE, task dequeued) — mirror the onBuildDone-throws pin.

Unit pin in src/orchestrator/workspace.test.ts:
- `dirFor(3)` → `/home/builder/projects/app-3`; `dirFor(gen)` === `dir()` for the current generation; non-integer input throws.
  </behavior>
  <action>
Write the tests above. Set `PREVIEW_DEV_SERVER_SETTLE_MS=0` in the e2e app env (existing knob) for deterministic reroot cycles. RED must fail for the RIGHT reasons: Test 1/2 fail with zero resurrection starts (today nothing restarts the server after a refusal); the build-session unit pins fail on the missing `onBuildTeardown` dep; workspace pin fails on missing `dirFor`. All untouched tqz/t8k pins in the file must stay green EXCEPT the deliberately-updated failed-switch expectations (D093-4). Do NOT touch src/compliance/** (concurrent task 260717-08w owns it) or src/preview/**.

Commit: `test(quick-260717-093): failing matrix — teardown kills the preview server, nothing resurrects it`
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/preview-reroot.e2e.test.ts src/orchestrator/build-session.test.ts src/orchestrator/workspace.test.ts 2>&1 | tail -20 — new tests FAIL on missing resurrection/hook/dirFor; pre-existing pins (minus the D093-4 updates) PASS</automated>
  </verify>
  <done>RED commit exists; failures are exactly the missing-feature class; no src/ edits yet.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — onBuildTeardown hook + holdover-aware reroot + recovery reroot</name>
  <files>src/orchestrator/workspace.ts, src/orchestrator/types.ts, src/orchestrator/build-session.ts, src/main.ts</files>
  <action>
Implement in this order (each step maps to a pinned decision):

1. **workspace.ts + types.ts (D093-1):** add `dirFor(generation: number): string` to WorkspaceView and createWorkspaceState — `Number.isInteger` guard (throw, matching activateExisting's validation style), template `/home/builder/projects/app-${generation}`; refactor `dir()` to delegate to it so the template exists ONCE.

2. **build-session.ts (D093-2, D093-5):** add the `onBuildTeardown?: (task: QueuedTask) => void` optional dep (doc comment mirroring onBuildDone's at :145-152, stating the four call sites and the not-on-done / not-on-refusal-freeze rules). Add a private `fireTeardownHook(task)` helper wrapping `deps.onBuildTeardown?.(task)` in try/catch + logger.error (the onBuildDone idiom at :526-533). Call it at the END of: finalize() guarded `if (stage !== "done")` (after the transition try/catch), finalizeAborted(), skipTask() (after the BUILD_STAGE_CHANGED emit), and in the watchdog branch at :880-898 right before its `return` (after enterDecision). Touch NOTHING else — emitStage order, dequeue-before-IDLE (t1n), CR-01 no-done-on-abort, onBuildDone done-only semantics all stay byte-compatible.

3. **main.ts supervisor composition (~:2341, D093-1):** change `workspaceDir: () => workspace.dir()` to a holdover-aware closure: `() => previewHoldoverGeneration !== null ? workspace.dirFor(previewHoldoverGeneration) : workspace.dir()` with a quick-260717-093 comment (safe for all existing callers — every immediate path clears the holdover first; the done-discharge nulls before rerooting).

4. **main.ts createBuildSession deps (~:1687, D093-3/D093-5):** wire `onBuildTeardown: () => { if (machine.mode === "HALTED") return; reRootPreview(); }` — comment: resurrects the dev server after any non-done settle, serving the holdover gen when armed; NEVER discharges the holdover (raw seam call, not rerootPreviewNow); HALTED stays dark by pinned decision, the recovery subscription below covers it.

5. **main.ts recovery reroot (inside the supervisor composition block after `reRootPreview = ...` at ~:2351, D093-3):** subscribe `machine.on(STATE_CHANGED, ...)` with a `let lastPreviewMode = machine.mode` closure; when the previous mode was "HALTED" and the new snapshot mode is not, call `reRootPreview()` (raw — holdover survives recovery). Import STATE_CHANGED is already in main.ts scope (verify; it is used by the playable clear at :494).

6. Run the full gates: `npx vitest run` (baseline 1386 + all new tests green, including the updated D093-4 expectations), `npx tsc --noEmit`, `npx biome check src/main.ts src/orchestrator/build-session.ts src/orchestrator/workspace.ts src/orchestrator/types.ts`.

Locked-rail proofs (include results in the SUMMARY):
- `git diff HEAD --stat -- src/preview/ src/compliance/` → EMPTY (D3-12 + concurrent-task isolation).
- `grep -v "^\s*//" src/main.ts | grep -c "reRootPreview()"` → 4 (clearing-helper body, onBuildDone discharge, onBuildTeardown handler, HALTED-exit handler) — update the tqz gate note from 2.
- `grep -v "^\s*//" src/orchestrator/build-session.ts | grep -c "fireTeardownHook("` → 5 (1 definition + 4 call sites).
- rll grep-gate stays green: exactly 1 `buildSession.startBuild(` call site.

Commit: `feat(quick-260717-093): resurrect the preview dev server after every non-done build teardown (holdover-aware)`
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5 && npx tsc --noEmit && git diff HEAD --stat -- src/preview/ src/compliance/ | wc -l</automated>
  </verify>
  <done>Full suite green (≥1386 + new tests, zero unrelated edits); tsc + biome clean; src/preview/** and src/compliance/** byte-identical; both grep gates at their pinned counts; GREEN commit exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat text → filesystem path | none crossed: the reroot dir derives ONLY from `previewHoldoverGeneration` / `workspace.generation()` — internally-generated integers through the single workspace.ts template; chat text structurally cannot reach `dirFor` |
| build teardown → host process control | unchanged: the supervisor stays pkill-by-port via the existing adapter exec seam; `wsl --terminate` remains exclusively the halt path's tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-093-01 | Tampering | dirFor(generation) | mitigate | Number.isInteger guard + template single-sourced in workspace.ts; holdover flag only ever assigned from workspace.generation() |
| T-093-02 | DoS | onBuildTeardown in finalize/skip/abort | mitigate | try/catch-wrapped helper (onBuildDone idiom); handler body is a mode guard + void fire-and-forget onto the serialized never-rejecting supervisor chain — cannot delay finalize or the IDLE transition |
| T-093-03 | DoS | reroot storm (retry loops) | accept | supervisor serializes overlapping reroots and fail-opens; worst case is sequential stop/start cycles of a localhost python server — no show-loop impact |
| T-093-04 | Elevation | reroot while HALTED | mitigate | explicit HALTED guard in the handler + e2e pin; recovery reroot fires only on a real HALTED-exit STATE_CHANGED |
</threat_model>

<verification>
- Full vitest suite green: 1386 baseline + new e2e matrix + unit pins, with ONLY the D093-4-superseded tqz expectations modified among pre-existing tests.
- `tsc --noEmit` and biome clean on all touched files.
- `git diff HEAD --stat -- src/preview/ src/compliance/` empty.
- Grep gates: `reRootPreview()` count 4 in main.ts (comment-filtered); `fireTeardownHook(` count 5 in build-session.ts (comment-filtered); rll startBuild single-call-site gate green.
- Behavior matrix proven e2e: refused tweak → current gen back; refused switch under holdover → holdover gen back + holdover armed + done discharges; halt → dark while HALTED, one reroot on recovery, holdover survives; done → unchanged (single discharge, no double reroot).
</verification>

<success_criteria>
- The exact live failure (COMP-02 in-flight refusal → "Between builds" indefinitely) is impossible: every non-done teardown path resurrects the dev server at the holdover-aware generation dir, proven by failing-first tests.
- tqz discharge semantics intact: only done discharges the holdover; operator paths still clear-and-reroot via rerootPreviewNow.
- Halt-path behavior explicitly pinned and tested (D093-3): dark while HALTED, resurrected on recovery.
- Two atomic commits scoped quick-260717-093 (RED, GREEN).
</success_criteria>

<output>
Create `.planning/quick/260717-093-preview-survives-refused-builds-holdover/260717-093-SUMMARY.md` when done, recording: the D093-1..5 decisions as applied, the D093-4 superseded-pin note (which tqz expectations changed and why), final test count, and both grep-gate values.
</output>
