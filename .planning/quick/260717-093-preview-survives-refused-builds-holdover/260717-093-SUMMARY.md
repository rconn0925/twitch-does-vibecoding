---
phase: quick-260717-093
plan: 01
subsystem: preview / dev-server supervision
tags: [preview, dev-server, reroot, holdover, teardown, obs]
requires: [quick-260716-tqz (holdover state + discharge semantics), quick-260711-t8k (supervisor + reroot seam), quick-260716-t1n (finalize ordering discipline)]
provides:
  - onBuildTeardown optional BuildSessionDeps hook fired on every non-done settle + the watchdog-teardown branch (src/orchestrator/build-session.ts)
  - WorkspaceView.dirFor(generation) — the single-sourced app-<N> path template (src/orchestrator/workspace.ts, types.ts)
  - holdover-aware supervisor workspaceDir + teardown-resurrection handler + HALTED-exit recovery reroot (src/main.ts)
  - refused-tweak / refused-switch-under-holdover / halt-recovery / done-unchanged e2e matrix (tests/e2e/preview-reroot.e2e.test.ts)
affects: [preview reroot timing on refused/failed/skipped/aborted builds (on-screen outcome unchanged: previous project stays visible)]
tech-stack:
  added: []
  patterns: [try/catch-wrapped optional hook (onBuildDone idiom), STATE_CHANGED last-mode edge detection, marker-driven COMP-02 in-flight rejection in e2e (gatedRunner "comp02" outcome)]
key-files:
  created: []
  modified:
    - src/main.ts
    - src/orchestrator/build-session.ts
    - src/orchestrator/build-session.test.ts
    - src/orchestrator/types.ts
    - src/orchestrator/workspace.ts
    - src/orchestrator/workspace.test.ts
    - tests/e2e/preview-reroot.e2e.test.ts
    - src/operator-console/server.test.ts (fake gains dirFor — deviation, see below)
    - tests/e2e/build-history.e2e.test.ts (fake gains dirFor — deviation, see below)
decisions:
  - "D093-1 applied: holdover-awareness lives in main.ts's supervisor workspaceDir closure (previewHoldoverGeneration !== null ? workspace.dirFor(holdover) : workspace.dir()); the app-<N> template is single-sourced in workspace.ts via dirFor (dir() delegates); src/preview/** byte-identical (D3-12)"
  - "D093-2 applied: onBuildTeardown fires from EXACTLY four try/catch-wrapped sites — finalize() stage !== done, finalizeAborted(), skipTask(), and the WR-07 watchdog-teardown branch; NEVER on done, NEVER on the agent-refusal decision freeze (no teardown happened — no pointless stop/start blip)"
  - "D093-3 applied: no reroot while HALTED (mode guard in main.ts's handler); ANY exit from HALTED fires exactly one holdover-aware reroot via a STATE_CHANGED last-mode subscription; the recovery reroot never discharges the holdover (raw reRootPreview, never rerootPreviewNow)"
  - "D093-4 applied: tqz 'failed/skipped project-switch builds never reroot' pins SUPERSEDED — those paths now reroot AT THE HOLDOVER DIR (same on-screen outcome, inverted mechanism); only done still discharges the holdover"
  - "D093-5 applied: the main.ts hook body is a HALTED guard + raw reRootPreview() only (void fire-and-forget onto the serialized never-rejecting supervisor chain); every build-session call site sits at the END of its function after the dequeue + guarded transition (t1n ordering held)"
metrics:
  duration: ~22 min
  completed: 2026-07-17T06:43Z
  tests: 1404 passing (+18 over the 1386 baseline)
  commits: 2 (RED 0d4bbda, GREEN 02f9274)
---

# Quick Task 260717-093: Preview Survives Refused Builds Summary

**One-liner:** Every non-done build teardown (COMP-02 in-flight refusal, failure, skip, abort, watchdog kill) now resurrects the 5555 preview dev server at `previewHoldoverGeneration ?? workspace.generation()` — the current project comes back after a refused tweak, the PREVIOUS project comes back after a refused project-switch, and leaving HALTED fires exactly one holdover-aware reroot — so the OBS LIVE BUILD slot can never sit on "Between builds" because a build was refused.

## What Was Built

Live incident 2026-07-16 20:25 (twitch-clone refusal): the in-flight COMP-02 rejection tore the WSL sandbox down, killing the python dev server on 5555 with it — and NOTHING restarted the server until the next generation-change reroot, so the preview sat on the standing-by card indefinitely. Ross's verbatim directive (2026-07-17): "when the second safety check goes off and prevents the current build from finishing … the previously approved project should stay on the screen."

### Task 1 — RED (commit `0d4bbda`)

- `gatedRunner()` gains a `"comp02"` outcome: the released build yields one more Write batch carrying `COMP02_MARKER`; the new describe's marker-aware `fakeClassifier` rejects exactly that batch in-flight (intake + pre-build screens stay approved) — the precise live incident shape, driven through the production COMP-02 seam.
- New e2e describe "preview TEARDOWN RESURRECTION e2e (quick-260717-093)" (fresh app — project_repos generation is PRIMARY KEY):
  - **Refused TWEAK:** COMP-02 in-flight rejection on the scaffolded current gen → exactly ONE stop+start pair at `/home/builder/projects/app-1`; playUrl unchanged.
  - **Refused PROJECT-SWITCH under holdover:** shipThenRotate rotates to gen 2 + arms the holdover → in-flight refusal → resurrection at `app-1` (the HOLDOVER dir), playUrl still gen-1, holdover armed; a later successful done → single discharge reroot at `app-2`, playUrl flips.
  - **HALT:** project-switch arms a holdover → halt mid-build → aborted turn unwinds → NO start while HALTED (drain tick given); `/api/recover reset-to-idle` → exactly ONE start at the holdover dir `app-2`; playUrl stays previous-gen (holdover survives recovery).
  - **Done unchanged:** holdover discharge = exactly ONE reroot (no double from the new hook); a plain done tweak = ZERO reroots.
- D093-4 update: the tqz HOLDOVER describe's FAILED-path test now expects skipTask to reroot at the holdover dir (was: expect none); no reroot until the decision resolves; playUrl stays previous throughout.
- Unit pins (build-session.test.ts): hook fires on finalize(refused)-via-compliance-rejected, finalizeAborted (non-halt self-abort AND halt — the HALTED guard is main.ts's job), skipTask, and the watchdog-timeout branch (hangingRunner + turnTimeoutMs 20); NOT on done, NOT on the agent-refusal decision freeze; a THROWING hook never disturbs finalize or skipTask (IDLE reached, task dequeued).
- Unit pins (workspace.test.ts): `dirFor(3)` template, `dirFor(generation()) === dir()`, non-integer input throws.
- RED failed for exactly the missing-feature class: 14 failures = zero resurrection starts (e2e timeouts) + missing `onBuildTeardown` dep + missing `dirFor`; all 105 untouched pins stayed green.

### Task 2 — GREEN (commit `02f9274`)

1. **workspace.ts + types.ts (D093-1):** `dirFor(generation)` with a `Number.isInteger` throw guard (activateExisting's validation style, T-093-01); `dir()` delegates to it — the `/home/builder/projects/app-${n}` template now exists exactly once.
2. **build-session.ts (D093-2/5):** `onBuildTeardown?: (task: QueuedTask) => void` dep (doc comment mirrors onBuildDone's, stating the four call sites and the not-on-done / not-on-refusal-freeze rules) + private `fireTeardownHook` try/catch helper; call sites at the END of finalize() (`if (stage !== "done")`), finalizeAborted(), skipTask() (after the BUILD_STAGE_CHANGED emit), and the watchdog branch right before its return (after enterDecision). emitStage order, dequeue-before-IDLE (t1n), CR-01 no-done-on-abort, onBuildDone done-only semantics all byte-compatible.
3. **main.ts supervisor composition (D093-1):** `workspaceDir` is now the holdover-aware closure — safe for all existing callers (every immediate path clears the holdover first; the done-discharge nulls before rerooting).
4. **main.ts createBuildSession deps (D093-3/5):** `onBuildTeardown: () => { if (machine.mode === "HALTED") return; reRootPreview(); }`.
5. **main.ts recovery reroot (D093-3):** `machine.on(STATE_CHANGED, ...)` with `let lastPreviewMode = machine.mode`; fires raw `reRootPreview()` when the previous mode was HALTED and the new one is not (holdover survives recovery).

## Verification (locked-rail proofs)

- Full suite: **1404 passed** (baseline 1386 +18), tsc `--noEmit` clean, biome clean on all touched files.
- `git diff HEAD --stat -- src/preview/ src/compliance/` → **EMPTY** (D3-12 + concurrent-task 260717-08w isolation; re-verified at GREEN commit).
- Grep gates:
  - `grep -v "^\s*//" src/main.ts | grep -c "reRootPreview()"` → **4** (clearing-helper body :480, onBuildDone discharge :1654, onBuildTeardown handler :1702, HALTED-exit handler :2390) — the tqz gate count of 2 is superseded by this task's two sanctioned additions.
  - `grep -v "^\s*//" src/orchestrator/build-session.ts | grep -c "fireTeardownHook("` → **5** (1 definition + 4 call sites).
  - rll gate: exactly **1** `buildSession.startBuild(` call site.
- Behavior matrix proven e2e: refused tweak → current gen back; refused switch under holdover → holdover gen back + holdover armed + only done discharges; halt → dark while HALTED, one reroot on recovery, holdover survives; done → unchanged (single discharge, no double reroot).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Two additional test fakes needed `dirFor`**
- **Found during:** Task 2 (GREEN), tsc gate
- **Issue:** `tests/e2e/build-history.e2e.test.ts` and `src/operator-console/server.test.ts` each hold a structural `WorkspaceView` fake; widening the interface with `dirFor` made them type-errors.
- **Fix:** one-line `dirFor: (target) => \`/home/builder/projects/app-${target}\`` added to each fake (plus the already-in-plan build-session.test.ts fake).
- **Files modified:** tests/e2e/build-history.e2e.test.ts, src/operator-console/server.test.ts
- **Commit:** 02f9274

No other deviations — plan executed as written.

## D093-4 Superseded-Pin Note

Changed tqz expectations (tests/e2e/preview-reroot.e2e.test.ts, HOLDOVER describe):
- "FAILED project-switch build: NO reroot … skip does not reroot either" → renamed and inverted: skipTask now expects exactly one reroot at `/home/builder/projects/app-2` (the holdover dir) with the holdover still armed (playUrl assertion unchanged). The decision-freeze window still expects NO reroot (not a teardown site).
- The describe docstring documents the supersession. tqz's discharge semantics are preserved verbatim: only a done build discharges the holdover; the HOLDOVER CORE / RECOVERY / holdover-absent pins pass unmodified.

## Known Stubs

None — no hardcoded empty values, placeholder text, or unwired data paths introduced.

## Threat Flags

None — no new network endpoints, auth paths, or trust-boundary surface. The reroot dir still derives only from internally-generated integers through the single workspace.ts template (T-093-01 mitigated by the `Number.isInteger` throw guard + single-sourcing); the supervisor remains pkill-by-port via the adapter seam (`wsl --terminate` stays the halt path's tool, e2e pin still green).

## Self-Check: PASSED

- tests/e2e/preview-reroot.e2e.test.ts, src/orchestrator/build-session.ts, src/orchestrator/workspace.ts, src/main.ts — all modified and present.
- Commits `0d4bbda` (RED) and `02f9274` (GREEN) exist on `worktree-agent-a1abaf36c3abec933`.
- Gate counts re-verified post-commit: 4 / 5 / 1; preview+compliance diffs empty.
